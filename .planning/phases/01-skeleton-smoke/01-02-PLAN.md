---
phase: 01-skeleton-smoke
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/chess_engine/engine/v7/include/board.hpp
  - src/chess_engine/engine/v7/include/movegen.hpp
  - src/chess_engine/engine/v7/include/magic.hpp
  - src/chess_engine/engine/v7/include/zobrist.hpp
  - src/chess_engine/engine/v7/include/tt.hpp
  - src/chess_engine/engine/v7/include/types.hpp
  - src/chess_engine/engine/v7/src/board.cpp
  - src/chess_engine/engine/v7/src/movegen.cpp
  - src/chess_engine/engine/v7/src/magic.cpp
  - src/chess_engine/engine/v7/src/tt.cpp
  - src/chess_engine/engine/v7/src/perft.cpp
  - src/chess_engine/engine/v7/src/python_bindings.cpp
  - src/chess_engine/engine/v7/include/engine.hpp
  - src/chess_engine/engine/v7/CMakeLists.txt
  - tests/test_v7_perft.py
autonomous: true
requirements:
  - FOUND-06

must_haves:
  truths:
    - "V7 reproduces V6's perft node counts EXACTLY to depth 5 on starting position, Kiwipete, position 3, position 4, position 5"
    - "V7 reproduces V6's perft node counts EXACTLY to depth 6 on Kiwipete and position 3 (opt-in slow tests; smaller positions checked to depth 6 by default)"
    - "v7_engine.perft(fen, depth) is exposed to Python and matches v6_engine.perft when both are available"
    - "Board, MoveList, generate_legal_moves, make_move, unmake_move, hash, from_fen, to_fen all live in namespace v7 with no name collisions vs namespace v6 — V1-V6 still load alongside V7 in the same Python process"
    - "Engine::new_game() now clears TT and repetition stack (real implementation, replacing plan 01's stub)"
  artifacts:
    - path: "src/chess_engine/engine/v7/include/board.hpp"
      provides: "v7::Board with bitboards, hash, castling_rights, ep_square, halfmove_clock, from_fen, to_fen, make_move, unmake_move, piece_at; v7::Move, v7::Square, v7::Piece, v7::Color typedefs in types.hpp"
    - path: "src/chess_engine/engine/v7/include/movegen.hpp"
      provides: "v7::MoveList (count, [] operator); v7::generate_legal_moves"
    - path: "src/chess_engine/engine/v7/include/magic.hpp"
      provides: "v7::pawn_attacks, knight_attacks, bishop_attacks, rook_attacks, queen_attacks, king_attacks; magic table init"
    - path: "src/chess_engine/engine/v7/include/zobrist.hpp"
      provides: "v7::zobrist_piece_square, zobrist_side, zobrist_castling, zobrist_ep arrays (extracted from V6's types.hpp/board.cpp wherever they currently live)"
    - path: "src/chess_engine/engine/v7/include/tt.hpp"
      provides: "v7::TT class with probe/store/clear (single-threaded, mirrors V6); Phase 3 replaces with lockless"
    - path: "src/chess_engine/engine/v7/src/perft.cpp"
      provides: "v7::perft_entry(fen, depth) — replaces plan 01's stub returning 0"
    - path: "tests/test_v7_perft.py"
      provides: "test_perft_starting_d5, test_perft_kiwipete_d4, test_perft_position3_d5, test_perft_position4_d4, test_perft_position5_d4; slow-marked: test_perft_kiwipete_d6, test_perft_position3_d6; parity check vs V6 perft when V6 is available"
  key_links:
    - from: "src/chess_engine/engine/v7/src/python_bindings.cpp"
      to: "v7::perft_entry"
      via: "m.def('perft', &v7::perft_entry, ..., py::call_guard<py::gil_scoped_release>())"
      pattern: "&v7::perft_entry"
    - from: "src/chess_engine/engine/v7/include/engine.hpp"
      to: "v7::TT, v7::Board"
      via: "Engine class members tt_, board_ replacing plan 01's forward-declared opaques"
      pattern: "TT tt_;|Board board_;"
    - from: "src/chess_engine/engine/v7/CMakeLists.txt"
      to: "board.cpp, movegen.cpp, magic.cpp, tt.cpp, perft.cpp"
      via: "TODO(plan-02) sources block replaced with explicit source list"
      pattern: "src/board\\.cpp"
---

<objective>
Port V6's board, movegen, magic-bitboards, zobrist, and TT C++ subsystems into the v7 namespace as a verbatim fork (search-and-replace v6→v7 / V6→V7), wire perft into the binding (replaces plan 01's stub), and prove perft parity vs V6 to depth 5 (depth 6 opt-in slow tests). This task is the largest single chunk of file motion in phase 1 but contains ZERO logic edits — every change is a rename or namespace bump.

Purpose: Lock the data-structure foundation that plans 03 (search), 04 (eval), 05 (syzygy via tbconfig.h that references magic.hpp), and 06 (smoke game) all build on. Perft parity to depth 6 on the canonical 5-position corpus is the most rigorous correctness test in chess (FOUND-06) and validates the entire move generation path in one shot.

Output: V7 namespace contains a working Board + MoveList + Magic + Zobrist + TT; v7_engine.perft(fen, depth) returns identical node counts to v6_engine.perft for the same inputs; tests/test_v7_perft.py is green at the per-task (depth-5) level and slow-marked (depth-6) for nightly/CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-skeleton-smoke/01-RESEARCH.md
@.planning/phases/01-skeleton-smoke/01-PATTERNS.md
@.planning/phases/01-skeleton-smoke/01-01-SUMMARY.md

# V6 sources the executor will fork into V7 namespace:
@src/chess_engine/engine/v6/include/board.hpp
@src/chess_engine/engine/v6/include/movegen.hpp
@src/chess_engine/engine/v6/include/magic.hpp
@src/chess_engine/engine/v6/include/types.hpp
@src/chess_engine/engine/v6/include/tt.hpp
@src/chess_engine/engine/v6/src/board.cpp
@src/chess_engine/engine/v6/src/movegen.cpp
@src/chess_engine/engine/v6/src/magic.cpp
@src/chess_engine/engine/v6/src/tt.cpp
@src/chess_engine/engine/v6/src/perft.cpp
@src/chess_engine/engine/v6/src/python_bindings.cpp

# Plan 01 outputs:
@src/chess_engine/engine/v7/include/engine.hpp
@src/chess_engine/engine/v7/src/python_bindings.cpp
@src/chess_engine/engine/v7/CMakeLists.txt
@tests/test_v7_engine.py

<interfaces>
<!-- Perft Test Corpus — RESEARCH.md §A2 lines 301-307 -->
<!-- Verify the executor uses EXACTLY these FENs and node counts. -->

| Position | FEN | Depth 5 | Depth 6 |
|----------|-----|---------|---------|
| Starting | `rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1` | 4865609 | 119060324 |
| Kiwipete | `r3k2r/p1ppqpb1/bn2pnp1/3PN3/1p2P3/2N2Q1p/PPPBBPPP/R3K2R w KQkq - 0 1` | 193690690 | 8031647685 |
| Position3 | `8/2p5/3p4/KP5r/1R3p1k/8/4P1P1/8 w - - 0 1` | 674624 | 11030083 |
| Position4 | `r3k2r/Pppp1ppp/1b3nbN/nP6/BBP1P3/q4N2/Pp1P2pP/R2Q1RK1 w kq - 0 1` | 15833292 | 706045033 |
| Position5 | `rnbq1k1r/pp1Pbppp/2p5/8/2B5/8/PPP1NnPP/RNBQK2R w KQ - 1 8` | 89941194 | 3048196529 |

<!-- V6 perft entry shape (PATTERNS.md lines 348-369) — V7 mirrors verbatim -->
<!-- NOTE: V6's perft re-FENs every node (slow); V7 keeps the same shape for parity, then Phase 4 may optimize. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fork V6 board/movegen/magic/tt/types/zobrist + perft into v7:: namespace; wire engine.hpp members; update CMakeLists; expand python_bindings perft</name>
  <files>
    src/chess_engine/engine/v7/include/board.hpp,
    src/chess_engine/engine/v7/include/movegen.hpp,
    src/chess_engine/engine/v7/include/magic.hpp,
    src/chess_engine/engine/v7/include/zobrist.hpp,
    src/chess_engine/engine/v7/include/tt.hpp,
    src/chess_engine/engine/v7/include/types.hpp,
    src/chess_engine/engine/v7/src/board.cpp,
    src/chess_engine/engine/v7/src/movegen.cpp,
    src/chess_engine/engine/v7/src/magic.cpp,
    src/chess_engine/engine/v7/src/tt.cpp,
    src/chess_engine/engine/v7/src/perft.cpp,
    src/chess_engine/engine/v7/include/engine.hpp,
    src/chess_engine/engine/v7/src/python_bindings.cpp,
    src/chess_engine/engine/v7/CMakeLists.txt
  </files>
  <read_first>
    src/chess_engine/engine/v6/include/board.hpp,
    src/chess_engine/engine/v6/include/movegen.hpp,
    src/chess_engine/engine/v6/include/magic.hpp,
    src/chess_engine/engine/v6/include/types.hpp,
    src/chess_engine/engine/v6/include/tt.hpp,
    src/chess_engine/engine/v6/src/board.cpp,
    src/chess_engine/engine/v6/src/movegen.cpp,
    src/chess_engine/engine/v6/src/magic.cpp,
    src/chess_engine/engine/v6/src/tt.cpp,
    src/chess_engine/engine/v6/src/perft.cpp,
    src/chess_engine/engine/v6/src/python_bindings.cpp,
    src/chess_engine/engine/v7/include/engine.hpp,
    src/chess_engine/engine/v7/CMakeLists.txt
  </read_first>
  <behavior>
    - After this task, building V7 succeeds and v7_engine.perft is functional
    - v7_engine.perft("rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1", 5) returns 4865609
    - v7_engine.perft("8/2p5/3p4/KP5r/1R3p1k/8/4P1P1/8 w - - 0 1", 6) returns 11030083 (this is the smallest depth-6 position; in default test runs)
    - For each test position at depth 4 and depth 5, v7_engine.perft(fen, d) == v6_engine.perft(fen, d) when v6_engine is available (parity check via direct comparison, not just literal numbers)
    - Importing both v6_engine and v7_engine in the same Python process does NOT cause symbol collisions (namespace v6 vs namespace v7 isolation)
    - Engine class's TT member can be constructed (new_game() actually clears it now, not just an atomic reset)
  </behavior>
  <action>
    Port V6's data-structure subsystem into the v7 namespace as a wholesale fork. NO LOGIC EDITS — every change is a rename or build-list update.

    Step 1: Fork each V6 source file into the corresponding V7 path. For every file:
    - Replace every `namespace v6` with `namespace v7` (including nested usages and `v6::X` qualified names)
    - Replace every `V6_*` macro (header guards, type aliases) with `V7_*`
    - Replace every comment containing `V6` with `V7`
    - DO NOT modify any algorithm, magic constant, attack table, zobrist random seed, TT entry layout, MoveList capacity, or function body

    Files to fork (V6 → V7):
    - include/types.hpp → include/types.hpp
    - include/board.hpp → include/board.hpp
    - include/movegen.hpp → include/movegen.hpp
    - include/magic.hpp → include/magic.hpp
    - include/tt.hpp → include/tt.hpp
    - src/board.cpp → src/board.cpp
    - src/movegen.cpp → src/movegen.cpp
    - src/magic.cpp → src/magic.cpp
    - src/tt.cpp → src/tt.cpp
    - src/perft.cpp → src/perft.cpp

    Step 2: Extract zobrist into its own header (PATTERNS.md classification: V6 keeps zobrist inline in board.hpp/types.hpp; V7 hoists to include/zobrist.hpp for cleaner plan-03 search includes). Mechanically: locate the zobrist arrays in V6 sources (`zobrist_piece_square[][][]`, `zobrist_side`, `zobrist_castling[]`, `zobrist_ep[]` or whatever names V6 uses — preserve them exactly), declare them in include/zobrist.hpp as extern arrays, and emit definitions inside src/board.cpp (or a new src/zobrist.cpp if the seed initializer logic is non-trivial). If V6 has the values inline in a header, keep that pattern and just rename — only hoist if necessary to avoid multiple-definition link errors.

    Step 3: Update src/chess_engine/engine/v7/include/engine.hpp:
    - Replace the forward declarations `class TT;` and `class Board;` with `#include "tt.hpp"` and `#include "board.hpp"`
    - Replace plan 01's placeholder `int best_move = 0` in SearchResult with `Move best_move = MOVE_NONE` (using the real v7::Move type from types.hpp)
    - Remove the temporary `constexpr int MOVE_NONE = 0;` from engine.hpp (now provided by types.hpp)
    - Add private members `TT tt_;` and `Board board_;` (placement only — plan 03 actually uses them)
    - Implement Engine::new_game() in engine.hpp inline or in a new src/engine.cpp:
      * Reset stop_flag_, tbhits_, nodes_ to 0 (already done in plan 01)
      * Call tt_.clear() (assuming V6 TT exposes clear(); if not, mirror V6's reset pattern)
      * Plan 03 adds repetition-stack clear

    Step 4: Update src/chess_engine/engine/v7/src/python_bindings.cpp:
    - Remove the plan-01 stub `static uint64_t perft_entry(...) { return 0; }`
    - Replace with `#include "perft.hpp"` (if perft.cpp exposes a header) OR forward-declare `uint64_t perft_entry(const std::string&, int);` (matches the pattern V6 uses where perft is in python_bindings.cpp directly)
    - Decision: place `v7::perft_entry` as a free function in src/perft.cpp (cleaner than V6's place-it-in-bindings pattern), declare it via `namespace v7 { uint64_t perft_entry(const std::string& fen, int depth); }` either in a new perft.hpp or inline in python_bindings.cpp. The function body mirrors V6 python_bindings.cpp lines 61-88 verbatim with v6→v7 renames.

    Step 5: Update src/chess_engine/engine/v7/CMakeLists.txt:
    - Replace the `# TODO(plan-02): add board.cpp, movegen.cpp, magic.cpp, tt.cpp here` comment block with explicit source entries:
      * src/board.cpp, src/movegen.cpp, src/magic.cpp, src/tt.cpp, src/perft.cpp (added to the V7_SOURCES variable AND to the v7_uci target_sources call)
    - Leave the `# TODO(plan-03): add search.cpp`, `# TODO(plan-04): add eval.cpp + generated src/coeffs.cpp`, `# TODO(plan-05): add syzygy.cpp + Fathom subdirectory` markers intact

    Constraint reminders:
    - Per PATTERNS.md "V7 C++ headers / impl files forked verbatim from V6" section: NO logic edits. If V6's code has a known bug (e.g., search.cpp's mate-TT handling — not in this plan's scope), preserve it; plan 03 audits and fixes search. Plan 02 is a pure fork.
    - Per RESEARCH.md §A2 Gotchas: V6 perft re-FENs every node (slow but deterministic). KEEP this — depth-6 Kiwipete takes minutes single-threaded; that's the cost of bit-exact parity. Slow tests are opt-in via @pytest.mark.slow.
    - Per RESEARCH.md §A2 Gotchas: magic table initialization at module load matters. Match V6's static-constructor pattern. If V6 inits magic via `ensure_init()` called from python_bindings.cpp, V7 mirrors.
  </action>
  <verify>
    <automated>python3 -m uv run --extra build python -m chess_engine.engine.v7.native_build 2>&1 | tail -20</automated>
    <automated>python3 -c "from chess_engine.engine.v7 import chess_algorithm as v7; m = v7.ensure_available(auto_build=True); n = m.perft('rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1', 5); assert n == 4865609, n; print('startpos d5 OK:', n)"</automated>
    <automated>python3 -c "from chess_engine.engine.v7 import chess_algorithm as v7; m = v7.ensure_available(auto_build=True); n = m.perft('8/2p5/3p4/KP5r/1R3p1k/8/4P1P1/8 w - - 0 1', 6); assert n == 11030083, n; print('position3 d6 OK:', n)"</automated>
    <automated>grep -c '^src/board\.cpp\|src/board\.cpp$' src/chess_engine/engine/v7/CMakeLists.txt</automated>
    <automated>grep -rn 'namespace v6' src/chess_engine/engine/v7/ | grep -v '^Binary'</automated>
  </verify>
  <acceptance_criteria>
    - All files listed in &lt;files&gt; exist with v7:: namespace and V7_* macros (no v6/V6 references in V7 sources — grep verifies)
    - Verify command 5 (grep for `namespace v6` in v7/) returns NO matches (any match is a missed rename)
    - CMake build succeeds; v7_engine module loads in Python (verify command 1 ends without error)
    - Verify command 2 (startpos depth-5 perft) returns exactly 4865609
    - Verify command 3 (position 3 depth-6 perft) returns exactly 11030083
    - tests/test_v7_perft.py task-2 below introduces the full corpus; verify commands 2-3 give early signal during task 1
    - CMakeLists.txt V7_SOURCES variable contains literal entries: src/board.cpp, src/movegen.cpp, src/magic.cpp, src/tt.cpp, src/perft.cpp, src/python_bindings.cpp (perft entries verified by command 4)
    - engine.hpp includes "tt.hpp" and "board.hpp" (verify with grep) and declares `TT tt_` and `Board board_` private members
    - Plan 01's placeholder `constexpr int MOVE_NONE = 0;` is REMOVED from engine.hpp (the real MOVE_NONE lives in types.hpp)
    - python_bindings.cpp no longer contains the plan-01 stub `return 0;` in perft_entry
    - V1-V6 directories untouched (`git diff --name-only -- src/chess_engine/engine/ | grep -v '^src/chess_engine/engine/v7/'` returns empty)
    - Both v6_engine and v7_engine can be imported in the same Python process without error: `python3 -c "import v6_engine, v7_engine; print('coexist OK')"` (only if V6 was already built)
  </acceptance_criteria>
  <done>V7 board/movegen/magic/zobrist/tt/perft sources are forked into v7:: namespace; CMake build green; engine.hpp wired to real TT/Board; perft returns canonical counts for startpos d5 and position 3 d6; V1-V6 still load alongside V7.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Perft parity test suite — full 5-position corpus, depth 4/5 default + depth 6 slow, with V6 cross-check</name>
  <files>
    tests/test_v7_perft.py
  </files>
  <read_first>
    tests/test_v6_native.py,
    tests/test_v7_engine.py,
    src/chess_engine/engine/v7/chess_algorithm.py,
    .planning/phases/01-skeleton-smoke/01-RESEARCH.md
  </read_first>
  <behavior>
    - tests/test_v7_perft.py exposes one test function per (position, depth) pair: 5 positions × {d4, d5} = 10 fast tests + 5 positions × {d6} = 5 slow tests
    - Each test asserts v7_engine.perft(fen, depth) == EXPECTED[position][depth] (literal node count from RESEARCH.md §A2)
    - Each test ALSO asserts v7 == v6 when v6_engine is importable (parametric @pytest.mark.parametrize with both engines via fixtures); if v6 is not built, this assertion is skipped (not failed)
    - Slow tests are marked @pytest.mark.slow; default `pytest -q` excludes them (project pytest config does not register markers by default — handle by checking marker registration and using filterwarnings if needed; otherwise skip via env var GSD_RUN_SLOW_TESTS)
    - Quick run command `python3 -m uv run --group dev pytest tests/test_v7_perft.py -q -m "not slow"` completes in < 60 seconds on the executor's hardware
  </behavior>
  <action>
    Create tests/test_v7_perft.py with the full perft corpus per RESEARCH.md §A2 lines 299-309.

    Structure:
    - Module-level constant PERFT_CORPUS: list of tuples (name, fen, {depth: expected_nodes}). Use the 5 positions with depths 4, 5, 6 from RESEARCH.md. Depth-4 expected values must be cross-checked against V6 perft output on the executor's machine BEFORE locking the test fixtures (per RESEARCH.md Assumption A2). For depth 4, if V6 is not available, compute V7's value and accept it as the seed but flag the test as `@pytest.mark.xfail(reason="depth-4 value not cross-checked")` until next CI run with V6 — better: precompute depth-4 by running V6 perft once and embed the literal value.

      Literal node counts (depths 5 and 6 from RESEARCH.md §A2 table — must use exactly):
      - Starting: d5=4865609, d6=119060324
      - Kiwipete: d5=193690690, d6=8031647685
      - Position3: d5=674624, d6=11030083
      - Position4: d5=15833292, d6=706045033
      - Position5: d5=89941194, d6=3048196529

      For depth 4 values, the executor should run V6's perft once during test setup to derive the truth values; cache them in the test module via a module-level dict populated lazily. If V6 is unavailable, the depth-4 test instead skips with reason "v6 unavailable for cross-check".

    - Fixture v7_native_engine: import from test_v7_engine (or duplicate the V6-style local fixture — mirror tests/test_v6_native.py pattern). The fixture skips the test if V7 native module is unavailable.

    - Fixture v6_native_engine (optional): try to import v6 chess_algorithm and call ensure_available(auto_build=False); if it fails, return None. Used for parity cross-check.

    - Test functions: 5 functions per depth (d4, d5, d6) instead of one parametrized one — keeps the test names self-documenting per V6 test_v6_native.py style. Naming: test_perft_<position>_d<N>. The depth-6 tests get @pytest.mark.slow.

    - Each test:
      1. Calls v7_engine.perft(FEN, DEPTH); asserts equality to literal expected count
      2. If v6_native_engine fixture is not None, also asserts v7 == v6_engine.perft(FEN, DEPTH); this catches the case where the literal in the test happens to match both v6 and v7 having the same bug

    Slow-mark handling: the project does not configure pytest markers by default (per CLAUDE.md "no linter/formatter/type-checker"). Use `pytest.importorskip` or environment-variable gating: `if os.environ.get("GSD_RUN_SLOW_TESTS") != "1": pytestmark.skip(...)` — applied per-test, not module-level. Alternative: use `@pytest.mark.skip(reason="slow")` and document that the executor must remove the skip for nightly perft runs. Choose env-var gating; document the env var at the top of the test file.

    Per-task verify map row (append to .planning/phases/01-skeleton-smoke/01-VALIDATION.md):
    - Task | Plan | Wave | Requirement | Test command
    - 02-T2 | 02 | 2 | FOUND-06 | `python3 -m uv run --group dev pytest tests/test_v7_perft.py -q -m "not slow"`
  </action>
  <verify>
    <automated>python3 -m uv run --group dev pytest tests/test_v7_perft.py -q --collect-only</automated>
    <automated>python3 -m uv run --group dev pytest tests/test_v7_perft.py::test_perft_starting_d5 tests/test_v7_perft.py::test_perft_position3_d5 -q</automated>
    <automated>GSD_RUN_SLOW_TESTS=1 python3 -m uv run --group dev pytest tests/test_v7_perft.py::test_perft_position3_d6 -q</automated>
    <automated>grep -c 'def test_perft_' tests/test_v7_perft.py</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_v7_perft.py exists and is importable
    - Verify command 1 collects exactly 15 tests (5 positions × 3 depths)
    - Verify command 4 (grep `def test_perft_`) returns 15
    - Verify command 2 (startpos d5 + position3 d5) passes
    - Verify command 3 (position3 d6 with slow gate flipped on) passes — proves slow tests are actually exercisable
    - All 10 default tests (depths 4 and 5) green: `python3 -m uv run --group dev pytest tests/test_v7_perft.py -q -m "not slow"` exits 0
    - Each depth-5 and depth-6 test contains the exact literal node count from RESEARCH.md §A2 (verify via `grep -n '4865609\|119060324\|193690690\|8031647685\|674624\|11030083\|15833292\|706045033\|89941194\|3048196529' tests/test_v7_perft.py | wc -l` returns 10 — all 10 depth-5+6 values present)
    - Slow tests gated on `os.environ.get("GSD_RUN_SLOW_TESTS") == "1"` (verify with grep)
    - When v6_engine is importable, parity assertions (`assert v7_count == v6_count`) execute; when v6 is not built, the parity check is skipped (not failed)
    - .planning/phases/01-skeleton-smoke/01-VALIDATION.md "Per-Task Verification Map" table has a new row referencing this task and command
  </acceptance_criteria>
  <done>tests/test_v7_perft.py exists with 15 tests; 10 default-fast pass; slow gate verified working; depth-5 and depth-6 literal counts match RESEARCH.md §A2; V6 cross-parity executes when V6 is available; FOUND-06 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → C++ Board::from_fen | Caller-controlled FEN string parsed in C++; same surface as V6. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | v7::Board::from_fen | mitigate | Inherited V6 parser — same validation as V6. Plan 06's smoke test exercises only well-formed FENs from GameManager. Plan 03's search adds bounds checks on ply/depth. |
| T-02-02 | Denial of Service | v7::perft at high depth | accept | Same surface as V6; v7_engine.perft(fen, 8) on Kiwipete takes hours but is single-threaded and CPU-bound. Local desktop tool — not network exposed. |
| T-02-03 | Denial of Service | Repetition-stack array overflow in long games | accept | Plan 03 wires the rep stack; sizes it to MAX_PLY + 100 per V6 pattern. Documented for plan 03. |
</threat_model>

<verification>
- v7_engine.perft returns canonical counts for all 5 positions at depths 4, 5, 6 (10 fast + 5 slow tests green)
- v7_engine.perft equals v6_engine.perft for all overlapping (fen, depth) pairs when V6 is built
- v6_engine and v7_engine coexist in the same Python process without symbol collision
- CMake build green; v7_engine.{pyd,so,dylib} produced
- engine.hpp now includes board.hpp and tt.hpp; Engine class has real TT and Board members
- No V1-V6 file modified
</verification>

<success_criteria>
- FOUND-06: V7 perft matches V6 perft on Kiwipete + position 3 + position 4 + starting position + position 5 to depth 6 (depth-6 verified via opt-in slow tests; default fast tests cover depth 5)
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton-smoke/01-02-SUMMARY.md` documenting:
- Files forked from V6 with verbatim namespace rename
- Engine.hpp updated: real TT and Board members
- perft is now operational; bindings-side stub from plan 01 removed
- CMakeLists.txt now lists all A2 sources; only B1/B2/B3 TODOs remain
- Plans 03/04/05 unblocked — they can include board.hpp / movegen.hpp / magic.hpp / tt.hpp
- Any V6 vs V7 perft discrepancies discovered during cross-check (if found, escalate via /gsd-discuss-phase as a Phase 1 blocker; perft mismatch means search will produce illegal moves)
</output>
