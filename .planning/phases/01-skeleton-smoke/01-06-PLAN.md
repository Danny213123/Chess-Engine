---
phase: 01-skeleton-smoke
plan: 06
type: execute
wave: 4
depends_on: [01-03, 01-04, 01-05]
files_modified:
  - src/chess_engine/server/game_manager.py
  - client/src/App.jsx
  - cli/src/config.js
  - cli/src/index.js
  - .chess-engine.example.json
  - src/chess_engine/engine/v7/src/uci_main.cpp
  - tests/test_v7_engine.py    # MODIFIED — Plan 01 created this; Plan 06 appends new tests using the existing v7_native_engine fixture
autonomous: false
requirements:
  - INT-01
  - INT-02
  - INT-03
  - INT-04
  - INT-05
  - INT-07
  - INT-08
  - INT-09
  - TB-09
  - FOUND-07

must_haves:
  truths:
    - "GameManager dispatches V7 in all three branches: AVAILABLE_ENGINES set, set_engine_version ladder, ai_move ladder (INT-01)"
    - "GameManager.stop_search() also calls algo_v7.stop_engine() when V7 is active — closing the FOUND-04 cancellation loop end-to-end through GameManager (validates plan 01's atomic stop_flag_ + plan 03's external_stop wiring via the GameManager pathway)"
    - "The V2-REF-01 string-dispatch ladder anti-pattern is preserved — Phase 1 does NOT refactor the engine ladder (CONTEXT defers this work)"
    - "React App.jsx dropdowns (white AND black) contain a V7 option placed ABOVE V6 per existing V5d→V5c→V5b ordering convention (INT-02)"
    - "App.jsx build-warning copy (around line 235) extends the V6 branch to also fire on `ver === 'v7'` (INT-03)"
    - "cli/src/config.js DEFAULT_CONFIG adds three keys with exact names and types: v7Built (bool false), syzygyPath (string|null, null), syzygyMaxPieces (number, 6) (INT-04)"
    - "cli/src/index.js exposes `chess-engine build v7` mirroring buildV6() shape (INT-05); subcommand invokes `uv run --extra build python -m chess_engine.engine.v7.native_build`"
    - "cli/src/index.js exposes `chess-engine syzygy download` writing to config.syzygyPath if set, else OS-specific default (Windows: %LOCALAPPDATA%/chess-engine/syzygy/; Linux: $XDG_DATA_HOME/chess-engine/syzygy/ or ~/.local/share/chess-engine/syzygy/; macOS: ~/Library/Application Support/chess-engine/syzygy/) (INT-05, TB-09)"
    - "tests/test_v7_engine.py is EXTENDED (not created — Plan 01 created the file with test_auto_build, test_gm_dispatch, test_smoke_game_v7_vs_v6 and the v7_native_engine module-scope fixture). Plan 06 APPENDS test_cancellation_via_gm (FOUND-04 end-to-end) and test_nps_sentinel (NPS within ~20% of V6 per CLAUDE.md, gated on RUN_BENCHMARKS=1) to the same file, using the existing v7_native_engine fixture"
    - "test_nps_sentinel is marked `@pytest.mark.benchmark`, gated on the `RUN_BENCHMARKS=1` environment variable, runs single-threaded on a fixed startpos search; default `pytest` invocations SKIP it. Documented host requirements: single-threaded build, fixed startpos, identical depth between V6 and V7 measurements; cites D-04 (smoke-scope: NPS in scope for Phase 1 sanity, full perf in Phase 2) and CLAUDE.md (~20% NPS constraint)"
    - "Existing V1-V6 pytest suite green AFTER all V7 changes land (INT-08 — regression invariant)"
    - "v7_uci standalone binary responds to `uci`, `isready`, `position startpos`, `go depth N`, `bestmove ...`, `stop`, `quit` per a minimal subset sufficient for fastchess (FOUND-07; full UCI replaces plan 01's stub)"
    - "Phase 1 D-04 smoke milestone is demonstrably reached: a single legal game V7-as-white vs V6 completes with no crashes, no illegal moves, no unhandled exceptions"
  artifacts:
    - path: "src/chess_engine/server/game_manager.py"
      provides: "Surgical V7 dispatch added in 3 places (AVAILABLE_ENGINES, set_engine_version, ai_move) + 1 stop_search enhancement; engine-ladder pattern preserved per V2-REF-01 deferral"
      contains: "v7|algo_v7|V7"
    - path: "client/src/App.jsx"
      provides: "V7 option added to white + black engine dropdowns above V6; build-warning copy extended for V7"
      contains: "v7|V7"
    - path: "cli/src/config.js"
      provides: "DEFAULT_CONFIG augmented with v7Built (bool), syzygyPath (null), syzygyMaxPieces (6)"
      contains: "v7Built|syzygyPath|syzygyMaxPieces"
    - path: "cli/src/index.js"
      provides: "`build v7` subcommand mirroring buildV6() (invokes native_build via uv); `syzygy download` subcommand fetching 3-4-5 men tables with OS-specific default destination"
      contains: "build.*v7|syzygy.*download"
    - path: "src/chess_engine/engine/v7/src/uci_main.cpp"
      provides: "Full minimal UCI loop for v7_uci binary: handles uci/isready/position/go depth/stop/quit; sufficient for fastchess (Phase 2 strength testing); REPLACES plan 01's stub"
    - path: "tests/test_v7_engine.py"
      provides: "EXTENDED by Plan 06 (Plan 01 created the file). New appended tests: test_cancellation_via_gm (FOUND-04 end-to-end) + test_nps_sentinel (CLAUDE.md ~20% NPS constraint, @pytest.mark.benchmark + RUN_BENCHMARKS=1 gated, default pytest skips). Reuses Plan 01's v7_native_engine module-scope fixture so the V7 build is amortized across all tests in the file"
  key_links:
    - from: "src/chess_engine/server/game_manager.py"
      to: "src/chess_engine/engine/v7/chess_algorithm.py"
      via: "import chess_algorithm as algo_v7 inside the ladder branches (lazy import — keeps GameManager startup cheap when V7 not selected)"
      pattern: "from chess_engine.engine.v7 import chess_algorithm"
    - from: "src/chess_engine/server/game_manager.py (stop_search)"
      to: "v7::Engine::stop() (C++ atomic)"
      via: "algo_v7.stop_engine() → chess_algorithm.stop_engine() → _engine.stop() → C++ stop_flag_.store(true) (no GIL needed because stop_flag_ is std::atomic; py::call_guard NOT applied to .stop per plan 01)"
      pattern: "algo_v7\\.stop_engine"
    - from: "client/src/App.jsx"
      to: "Server's /api/set_engine endpoint"
      via: "dropdown onChange → axios POST to /api/set_engine with body {version: 'v7', color: 'white'} → server invokes GameManager.set_engine_version('v7', 'white') → INT-01 ladder hits the algo_v7 import + ensure_available branch"
      pattern: "v7"
    - from: "cli/src/index.js `build v7`"
      to: "src/chess_engine/engine/v7/native_build.py"
      via: "subprocess: `uv run --extra build python -m chess_engine.engine.v7.native_build` (mirrors buildV6 line 148)"
      pattern: "native_build"
    - from: "tests/test_v7_engine.py::test_cancellation_via_gm (appended by Plan 06)"
      to: "GameManager.stop_search() → algo_v7.stop_engine() → C++ atomic stop_flag_"
      via: "Threads off ai_move with deep depth, calls gm.stop_search() within 100ms, asserts return within 500ms with non-None move (best-so-far) — closes FOUND-04 end-to-end through the GameManager pathway"
      pattern: "stop_search.*algo_v7"
    - from: "tests/test_v7_engine.py::test_nps_sentinel (appended by Plan 06)"
      to: "v6.find_best_move + v7.find_best_move (apples-to-apples NPS comparison)"
      via: "Default-skip via RUN_BENCHMARKS env gate; when RUN_BENCHMARKS=1 set, runs single-threaded fixed-startpos identical-depth measurements and asserts v7_nps >= 0.8 * v6_nps (CLAUDE.md ~20% constraint)"
      pattern: "RUN_BENCHMARKS|nps"
---

<objective>
Wire V7 end-to-end through the existing GameManager / React UI / Node CLI surfaces with surgical edits (INT-01..05), implement the full v7_uci UCI loop (FOUND-07 — replaces plan 01's stub), add the optional Syzygy download CLI subcommand (TB-09), and prove the D-04 smoke milestone with tests EXTENDED into Plan 01's existing tests/test_v7_engine.py: V7-as-white plays one legal game vs V6 at fixed depth through the GameManager — no crashes, no illegal moves, no unhandled exceptions, cancellation works mid-search (FOUND-04 end-to-end), V1-V6 regression suite stays green (INT-08), NPS stays within ~20% of V6 (CLAUDE.md constraint, default-skip benchmark).

Purpose: This is the C1 integration phase that turns the parallel B1/B2/B3 work into a shippable user-visible feature. The engine-ladder dispatch anti-pattern is preserved per V2-REF-01 deferral — Phase 1 does NOT refactor; it adds V7 to the existing string-match ladder in the minimum-touch way. Closes Phase 1's "single legal game vs V6 through the React UI" acceptance criterion (CONTEXT D-04). The cancellation test ALSO validates the SearchInfo cancellation pipeline end-to-end: GameManager.stop_search → algo_v7.stop_engine → C++ atomic stop_flag_ → search-thread polls and returns best-so-far. If any link in that chain is broken, the cancellation test fails — which is exactly the FOUND-04 / V6-bug closure invariant.

Wave 4 (after wave 3's plans 03/04/05): depends_on = [01-03 (search/engine), 01-04 (eval), 01-05 (syzygy)]. The wave-collapse design (Plan 02 Task 3 pre-staged CMakeLists.txt with empty .cpp stubs) means plans 03/04/05 ran in parallel without a CMakeLists.txt conflict; Plan 06 picks up after all three complete because the smoke game requires search + eval + syzygy all working.

Output: GameManager + React UI + Node CLI all V7-aware; v7_uci binary speaks enough UCI for fastchess (Phase 2); tests/test_v7_engine.py EXTENDED with cancellation + NPS-sentinel coverage on top of Plan 01's existing tests; Phase 1 acceptance criteria fully satisfied; ready for Phase 2 strength-gate work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-skeleton-smoke/01-CONTEXT.md
@.planning/phases/01-skeleton-smoke/01-RESEARCH.md
@.planning/phases/01-skeleton-smoke/01-PATTERNS.md
@.planning/phases/01-skeleton-smoke/01-01-SUMMARY.md
@.planning/phases/01-skeleton-smoke/01-02-SUMMARY.md
@.planning/phases/01-skeleton-smoke/01-03-SUMMARY.md
@.planning/phases/01-skeleton-smoke/01-04-SUMMARY.md
@.planning/phases/01-skeleton-smoke/01-05-SUMMARY.md

# Files to patch (verified locations from RESEARCH.md §C1):
@src/chess_engine/server/game_manager.py
@client/src/App.jsx
@cli/src/config.js
@cli/src/index.js
@.chess-engine.example.json

# V7 adapter (from plan 01) the GameManager imports:
@src/chess_engine/engine/v7/chess_algorithm.py

# V6 reference patterns (mirror their shape):
@src/chess_engine/engine/v6/chess_algorithm.py
@src/chess_engine/engine/v6/native_build.py

# Plan 01's uci_main.cpp stub (will be expanded in this plan):
@src/chess_engine/engine/v7/src/uci_main.cpp

# Plan 01's existing tests/test_v7_engine.py (Plan 06 EXTENDS it — does NOT recreate):
@tests/test_v7_engine.py

# Existing tests (INT-08 regression baseline):
@tests/

<interfaces>
<!-- INT-01: GameManager 4-point edit (RESEARCH.md §C1 lines 724-752) -->
<!-- Exact line numbers approximate; identify by structural anchors (AVAILABLE_ENGINES set, set_engine_version method, ai_move method, stop_search method) -->

  # game_manager.py — Point 1: AVAILABLE_ENGINES set (add "v7")
  AVAILABLE_ENGINES = {"human", "v3", "v4b", "v4c", "v5", "v5b", "v5c", "v5d", "v6", "v7"}

  # Point 2: set_engine_version ladder — add v7 branch (after v6 branch)
  elif version == "v7":
      from chess_engine.engine.v7 import chess_algorithm as algo_v7
      algo_v7.ensure_available(auto_build=True)

  # Point 3: ai_move ladder — add v7 dispatch (after v6 branch)
  elif self.engine_version == "v7":
      from chess_engine.engine.v7 import chess_algorithm as algo_v7
      move, stats = algo_v7.find_best_move(
          self.game_state, valid_moves, "v7", self.current_search_info)

  # Point 4: stop_search enhancement — also flip V7's C++ atomic
  def stop_search(self):
      if self.current_search_info:
          self.current_search_info.stopped = True
      if self.engine_version == "v7":
          try:
              from chess_engine.engine.v7 import chess_algorithm as algo_v7
              if getattr(algo_v7, "V7_AVAILABLE", False):
                  algo_v7.stop_engine()   # closes FOUND-04 end-to-end
          except Exception:
              pass   # never let cancellation throw

<!-- INT-02: React App.jsx dropdowns (lines 408, 423 area) -->
  // White dropdown — add ABOVE V6 (newer = higher per V5d→V5c→V5b ordering)
  <option value="v7">White: V7 (C++)</option>
  <option value="v6">White: V6 (C++)</option>

  // Black dropdown — same option
  <option value="v7">Black: V7 (C++)</option>

<!-- INT-03: App.jsx build-warning copy (line 235 area) -->
  if (ver === "v6" || ver === "v7") {
    const message = `Preparing ${ver.toUpperCase()} native engine; first use may build locally.`;
    setEngineStatus(message);
    log(message);
  }

<!-- INT-04: cli/src/config.js DEFAULT_CONFIG additions -->
  const DEFAULT_CONFIG = {
    threads: null,
    ttSize: 64,
    maxMemory: 512,
    timeLimit: 5000,
    v6Built: false,
    v7Built: false,                    // INT-04
    syzygyPath: null,                  // INT-04
    syzygyMaxPieces: 6,                // INT-04
    lastBuildTime: null,
    cmakePath: null,
  };

<!-- INT-05 / TB-09: `syzygy download` OS-specific default paths (RESEARCH.md §B3 line 929 + §C1 line 787) -->
  // On Windows: process.env.LOCALAPPDATA + '\\chess-engine\\syzygy\\'
  // On Linux:   process.env.XDG_DATA_HOME || (homedir + '/.local/share') + '/chess-engine/syzygy/'
  // On macOS:   homedir + '/Library/Application Support/chess-engine/syzygy/'
  // If config.syzygyPath is non-null, use it. Otherwise use the OS default above.

<!-- v7_uci minimal UCI command surface (FOUND-07) — sufficient for fastchess Phase 2 -->
  uci                              -> id name V7\nid author chess-engine\nuciok
  isready                          -> readyok
  ucinewgame                       -> (clears TT / repetition)
  position startpos [moves m1 m2]  -> set internal board
  position fen <FEN> [moves ...]   -> set internal board from FEN
  go depth N                       -> search to depth N; on completion: bestmove <uci>
  go movetime MS                   -> search for MS ms; on completion: bestmove <uci>
  stop                             -> flip atomic stop_flag_; current search returns best-so-far + emits bestmove
  quit                             -> exit(0)
  (other commands)                 -> ignore silently (do NOT respond to setoption / debug / register in Phase 1)

<!-- Plan 01's existing test fixture and tests in tests/test_v7_engine.py — Plan 06 reuses this fixture verbatim -->

  # Existing in tests/test_v7_engine.py (created by Plan 01):
  @pytest.fixture(scope="module")
  def v7_native_engine():
      from chess_engine.engine.v7 import chess_algorithm as v7_algo
      v7_algo.ensure_available(auto_build=True)
      return v7_algo

  # Existing tests (Plan 01): test_auto_build, test_gm_dispatch, test_smoke_game_v7_vs_v6
  # Plan 06 APPENDS to this file: test_cancellation_via_gm, test_nps_sentinel
  # Plan 06 does NOT redefine the fixture; it imports/uses v7_native_engine by name.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Surgical INT-01..05 wiring (GameManager + React App.jsx + CLI config.js + CLI index.js); implement full v7_uci UCI loop replacing plan-01 stub</name>
  <files>
    src/chess_engine/server/game_manager.py,
    client/src/App.jsx,
    cli/src/config.js,
    cli/src/index.js,
    .chess-engine.example.json,
    src/chess_engine/engine/v7/src/uci_main.cpp
  </files>
  <read_first>
    src/chess_engine/server/game_manager.py,
    src/chess_engine/engine/v6/chess_algorithm.py,
    src/chess_engine/engine/v7/chess_algorithm.py,
    client/src/App.jsx,
    cli/src/config.js,
    cli/src/index.js,
    .chess-engine.example.json,
    src/chess_engine/engine/v7/src/uci_main.cpp,
    src/chess_engine/engine/v7/include/engine.hpp
  </read_first>
  <behavior>
    - GameManager.AVAILABLE_ENGINES contains "v7" after the edit
    - GameManager.set_engine_version("v7", "white") triggers `algo_v7.ensure_available(auto_build=True)` (verified by mocking ensure_available + asserting call)
    - GameManager.ai_move with engine_version == "v7" calls `algo_v7.find_best_move(self.game_state, valid_moves, "v7", self.current_search_info)`
    - GameManager.stop_search() with V7 active calls `algo_v7.stop_engine()` AND does not raise if V7 is unavailable (try/except wrapper)
    - V1-V6 dispatch branches in GameManager are byte-identical to pre-edit state (regression-safe — verify via line-count comparison or AST diff)
    - React App.jsx contains exactly one `<option value="v7">` in EACH of the two dropdowns (white + black) AND V7 appears in the source BEFORE V6 (file-order proxy for visual ordering)
    - App.jsx build-warning branch matches `(ver === "v6" || ver === "v7")` literal
    - cli/src/config.js DEFAULT_CONFIG has exactly the three new keys with exact names + types specified
    - cli/src/index.js `build v7` subcommand exists; running `node cli/bin/chess-engine.js build v7 --help` (or similar dry-run) exits 0
    - cli/src/index.js `syzygy download` subcommand exists; running with --help or --dry-run exits 0
    - cli/src/index.js syzygy download default-path resolution: on Linux uses `${XDG_DATA_HOME:-~/.local/share}/chess-engine/syzygy/`; on Windows uses `%LOCALAPPDATA%\chess-engine\syzygy\`; on macOS uses `~/Library/Application Support/chess-engine/syzygy/`
    - .chess-engine.example.json mirrors DEFAULT_CONFIG (template stays in sync)
    - v7_uci binary: `echo "uci\nquit" | ./v7_uci` outputs `id name V7\n...uciok\n` and exits 0
    - v7_uci handles `position startpos\ngo depth 4\nquit` cleanly: emits a `bestmove <uci>` line then exits
  </behavior>
  <action>
    Step 1 — Edit src/chess_engine/server/game_manager.py (4-point surgical patch):
    - Read the file fully to identify the exact line locations of AVAILABLE_ENGINES, set_engine_version, ai_move, and stop_search (RESEARCH.md cites approximate lines 13/41/68/286 but anchor by structural pattern, not line number)
    - Point 1 (AVAILABLE_ENGINES): add `"v7"` to the set literal, keeping the existing engines alphabetically/ordering-wise grouped with v6
    - Point 2 (set_engine_version): after the `elif version == "v6":` branch (which itself does the V6 ensure_available), add a parallel `elif version == "v7":` branch importing `from chess_engine.engine.v7 import chess_algorithm as algo_v7` and calling `algo_v7.ensure_available(auto_build=True)`. The import is intentionally INSIDE the branch (lazy import — keeps server startup cheap when V7 isn't selected; mirrors V6 pattern)
    - Point 3 (ai_move): after the `elif self.engine_version == "v6":` branch, add a parallel `elif self.engine_version == "v7":` branch importing algo_v7 and calling `algo_v7.find_best_move(self.game_state, valid_moves, "v7", self.current_search_info)`. The 4th positional arg `self.current_search_info` is the FOUND-04 wiring — V6's call is the same pattern; the V7 adapter is what actually uses it (closing the V6 bug)
    - Point 4 (stop_search): extend the existing stop_search method body. After the existing `self.current_search_info.stopped = True` line (or whatever sets the Python-side flag), add a guarded block calling `algo_v7.stop_engine()` per the interfaces block. The try/except is critical — stop_search MUST NEVER throw because it's called from request-handler paths where an exception aborts the HTTP response
    - Do NOT modify the V1-V6 dispatch branches at all (INT-08 regression invariant). After editing, scan the diff to confirm only ADDITIONS happen — no V1-V6 line was reordered or modified
    - V2-REF-01 deferral note: do NOT collapse the ladder into a dispatch dict, do NOT introduce a strategy pattern, do NOT extract a base class. Phase 1 ADDS V7 to the existing ladder; refactoring is explicitly deferred per CONTEXT

    Step 2 — Edit client/src/App.jsx:
    - Read App.jsx and locate the two `<select>` dropdowns (the file has a white-engine select and a black-engine select; RESEARCH cites lines 408 and 423 but identify by surrounding context: white dropdown has options for white engines, black dropdown for black)
    - In each dropdown, add `<option value="v7">{Color}: V7 (C++)</option>` IMMEDIATELY BEFORE the existing `<option value="v6">{Color}: V6 (C++)</option>` line (V7 above V6 — newer = higher; matches existing V5d→V5c→V5b convention per PATTERNS.md)
    - Locate the build-warning block (cited ~line 235). It currently has `if (ver === "v6")` or similar. Extend the condition to `if (ver === "v6" || ver === "v7")` and use template-literal copy: `Preparing ${ver.toUpperCase()} native engine; first use may build locally.`
    - No CSS changes; no new components; no API client changes (existing /api/set_engine path already accepts arbitrary engine strings — the server-side allowlist is in GameManager.AVAILABLE_ENGINES which we just updated)
    - Run `npm run lint --prefix client` after editing to confirm ESLint stays green (no unused imports, no react-hooks violations introduced)

    Step 3 — Edit cli/src/config.js:
    - Read the file; locate the DEFAULT_CONFIG object literal (the current `v6Built: false` line is the anchor)
    - Insert the three new keys per the interfaces block: `v7Built: false`, `syzygyPath: null`, `syzygyMaxPieces: 6`
    - Position: after `v6Built: false,` for grouping; preserve trailing-comma style of the file
    - If the file does any DEFAULT_CONFIG validation / schema enforcement elsewhere, update that too

    Step 4 — Update .chess-engine.example.json (template mirrors DEFAULT_CONFIG):
    - Add the three new keys with the same defaults so users copying the example get the right schema
    - File is gitignored as `.chess-engine.json` per CLAUDE.md; the `.example.json` IS committed

    Step 5 — Edit cli/src/index.js (`build v7` subcommand):
    - Read the file; locate the existing `buildV6()` function (cited line 148) and the subcommand-dispatch table that maps `build v6` → buildV6
    - Create `buildV7()` mirroring buildV6's shape exactly. The body should:
      * Spawn `uv` with args `["run", "--extra", "build", "python", "-m", "chess_engine.engine.v7.native_build"]`
      * Use the same PYTHONPATH env (`process.env.PYTHONPATH = path.join(ROOT_DIR, 'src')` style — confirm by reading buildV6's actual impl)
      * On success, set config.v7Built = true and config.lastBuildTime = Date.now(); save config to .chess-engine.json (mirror buildV6's persistence)
      * On failure, print a clear error message including the uv command that failed
    - Register `build v7` in whatever dispatch / commander / yargs table buildV6 uses
    - Do NOT remove or alter `build v6`

    Step 6 — Add `syzygy download` subcommand to cli/src/index.js (INT-05 / TB-09):
    - New top-level command `syzygy` with subcommand `download`
    - Default destination resolution (TB-09):
      ```js
      function defaultSyzygyPath() {
        if (config.syzygyPath) return config.syzygyPath;
        if (process.platform === 'win32') {
          return path.join(process.env.LOCALAPPDATA || path.join(os.homedir(), 'AppData', 'Local'),
                           'chess-engine', 'syzygy');
        }
        if (process.platform === 'darwin') {
          return path.join(os.homedir(), 'Library', 'Application Support', 'chess-engine', 'syzygy');
        }
        // linux + freebsd default
        const xdg = process.env.XDG_DATA_HOME || path.join(os.homedir(), '.local', 'share');
        return path.join(xdg, 'chess-engine', 'syzygy');
      }
      ```
    - Download source: the 3-4-5 men Syzygy tables (~1GB total). Authoritative public mirror: `https://tablebase.lichess.ovh/tables/standard/3-4-5/` (Lichess hosts a stable mirror). The download fetches all *.rtbw and *.rtbz files in that directory; mkdir -p the destination first
    - Implementation can be HTTP fetch of a published index file OR a hardcoded list of expected filenames for the 3-4-5 men set (the latter is more deterministic; ~290 files; document the list source in a code comment citing chessprogramming.org/Syzygy_Bases). Phase 1 acceptable shortcut: print the URL + destination + instructions to manually `wget`/`curl` if a full implementation is too heavy (PROJECT.md says "optional download script" — script presence is what TB-09 requires, not necessarily a polished implementation). Choose the simpler path; document the choice in the SUMMARY
    - Update config.syzygyPath = defaultSyzygyPath() on successful download so subsequent runs auto-use the location
    - Add `--dry-run` flag that prints the destination and the file count it WOULD download without doing anything (lets tests verify the subcommand wires without actually downloading 1GB)

    Step 7 — Implement full v7_uci UCI loop in src/chess_engine/engine/v7/src/uci_main.cpp (REPLACE plan 01's stub):
    - Read the existing stub; preserve any includes that already work
    - Includes needed: `<iostream>`, `<string>`, `<sstream>`, `<thread>`, `<atomic>`, `"engine.hpp"`, `"board.hpp"`
    - Main loop reads stdin line-by-line. For each line, tokenize on whitespace; dispatch on first token:
      * `uci` → print `id name V7` + `id author chess-engine` + `uciok\n` (flush stdout each command)
      * `isready` → print `readyok\n`
      * `ucinewgame` → call `engine.new_game()` (clears TT + repetition stack — assumes plan 01/02/03 provide new_game; if not, this is the place to add a minimal one)
      * `position startpos [moves m1 m2 ...]` → reset board to startpos; apply each move in UCI notation (e.g., e2e4, g1f3, e7e8q for promotion)
      * `position fen <fen> [moves ...]` → reset board from FEN; apply moves
      * `go depth N` → spawn a thread (or run synchronously since this binary is single-game) calling `engine.search(fen, N, INT_MAX_TIME)`; when search returns, print `bestmove <uci>\n`
      * `go movetime MS` → call `engine.search(fen, INT_MAX_DEPTH, MS)`; print bestmove
      * `go` (no args) → default to `go depth 6` (matches D-04 smoke default)
      * `stop` → call `engine.stop()` (atomic flag flip; safe to call from any thread)
      * `quit` → break loop, return 0
      * Anything else → silently ignore (per Phase 1 scope: do NOT implement setoption / debug / register; FOUND-07 minimal subset)
    - All output flushed via `std::cout << ... << std::endl;` (UCI consumers expect immediate flush after each line)
    - Plan 01's stub had basic uci/isready/quit; preserve that contract and extend
    - Document at the top of the file: `// V7 UCI binary — minimal subset for fastchess (Phase 2). Phase 4 will add setoption Threads/Hash/SyzygyPath.`
    - If the search runs synchronously (no thread), `stop` from the SAME thread can't fire mid-search — for Phase 1 this is acceptable because fastchess runs each engine as a separate process with stdin/stdout, and stop comes via stdin which we won't read until search returns anyway. Document this limitation: "v7_uci runs search synchronously per command; stop is honored at the START of the next command. Sufficient for fastchess depth-fixed games (Phase 2); Phase 4 may add async search via std::thread for go infinite / pondering."
  </action>
  <verify>
    <automated>grep -n '"v7"' src/chess_engine/server/game_manager.py | head -5</automated>
    <automated>grep -nE 'algo_v7|chess_engine\.engine\.v7' src/chess_engine/server/game_manager.py</automated>
    <automated>grep -c 'value="v7"' client/src/App.jsx</automated>
    <automated>grep -nE 'ver === "v6" \|\| ver === "v7"|ver === "v7" \|\| ver === "v6"' client/src/App.jsx</automated>
    <automated>grep -nE 'v7Built|syzygyPath|syzygyMaxPieces' cli/src/config.js</automated>
    <automated>grep -nE 'v7Built|syzygyPath|syzygyMaxPieces' .chess-engine.example.json</automated>
    <automated>grep -nE 'build.*v7|buildV7|syzygy.*download' cli/src/index.js</automated>
    <automated>node cli/bin/chess-engine.js --help 2>&1 | grep -iE 'v7|syzygy' | head -10</automated>
    <automated>printf 'uci\nquit\n' | ./build/v7/v7_uci 2>&1 | head -10</automated>
    <automated>printf 'uci\nposition startpos\ngo depth 3\nquit\n' | timeout 30 ./build/v7/v7_uci 2>&1 | grep -E 'bestmove|uciok|readyok' | head -10</automated>
    <automated>npm run lint --prefix client 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - game_manager.py contains "v7" string at least 4 times (AVAILABLE_ENGINES + set_engine_version + ai_move + stop_search) — verify command 1
    - game_manager.py has at least 3 references to algo_v7 / chess_engine.engine.v7 (one per dispatch point) — verify command 2
    - App.jsx contains exactly 2 `value="v7"` matches (white + black dropdowns) — verify command 3 returns 2
    - App.jsx build-warning branch matches both v6 and v7 (verify command 4)
    - cli/src/config.js DEFAULT_CONFIG has all three new keys (verify command 5 finds each)
    - .chess-engine.example.json mirrors the new keys (verify command 6 finds each)
    - cli/src/index.js exposes `build v7` AND `syzygy download` (verify command 7 returns both)
    - Running `chess-engine --help` shows v7 and syzygy mentions (verify command 8)
    - v7_uci responds to `uci\nquit` (verify command 9 includes `uciok`)
    - v7_uci responds to `position startpos\ngo depth 3` with a bestmove line within 30s (verify command 10)
    - Client ESLint green after edits (verify command 11 — no new errors)
    - V1-V6 dispatch branches in game_manager.py are byte-identical to pre-edit (manual diff check during execution; document in SUMMARY)
    - V7 dropdown options appear BEFORE V6 options in App.jsx source (line-number ordering)
  </acceptance_criteria>
  <done>All INT-01..05 surgical edits applied; v7_uci speaks minimal UCI; CLI build/syzygy subcommands wired; example config mirrors new keys; lint green; V1-V6 dispatch unmodified. Ready for task 2's end-to-end smoke validation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests/test_v7_engine.py (Plan 01 created it; Plan 06 APPENDS test_cancellation_via_gm + test_nps_sentinel reusing the existing v7_native_engine fixture); run full pytest including V1-V6 regression (INT-08)</name>
  <files>
    tests/test_v7_engine.py
  </files>
  <read_first>
    tests/test_v7_engine.py,
    src/chess_engine/engine/v7/chess_algorithm.py,
    src/chess_engine/server/game_manager.py,
    tests/test_v7_bindings.py,
    tests/test_v7_perft.py,
    tests/test_v7_search.py,
    tests/test_v7_eval.py,
    tests/test_v7_syzygy.py,
    .planning/phases/01-skeleton-smoke/01-CONTEXT.md
  </read_first>
  <behavior>
    - tests/test_v7_engine.py is MODIFIED (extended) — NOT created. Plan 01 already created the file with: `v7_native_engine` module-scope fixture, test_auto_build, test_gm_dispatch, test_smoke_game_v7_vs_v6
    - After this task, the file ALSO contains: test_cancellation_via_gm and test_nps_sentinel
    - Both new tests USE the existing `v7_native_engine` fixture (do NOT create a new fixture; do NOT re-import the V7 module — Plan 01's fixture already amortizes the build)
    - test_cancellation_via_gm (FOUND-04 end-to-end): set V7, kick off ai_move on a separate thread with a deep search (depth 30), call gm.stop_search() within 100ms, assert ai_move returns within 500ms with a non-None move (the best-so-far at the moment of cancel) — this validates GameManager.stop_search → algo_v7.stop_engine → C++ atomic → search-thread polls → returns best-so-far
    - test_nps_sentinel: marked `@pytest.mark.benchmark`, gated on `RUN_BENCHMARKS=1` env var (default-skip). When run, executes single-threaded fixed-startpos identical-depth searches on V6 and V7, asserts v7_nps >= 0.8 * v6_nps (within 20% per CLAUDE.md). Skips cleanly when V6 unavailable
    - All existing V1-V6 tests still pass: `pytest tests/ --ignore=tests/test_v7_*.py -q` exits 0 (INT-08)
    - All V7 tests pass at default invocation (NPS sentinel is skipped without RUN_BENCHMARKS=1): `pytest tests/test_v7_*.py -q` exits 0
    - When `RUN_BENCHMARKS=1 pytest tests/test_v7_engine.py::test_nps_sentinel -q` is invoked, the benchmark RUNS and the assertion fires
  </behavior>
  <action>
    Step 1 — EXTEND tests/test_v7_engine.py (do NOT recreate; do NOT redefine the v7_native_engine fixture):

    Read the existing file first (Plan 01 created it). Confirm the existing module-scope `v7_native_engine` fixture and the existing tests (test_auto_build, test_gm_dispatch, test_smoke_game_v7_vs_v6). The two new tests below are APPENDED to the same file and use the same fixture by parameter name.

    If Plan 01's file does NOT yet declare the `pytest.mark.benchmark` marker registration, add a single registration line near the top of the file (after the existing imports):
    ```python
    # Benchmark marker — used by test_nps_sentinel; registered here to silence PytestUnknownMarkWarning
    # Tests marked @pytest.mark.benchmark are SKIPPED unless RUN_BENCHMARKS=1 is set in the environment.
    pytestmark_benchmark_collected = True
    ```
    If a `pytest.ini` / `pyproject.toml [tool.pytest.ini_options]` markers list exists, register `benchmark: long-running benchmark; gated on RUN_BENCHMARKS=1` there instead. The execution-time fix is whatever makes `pytest --strict-markers tests/test_v7_engine.py` pass.

    Append to the file:

    ```python
    import os
    import time
    import threading
    import pytest

    # ----- Plan 06 appends: test_cancellation_via_gm + test_nps_sentinel -----
    # Both tests REUSE Plan 01's existing v7_native_engine module-scope fixture.
    # Do NOT redefine the fixture below — pytest will resolve it by parameter name.

    def test_cancellation_via_gm(v7_native_engine):
        """FOUND-04 end-to-end: GameManager.stop_search → V7 C++ atomic → search returns best-so-far <500ms.

        Validates the full cancellation chain through the GameManager pathway:
            gm.stop_search()
              -> algo_v7.stop_engine()
                -> chess_algorithm.stop_engine()
                  -> _engine.stop()
                    -> C++ stop_flag_.store(true)         # plan 01 atomic
                      -> search-thread polls in alpha_beta # plan 03 SearchInfo::external_stop
                        -> returns best-so-far
        """
        from chess_engine.server.game_manager import GameManager
        gm = GameManager()
        gm.new_game()
        gm.set_engine_version("v7", "white")

        result = {}

        def _search():
            t0 = time.perf_counter()
            move, stats = gm.ai_move()   # default depth from GM/algo_v7 — should be deep enough to take >100ms
            result["move"] = move
            result["elapsed_ms"] = (time.perf_counter() - t0) * 1000.0

        t = threading.Thread(target=_search)
        t.start()
        time.sleep(0.1)         # let search start
        gm.stop_search()
        t.join(timeout=2.0)
        assert not t.is_alive(), "Search thread did not return within 2s of stop_search — FOUND-04 broken"
        assert result.get("move") is not None, "Cancelled search must return best-so-far, not None"
        assert result["elapsed_ms"] < 500, f"Cancellation latency {result['elapsed_ms']:.1f}ms > 500ms"


    @pytest.mark.benchmark
    @pytest.mark.skipif(
        os.environ.get("RUN_BENCHMARKS") != "1",
        reason="NPS sentinel is a benchmark; set RUN_BENCHMARKS=1 to execute it. "
               "Default pytest invocations skip it because measurement requires a quiet, "
               "single-threaded host (background load skews NPS comparisons against the ~20% floor).",
    )
    def test_nps_sentinel(v7_native_engine):
        """CLAUDE.md: V7 NPS must remain within ~20% of V6 NPS on same hardware.

        D-04 smoke-scope: NPS is in scope for Phase 1 sanity (smoke gate); full perf-tuning + perf
        regression CI live in Phase 2 strength-gate work.

        Host requirements (MUST be satisfied for the assertion to be meaningful):
          1. Single-threaded build (engine threads = 1 — no Lazy SMP scheduling jitter)
          2. Fixed startpos search (no opening-book / no per-position variance)
          3. Identical depth between V6 and V7 measurements (apples-to-apples)
          4. Quiet host — no other CPU-bound processes running (tests run with `RUN_BENCHMARKS=1`
             explicitly because background load can sink V7 below the 0.8 floor on its own)
          5. Same Engine instance reused for back-to-back measurement of each engine to amortize
             cache warm-up (first measurement is discarded as warm-up if needed)

        Cites: D-04 (smoke-scope: NPS in scope for Phase 1 sanity); CLAUDE.md ("V7 NPS must remain
        within roughly 20% of V6 NPS on the same hardware so UI responsiveness is preserved").
        """
        try:
            from chess_engine.engine.v6 import chess_algorithm as v6
            v6.ensure_available(auto_build=True)
        except Exception as e:
            pytest.skip(f"V6 unavailable for NPS comparison: {e}")

        from chess_engine.core.game_state import GameState   # confirm exact path at execution time
        gs = GameState()   # startpos — fixed, deterministic

        v7 = v7_native_engine   # reuse Plan 01's module-scope fixture

        def measure(engine_mod, label):
            # Single-threaded (engine default), startpos, identical depth chosen by adapter default
            t0 = time.perf_counter()
            move, stats = engine_mod.find_best_move(gs, gs.get_valid_moves(), label, None)
            elapsed = time.perf_counter() - t0
            nodes = stats.get("nodes", 0)
            return nodes / max(elapsed, 1e-6)

        v6_nps = measure(v6, "v6")
        v7_nps = measure(v7, "v7")
        ratio = v7_nps / max(v6_nps, 1.0)
        assert ratio >= 0.8, (
            f"V7 NPS {v7_nps:.0f} / V6 NPS {v6_nps:.0f} ratio {ratio:.2f} below 0.8 floor "
            f"(CLAUDE.md ~20% NPS constraint). Re-run on a quiet host before treating as a regression."
        )
    ```

    Implementation notes:
    - The deep-search trick in test_cancellation_via_gm assumes GameManager's default search depth (or the V7 adapter's default depth) is enough to take >100ms on startpos. If empirical measurement shows the default finishes faster than 100ms, set the engine's depth via the underlying adapter (`v7_native_engine.set_depth(30)` or whatever the V7 adapter exposes) before kicking off ai_move. If no API exists, the test must construct a SearchInfo + call algo_v7.find_best_move directly with deep depth — document either approach in the SUMMARY.
    - The NPS sentinel exact GameState construction + get_valid_moves API call shape comes from reading the existing V6 test patterns in tests/test_v6_native.py — adapt to match the real call surface.
    - The `v7_native_engine` parameter MUST match the fixture name Plan 01 declared. Re-read Plan 01's tests/test_v7_engine.py fixture definition to confirm the exact name; if Plan 01 used a different name (e.g., `v7_engine` or `v7_module`), use that name in both new tests instead of `v7_native_engine`. Document the actual fixture name in the SUMMARY.

    Step 2 — Append validation map rows to .planning/phases/01-skeleton-smoke/01-VALIDATION.md:
    - INT-01 → test_gm_dispatch (Plan 01 task 1, file existed before Plan 06)
    - INT-02, INT-03 → manual (App.jsx visual; documented in SUMMARY as user-verifiable)
    - INT-04 → grep verify in plan 06 task 1 (no pytest needed for config defaults)
    - INT-05 → `node cli/bin/chess-engine.js build v7 --dry-run` (smoke verify in plan 06 task 1)
    - INT-07 → test_smoke_game_v7_vs_v6 (Plan 01 task 1, file existed before Plan 06)
    - INT-08 → existing V1-V6 pytest run unchanged
    - INT-09 → test_auto_build (Plan 01 task 1, file existed before Plan 06)
    - TB-09 → `node cli/bin/chess-engine.js syzygy download --dry-run` (smoke verify in plan 06 task 1)
    - FOUND-04 (end-to-end via GameManager) → test_cancellation_via_gm (this plan task 2 — APPENDED to Plan 01's file)
    - FOUND-07 → v7_uci binary verify commands in plan 06 task 1
    - NPS constraint → test_nps_sentinel (this plan task 2 — APPENDED, default-skip; runs with RUN_BENCHMARKS=1)

    Step 3 — Run the complete regression + V7 test suite from the working tree:
    - Build V7 fresh: `node cli/bin/chess-engine.js build v7` (or `uv run --extra build python -m chess_engine.engine.v7.native_build`)
    - Run V1-V6 regression: `python3 -m uv run --group dev pytest tests/ --ignore=tests/test_v7_engine.py --ignore=tests/test_v7_bindings.py --ignore=tests/test_v7_perft.py --ignore=tests/test_v7_search.py --ignore=tests/test_v7_eval.py --ignore=tests/test_v7_syzygy.py -q` — MUST exit 0 (INT-08)
    - Run V7 suite at default settings (NPS sentinel SKIPS): `python3 -m uv run --group dev pytest tests/test_v7_*.py -q` — MUST exit 0; the NPS sentinel test is reported as skipped
    - Run NPS sentinel explicitly on a quiet host: `RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest tests/test_v7_engine.py::test_nps_sentinel -q` — MUST exit 0; assertion ratio ≥ 0.8
    - If any test fails, fix the underlying code/test pair (NOT the test alone — fixing the test to match buggy code is the anti-pattern explicitly called out in CLAUDE.md)
  </action>
  <verify>
    <automated>grep -cE 'def test_(auto_build|gm_dispatch|smoke_game_v7_vs_v6|cancellation_via_gm|nps_sentinel)' tests/test_v7_engine.py</automated>
    <automated>grep -nE '@pytest.mark.benchmark|RUN_BENCHMARKS' tests/test_v7_engine.py | head -5</automated>
    <automated>grep -c 'def v7_native_engine' tests/test_v7_engine.py</automated>
    <automated>python3 -m uv run --group dev pytest tests/test_v7_engine.py -q --tb=short</automated>
    <automated>python3 -m uv run --group dev pytest tests/ --ignore=tests/test_v7_engine.py --ignore=tests/test_v7_bindings.py --ignore=tests/test_v7_perft.py --ignore=tests/test_v7_search.py --ignore=tests/test_v7_eval.py --ignore=tests/test_v7_syzygy.py -q --tb=short 2>&1 | tail -20</automated>
    <automated>python3 -m uv run --group dev pytest tests/test_v7_*.py -q --tb=short 2>&1 | tail -20</automated>
    <automated>RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest tests/test_v7_engine.py::test_nps_sentinel -q --tb=short 2>&1 | tail -10</automated>
    <automated>grep -nE 'INT-01|INT-07|INT-09|FOUND-04|RUN_BENCHMARKS' .planning/phases/01-skeleton-smoke/01-VALIDATION.md | head -10</automated>
  </verify>
  <acceptance_criteria>
    - Verify command 1: 5 test functions present in tests/test_v7_engine.py (3 from Plan 01 + 2 appended by Plan 06)
    - Verify command 2: `@pytest.mark.benchmark` marker AND `RUN_BENCHMARKS` env gate present (default-skip discipline for NPS sentinel)
    - Verify command 3: EXACTLY 1 definition of v7_native_engine fixture — proves Plan 06 did NOT redefine it (reuses Plan 01's fixture)
    - Verify command 4: full V7 engine test file passes at default settings (test_nps_sentinel reported as skipped because RUN_BENCHMARKS not set)
    - Verify command 5: V1-V6 regression suite green (INT-08 invariant)
    - Verify command 6: full V7 suite (engine + bindings + perft + search + eval + syzygy) green at default settings
    - Verify command 7: NPS sentinel runs and passes when RUN_BENCHMARKS=1 explicitly set (proves the gate works AND the assertion fires when invoked)
    - Verify command 8: 01-VALIDATION.md has rows for INT-01, INT-07, INT-09, FOUND-04, and a row noting NPS sentinel runs under RUN_BENCHMARKS=1
    - test_cancellation_via_gm asserts elapsed_ms < 500 (FOUND-04 end-to-end latency budget)
    - test_nps_sentinel ratio assertion ≥ 0.8 (or test skips cleanly when RUN_BENCHMARKS not set OR V6 unavailable)
    - No V1-V6 file modified outside server/game_manager.py (which only ADDED v7 branches)
    - 01-VALIDATION.md updated with task-2 row pointers for each INT-* / FOUND-04 / NPS check (NPS row notes the RUN_BENCHMARKS=1 gating + D-04/CLAUDE.md citations)
  </acceptance_criteria>
  <done>tests/test_v7_engine.py extended (NOT recreated): test_cancellation_via_gm + test_nps_sentinel appended using Plan 01's existing v7_native_engine fixture; NPS sentinel default-skips with @pytest.mark.benchmark + RUN_BENCHMARKS=1 gate; documented host requirements; D-04 smoke milestone reached at default pytest invocation; FOUND-04 cancellation works end-to-end through GameManager; V1-V6 regression suite passes (INT-08); when RUN_BENCHMARKS=1 set on a quiet host, NPS sentinel asserts V7 within 20% of V6 (CLAUDE.md constraint); validation map populated. Phase 1 complete and ready for Phase 2 strength-gate work.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human UI smoke verify — V7-as-white vs V6 game through the React UI (D-04 visual confirmation)</name>
  <files>(no file changes — human verification of the running UI)</files>
  <action>Pause execution and present the verification steps in &lt;how-to-verify&gt; to the human. Do not proceed until they reply with the resume-signal.</action>
  <verify>Human inspection per the &lt;how-to-verify&gt; steps below; resume on "approved" signal.</verify>
  <done>Human typed "approved" confirming the D-04 smoke game completed legally through the React UI with no console/network/server errors.</done>
  <what-built>
    - GameManager dispatches V7 (INT-01)
    - React UI exposes V7 in both engine dropdowns above V6 (INT-02)
    - Build-warning copy fires for V7 (INT-03)
    - V7 auto-builds on first selection (INT-09)
    - GameManager.stop_search cancels V7 mid-search (FOUND-04 end-to-end)
    - tests/test_v7_engine.py covers all the above headlessly (Plan 01 + Plan 06 combined), but D-04 explicitly references "through the React UI" — the test suite proves the wiring; this checkpoint confirms the user-visible experience
  </what-built>
  <how-to-verify>
    1. From repo root, build V7 if not already: `node cli/bin/chess-engine.js build v7`
       Expected: build completes successfully; .chess-engine.json now shows `v7Built: true`.

    2. Start the dev stack: `npm run dev` (root) — this should start the FastAPI server (port 8000) and the Vite dev server (port 5173).
       Expected: both servers start cleanly; no V7-related errors in either log.

    3. Open http://localhost:5173 in a browser. Open the browser devtools console BEFORE proceeding (so you can see network errors).
       Expected: chess UI loads with starting position; engine dropdowns visible.

    4. Click the white engine dropdown.
       Expected: "White: V7 (C++)" appears as the TOP entry above "White: V6 (C++)" (INT-02 ordering).
       Select "V7".
       Expected: a transient build-warning message appears (INT-03 copy: "Preparing V7 native engine; first use may build locally.") — IF V7 already built, this message may flash briefly or not at all; either is acceptable as long as no error appears.

    5. Click the black engine dropdown.
       Expected: same V7 above V6 ordering. Select "V6".

    6. Set search depth/time to a small value (default depth 6 is fine for smoke; if you prefer faster, set 4).

    7. Click "Start game" or whatever the existing UI button is to begin AI-vs-AI play.
       Expected: V7 plays white, V6 plays black; moves appear on the board with no crashes, no JS errors in console, no 500 errors in network tab. Game runs to completion (checkmate / stalemate / 50-move / repetition / ply cap) without intervention.

    8. While a search is in progress (e.g., after several moves), click whatever "Stop" or "Cancel" button exists in the UI (if present) OR refresh the page mid-search.
       Expected: search aborts cleanly; if a stop button exists, the next move is returned within 1 second using the best-so-far; if refresh, server log shows no unhandled exception. This validates FOUND-04 end-to-end through the UI surface (the headless test_cancellation_via_gm proves the same wiring).

    9. Verify the network tab shows successful 200 responses from /api/set_engine, /api/ai_move, etc. — no 4xx/5xx for V7-related calls.

    10. If Syzygy tables are installed at ~/.local/share/chess-engine/syzygy/ (or wherever syzygyPath points): server stderr (FastAPI log) shows NO `[v7] syzygy:` warning lines. If tables are NOT installed: stderr shows the appropriate D-08 verbatim line (e.g., `[v7] syzygy: no path configured; tbhits will be 0`). Either outcome is acceptable.

    Acceptance: V7 plays a complete legal game as white vs V6, the UI never errors, console + network tab clean, server logs show only expected D-08 syzygy line (if any). Document the actual game length + final state (checkmate / draw / ply cap reached) in your response.
  </how-to-verify>
  <resume-signal>
    Type "approved" if the smoke game completed legally and the UI showed no errors.
    Type "issues: <description>" with specifics if anything failed (e.g., dropdown ordering wrong, build-warning missing, JS console error, server 500, illegal move accepted, hang) — the executor will fix and resume.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → FastAPI /api/set_engine | Untrusted user-supplied `version` string; GameManager.AVAILABLE_ENGINES is the allowlist that gates ladder dispatch. |
| Node CLI → uv/python subprocess | `build v7` spawns a subprocess; arguments are hardcoded (not user-supplied) so injection surface is low. |
| `syzygy download` → external HTTP fetch (tablebase.lichess.ovh) | Network egress to a known mirror; downloads .rtbw/.rtbz files of substantial size (~1GB). |
| v7_uci → stdin | UCI protocol consumer (fastchess) sends command lines; parsing must reject malformed input without crashing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Browser POST /api/set_engine with version="../../etc/passwd" or similar | mitigate | GameManager.AVAILABLE_ENGINES allowlist rejects unknown versions before dispatch; existing V6 dispatch already enforces this. V7 ADDS to the set; does not weaken the check. |
| T-06-02 | DoS | Multi-process Syzygy download hammers tablebase.lichess.ovh | accept | Phase 1 download script is single-run / user-invoked; mirror's rate limiting handles excess. Document polite-use note in CLI help text. |
| T-06-03 | Tampering | Downloaded .rtbw file corrupted in transit | mitigate | Phase 1 syzygy.cpp's KRk smoke probe (plan 05 TB-10) catches corruption at engine init; download script can optionally verify against published SHA256 in Phase 4. |
| T-06-04 | DoS | v7_uci malformed input crashes the binary | mitigate | UCI parser uses std::getline + std::istringstream; unknown commands silently ignored; no fixed-size buffers or sscanf-style parsing. Phase 1 verify command 9 exercises basic protocol. |
| T-06-05 | Information Disclosure | GameManager.stop_search exception leaks stack trace to HTTP response | mitigate | try/except wrapper around algo_v7.stop_engine swallows all exceptions silently (intentional — stop must be best-effort). Existing FastAPI exception handler returns 500 without trace in prod mode. |
| T-06-06 | Tampering | npm install hijack injects malicious cli code | accept | npm supply chain is out of Phase 1 scope; lockfile (client/package-lock.json) pins versions. Phase 4 may add npm audit gate. |
| T-06-07 | Repudiation | NPS sentinel falsely passes/fails on a noisy host masking real perf regressions | mitigate | @pytest.mark.benchmark + RUN_BENCHMARKS=1 gate documents host requirements; default pytest invocations skip; SUMMARY records measured ratio when run; Phase 2 perf-CI will run on a controlled hardware profile. |
</threat_model>

<verification>
- GameManager dispatches V7 in all 4 points (AVAILABLE_ENGINES + set_engine_version + ai_move + stop_search)
- V1-V6 dispatch unchanged (INT-08 regression invariant — verified by full pytest run)
- React App.jsx has V7 option in both dropdowns above V6; build-warning fires for V7
- cli/src/config.js has v7Built/syzygyPath/syzygyMaxPieces defaults
- cli/src/index.js exposes `build v7` and `syzygy download`
- v7_uci speaks minimal UCI (uci/isready/position/go depth/stop/quit)
- tests/test_v7_engine.py has 5 tests total: 3 from Plan 01 (test_auto_build, test_gm_dispatch, test_smoke_game_v7_vs_v6) + 2 APPENDED by Plan 06 (test_cancellation_via_gm, test_nps_sentinel)
- Plan 06 reuses Plan 01's v7_native_engine module-scope fixture (verified: file has exactly 1 fixture definition)
- D-04 smoke milestone reached: V7-as-white vs V6, fixed depth, legal game completes (test_smoke_game_v7_vs_v6 + UI checkpoint)
- FOUND-04 cancellation works end-to-end through GameManager (<500ms latency)
- NPS sentinel: default-skip via @pytest.mark.benchmark + RUN_BENCHMARKS=1 env gate; runs on demand and asserts V7 ≥ 0.8 * V6 NPS
- Existing V1-V6 pytest suite green after V7 changes (INT-08)
- 01-VALIDATION.md populated with rows for INT-01/07/09 + FOUND-04 + FOUND-07 + NPS (with RUN_BENCHMARKS=1 note) + TB-09
</verification>

<success_criteria>
- INT-01: GameManager dispatches V7 (4-point surgical patch; V2-REF-01 ladder anti-pattern preserved per deferral)
- INT-02: React dropdowns expose V7 above V6 in both white + black selects
- INT-03: Build-warning copy extended to cover V7
- INT-04: cli/src/config.js DEFAULT_CONFIG has v7Built / syzygyPath / syzygyMaxPieces with correct types
- INT-05: cli/src/index.js exposes `build v7` (mirrors buildV6) AND `syzygy download` (TB-09)
- INT-07: tests/test_v7_engine.py covers smoke + cancel + NPS sentinel + auto-build + dispatch (across Plan 01 + Plan 06 contributions)
- INT-08: V1-V6 pytest suite green after V7 additions
- INT-09: First-import auto-build path verified by test_auto_build (Plan 01 contribution)
- TB-09: `chess-engine syzygy download` subcommand present with OS-specific default destinations
- FOUND-07: v7_uci binary speaks minimal UCI sufficient for fastchess (Phase 2 strength testing)
- D-04: V7-as-white vs V6 single game completes legally (test_smoke_game_v7_vs_v6 + UI checkpoint)
- CLAUDE.md NPS constraint: V7 NPS >= 0.8 * V6 NPS (test_nps_sentinel under RUN_BENCHMARKS=1)
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton-smoke/01-06-SUMMARY.md` documenting:
- Exact diff applied to game_manager.py (line ranges + before/after snippets for each of the 4 points)
- Exact App.jsx changes (which dropdowns + line numbers; the literal V7 option strings used)
- cli/src/config.js + .chess-engine.example.json schema additions
- cli/src/index.js subcommand registrations (which dispatch / commander block)
- v7_uci command surface implemented vs deferred (e.g., setoption deferred to Phase 4)
- syzygy download implementation choice: full HTTP fetch vs print-instructions stub (TB-09 — script presence is the requirement)
- D-04 smoke milestone outcome: actual game length + terminal state (e.g., "V6 won by checkmate at move 47" or "draw by 50-move at move 80")
- FOUND-04 cancellation latency measured in test_cancellation_via_gm
- NPS sentinel: actual fixture name reused (`v7_native_engine` or whatever Plan 01 declared); ratio measured when RUN_BENCHMARKS=1 invoked on the planning host; note that default pytest runs skip the test
- V1-V6 regression result: pytest exit code + test count
- Any pitfalls hit during integration (e.g., GameManager attribute names that differed from RESEARCH expectations)
- Phase 1 completion handshake: all phase-1 requirement IDs from ROADMAP have at least one row in 01-VALIDATION.md
- Next-phase readiness: link to Phase 2 plan kickoff (search strength gates + fastchess integration)
</output>
