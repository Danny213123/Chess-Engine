---
phase: 01-skeleton-smoke
plan: 05
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - .gitmodules
  - src/chess_engine/engine/v7/extern/fathom
  - src/chess_engine/engine/v7/include/tbconfig.h
  - src/chess_engine/engine/v7/include/syzygy.hpp
  - src/chess_engine/engine/v7/src/syzygy.cpp
  - tests/test_v7_syzygy.py
autonomous: true
requirements:
  - TB-01
  - TB-02
  - TB-03
  - TB-04
  - TB-05
  - TB-06
  - TB-07
  - TB-08
  - TB-10
  - INT-06

must_haves:
  truths:
    - "Fathom (jdart1) is a pinned git submodule at src/chess_engine/engine/v7/extern/fathom/ (TB-01) with the SHA recorded in .gitmodules and the parent commit"
    - "V7's include/tbconfig.h overrides Fathom's by appearing FIRST on the include path (TB-02) — confirmed by the EXISTS-guarded Fathom block in plan-02-task-3's CMakeLists.txt: target_include_directories puts V7's include/ BEFORE ${FATHOM_DIR}/src; this plan does NOT edit CMakeLists.txt"
    - "tbconfig.h delegates TB_PAWN_ATTACKS / TB_KNIGHT_ATTACKS / TB_BISHOP_ATTACKS / TB_ROOK_ATTACKS / TB_QUEEN_ATTACKS / TB_KING_ATTACKS / TB_pop_lsb / TB_popcount to v7:: magic-bitboard + bitboard helper functions (no duplicate attack tables)"
    - "Engine::set_syzygy_path emits one of the four D-08 verbatim log strings on stderr for each failure case AND never throws (always-log-never-fail; TB-03 + D-07). The Engine forwarding wrapper lives in plan 03's src/engine.cpp; the actual log-emitting implementation lives in this plan's src/syzygy.cpp::SyzygyState::set_path. The pybind11 `set_syzygy_path` binding is pre-staged by plan 01 — this plan makes ZERO edits to src/python_bindings.cpp and ZERO edits to include/engine.hpp (Wave 3 file-ownership invariant; checker blocker fix)"
    - "tb_init succeeds → KRk smoke probe (TB-10) runs → success populates internal state; failure emits the D-08 smoke-probe log line and calls tb_free()"
    - "In-search probe wrapper returns std::optional<int> — TB_RESULT_FAILED maps to std::nullopt and emits a stderr log; NEVER mapped to TB_DRAW / 0 score (TB-06, D-09)"
    - "tbhits_ is a std::atomic<uint64_t> incremented only on successful probes; Engine exposes tbhits() to Python (TB-07)"
    - "syzygyMaxPieces clamps in-search probe gating: probes only fire when piece_count <= min(syzygy_max_pieces_, TB_LARGEST) AND side-to-move is not in check (TB-08; Fathom undefined-behavior guard)"
    - "TB-04 root probe is FULLY IMPLEMENTED in this plan (per locked decision): probe_root_dtz calls Fathom's tb_probe_root_dtz directly with TB_RESULT_FAILED → std::nullopt mapping (D-09 invariant); no stub"
    - "Tests gated on tablebase files being present (pytest.mark.skipif); init-log discipline tests run unconditionally and capture stderr; KRk endpoint WDL probe assertion runs when SYZYGY_PATH set"
  artifacts:
    # OWNERSHIP NOTES (Wave 3 file-ownership invariant; checker blocker fix):
    # - src/python_bindings.cpp is OWNED by plan 01 (set_syzygy_path + tbhits bindings pre-staged); this plan does NOT list it in files_modified and does NOT edit it.
    # - include/engine.hpp is OWNED by plan 03 (syzygy_ member declaration + `#include "syzygy.hpp"` + Engine::set_syzygy_path forwarding wrapper live there); this plan does NOT list it in files_modified and does NOT edit it.
    - path: ".gitmodules"
      provides: "Submodule entry pointing src/chess_engine/engine/v7/extern/fathom/ at https://github.com/jdart1/Fathom.git (INT-06)"
      contains: "src/chess_engine/engine/v7/extern/fathom"
    - path: "src/chess_engine/engine/v7/extern/fathom"
      provides: "Pinned Fathom submodule (TB-01); contains src/tbprobe.c + src/tbprobe.h consumed by V7's build"
    - path: "src/chess_engine/engine/v7/include/tbconfig.h"
      provides: "TB_*_ATTACKS + TB_pop_lsb + TB_popcount macros delegating to v7:: magic / bitboard functions (TB-02); included by Fathom's tbprobe.c via the override path-ordering trick"
    - path: "src/chess_engine/engine/v7/include/syzygy.hpp"
      provides: "ProbeResult struct; SyzygyState class with set_path / probe_wdl / probe_root_dtz / smoke_probe_krk / tbhits / set_max_pieces"
    - path: "src/chess_engine/engine/v7/src/syzygy.cpp"
      provides: "set_syzygy_path with D-08 verbatim log discipline; KRk smoke probe (TB-10); in-search probe_wdl wrapper with TB_RESULT_FAILED → std::nullopt (D-09); REAL probe_root_dtz calling tb_probe_root_dtz directly (TB-04); tbhits_ atomic counter"
    - path: "tests/test_v7_syzygy.py"
      provides: "test_syzygy_unset_logs_message, test_syzygy_missing_path_logs_message, test_syzygy_empty_dir_logs_message, test_syzygy_set_path_never_throws, test_tbhits_starts_zero, test_in_search_probe_failure_returns_none (mocked via gating path), test_krk_endpoint_wdl_probe (skipped if no tables — asserts ProbeResult::Kind::WIN), test_root_dtz_probe_returns_some_when_tables_present (skipped if no tables); pytest.mark.skipif gating via SYZYGY_PATH env var"
  key_links:
    - from: "src/chess_engine/engine/v7/CMakeLists.txt (pre-staged by plan 02 task 3)"
      to: "src/chess_engine/engine/v7/include/tbconfig.h"
      via: "EXISTS-guarded Fathom block in plan-02-task-3: target_include_directories(v7_engine PRIVATE include/ ${FATHOM_DIR}/src) — V7's include/ FIRST so #include \"tbconfig.h\" from Fathom's tbprobe.c resolves to OURS not Fathom's bundled one. This plan relies on that pre-staging — does NOT touch CMakeLists.txt."
      pattern: "target_include_directories.*include.*FATHOM_DIR"
    - from: "src/chess_engine/engine/v7/src/syzygy.cpp"
      to: "extern/fathom/src/tbprobe.h (Fathom C API)"
      via: "extern \"C\" { #include \"tbprobe.h\" } — Fathom is C; V7 wraps with C++ std::optional return type"
      pattern: "extern \"C\""
    - from: "src/chess_engine/engine/v7/include/engine.hpp (from plan 01/03)"
      to: "src/chess_engine/engine/v7/include/syzygy.hpp"
      via: "Engine class declares SyzygyState syzygy_; member; set_syzygy_path forwards to syzygy module; tbhits() reads syzygy_.tbhits_.load()"
      pattern: "syzygy"
    - from: "src/chess_engine/engine/v7/src/python_bindings.cpp (PRE-STAGED by plan 01 — NOT edited by this plan)"
      to: "Engine::set_syzygy_path, Engine::tbhits"
      via: "py::class_<Engine>().def(\"set_syzygy_path\", ...).def(\"tbhits\", ...) bindings already exist from plan 01 (verified at plan 01 lines 56-57 + 156-165 + 342). This plan provides ONLY the SyzygyState implementation in include/syzygy.hpp + src/syzygy.cpp; the Engine forwarding wrapper that the bindings call into is written by plan 03 in src/engine.cpp. NO edits to python_bindings.cpp from this plan."
      pattern: "set_syzygy_path|tbhits"
---

<objective>
Integrate Fathom (jdart1 fork) as a pinned git submodule under `src/chess_engine/engine/v7/extern/fathom/`, write the V7-owned `tbconfig.h` override that shares the magic-bitboard attack tables (Fathom build itself is enabled by plan-02-task-3's EXISTS-guarded CMake block — this plan does NOT edit CMakeLists.txt), and implement `Engine::set_syzygy_path` + KRk smoke probe + in-search probe wrappers + REAL root-DTZ probe (TB-04) obeying D-07/D-08/D-09 verbatim. Output: V7 can probe Syzygy WDL/DTZ when tablebases are present, emits exact verbatim init-time log strings when they are not, and never silently maps probe failures to DRAW scores.

Purpose: Phase 1 must close TB-01..08 and TB-10 (TB-09 = optional download script lives in plan 06 CLI work) without bundling tablebases. Init-time log discipline (D-07/D-08) is the most user-visible behavior — users grep logs to confirm the engine sees their tables. TB-06/D-09 is the most important correctness invariant — silently scoring a probe failure as DRAW (Fathom's TB_RESULT_FAILED is 0xFFFFFFFF; naive casts to signed int can wrap to small numbers) would corrupt search results. TB-04 root-DTZ is fully implemented per locked user decision (no stub) so endgame play under tablebases is correct from Phase 1. Wave 3 (parallel with plans 03 and 04) because plan-02-task-3 pre-staged CMakeLists.txt with EXISTS-guarded Fathom + coeffs blocks; this plan only adds files inside `extern/fathom`, `include/`, `src/` — no CMakeLists.txt edits → no same-wave file conflict.

Output: Working Fathom build linked into v7_engine and v7_uci (linkage activates automatically once submodule is initialized — plan-02-task-3's EXISTS guard flips true); set_syzygy_path callable from Python with the four verbatim D-08 log strings exercised; tbhits counter exposed; root DTZ probe returns real DTZ values when tables present; tests green (init-log tests always run; probe-needs-tables tests skipped when SYZYGY_PATH unset; KRk endpoint WDL assertion runs when SYZYGY_PATH set with KRvK tables).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-skeleton-smoke/01-CONTEXT.md
@.planning/phases/01-skeleton-smoke/01-RESEARCH.md
@.planning/phases/01-skeleton-smoke/01-PATTERNS.md
@.planning/phases/01-skeleton-smoke/01-02-SUMMARY.md

# Plan 01 outputs needed (Engine class declares set_syzygy_path / tbhits / stop_flag):
@src/chess_engine/engine/v7/include/engine.hpp
@src/chess_engine/engine/v7/src/python_bindings.cpp

# Plan 02 outputs needed (magic-bitboard attack functions tbconfig.h delegates to + pre-staged CMakeLists.txt):
@src/chess_engine/engine/v7/include/magic.hpp
@src/chess_engine/engine/v7/include/bitboard.hpp
@src/chess_engine/engine/v7/include/board.hpp
@src/chess_engine/engine/v7/CMakeLists.txt

# Submodule root-level config:
@.gitmodules
@.gitignore

<interfaces>
<!-- D-08 verbatim log strings — MUST appear character-for-character in stderr output -->
<!-- Users grep for these literal substrings; any deviation breaks user tooling. -->

  Case 1 (syzygyPath unset):
    [v7] syzygy: no path configured; tbhits will be 0

  Case 2 (path set, directory missing):
    [v7] syzygy: path not found: <path>; tbhits will be 0

  Case 3 (path set, directory empty / no .rtbw files):
    [v7] syzygy: no tablebase files at <path>; tbhits will be 0

  Case 4 (path set, KRk smoke probe fails):
    [v7] syzygy: smoke probe failed (corrupt or wrong format) at <path>; tbhits will be 0

  Extra (tb_init itself fails before smoke probe — not in D-08 but covered by D-07's "always log"):
    [v7] syzygy: tb_init failed at <path>; tbhits will be 0

<!-- Fathom API surface (jdart1 fork) — RESEARCH.md §B3 lines 638-646 -->

  extern "C" {
    #include "tbprobe.h"
  }

  bool tb_init(const char* path);             // returns true on success
  void tb_free(void);
  extern unsigned TB_LARGEST;                 // 0 if no TBs; max piece count loaded

  unsigned tb_probe_wdl(uint64_t white, uint64_t black,
                        uint64_t kings, uint64_t queens, uint64_t rooks,
                        uint64_t bishops, uint64_t knights, uint64_t pawns,
                        unsigned rule50, unsigned castling, unsigned ep,
                        bool turn);
  // Returns: TB_WIN, TB_DRAW, TB_LOSS, TB_BLESSED_LOSS, TB_CURSED_WIN, TB_RESULT_FAILED (0xFFFFFFFF)

  unsigned tb_probe_root(uint64_t white, uint64_t black,
                         uint64_t kings, uint64_t queens, uint64_t rooks,
                         uint64_t bishops, uint64_t knights, uint64_t pawns,
                         unsigned rule50, unsigned castling, unsigned ep,
                         bool turn,
                         unsigned* results);   // results[] receives candidate moves; returns root WDL+DTZ packed
  // jdart1 fork additionally exposes tb_probe_root_dtz which returns DTZ-only encoding suitable for endgame play

<!-- syzygy.hpp public surface V7 owns -->

  namespace v7 {
    struct ProbeResult {
      int score_cp;     // mapped from WDL with cursed/blessed adjustments
      enum Kind { WIN, DRAW, LOSS, CURSED_WIN, BLESSED_LOSS } kind;
    };

    class SyzygyState {
    public:
      void set_path(const std::string& path);   // D-08 log discipline
      bool initialized() const;
      uint64_t tbhits() const;
      std::optional<ProbeResult> probe_wdl(const Board& b) const;   // TB-05; nullopt on failure
      std::optional<int> probe_root_dtz(const Board& b) const;      // TB-04; REAL — calls tb_probe_root_dtz; nullopt on failure
      void set_max_pieces(unsigned n);
      ~SyzygyState();   // calls tb_free() if initialized
    private:
      bool initialized_ = false;
      unsigned max_pieces_ = 6;
      mutable std::atomic<uint64_t> tbhits_{0};
      bool smoke_probe_krk() const;
    };
  }

<!-- tbconfig.h override skeleton (RESEARCH.md §B3 lines 617-633) -->
<!-- Fathom macros being redirected (enumerated EXPLICITLY per checker issue #6): -->
<!--   TB_PAWN_ATTACKS(sq, color)   — Fathom expects (sq, color) two-arg form -->
<!--   TB_KNIGHT_ATTACKS(sq)        — single-arg jump piece -->
<!--   TB_BISHOP_ATTACKS(sq, occ)   — sliding piece, needs occupancy -->
<!--   TB_ROOK_ATTACKS(sq, occ)     — sliding piece, needs occupancy -->
<!--   TB_QUEEN_ATTACKS(sq, occ)    — sliding piece, needs occupancy -->
<!--   TB_KING_ATTACKS(sq)          — single-arg jump piece -->
<!--   TB_pop_lsb(b)                — pop the lowest set bit; Fathom uses it in inner loops -->
<!--   TB_popcount(b)               — population count; Fathom uses it for piece counts -->

  // src/chess_engine/engine/v7/include/tbconfig.h
  #pragma once
  #include "magic.hpp"
  #include "bitboard.hpp"

  #ifdef _MSC_VER
    #include <intrin.h>
    inline unsigned tb_lsb(uint64_t b) {
      unsigned long i; _BitScanForward64(&i, b); return i;
    }
  #else
    inline unsigned tb_lsb(uint64_t b) { return __builtin_ctzll(b); }
  #endif

  #define TB_CUSTOM_LSB
  #define TB_PAWN_ATTACKS(sq, color) (v7::pawn_attacks(sq, color))
  #define TB_KNIGHT_ATTACKS(sq)      (v7::knight_attacks(sq))
  #define TB_BISHOP_ATTACKS(sq, occ) (v7::bishop_attacks(sq, occ))
  #define TB_ROOK_ATTACKS(sq, occ)   (v7::rook_attacks(sq, occ))
  #define TB_QUEEN_ATTACKS(sq, occ)  (v7::queen_attacks(sq, occ))
  #define TB_KING_ATTACKS(sq)        (v7::king_attacks(sq))
  #define TB_pop_lsb(b)              (v7::pop_lsb(b))     // delegates to v7::pop_lsb in bitboard.hpp
  #define TB_popcount(b)             (v7::popcount(b))    // delegates to v7::popcount in bitboard.hpp

<!-- Pre-staged CMakeLists.txt block (FROM PLAN 02 TASK 3 — read-only reference): -->
<!-- This plan does NOT edit CMakeLists.txt. The block below is what plan-02-task-3 -->
<!-- already wrote; once this plan adds the submodule (Step 1) and tbconfig.h (Step 3), -->
<!-- the EXISTS guard flips true on the next CMake configure and Fathom links automatically. -->

  # ===== Fathom (Syzygy probing) integration — PRE-STAGED, EXISTS-guarded =====
  enable_language(C)
  set(FATHOM_DIR ${CMAKE_CURRENT_SOURCE_DIR}/extern/fathom)
  if(EXISTS ${FATHOM_DIR}/src/tbprobe.c)
      target_include_directories(v7_engine PRIVATE
          ${CMAKE_CURRENT_SOURCE_DIR}/include   # OUR tbconfig.h FIRST (TB-02)
          ${FATHOM_DIR}/src                     # Fathom headers (tbprobe.h)
      )
      target_sources(v7_engine PRIVATE
          ${FATHOM_DIR}/src/tbprobe.c
          ${CMAKE_CURRENT_SOURCE_DIR}/src/syzygy.cpp
      )
      target_include_directories(v7_uci PRIVATE
          ${CMAKE_CURRENT_SOURCE_DIR}/include
          ${FATHOM_DIR}/src
      )
      target_sources(v7_uci PRIVATE
          ${FATHOM_DIR}/src/tbprobe.c
          ${CMAKE_CURRENT_SOURCE_DIR}/src/syzygy.cpp
      )
  endif()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Fathom submodule + write tbconfig.h override (NO CMakeLists.txt edits — plan 02 task 3 pre-staged the EXISTS-guarded Fathom block)</name>
  <files>
    .gitmodules,
    src/chess_engine/engine/v7/extern/fathom,
    src/chess_engine/engine/v7/include/tbconfig.h
  </files>
  <read_first>
    .planning/phases/01-skeleton-smoke/01-RESEARCH.md,
    .planning/phases/01-skeleton-smoke/01-PATTERNS.md,
    src/chess_engine/engine/v7/CMakeLists.txt,
    src/chess_engine/engine/v7/include/magic.hpp,
    src/chess_engine/engine/v7/include/bitboard.hpp,
    .gitmodules
  </read_first>
  <behavior>
    - `git submodule status src/chess_engine/engine/v7/extern/fathom` reports a non-empty SHA (submodule registered + initialized)
    - `cat .gitmodules` shows the Fathom submodule entry pointing at `https://github.com/jdart1/Fathom.git`
    - `ls src/chess_engine/engine/v7/extern/fathom/src/tbprobe.c` exists (proof submodule contents are checked out)
    - `ls src/chess_engine/engine/v7/extern/fathom/src/tbprobe.h` exists
    - V7's CMakeLists.txt is UNCHANGED by this plan — plan-02-task-3 already wrote the EXISTS-guarded Fathom block
    - The pre-staged CMakeLists.txt orders target_include_directories so V7's `include/` precedes `${FATHOM_DIR}/src` (TB-02 override path-ordering)
    - CMake configure step (`cmake -S src/chess_engine/engine/v7 -B build/v7`) succeeds without errors, and now (because Fathom files exist) lists `${FATHOM_DIR}/src/tbprobe.c` as a v7_engine + v7_uci source
    - tbconfig.h enumerates ALL Fathom macros being redirected: TB_PAWN_ATTACKS / TB_KNIGHT_ATTACKS / TB_BISHOP_ATTACKS / TB_ROOK_ATTACKS / TB_QUEEN_ATTACKS / TB_KING_ATTACKS / TB_pop_lsb / TB_popcount
  </behavior>
  <action>
    Step 1 — Add the Fathom submodule:
    - From the repo root: `git submodule add https://github.com/jdart1/Fathom.git src/chess_engine/engine/v7/extern/fathom`
    - This creates the .gitmodules entry AND clones Fathom into the directory
    - Pin to the current `master` SHA: `cd src/chess_engine/engine/v7/extern/fathom && git rev-parse HEAD` — record the SHA in the SUMMARY for traceability (matches RESEARCH.md §B3 expectation that a specific SHA is pinned)
    - Confirm `.gitmodules` now contains:
      ```
      [submodule "src/chess_engine/engine/v7/extern/fathom"]
          path = src/chess_engine/engine/v7/extern/fathom
          url = https://github.com/jdart1/Fathom.git
      ```
    - On Windows: if `git submodule add` fails due to path separators, use forward slashes in the path argument (git handles the conversion)
    - The submodule directory (`extern/fathom`) is itself a git-tracked entry (gitlink); .gitignore must NOT exclude it (verify by `git check-ignore src/chess_engine/engine/v7/extern/fathom` exits non-zero meaning NOT ignored)

    Step 2 — Verify Fathom's actual API surface matches RESEARCH.md Assumption A3:
    - Read `src/chess_engine/engine/v7/extern/fathom/src/tbprobe.h` to confirm function names exactly: `tb_init`, `tb_free`, `tb_probe_wdl`, `tb_probe_root`, `tb_probe_root_dtz` (jdart1 fork-specific — required for TB-04 real implementation), `TB_LARGEST`, `TB_RESULT_FAILED`, `TB_WIN`/`TB_DRAW`/`TB_LOSS`/`TB_CURSED_WIN`/`TB_BLESSED_LOSS`
    - If `tb_probe_root_dtz` is NOT present in the jdart1 fork's tbprobe.h (i.e., the fork structure has changed since RESEARCH.md was written), document the deviation in the SUMMARY and use `tb_probe_root` instead, decoding the DTZ field from its packed result per Fathom's `TB_GET_DTZ(res)` macro — the locked decision is "implement TB-04 now using Fathom's root-probing API directly", not specifically tb_probe_root_dtz; whichever real Fathom function provides the DTZ at root is acceptable
    - Read `extern/fathom/src/tbconfig.h` to confirm the macro names V7 must override (TB_PAWN_ATTACKS / TB_KNIGHT_ATTACKS / TB_BISHOP_ATTACKS / TB_ROOK_ATTACKS / TB_QUEEN_ATTACKS / TB_KING_ATTACKS, TB_pop_lsb, TB_popcount; plus any newer additions)
    - If Fathom's macro set differs from RESEARCH.md's list, update tbconfig.h (step 3) accordingly and document the delta in the SUMMARY — this is exactly the "verify exact API surface at integration time" caveat called out in RESEARCH.md §A3

    Step 3 — Create src/chess_engine/engine/v7/include/tbconfig.h:
    - `#pragma once`
    - `#include "magic.hpp"` and `#include "bitboard.hpp"` to make v7:: attack and bitboard helper functions visible
    - Provide cross-platform tb_lsb: __builtin_ctzll on GCC/Clang, _BitScanForward64 on MSVC (wrap in `#ifdef _MSC_VER` per the interfaces block above)
    - Define `TB_CUSTOM_LSB` so Fathom uses ours
    - Enumerate ALL of the following macro redirections explicitly (per checker issue #6 — ambiguity here lets Fathom silently fall back to its own implementations and waste cache; an exhaustive list is the only acceptable form):
      * `#define TB_PAWN_ATTACKS(sq, color) (v7::pawn_attacks(sq, color))`
      * `#define TB_KNIGHT_ATTACKS(sq)      (v7::knight_attacks(sq))`
      * `#define TB_BISHOP_ATTACKS(sq, occ) (v7::bishop_attacks(sq, occ))`
      * `#define TB_ROOK_ATTACKS(sq, occ)   (v7::rook_attacks(sq, occ))`
      * `#define TB_QUEEN_ATTACKS(sq, occ)  (v7::queen_attacks(sq, occ))`
      * `#define TB_KING_ATTACKS(sq)        (v7::king_attacks(sq))`
      * `#define TB_pop_lsb(b)              (v7::pop_lsb(b))`
      * `#define TB_popcount(b)             (v7::popcount(b))`
    - The exact v7:: function signatures come from the magic.hpp / bitboard.hpp headers from plan 02 — read them first to confirm parameter shape (e.g., is `pawn_attacks` `(square, color)` or `(color, square)`?). If V7's function signature does not match, write a thin inline adapter in tbconfig.h that bridges Fathom's expected `(sq, color)` shape to V7's actual shape — DO NOT modify plan 02's headers. If `v7::pop_lsb` / `v7::popcount` are not in bitboard.hpp under those names, alias to the actual names in tbconfig.h with a comment naming the source helper
    - Add a top-of-file comment documenting: this file MUST be on the include path BEFORE Fathom's `extern/fathom/src/tbconfig.h` so the override wins (TB-02). Reference the pre-staged target_include_directories ordering in CMakeLists.txt
    - Include comments referencing TB-02 and citing RESEARCH.md §B3 lines 617-633

    Step 4 — DO NOT EDIT CMakeLists.txt:
    - Plan-02-task-3 already wrote the EXISTS-guarded Fathom block (see &lt;interfaces&gt; for the exact pre-staged shape). Once Step 1 (submodule add) and Step 3 (tbconfig.h create) complete, the EXISTS guard `if(EXISTS ${FATHOM_DIR}/src/tbprobe.c)` flips from false to true on the next CMake configure → Fathom's tbprobe.c + V7's syzygy.cpp are added to v7_engine and v7_uci targets automatically with the correct include-path ordering
    - Sanity-check: `grep -c 'EXISTS.*tbprobe.c' src/chess_engine/engine/v7/CMakeLists.txt` MUST return ≥1. If zero, plan-02-task-3 did not pre-stage correctly — STOP and surface the regression to the orchestrator (do NOT silently add the block here, that would re-introduce the same-wave file conflict the wave-collapse design eliminated)
    - .gitignore housekeeping:
      * Confirm `.gitignore` does NOT contain a pattern that would exclude `src/chess_engine/engine/v7/extern/fathom` (would block the submodule gitlink)
      * Do NOT add Fathom's build outputs separately — they go into the V7 CMake build directory which is already gitignored by plan 01's V7 build artifacts block
  </action>
  <verify>
    <automated>git submodule status src/chess_engine/engine/v7/extern/fathom | head -1</automated>
    <automated>test -f src/chess_engine/engine/v7/extern/fathom/src/tbprobe.c && test -f src/chess_engine/engine/v7/extern/fathom/src/tbprobe.h && echo "fathom sources present"</automated>
    <automated>grep -c 'src/chess_engine/engine/v7/extern/fathom' .gitmodules</automated>
    <automated>grep -vE '^\s*//' src/chess_engine/engine/v7/include/tbconfig.h | grep -cE 'TB_(PAWN|KNIGHT|BISHOP|ROOK|QUEEN|KING)_ATTACKS|TB_pop_lsb|TB_popcount'</automated>
    <automated>grep -c 'EXISTS.*tbprobe\.c' src/chess_engine/engine/v7/CMakeLists.txt</automated>
    <automated>git diff --name-only HEAD -- src/chess_engine/engine/v7/CMakeLists.txt | wc -l</automated>
    <automated>cmake -S src/chess_engine/engine/v7 -B build/v7 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - .gitmodules contains the Fathom submodule entry (verify command 3 returns ≥1)
    - extern/fathom/src/tbprobe.c and tbprobe.h exist (verify command 2 prints "fathom sources present")
    - include/tbconfig.h defines at least 8 redirection macros — 6 TB_*_ATTACKS plus TB_pop_lsb plus TB_popcount (verify command 4 returns ≥8, with `^//` comment lines stripped so header prose cannot self-validate per the grep-gate-hygiene rule)
    - include/tbconfig.h starts with `#pragma once` and includes V7's magic.hpp + bitboard.hpp
    - CMakeLists.txt was pre-staged by plan 02 task 3 with the EXISTS-guarded Fathom block (verify command 5 returns ≥1) — proves the wave-collapse pre-staging is intact
    - This plan made ZERO edits to CMakeLists.txt (verify command 6 returns 0) — proves the same-wave file-ownership invariant for plans 03/04/05
    - target_include_directories puts `${CMAKE_CURRENT_SOURCE_DIR}/include` BEFORE `${FATHOM_DIR}/src` for both v7_engine and v7_uci (TB-02) — already pre-staged, sanity-check at configure time
    - `cmake -S src/chess_engine/engine/v7 -B build/v7` succeeds (verify command 7) — configure phase only; full build happens in task 2 after syzygy.cpp exists. Now that Fathom files exist, the EXISTS branch activates and tbprobe.c is listed as a target source
    - V1-V6 untouched
    - Pinned Fathom SHA recorded in SUMMARY
  </acceptance_criteria>
  <done>Fathom submodule added and pinned; tbconfig.h override created with 8 enumerated macro redirections; plan-02-task-3's EXISTS-guarded CMake block now activates (Fathom files present → tbprobe.c added to targets); CMakeLists.txt unchanged by this plan (Wave 3 file-ownership invariant preserved); CMake configure green; ready for task 2 to provide syzygy.cpp implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement syzygy.hpp + syzygy.cpp (D-08 verbatim log discipline + KRk smoke probe + in-search probe wrappers + REAL TB-04 root-DTZ probe) + bindings + tests with KRk endpoint WDL assertion</name>
  <files>
    src/chess_engine/engine/v7/include/syzygy.hpp,
    src/chess_engine/engine/v7/src/syzygy.cpp,
    tests/test_v7_syzygy.py
  </files>
  <read_first>
    .planning/phases/01-skeleton-smoke/01-RESEARCH.md,
    src/chess_engine/engine/v7/extern/fathom/src/tbprobe.h,
    src/chess_engine/engine/v7/include/engine.hpp,
    src/chess_engine/engine/v7/include/board.hpp,
    src/chess_engine/engine/v7/include/tbconfig.h
  </read_first>
  <behavior>
    - Engine::set_syzygy_path("") emits exactly `[v7] syzygy: no path configured; tbhits will be 0\n` to stderr and returns without throwing (D-07/D-08 case 1)
    - Engine::set_syzygy_path("/no/such/path") emits exactly `[v7] syzygy: path not found: /no/such/path; tbhits will be 0\n` and returns without throwing (D-08 case 2)
    - Engine::set_syzygy_path("<empty existing dir>") emits exactly `[v7] syzygy: no tablebase files at <empty existing dir>; tbhits will be 0\n` and returns without throwing (D-08 case 3)
    - When real tables present and tb_init succeeds, the KRk smoke probe runs; if it fails the D-08 case 4 line emits AND tb_free() is called (cleanup)
    - tbhits() returns 0 initially; increments only on successful probes
    - In-search probe_wdl returns std::nullopt on TB_RESULT_FAILED, emits a single stderr log line, NEVER returns a DRAW score (TB-06 / D-09)
    - probe_root_dtz is REAL (TB-04 per locked decision): calls tb_probe_root_dtz (or tb_probe_root + DTZ decoding) directly; TB_RESULT_FAILED → std::nullopt; success → returns DTZ ply count as std::optional<int>
    - syzygy_max_pieces_ defaults to 6 (TB-08); probe_wdl checks piece_count <= min(max_pieces_, TB_LARGEST) AND !in_check before calling Fathom
    - v7_engine module imports cleanly after build; `v7_engine.Engine().set_syzygy_path("")` works from Python (call already declared as binding in plan 01)
    - test_v7_syzygy.py: 4 init-log tests run unconditionally (no tables needed); KRk endpoint WDL probe test runs when SYZYGY_PATH set with KRvK tables and asserts ProbeResult::Kind::WIN with score ~+20000cp; root-DTZ probe test runs when tables present and asserts non-nullopt DTZ
  </behavior>
  <action>
    Step 1 — Create src/chess_engine/engine/v7/include/syzygy.hpp:
    - `#pragma once`
    - `#include <atomic>`, `<optional>`, `<string>`, `<cstdint>`
    - Forward-declare or include `class Board;` from board.hpp (use forward declare if board.hpp is heavy; full include if probe_wdl inlines bitboard accessors)
    - Declare the SyzygyState class per the interfaces block above:
      ```cpp
      namespace v7 {
        struct ProbeResult {
          int score_cp;
          enum class Kind { WIN, DRAW, LOSS, CURSED_WIN, BLESSED_LOSS } kind;
        };

        class SyzygyState {
        public:
          SyzygyState() = default;
          ~SyzygyState();
          void set_path(const std::string& path);            // D-08 log discipline
          void set_max_pieces(unsigned n);
          bool initialized() const noexcept;
          uint64_t tbhits() const noexcept;
          std::optional<ProbeResult> probe_wdl(const Board& b) const;
          std::optional<int> probe_root_dtz(const Board& b) const;   // TB-04 REAL — calls tb_probe_root_dtz

        private:
          bool initialized_ = false;
          unsigned max_pieces_ = 6;
          mutable std::atomic<uint64_t> tbhits_{0};
          bool smoke_probe_krk() const;
        };
      }
      ```
    - **DO NOT edit src/chess_engine/engine/v7/include/engine.hpp** (checker blocker fix; Wave 3 file-ownership invariant). The `v7::SyzygyState syzygy_;` member on Engine is OWNED by plan 03 (Wave 3 sibling) — plan 03 Step 5 declares it alongside `v7::RepStack rep_stack_;`, adds `#include "syzygy.hpp"` to engine.hpp, and writes the `Engine::set_syzygy_path` forwarding wrapper in src/engine.cpp. This plan's responsibility is to PROVIDE the syzygy.hpp header that plan 03 includes (this Step 1) and the implementation body (Step 2). No engine.hpp edits from this plan.
    - **File-ownership gate:** `git diff --name-only HEAD -- src/chess_engine/engine/v7/include/engine.hpp` MUST show zero changes from this plan; any edits to engine.hpp belong to plan 03.

    Step 2 — Create src/chess_engine/engine/v7/src/syzygy.cpp:
    - Includes:
      ```cpp
      #include "syzygy.hpp"
      #include "board.hpp"
      #include <iostream>
      #include <filesystem>
      extern "C" {
        #include "tbprobe.h"
      }
      ```
    - Implement set_path EXACTLY per RESEARCH.md §B3 lines 651-681 with the verbatim D-08 log strings (use the strings listed in the &lt;interfaces&gt; block; any deviation breaks user grep workflows):
      * Empty path → case 1 log; return
      * Path does not exist OR is not a directory → case 2 log; return
      * Path is a directory but contains zero .rtbw files → case 3 log; return
      * tb_init returns false → case 5 log (extra `tb_init failed`); return
      * tb_init returns true → run smoke_probe_krk(); if false → case 4 log + tb_free(); else set initialized_ = true
    - smoke_probe_krk(): construct a Board from FEN `"4k3/8/8/8/8/8/4R3/4K3 w - - 0 1"` and call probe_wdl; success = returns non-nullopt with kind == WIN (white-to-move with KR vs k is a win); failure = nullopt OR unexpected DRAW/LOSS. Document the expected outcome in a comment. If Board's FEN parser is invoked, this needs board.hpp's from_fen method (plan 02 provides)
    - Implement probe_wdl with full gating:
      ```cpp
      std::optional<ProbeResult> SyzygyState::probe_wdl(const Board& b) const {
        if (!initialized_) return std::nullopt;
        if (TB_LARGEST == 0) return std::nullopt;
        unsigned pc = b.piece_count();   // total pieces on the board including kings
        if (pc > std::min<unsigned>(max_pieces_, TB_LARGEST)) return std::nullopt;
        if (b.in_check()) return std::nullopt;   // Fathom undefined for in-check
        if (b.castling_rights() != 0) return std::nullopt;  // Fathom requires no castling rights for WDL

        unsigned result = tb_probe_wdl(
            b.white_occ(), b.black_occ(),
            b.kings(), b.queens(), b.rooks(), b.bishops(), b.knights(), b.pawns(),
            b.halfmove_clock(), 0 /*castling*/, b.ep_square(),
            b.side_to_move() == WHITE);

        if (result == TB_RESULT_FAILED) {
          std::cerr << "[v7] syzygy: in-search probe failed at fen=" << b.to_fen() << "\n";
          return std::nullopt;   // D-09: NEVER treat as DRAW
        }
        tbhits_.fetch_add(1, std::memory_order_relaxed);
        ProbeResult r;
        switch (result) {
          case TB_WIN:           r.kind = ProbeResult::Kind::WIN;           r.score_cp =  20000; break;
          case TB_LOSS:          r.kind = ProbeResult::Kind::LOSS;          r.score_cp = -20000; break;
          case TB_DRAW:          r.kind = ProbeResult::Kind::DRAW;          r.score_cp =      0; break;
          case TB_CURSED_WIN:    r.kind = ProbeResult::Kind::CURSED_WIN;    r.score_cp =      1; break;  // mapped per 50-move rule
          case TB_BLESSED_LOSS:  r.kind = ProbeResult::Kind::BLESSED_LOSS;  r.score_cp =     -1; break;
          default:               return std::nullopt;
        }
        return r;
      }
      ```
    - The exact accessor names (b.white_occ(), b.kings(), b.in_check(), b.piece_count(), b.to_fen(), etc.) come from plan 02's board.hpp — if names differ, adapt the call sites here; do NOT modify plan 02's headers. If board.hpp lacks one of these (e.g., piece_count), add a small inline helper at the top of syzygy.cpp using bitboard popcount on the occupancy bitboards

    - Implement probe_root_dtz REAL (TB-04 — locked user decision: NO STUB; call Fathom's root-probing API directly):
      ```cpp
      std::optional<int> SyzygyState::probe_root_dtz(const Board& b) const {
        if (!initialized_) return std::nullopt;
        if (TB_LARGEST == 0) return std::nullopt;
        unsigned pc = b.piece_count();
        if (pc > std::min<unsigned>(max_pieces_, TB_LARGEST)) return std::nullopt;
        if (b.castling_rights() != 0) return std::nullopt;  // Fathom root-probe also requires no castling

        // jdart1 fork exposes tb_probe_root_dtz (returns DTZ-only encoding suitable for root-play).
        // If the fork's header instead exposes only tb_probe_root (older signature), call that and
        // decode DTZ via Fathom's TB_GET_DTZ macro — see Step 1 fallback documented in tbprobe.h.
        unsigned result = tb_probe_root_dtz(
            b.white_occ(), b.black_occ(),
            b.kings(), b.queens(), b.rooks(), b.bishops(), b.knights(), b.pawns(),
            b.halfmove_clock(), 0 /*castling*/, b.ep_square(),
            b.side_to_move() == WHITE,
            nullptr /*results array — not needed for the DTZ-only return*/);

        if (result == TB_RESULT_FAILED) {
          std::cerr << "[v7] syzygy: root probe failed at fen=" << b.to_fen() << "\n";
          return std::nullopt;   // D-09: NEVER fabricate a DTZ value on failure
        }
        tbhits_.fetch_add(1, std::memory_order_relaxed);
        // Decode DTZ ply count from the packed result. If the jdart1 header provides
        // TB_GET_DTZ(res), use it; otherwise the DTZ-only variant returns the ply count directly.
        int dtz = static_cast<int>(TB_GET_DTZ(result));
        return dtz;
      }
      ```
    - If the Fathom header read in Task 1 Step 2 confirms `tb_probe_root_dtz` is NOT present (jdart1 fork header has changed since RESEARCH.md), substitute `tb_probe_root` with the same argument shape plus a small results buffer (`unsigned results[TB_MAX_MOVES]`) and decode DTZ via `TB_GET_DTZ(result)` — both are real Fathom APIs; the locked decision is "implement TB-04 now using Fathom directly", not specifically tb_probe_root_dtz. Document the chosen variant in the SUMMARY
    - Implement ~SyzygyState destructor: if initialized_, call tb_free() and set initialized_ = false
    - Implement set_max_pieces: max_pieces_ = std::clamp(n, 0u, 7u) (Fathom supports up to 7-piece in some builds; default 6)
    - All log strings MUST match D-08 character-for-character. Tests will assert byte-exact substrings

    Step 3 — DO NOT edit src/chess_engine/engine/v7/src/python_bindings.cpp (checker blocker fix; Wave 3 file-ownership invariant):
    - Plan 01 (01-01-PLAN.md) PRE-STAGED both the `set_syzygy_path` AND `tbhits` py::class_<Engine> bindings — verified by reading plan 01 lines 56-57 (must_haves artifact for python_bindings.cpp lists "py::class_<Engine>" with set_syzygy_path on the GIL-released path), 156-165 (interfaces block showing `.def("set_syzygy_path", ...)` and `.def("tbhits", ...)` entries), and acceptance criterion at plan 01 line 342 ("python_bindings.cpp contains exactly 3 occurrences of `py::call_guard<py::gil_scoped_release>` (search, set_syzygy_path, perft)"). The bindings are stable Python entry points; their C++ implementation transparently switches from plan 01's stub body to the real body once: (a) plan 03 writes src/engine.cpp's `Engine::set_syzygy_path` body forwarding to `syzygy_.set_path(...)`, and (b) this plan provides the SyzygyState implementation that the forwarding call lands in.
    - This plan provides ONLY: include/syzygy.hpp (Step 1) and src/syzygy.cpp (Step 2). The Engine forwarding wrapper is written by plan 03 in src/engine.cpp. The pybind11 bindings are written by plan 01 in src/python_bindings.cpp. This plan touches NEITHER.
    - **Acceptance gate:** `git diff --name-only HEAD -- src/chess_engine/engine/v7/src/python_bindings.cpp | wc -l` MUST return 0 (no changes from this plan). If plan 01's bindings are somehow missing the `tbhits` or `set_syzygy_path` `.def` entries when this plan runs, STOP and surface the regression to the orchestrator — do NOT silently re-add them here (that re-introduces the Wave 3 file-ownership conflict the checker flagged).

    Step 4 — Create tests/test_v7_syzygy.py with these 8 tests (KRk endpoint WDL probe assertion is new per checker issue #6):

    Test file header:
    ```python
    import os
    import sys
    import subprocess
    import tempfile
    import pathlib
    import pytest

    SYZYGY_PATH = os.environ.get("SYZYGY_PATH")
    HAS_TABLES = SYZYGY_PATH and pathlib.Path(SYZYGY_PATH).is_dir() and \
                 any(pathlib.Path(SYZYGY_PATH).glob("*.rtbw"))
    HAS_KRVK = HAS_TABLES and any(
        pathlib.Path(SYZYGY_PATH).glob("KRvK.rtbw")
    )

    @pytest.fixture(scope="module")
    def v7():
        from chess_engine.engine.v7 import chess_algorithm as v7_algo
        return v7_algo.ensure_available(auto_build=True)
    ```

    Tests:

    1. test_set_syzygy_path_empty_logs_message(v7, capfd):
       - eng = v7.Engine()
       - eng.set_syzygy_path("")
       - captured = capfd.readouterr()
       - Note: capfd may not capture C++-level stderr; if so, use a subprocess invocation that captures stderr instead. Implementation note: `subprocess.run([sys.executable, "-c", "from chess_engine.engine.v7 import chess_algorithm as v7; m = v7.ensure_available(auto_build=True); e = m.Engine(); e.set_syzygy_path('')"], capture_output=True, text=True)`
       - assert "[v7] syzygy: no path configured; tbhits will be 0" in result.stderr

    2. test_set_syzygy_path_missing_logs_message: subprocess pattern same as #1 with path "/this/path/does/not/exist/xyz"; assert "[v7] syzygy: path not found: /this/path/does/not/exist/xyz; tbhits will be 0" in stderr

    3. test_set_syzygy_path_empty_dir_logs_message: tempfile.TemporaryDirectory() → call set_syzygy_path on empty dir; assert "[v7] syzygy: no tablebase files at" in stderr AND "tbhits will be 0" in stderr

    4. test_set_syzygy_path_never_throws: All three above cases also assert subprocess returncode == 0 (no exception escaped Python)

    5. test_tbhits_starts_zero(v7): eng = v7.Engine(); assert eng.tbhits() == 0

    6. test_in_search_probe_failure_returns_nullopt: This test asserts the C++-level behavior indirectly. Hard to mock TB_RESULT_FAILED without a real broken file. Acceptable approach: write a smoke that calls probe_wdl on a position with castling rights (which we documented as a fast-skip path returning nullopt); assert returns None and no log line emitted. This proves the gating works without needing a corrupted file.

    7. test_krk_endpoint_wdl_probe(v7) [NEW per checker issue #6]:
       - `pytest.mark.skipif(not HAS_KRVK, reason="no SYZYGY_PATH or no KRvK tables")`
       - Invoke set_syzygy_path(SYZYGY_PATH) via subprocess (or directly if Python binding exposes probe_wdl), then probe FEN `"4k3/8/8/8/8/8/4R3/4K3 w - - 0 1"`
       - Expected: ProbeResult.kind == WIN AND ProbeResult.score_cp ≈ +20000 (white-to-move with KR vs k is a known win)
       - If `probe_wdl` is not yet exposed via pybind11 (Phase 1 may keep it C++-internal and only surface tbhits to Python), exercise the assertion via a tiny C++ harness binary built into tests/cpp/ OR via the smoke_probe_krk() return value reflected back through tbhits: after set_syzygy_path with valid KRvK tables, eng.tbhits() must be ≥1 (the KRk smoke probe is itself the WIN-endpoint assertion). Whichever path Phase 1 chooses, the TEST MUST FAIL if the WIN result is not produced — silent skips are not acceptable
       - Document the chosen exposure path (direct Python binding vs tbhits-reflected) in the SUMMARY

    8. test_root_dtz_probe_returns_some_when_tables_present(v7) [TB-04 REAL coverage]:
       - `pytest.mark.skipif(not HAS_KRVK, reason="no SYZYGY_PATH or no KRvK tables")`
       - Same KRk FEN as test 7
       - Expected: probe_root_dtz returns a non-None integer (DTZ ply count to the nearest mate-or-conversion)
       - If probe_root_dtz is not directly exposed to Python in Phase 1, exercise via the same mechanism as test 7

    Append validation map rows to .planning/phases/01-skeleton-smoke/01-VALIDATION.md for TB-01..TB-10 except TB-09 (TB-09 is in plan 06). TB-04 row: "FULLY IMPLEMENTED in plan 05 task 2 step 2 — calls tb_probe_root_dtz directly; KRk DTZ probe assertion in test_root_dtz_probe_returns_some_when_tables_present"
  </action>
  <verify>
    <automated>git diff --name-only HEAD -- src/chess_engine/engine/v7/src/python_bindings.cpp src/chess_engine/engine/v7/include/engine.hpp | wc -l</automated>
    <automated>cmake --build build/v7 --target v7_engine 2>&1 | tail -20</automated>
    <automated>python3 -c "from chess_engine.engine.v7 import chess_algorithm as v7; m = v7.ensure_available(auto_build=True); e = m.Engine(); print('Engine created:', e); print('tbhits:', e.tbhits())"</automated>
    <automated>python3 -c "import subprocess, sys; r = subprocess.run([sys.executable, '-c', 'from chess_engine.engine.v7 import chess_algorithm as v7; m = v7.ensure_available(auto_build=True); e = m.Engine(); e.set_syzygy_path(\"\")'], capture_output=True, text=True); print('STDERR:', r.stderr); assert '[v7] syzygy: no path configured; tbhits will be 0' in r.stderr</automated>
    <automated>python3 -c "import subprocess, sys; r = subprocess.run([sys.executable, '-c', 'from chess_engine.engine.v7 import chess_algorithm as v7; m = v7.ensure_available(auto_build=True); e = m.Engine(); e.set_syzygy_path(\"/no/such/path/xyz\")'], capture_output=True, text=True); assert '[v7] syzygy: path not found: /no/such/path/xyz; tbhits will be 0' in r.stderr, r.stderr</automated>
    <automated>python3 -m uv run --group dev pytest tests/test_v7_syzygy.py -q --tb=short</automated>
    <automated>grep -nE 'TB_RESULT_FAILED|nullopt' src/chess_engine/engine/v7/src/syzygy.cpp | head -10</automated>
    <automated>grep -nE 'no path configured|path not found|no tablebase files|smoke probe failed|tb_init failed' src/chess_engine/engine/v7/src/syzygy.cpp</automated>
    <automated>grep -nE 'tb_probe_root_dtz|tb_probe_root' src/chess_engine/engine/v7/src/syzygy.cpp</automated>
  </verify>
  <acceptance_criteria>
    - **File-ownership gate (checker blocker fix):** verify command 1 (`git diff --name-only HEAD -- src/chess_engine/engine/v7/src/python_bindings.cpp src/chess_engine/engine/v7/include/engine.hpp | wc -l`) returns 0 — this plan made ZERO edits to python_bindings.cpp (owned by plan 01) and ZERO edits to engine.hpp (owned by plan 03). Wave 3 file-ownership invariant preserved.
    - cmake --build target v7_engine succeeds (verify command 2) — proves syzygy.cpp compiles + links + tbconfig.h override works AND plan-02-task-3's EXISTS-guarded Fathom block linked tbprobe.c successfully; ALSO proves plan 01's pre-staged `set_syzygy_path` / `tbhits` bindings successfully resolve to plan 03's forwarding wrappers which call into this plan's SyzygyState implementation
    - Verify command 3: Engine creates, tbhits() == 0
    - Verify command 4: empty-path subprocess prints the EXACT D-08 case-1 verbatim string to stderr
    - Verify command 5: missing-path subprocess prints the EXACT D-08 case-2 verbatim string with the path substituted
    - Verify command 6: all 8 tests in tests/test_v7_syzygy.py pass (or skip cleanly when no SYZYGY_PATH / no KRvK)
    - Verify command 7: src/syzygy.cpp contains both `TB_RESULT_FAILED` check AND `nullopt` return (TB-06 / D-09 invariant present in BOTH probe_wdl and probe_root_dtz)
    - Verify command 8: all 5 D-08 verbatim strings appear in src/syzygy.cpp character-for-character (no path configured / path not found / no tablebase files / smoke probe failed / tb_init failed)
    - Verify command 9: src/syzygy.cpp calls tb_probe_root_dtz (or tb_probe_root) — proves TB-04 is REAL, not a stub
    - tbhits never incremented on probe failure (Engine constructed → set bad path → tbhits() still 0)
    - probe_wdl never returns a numeric DRAW score (0) for a TB_RESULT_FAILED — always nullopt (D-09)
    - probe_root_dtz never returns a fabricated DTZ value on TB_RESULT_FAILED — always nullopt (D-09)
    - KRk endpoint WDL probe asserts ProbeResult::Kind::WIN with score ~+20000cp (test 7) when tables present
    - KRk endpoint root-DTZ probe returns non-None (test 8) when tables present
    - V1-V6 untouched
    - 01-VALIDATION.md appended with TB-01..TB-08 and TB-10 rows pointing to plan 05 task 2; TB-04 row notes FULLY IMPLEMENTED (no deferred-DTZ language)
  </acceptance_criteria>
  <done>SyzygyState fully implemented with D-08 verbatim log discipline; KRk smoke probe wired; in-search probe failures return std::nullopt (never DRAW); TB-04 root-DTZ probe is REAL (calls tb_probe_root_dtz directly per locked decision); KRk endpoint WDL assertion verifies WIN when tables present; tbhits exposed; tests green; V7 can probe real Syzygy tables (WDL + root DTZ) when SYZYGY_PATH is set, gracefully degrades to "tbhits will be 0" otherwise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem: SYZYGY_PATH input from CLI / config | Untrusted user-supplied path; passed to fs::exists, fs::directory_iterator, and tb_init. Fathom's tb_init opens .rtbw/.rtbz files from this path. |
| C/C++ FFI: V7 ↔ Fathom | Fathom is C99 code compiled into our module; uncaught C-level crashes (segfault on corrupted TB file) take down the Python process. |
| stderr output: log strings | User-grep'd; format is a stability promise (D-08). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Malicious .rtbw file at SYZYGY_PATH causes tb_init or tb_probe to corrupt memory | accept | Syzygy is a trusted external dataset; users supply their own paths. Phase 1 mitigates by (a) requiring directory + .rtbw filter before tb_init, (b) KRk smoke probe (TB-10) catches format errors at init time, (c) Phase 4 may add file-hash validation. |
| T-05-02 | Denial of Service | Very large or recursive SYZYGY_PATH directory hangs fs::directory_iterator | mitigate | Iterator uses non-recursive directory_iterator (not recursive_directory_iterator); only first .rtbw match triggers continuation. |
| T-05-03 | Information Disclosure | Log line `[v7] syzygy: path not found: <path>` echoes user-supplied path to stderr | accept | Path is supplied by the same user reading stderr; no cross-tenant exposure. Phase 4 may sanitize for shared deployments. |
| T-05-04 | Repudiation | TB_RESULT_FAILED silently treated as DRAW would invalidate game results | mitigate | D-09 invariant enforced in BOTH probe_wdl and probe_root_dtz: each returns std::nullopt on TB_RESULT_FAILED and emits a stderr log; test_in_search_probe_failure_returns_nullopt verifies probe_wdl; KRk endpoint WDL test verifies the success path. |
| T-05-05 | Tampering | Submodule SHA changes without review (supply-chain) | mitigate | .gitmodules pin + parent commit records the SHA; SUMMARY documents pinned SHA; future SHA bumps require explicit `git submodule update --remote` review. |
| T-05-06 | Spoofing | Log strings altered would break user tooling that greps for them (D-08) | mitigate | Tests assert exact verbatim substrings; verify command 7 greps for all 5 strings in syzygy.cpp source. |
| T-05-07 | Information Disclosure | TB-04 root probe fabricating a DTZ value on failure would mislead UCI clients showing "+M3" when no real mate exists | mitigate | probe_root_dtz returns std::nullopt on TB_RESULT_FAILED; verify command 6 confirms the nullopt-on-failure pattern; verify command 8 confirms the real Fathom call site exists. |
</threat_model>

<verification>
- **File-ownership invariant (Wave 3, checker blocker fix):** ZERO edits to src/chess_engine/engine/v7/src/python_bindings.cpp (owned by plan 01) and ZERO edits to src/chess_engine/engine/v7/include/engine.hpp (owned by plan 03) — verified by the file-ownership git-diff gate at the head of Task 2 verify
- Fathom submodule pinned, .gitmodules entry present, extern/fathom/src/tbprobe.c reachable
- include/tbconfig.h overrides Fathom's via CMake include-path ordering pre-staged in plan 02 task 3 (TB-02 enforced)
- Plan 05 made ZERO edits to CMakeLists.txt (Wave 3 file-ownership invariant preserved)
- v7_engine + v7_uci link with tbprobe.c + syzygy.cpp (EXISTS guard activated by submodule presence)
- set_syzygy_path emits the four D-08 verbatim log strings on stderr; never throws (D-07)
- KRk smoke probe runs after successful tb_init; failure path emits case-4 log AND calls tb_free
- In-search probe_wdl returns std::nullopt on TB_RESULT_FAILED (TB-06 / D-09); NEVER returns 0/DRAW
- TB-04 probe_root_dtz is REAL: calls tb_probe_root_dtz directly; TB_RESULT_FAILED → std::nullopt
- tbhits atomic counter exposed; starts at 0; increments only on successful probes (WDL or root)
- syzygyMaxPieces defaults to 6; clamps probes; in-check positions skipped (Fathom undefined behavior)
- KRk endpoint WDL probe assertion: when SYZYGY_PATH set with KRvK tables, ProbeResult::Kind::WIN with score ~+20000cp
- KRk endpoint root-DTZ probe assertion: when tables present, returns non-None DTZ
- All 8 tests in tests/test_v7_syzygy.py pass or skip cleanly when SYZYGY_PATH unset
- V1-V6 untouched
</verification>

<success_criteria>
- TB-01: Fathom (jdart1) submodule added at src/chess_engine/engine/v7/extern/fathom/ (.gitmodules entry, pinned SHA recorded in SUMMARY)
- TB-02: tbconfig.h override on include path BEFORE Fathom's; 8 macros enumerated (6 attack + TB_pop_lsb + TB_popcount) delegating to v7:: functions
- TB-03: tb_init(syzygyPath) called from Engine::set_syzygy_path with D-07 always-log-never-fail semantics
- TB-04: probe_root_dtz FULLY IMPLEMENTED — calls tb_probe_root_dtz (or tb_probe_root with TB_GET_DTZ decoding) directly; TB_RESULT_FAILED → std::nullopt; KRk DTZ probe assertion in tests
- TB-05: tb_probe_wdl wired in syzygy.cpp; gated by initialized_, TB_LARGEST, max_pieces_, !in_check, no castling
- TB-06: TB_RESULT_FAILED → std::nullopt in BOTH probe_wdl and probe_root_dtz (never DRAW / never fabricated DTZ); stderr log emitted (D-09)
- TB-07: tbhits() atomic counter exposed via Engine binding; increments only on success (WDL or root)
- TB-08: syzygy_max_pieces_ defaults to 6; probe gating uses min(max_pieces_, TB_LARGEST)
- TB-10: KRk smoke probe runs after tb_init; failure → case-4 log + tb_free()
- INT-06: .gitmodules updated with Fathom entry
- (TB-09 syzygy download script lives in plan 06 CLI work)
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton-smoke/01-05-SUMMARY.md` documenting:
- Pinned Fathom submodule SHA (record the exact SHA from `git rev-parse HEAD` inside extern/fathom)
- Any deltas between RESEARCH.md §A3 assumed API and Fathom's actual tbprobe.h API (function names, return constants, macro names in tbconfig.h)
- TB-04 implementation note: which Fathom function was used (tb_probe_root_dtz directly, or tb_probe_root + TB_GET_DTZ decoding); confirm KRk DTZ assertion passes when KRvK tables present
- Test-7 KRk endpoint WDL exposure path: direct Python binding vs tbhits-reflected smoke_probe_krk return — document the chosen mechanism
- TB-08 default: max_pieces_ = 6 (matches CLI config default in plan 06)
- D-08 verbatim log strings: list all 5 strings as they appear in syzygy.cpp for future grep audits
- CMakeLists.txt coexistence note: this plan made ZERO CMakeLists.txt edits; plan-02-task-3's EXISTS-guarded Fathom block activated automatically when submodule files appeared (Wave 3 file-ownership invariant preserved)
- **python_bindings.cpp coexistence note (checker blocker fix):** this plan made ZERO edits to src/python_bindings.cpp — plan 01 pre-staged the `set_syzygy_path` and `tbhits` py::class_<Engine> bindings; this plan only provided the SyzygyState that those bindings indirectly call into via plan 03's forwarding wrappers in src/engine.cpp
- **engine.hpp coexistence note (checker blocker fix):** this plan made ZERO edits to include/engine.hpp — plan 03 owns engine.hpp this wave and is responsible for adding both `v7::RepStack rep_stack_;` AND `v7::SyzygyState syzygy_;` private members + `#include "syzygy.hpp"` near the top
- Pawn-hash cache for EVAL-08 (deferred to Phase 4 NPS recovery) — re-stated here for cross-reference
- Windows MSVC build notes if any specific flags or workarounds were needed for Fathom's C99 sources
</output>
