---
phase: 01-skeleton-smoke
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/chess_engine/engine/v7/include/search.hpp
  - src/chess_engine/engine/v7/src/search.cpp
  - src/chess_engine/engine/v7/include/engine.hpp
  - src/chess_engine/engine/v7/src/engine.cpp
  - src/chess_engine/engine/v7/CMakeLists.txt
  - tests/test_v7_search.py
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-13
  - SRCH-14
  - SRCH-15
  - FOUND-04

must_haves:
  truths:
    - "V7's iterative-deepening loop produces a best move at each completed depth (SRCH-01)"
    - "Aspiration window re-search caps at 4 widenings, then opens to full window (SRCH-02) — pathological positions cannot loop infinitely"
    - "Mate-in-N scores are correctly adjusted by ply on TT store (score_to_tt) and TT probe (score_from_tt) — a position reachable via transposition returns the SAME mate distance whether searched directly or via TT hit (SRCH-13, PITFALLS #1)"
    - "Three-fold repetition inside the search tree returns score 0 (not only at root) — SRCH-14, PITFALLS #7"
    - "TT cutoff is refused when board.halfmove_clock is near the 50-move boundary (≥80 half-moves) — SRCH-14 second part"
    - "Time management produces a budget that leaves ≥10% safety margin; soft deadline at 50% of budget gates new ID iterations; hard deadline at 90% interrupts mid-iteration (SRCH-15)"
    - "Search polls Engine::stop_flag_ via SearchInfo::external_stop pointer every node — calling Engine.stop() from Python interrupts the search within 50 ms at ≥1Mnps (FOUND-04 end-to-end at the C++ level; plan 06 wires GameManager.stop_search → algo_v7.stop_engine)"
  artifacts:
    - path: "src/chess_engine/engine/v7/include/search.hpp"
      provides: "v7::SearchInfo struct with external_stop pointer; v7::TimeManager struct with allocate/budget; v7::score_to_tt and v7::score_from_tt inline functions; v7::ASPIRATION_MAX_REWIDENS constant; SearchResult full definition"
    - path: "src/chess_engine/engine/v7/src/search.cpp"
      provides: "v7::iterative_deepening, v7::alpha_beta (PVS), v7::quiescence, in-tree repetition detection"
    - path: "src/chess_engine/engine/v7/src/engine.cpp"
      provides: "v7::Engine::search() body wiring SearchInfo.external_stop = &stop_flag_; v7::Engine::new_game() clears tt_ and rep_stack_"
    - path: "tests/test_v7_search.py"
      provides: "test_iterative_deepening, test_aspiration_cap, test_mate_score_tt, test_repetition_in_tree, test_50move_tt_cutoff, test_time_management_budget, test_time_management_safety_margin, test_time_management_returns_best_so_far, test_cancellation_latency_during_real_search"
  key_links:
    - from: "src/chess_engine/engine/v7/src/engine.cpp::Engine::search"
      to: "v7::iterative_deepening"
      via: "SearchInfo info; info.external_stop = &stop_flag_; info.time_limit_ms = time_ms; iterative_deepening(board_, info, ...)"
      pattern: "info\\.external_stop\\s*=\\s*&stop_flag_"
    - from: "src/chess_engine/engine/v7/src/search.cpp"
      to: "v7::SearchInfo::check_time"
      via: "polled inside alpha_beta and quiescence per V6 pattern; check_time also consults external_stop atomic"
      pattern: "external_stop->load"
    - from: "src/chess_engine/engine/v7/src/search.cpp TT store/probe sites"
      to: "v7::score_to_tt / v7::score_from_tt"
      via: "every TT.store call wraps score with score_to_tt(s, ply); every TT.probe consumer wraps with score_from_tt(s, ply)"
      pattern: "score_to_tt|score_from_tt"
---

<objective>
Port V6's sequential PVS + iterative deepening + aspiration + quiescence search into namespace v7 and apply the four pre-C1 must-fixes that close known V6 anti-patterns: (1) wire SearchInfo.external_stop to Engine::stop_flag_ so Python-side cancellation actually reaches the C++ search (FOUND-04 — the V6 bug from python_bindings.cpp), (2) add score_to_tt / score_from_tt mate-ply correction at every TT store and probe (SRCH-13, PITFALLS #1), (3) add in-tree repetition detection plus high-halfmove TT-cutoff refusal (SRCH-14, PITFALLS #7), (4) add a TimeManager with soft + hard deadlines and ≥10% safety margin (SRCH-15, PITFALLS #33). Also add the SRCH-02 aspiration re-search cap (4 widenings then full window).

Purpose: Lock the search contract for plan 06's smoke game (D-04 requires V7-as-white plays a complete legal game vs V6 at fixed depth — that requires a search that returns legal moves, observes cancellation, and doesn't bust the time control on the few moves where time management is exercised). The four fixes are independently testable; this plan builds unit tests for each. The full end-to-end smoke game lives in plan 06.

Output: V7 search.cpp + search.hpp ported and patched; engine.cpp written with the SearchInfo.external_stop wiring; CMakeLists.txt search source added; tests/test_v7_search.py exercises each fix in isolation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-skeleton-smoke/01-RESEARCH.md
@.planning/phases/01-skeleton-smoke/01-PATTERNS.md
@.planning/phases/01-skeleton-smoke/01-01-SUMMARY.md
@.planning/phases/01-skeleton-smoke/01-02-SUMMARY.md

# V6 sources to fork + audit:
@src/chess_engine/engine/v6/include/search.hpp
@src/chess_engine/engine/v6/src/search.cpp
@src/chess_engine/engine/v6/src/tt.cpp
@src/chess_engine/engine/v6/include/tt.hpp
@src/chess_engine/engine/v6/src/eval.cpp

# Plan 02 outputs (V7 board/movegen/magic/tt now exist):
@src/chess_engine/engine/v7/include/board.hpp
@src/chess_engine/engine/v7/include/movegen.hpp
@src/chess_engine/engine/v7/include/tt.hpp
@src/chess_engine/engine/v7/include/engine.hpp
@src/chess_engine/engine/v7/CMakeLists.txt

<interfaces>
<!-- SearchInfo V7 shape (PATTERNS.md lines 397-421, RESEARCH.md §B1 "Cross-Cutting: SearchInfo Wiring") -->
namespace v7 {
struct SearchInfo {
    std::chrono::steady_clock::time_point start_time;
    int time_limit_ms = 5000;
    std::atomic<uint64_t> nodes{0};
    int depth = 0;
    int seldepth = 0;
    int score = 0;
    Move best_move = MOVE_NONE;
    std::atomic<bool> stopped{false};
    int num_threads = 1;
    std::atomic<bool>* external_stop = nullptr;  // NEW — points at Engine::stop_flag_
    int soft_deadline_ms = 0;                    // NEW — set by TimeManager
    int hard_deadline_ms = 0;                    // NEW — set by TimeManager (== time_limit_ms)
    void reset();                                // see implementation in PATTERNS.md
    bool check_time();                           // polls clock + external_stop
    int elapsed_ms() const;
};
constexpr int ASPIRATION_WINDOW = 25;            // cp; inherit V6 value
constexpr int ASPIRATION_MAX_REWIDENS = 4;       // NEW for SRCH-02 cap
constexpr int MATE_VALUE = 32000;                // confirm V6's value; inherit
constexpr int MATE_IN_MAX_PLY = MATE_VALUE - 256;
constexpr int INFINITY_SCORE = 32500;            // confirm V6 value
inline int score_to_tt(int score, int ply);
inline int score_from_tt(int score, int ply);
struct TimeManager {
    int hard_deadline_ms;
    int soft_deadline_ms;
    static TimeManager allocate(int remaining_ms, int increment_ms, int moves_to_go = 30);
};
SearchResult iterative_deepening(Board& board, SearchInfo& info, bool verbose = false);
int alpha_beta(Board& board, int depth, int alpha, int beta, SearchInfo& info, int ply, std::vector<Move>& pv, bool do_null);
int quiescence(Board& board, int alpha, int beta, SearchInfo& info, int ply);
} // namespace v7
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fork V6 search + audit V6 for mate-TT and repetition status; apply SRCH-01/02/13/14 patches; wire Engine::search() to SearchInfo.external_stop</name>
  <files>
    src/chess_engine/engine/v7/include/search.hpp,
    src/chess_engine/engine/v7/src/search.cpp,
    src/chess_engine/engine/v7/include/engine.hpp,
    src/chess_engine/engine/v7/src/engine.cpp,
    src/chess_engine/engine/v7/CMakeLists.txt
  </files>
  <read_first>
    src/chess_engine/engine/v6/include/search.hpp,
    src/chess_engine/engine/v6/src/search.cpp,
    src/chess_engine/engine/v6/src/tt.cpp,
    src/chess_engine/engine/v6/include/tt.hpp,
    src/chess_engine/engine/v7/include/engine.hpp,
    src/chess_engine/engine/v7/include/board.hpp,
    src/chess_engine/engine/v7/include/tt.hpp,
    src/chess_engine/engine/v7/CMakeLists.txt,
    .planning/phases/01-skeleton-smoke/01-RESEARCH.md,
    .planning/phases/01-skeleton-smoke/01-PATTERNS.md
  </read_first>
  <behavior>
    - After this task, building V7 succeeds; Engine.search(fen, depth, time_ms) returns a SearchResult with a legal best_move (not MOVE_NONE) for the starting position at depth ≥ 1
    - Calling Engine.stop() from a Python thread while Engine.search runs at depth 8+ on the starting position interrupts within 50 ms (FOUND-04 end-to-end)
    - In a transposition setup (position A → move X → position B == position C reachable via different move sequence), if position B is found mate-in-3 and stored in TT, then probing TT for position C returns mate-in-3 — NOT mate-in-(3 + ply_delta) (SRCH-13)
    - The aspiration loop in iterative_deepening caps re-searches at ASPIRATION_MAX_REWIDENS=4, then sets alpha=-INFINITY_SCORE, beta=INFINITY_SCORE (SRCH-02)
    - In-tree repetition: if the same position appears 3 times in the search line since the last irreversible move (capture or pawn move), alpha_beta returns 0 (SRCH-14)
    - Halfmove-clock TT guard: when board.halfmove_clock >= 80, TT probe does NOT return a cached non-draw score for cutoff
  </behavior>
  <action>
    Step 1 — Audit V6 first (RESEARCH.md Open Question 1, Assumptions A4 + A5):
    - Grep V6 search.cpp + tt.cpp for `MATE`, `MATE_IN`, `MATE_VALUE`, `score_to_tt`, `score_from_tt`. Document findings inline in 01-03-SUMMARY.md (do this as part of writing the SUMMARY at task completion):
      * If V6 already implements score_to_tt / score_from_tt correctly: V7 inherits the pattern via the fork, no diff needed
      * If V6 omits the adjustment: V7 ADDS the inline functions per the `<interfaces>` block and modifies every TT store/probe site
    - Grep V6 search.cpp for `repetition`, `is_threefold`, `rep_stack`, `halfmove`:
      * If V6 already maintains a per-search repetition stack with in-tree detection: V7 inherits
      * If V6 only checks repetition at root: V7 ADDS the rep-stack + per-node check per RESEARCH.md §B1 "Cross-Cutting: Repetition Detection"
    - The audit RESULT determines whether SRCH-13 / SRCH-14 are no-op renames or active patches. Either outcome is correct and committed.

    Step 2 — Fork V6 search files:
    - src/chess_engine/engine/v6/include/search.hpp → src/chess_engine/engine/v7/include/search.hpp (v6→v7 / V6→V7 renames; same as plan 02 fork mechanics)
    - src/chess_engine/engine/v6/src/search.cpp → src/chess_engine/engine/v7/src/search.cpp (same)

    Step 3 — Apply the four patches to the V7 forks (RESEARCH.md §B1 + PATTERNS.md "search.cpp" section):

    Patch A (FOUND-04, SearchInfo external_stop):
    - In search.hpp SearchInfo struct, add `std::atomic<bool>* external_stop = nullptr;` after the `stopped` field
    - In search.hpp SearchInfo, add `int soft_deadline_ms = 0;` and `int hard_deadline_ms = 0;` (used by TimeManager)
    - In SearchInfo::reset() body, do NOT clobber external_stop (preserve across reset); also propagate `external_stop->load()` into `stopped` so a stop request set before search starts is respected
    - In SearchInfo::check_time() body, FIRST check `external_stop && external_stop->load(std::memory_order_relaxed)` and if true set stopped=true and return true; then do the existing clock check against hard_deadline_ms (preferred) or time_limit_ms (V6 field; soft fallback)
    - Use the exact bodies from PATTERNS.md "V7 deltas (FOUND-04)" section

    Patch B (SRCH-02 aspiration cap):
    - In src/search.cpp iterative_deepening, replace V6's single full-window re-search (PATTERNS.md "V7 SRCH-02 fix shape") with the bounded widening loop:
      * Track `int rewidens = 0; int delta = ASPIRATION_WINDOW;`
      * `while (score <= alpha || score >= beta)` loop
      * Inside: if rewidens >= ASPIRATION_MAX_REWIDENS open to full window; else double delta and adjust alpha/beta
      * Break on info.stopped

    Patch C (SRCH-13 mate-TT correction):
    - In src/search.hpp, add inline `score_to_tt(int, int)` and `score_from_tt(int, int)` per the `<interfaces>` block above
    - If audit found V6 ALREADY does this: rename only (V6's existing functions become v7's via namespace rename). If V6 does NOT: add the new inline functions and wrap every TT.store(...) and TT.probe(...) consumer site in search.cpp. The TT.probe returns a struct or score; wrap the score read with `score_from_tt(entry.score, ply)`. The TT.store call sites pass the score; wrap with `score_to_tt(best_score, ply)`.
    - Use grep to find all TT store/probe sites in search.cpp before editing (search for `tt_.store`, `tt_.probe`, or whatever V6 names use). EVERY site must be wrapped — partial wrapping silently breaks transposition handling.

    Patch D (SRCH-14 repetition + 50-move TT guard):
    - Repetition stack location decision: if V6 has a rep stack on Board, use it; else add it to Board (`uint64_t rep_stack[MAX_PLY + 256]; int rep_top = 0;` in board.hpp). Plan 02 was a pure fork so if V6 lacked it, V7 lacks it — this patch is the ONLY plan 03 addition to board.hpp. Modify board.hpp ONLY if necessary; prefer extending Board over thread-local statics.
    - make_move: push board.hash to rep_stack[rep_top++]; on irreversible move (capture or pawn move) reset rep_top to 0 (or push a sentinel; mirror V6 if V6 has a pattern)
    - unmake_move: pop rep_stack
    - In alpha_beta, near the top after stop-check, add the in-tree repetition check per PATTERNS.md "SRCH-14 (repetition in tree) shape": iterate rep_stack backwards by 2 from rep_top-2 down to rep_top - halfmove_clock, counting matches against board.hash; if count >= 2 return 0 (3-fold = current + 2 prior)
    - In TT probe consumer site, guard cutoff: `if (board.halfmove_clock >= 80) skip_tt_cutoff;` — V7 still PROBES TT for move ordering (the best_move field) but does not return the cached score as a cutoff. The exact threshold (80) matches RESEARCH.md §B1 50-move TT cutoff guidance.

    Patch E (TimeManager, SRCH-15 — split into Task 2 for testing isolation; this task only DECLARES the TimeManager struct in search.hpp with its allocate() static, and integrates the soft_deadline / hard_deadline reads in iterative_deepening loop guard. Task 2 implements the body and tests):
    - Declare `struct TimeManager { int hard_deadline_ms; int soft_deadline_ms; static TimeManager allocate(int remaining_ms, int increment_ms, int moves_to_go = 30); };` in search.hpp
    - Add minimal body in search.cpp: budget = remaining/max(moves_to_go,1) + increment*95/100; clamp to remaining*9/10 (≥10% safety); return {budget, budget/2}
    - In iterative_deepening: between ID iterations, if `info.soft_deadline_ms > 0 && info.elapsed_ms() >= info.soft_deadline_ms` then break (do not start a new iteration)
    - check_time hard-deadline: if `info.hard_deadline_ms > 0 && info.elapsed_ms() >= info.hard_deadline_ms` set stopped=true

    Step 4 — Write src/chess_engine/engine/v7/src/engine.cpp:
    - Implement `v7::Engine::Engine()` — default; may pre-allocate TT in v6 style
    - Implement `v7::Engine::new_game()` — calls tt_.clear() (or reset, per V6 pattern); resets stop_flag_, tbhits_, nodes_; clears board_'s repetition stack if Board owns one
    - Implement `v7::Engine::search(const std::string& fen, int depth, int time_ms)`:
      * board_.from_fen(fen)
      * Construct SearchInfo info; set info.external_stop = &stop_flag_; info.time_limit_ms = time_ms
      * Construct TimeManager tm = TimeManager::allocate(time_ms, 0); info.soft_deadline_ms = tm.soft_deadline_ms; info.hard_deadline_ms = tm.hard_deadline_ms
      * Cap depth at min(depth, 64)
      * stop_flag_.store(false) at the top of search (so a stop() from a PREVIOUS search doesn't immediately cancel this one)
      * SearchResult r = v7::iterative_deepening(board_, info)
      * Increment nodes_ counter to match info.nodes (or restructure: have SearchInfo write directly into engine's atomic — V6 pattern likely uses local atomic)
      * Return r

    Step 5 — Update CMakeLists.txt:
    - Replace `# TODO(plan-03): add search.cpp` comment with explicit `src/search.cpp` and `src/engine.cpp` entries in V7_SOURCES
    - Same additions to v7_uci target_sources call (search and engine are linked into the standalone binary too)

    Step 6 — Update include/engine.hpp:
    - Remove plan 01's stub Engine member function definitions if they were inline (move bodies to engine.cpp)
    - Keep stop() inline (single atomic write, performance sensitive)
    - Keep tbhits() / nodes() inline accessors
    - Engine::search and Engine::new_game move to engine.cpp
  </action>
  <verify>
    <automated>python3 -m uv run --extra build python -m chess_engine.engine.v7.native_build 2>&1 | tail -20</automated>
    <automated>python3 -c "from chess_engine.engine.v7 import chess_algorithm as v7; m = v7.ensure_available(auto_build=True); e = m.Engine(); r = e.search('rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1', 4, 5000); print('best_move:', r.best_move, 'score:', r.score, 'depth:', r.depth, 'nodes:', r.nodes); assert r.depth >= 1 and r.nodes > 0"</automated>
    <automated>python3 -c "
import time, threading
from chess_engine.engine.v7 import chess_algorithm as v7
m = v7.ensure_available(auto_build=True); e = m.Engine()
def runner(): e.search('rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1', 12, 30000)
t = threading.Thread(target=runner); t.start()
time.sleep(0.2)
t0 = time.monotonic(); e.stop(); t.join(); dt = time.monotonic() - t0
print('stop-to-exit ms:', dt*1000); assert dt < 0.5, f'cancellation too slow: {dt*1000}ms'
"</automated>
    <automated>grep -c 'external_stop' src/chess_engine/engine/v7/include/search.hpp src/chess_engine/engine/v7/src/search.cpp src/chess_engine/engine/v7/src/engine.cpp</automated>
    <automated>grep -c 'score_to_tt\|score_from_tt' src/chess_engine/engine/v7/src/search.cpp</automated>
    <automated>grep -c 'ASPIRATION_MAX_REWIDENS' src/chess_engine/engine/v7/include/search.hpp src/chess_engine/engine/v7/src/search.cpp</automated>
  </verify>
  <acceptance_criteria>
    - CMake build green; v7_engine module loads
    - Verify command 2: Engine.search returns a SearchResult with depth>=1 and nodes>0 for startpos at depth 4
    - Verify command 3: stop()-to-thread-exit elapsed < 500 ms (target was 50ms but with reasonable margin; SRCH-15 unit tests in task 2 tighten this)
    - search.hpp SearchInfo struct contains `std::atomic<bool>* external_stop = nullptr` (grep verify), `int soft_deadline_ms`, `int hard_deadline_ms`, `ASPIRATION_MAX_REWIDENS` constant (=4)
    - SearchInfo::check_time() body reads external_stop before the clock check (verify with `grep -A3 'check_time' src/chess_engine/engine/v7/src/search.cpp | grep external_stop`)
    - search.cpp iterative_deepening: aspiration loop counts widenings up to ASPIRATION_MAX_REWIDENS, then sets alpha=-INFINITY_SCORE, beta=INFINITY_SCORE (verify with grep for ASPIRATION_MAX_REWIDENS in search.cpp and confirm `INFINITY_SCORE` appears nearby)
    - score_to_tt and score_from_tt declared in search.hpp; called at TT store and probe sites in search.cpp (verify command 5 returns ≥2 — both functions referenced)
    - Repetition detection: search.cpp contains a loop over a rep stack with halfmove_clock bounds (verify with `grep -c 'halfmove_clock' src/chess_engine/engine/v7/src/search.cpp` returns ≥1, and `grep -c 'rep_stack\|repetition' src/chess_engine/engine/v7/src/search.cpp` returns ≥1)
    - TT cutoff guard: `grep -c 'halfmove_clock >= 80\|halfmove_clock >= MAX_HALFMOVE_FOR_TT_CUTOFF' src/chess_engine/engine/v7/src/search.cpp` returns ≥1
    - engine.cpp `Engine::search` body contains the literal `info.external_stop = &stop_flag_` (verify command 4)
    - engine.cpp `Engine::search` calls `stop_flag_.store(false)` at the top of search (so prior stop doesn't poison new search)
    - engine.cpp `Engine::new_game` calls `tt_.clear()` (or whatever V6's TT reset method is named)
    - CMakeLists.txt V7_SOURCES now contains `src/search.cpp` and `src/engine.cpp` literals
    - V1-V6 directories untouched
    - 01-03-SUMMARY.md (drafted at end of task) documents the V6 audit finding (V6 already does score_to_tt / V6 does not; V6 already has rep stack / V6 does not — so the next executor knows whether these were V7 patches or V7 inheritance)
  </acceptance_criteria>
  <done>V7 search compiled and produces legal moves at depth >=1; cancellation works through SearchInfo.external_stop with sub-500ms latency; aspiration cap, mate-TT correction, repetition detection, and 50-move TT guard all present in source; engine.cpp wires Engine::search to SearchInfo.external_stop pointer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for SRCH-01/02/13/14/15 and FOUND-04 — one focused test per concern with deterministic inputs</name>
  <files>
    tests/test_v7_search.py
  </files>
  <read_first>
    tests/test_v6_native.py,
    tests/test_v7_engine.py,
    src/chess_engine/engine/v7/include/search.hpp,
    src/chess_engine/engine/v7/src/search.cpp,
    src/chess_engine/engine/v7/src/engine.cpp,
    .planning/phases/01-skeleton-smoke/01-RESEARCH.md
  </read_first>
  <behavior>
    - All 9 tests pass with v7_native_engine fixture
    - test_cancellation_latency_during_real_search: stop()-to-exit < 50 ms during an actively-running depth-15 search on startpos (this is the tight version of plan 01's binding-only test; depends on plan 03's per-node external_stop polling)
    - test_time_management_budget: TimeManager.allocate(remaining=10000, increment=0, moves_to_go=40) produces hard_deadline_ms ≈ 250 (10000/40) and soft_deadline_ms ≈ 125 (half of hard)
    - test_time_management_safety_margin: TimeManager.allocate(remaining=1000, increment=0, moves_to_go=1) caps at 900 (≥10% safety margin: 1000 * 9/10 = 900), NOT 1000 — proves the clamp
    - test_time_management_returns_best_so_far: Engine.search at time_ms=200 on a moderately complex position returns a SearchResult with best_move != MOVE_NONE (the search never returns MOVE_NONE because the soft deadline gates new iterations after at least depth 1 completes)
    - test_iterative_deepening: Engine.search at depth=5 reports depth >= 1 in the SearchResult (proves ID actually ran multiple iterations rather than directly jumping to depth=5)
    - test_aspiration_cap: contrived adversarial position designed to cause repeated fail-high/low does NOT loop infinitely (test asserts search completes within 10 seconds; uses time.monotonic guard)
    - test_mate_score_tt: position with mate-in-3 found at the search root returns score ≥ MATE_VALUE - 6 (or however V7 encodes mate-in-3); same position after one make_move/unmake step (ply offset 1) still returns the correct mate distance via TT hit — uses a known mate-in-3 position
    - test_repetition_in_tree: position designed to allow a 3-fold repetition inside depth 5 search returns a draw-evaluation score (~0) for the position
    - test_50move_tt_cutoff: position with halfmove_clock=85 does NOT return a previously-stored non-draw TT score as cutoff (verified by comparing search of position with halfmove=0 vs halfmove=85)
  </behavior>
  <action>
    Create tests/test_v7_search.py exercising the SRCH-* and FOUND-04 patches in plan 03's task 1.

    Module structure:
    - Imports: pytest, time, threading, the v7 chess_algorithm
    - Local fixture v7_native_engine (mirror tests/test_v7_engine.py / test_v6_native.py pattern)
    - Constants:
      * STARTPOS_FEN
      * Known mate-in-3 position (use a published puzzle; e.g., "r1b1k2r/pppp1ppp/2n2n2/2b1p2Q/2B1P3/2N5/PPPP1PPP/R1B1K1NR w KQkq - 0 1" or similar — verify the position is actually mate-in-3 before locking the test by running V6 search first if available)
      * Repetition setup position with halfmove_clock that allows perpetual check (a "perpetual check" position is the standard test — use a published one; the test forces the engine to find the draw)
      * Position with halfmove_clock=85 for 50-move TT test (construct by setting halfmove_clock high in the FEN: e.g., a quiet middlegame position with halfmove counter manipulated)
      * Adversarial aspiration position (RESEARCH.md doesn't supply one — a position with large eval swings between iterations is sufficient; if no canonical position exists, use a tactical sharp position and a tight time budget that forces multiple re-searches; assert wall-clock bound rather than exact behavior)

    Test contents:
    1. test_iterative_deepening(v7_native_engine):
       - e = v7_native_engine.Engine(); r = e.search(STARTPOS_FEN, depth=5, time_ms=10000)
       - assert r.depth >= 5; assert r.best_move != 0 (or != MOVE_NONE constant if exposed)
       - assert r.nodes > 0

    2. test_aspiration_cap(v7_native_engine):
       - e = v7_native_engine.Engine()
       - t0 = time.monotonic()
       - e.search(ADVERSARIAL_FEN, depth=8, time_ms=5000)
       - assert time.monotonic() - t0 < 10.0 (cap prevents infinite re-search; 10s is generous for a depth-8 search with potential aspiration storms)

    3. test_mate_score_tt(v7_native_engine):
       - e = v7_native_engine.Engine(); e.new_game()
       - r1 = e.search(MATE_IN_3_FEN, depth=8, time_ms=5000)
       - assert r1.score >= MATE_THRESHOLD (where MATE_THRESHOLD is a value high enough to represent "mate" — e.g., 30000; do not hardcode the exact mate distance encoding without inspecting V7's MATE_VALUE)
       - The TT-correctness aspect: search the same position via a one-move-then-undo transposition so the TT entry is stored at ply 1; then re-search the original position at ply 0. Both should report mate scores in the same range (within ply-distance tolerance). This is awkward at the binding level — alternative: verify SRCH-13 indirectly by asserting V7 doesn't claim mate-in-N when V6 (or a fresh search without TT) doesn't. Use V6 as oracle if available; if not, accept the weaker assertion that mate detection works.

    4. test_repetition_in_tree(v7_native_engine):
       - e = v7_native_engine.Engine(); e.new_game()
       - r = e.search(PERPETUAL_CHECK_FEN, depth=6, time_ms=5000)
       - assert -50 < r.score < 50 (draw range; absolute value < 50cp indicates the engine sees the draw rather than a winning continuation)
       - If the engine returns the perpetual-check move as best, that's strong evidence repetition-as-draw was scored at the leaf

    5. test_50move_tt_cutoff(v7_native_engine):
       - e = v7_native_engine.Engine(); e.new_game()
       - r_fresh = e.search(POSITION_FEN_HALFMOVE_0, depth=6, time_ms=2000)
       - e2 = v7_native_engine.Engine(); e2.new_game()
       - r_high = e2.search(POSITION_FEN_HALFMOVE_85, depth=6, time_ms=2000)
       - The exact assertion depends on whether the position is genuinely affected by 50-move rule; use a position where halfmove=85 makes 50-move-draw reachable within 15 plies and the search should see it; assert r_high.score is closer to 0 than r_fresh.score (or assert both finish without crash and execute the code path — the existence test is the floor; the behavioral test is the ceiling)

    6. test_time_management_budget(v7_native_engine):
       - Expose TimeManager.allocate via the pybind11 module (or via a static method on Engine — quickest path is to add a free function `m.def("compute_time_budget", &v7::TimeManager::allocate_for_binding, ...)` in python_bindings.cpp; this is a small test-only binding addition. ALTERNATIVELY: test indirectly by calling Engine.search with time_ms=10000 and asserting elapsed wall time stays under 9000ms which proves the 10% safety margin is in effect)
       - Decision: skip the bind-TimeManager-to-Python approach (test surface bloat); use the indirect test. The test verifies safety margin via wall clock:
         * t0 = time.monotonic(); e.search(STARTPOS_FEN, depth=99, time_ms=2000); dt = (time.monotonic() - t0) * 1000
         * assert dt < 1900 (10% safety margin on 2000ms budget)

    7. test_time_management_safety_margin(v7_native_engine):
       - Same shape as test 6, with time_ms=1000; assert elapsed < 950ms

    8. test_time_management_returns_best_so_far(v7_native_engine):
       - e = v7_native_engine.Engine(); r = e.search(STARTPOS_FEN, depth=99, time_ms=500)
       - assert r.best_move != 0 (or != MOVE_NONE) — even with an aggressive 500ms budget, ID completes at least depth 1 and the best move is returned

    9. test_cancellation_latency_during_real_search(v7_native_engine):
       - Same shape as plan 01 test_cancellation_latency but with REAL search (post-plan-03):
       - e = v7_native_engine.Engine()
       - def runner(): e.search(STARTPOS_FEN, depth=20, time_ms=60000)
       - t = threading.Thread(target=runner); t.start()
       - time.sleep(0.3) (let search reach depth 4-6)
       - t0 = time.monotonic(); e.stop(); t.join()
       - dt = time.monotonic() - t0
       - assert dt < 0.05, f"cancellation latency too high: {dt*1000:.1f}ms"
       - 50 ms is the FOUND-04 contract from Phase 1 success criteria #4

    Mark slow tests (those needing > 5 seconds of search) per project convention. If position FENs are uncertain, use V6 as oracle: compute the expected behavior by running V6's search on the same FEN and embed the result as the expected V7 behavior. Cross-engine oracles are documented in RESEARCH.md §B1 as the pattern.

    Append rows to .planning/phases/01-skeleton-smoke/01-VALIDATION.md per-task verification map:
    - 03-T2 | 03 | 3 | SRCH-01 | pytest tests/test_v7_search.py::test_iterative_deepening -x
    - 03-T2 | 03 | 3 | SRCH-02 | pytest tests/test_v7_search.py::test_aspiration_cap -x
    - 03-T2 | 03 | 3 | SRCH-13 | pytest tests/test_v7_search.py::test_mate_score_tt -x
    - 03-T2 | 03 | 3 | SRCH-14 | pytest tests/test_v7_search.py::test_repetition_in_tree -x
    - 03-T2 | 03 | 3 | SRCH-14 | pytest tests/test_v7_search.py::test_50move_tt_cutoff -x
    - 03-T2 | 03 | 3 | SRCH-15 | pytest tests/test_v7_search.py::test_time_management_budget -x
    - 03-T2 | 03 | 3 | SRCH-15 | pytest tests/test_v7_search.py::test_time_management_safety_margin -x
    - 03-T2 | 03 | 3 | SRCH-15 | pytest tests/test_v7_search.py::test_time_management_returns_best_so_far -x
    - 03-T2 | 03 | 3 | FOUND-04 | pytest tests/test_v7_search.py::test_cancellation_latency_during_real_search -x
  </action>
  <verify>
    <automated>python3 -m uv run --group dev pytest tests/test_v7_search.py --collect-only -q</automated>
    <automated>python3 -m uv run --group dev pytest tests/test_v7_search.py -q --tb=short</automated>
    <automated>grep -c 'def test_' tests/test_v7_search.py</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_v7_search.py exists and contains exactly 9 test functions (verify command 3 returns 9)
    - All 9 tests pass (verify command 2 exits 0)
    - test_cancellation_latency_during_real_search asserts dt < 0.05 (50 ms) using time.monotonic — verify with grep for `0.05` and `monotonic` in the test body
    - test_time_management_safety_margin asserts elapsed < 950ms for a 1000ms budget — verify with grep for `< 950` or equivalent
    - test_iterative_deepening asserts r.depth >= 5
    - .planning/phases/01-skeleton-smoke/01-VALIDATION.md per-task verification map has 9 new rows (one per test) referencing this plan and task
    - No V1-V6 directories modified
    - test_v7_search.py uses the v7_native_engine fixture pattern from test_v6_native.py (mirror, do not invent)
  </acceptance_criteria>
  <done>All SRCH-01/02/13/14/15 and FOUND-04 unit tests written and green; validation map updated; cancellation latency proven < 50 ms in real search; time management proven to honor 10% safety margin via wall-clock assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python Thread A (Engine.search held by C++) ↔ Python Thread B (calls Engine.stop()) | Cross-thread atomic communication; correctness of the GIL-released search requires the SearchInfo.external_stop pointer to remain valid for the lifetime of any in-flight search. Engine is heap-allocated by the Python module and outlives any single search call, so the pointer is stable. |
| TT entries written at ply N, probed at ply M | Mate scores are ply-dependent. SRCH-13 fix mitigates the in-engine threat that "a TT hit at a different ply returns a wrong mate distance". Failure mode: claims of mate that don't exist OR misses real mates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | TT poisoning via mate-score off-by-one (PITFALLS #1) | mitigate | score_to_tt/score_from_tt applied at every TT store/probe — verified by grep count ≥2 in acceptance criteria. |
| T-03-02 | Denial of Service | Aspiration window infinite re-search (V6 anti-pattern) | mitigate | ASPIRATION_MAX_REWIDENS=4 cap; test_aspiration_cap asserts wall-clock bound. |
| T-03-03 | Denial of Service | Time-control bust → tournament forfeit (PITFALLS #33) | mitigate | TimeManager ≥10% safety margin clamp; test_time_management_safety_margin proves the clamp via wall-clock measurement. |
| T-03-04 | Repudiation | Search returns MOVE_NONE silently when budget exhausted before depth 1 completes | mitigate | test_time_management_returns_best_so_far asserts non-MOVE_NONE even at tight budgets; the engine must complete at least depth 1 before honoring soft deadline. |
| T-03-05 | Information Disclosure | std::cerr verbose search prints | accept | No PII; matches V6 pattern. |
</threat_model>

<verification>
- v7_engine.Engine().search returns legal moves at all tested depths
- Engine.stop() interrupts an active search within 50 ms (FOUND-04 end-to-end at C++ level)
- All 9 unit tests in tests/test_v7_search.py pass
- search.hpp + search.cpp + engine.cpp build clean; no missed external_stop wiring (grep verify)
- Validation map in 01-VALIDATION.md has new rows for SRCH-01/02/13/14/15 + FOUND-04
- V1-V6 untouched
</verification>

<success_criteria>
- SRCH-01: iterative deepening produces best move at each completed depth (test_iterative_deepening)
- SRCH-02: aspiration window re-search capped at 4 widenings then full window (test_aspiration_cap)
- SRCH-13: mate scores correctly adjusted by ply when stored/retrieved (test_mate_score_tt; V6 audit documented in SUMMARY)
- SRCH-14: in-tree repetition + 50-move TT cutoff guard (test_repetition_in_tree, test_50move_tt_cutoff)
- SRCH-15: time management with ≥10% safety margin; unit tests proving budget and margin (test_time_management_budget, test_time_management_safety_margin, test_time_management_returns_best_so_far)
- FOUND-04: SearchInfo.external_stop wired to Engine::stop_flag_; Python stop() interrupts within 50 ms (test_cancellation_latency_during_real_search)
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton-smoke/01-03-SUMMARY.md` documenting:
- V6 audit findings: whether V6 already implemented score_to_tt/score_from_tt and in-tree repetition (per RESEARCH.md Open Question 1 / Assumptions A4, A5). Record the specific V6 lines inspected and the verdict. This unblocks future plans (if V6 was correct, V7 inheritance is clean; if V6 was wrong, the deferred V6 bug should be tracked as a separate cleanup track per CONTEXT deferred-ideas).
- Patches applied to V7 fork (external_stop wiring, aspiration cap, mate-TT correction if needed, repetition + 50-move guard if needed, TimeManager)
- engine.cpp's Engine::search wiring confirmed (info.external_stop = &stop_flag_)
- Notes for plan 04 (eval) and plan 05 (syzygy) that engine.hpp / search.hpp are now stable contracts they can include
- Notes for plan 06 (smoke) that GameManager.stop_search will need to call algo_v7.stop_engine() which calls Engine.stop() — already wired in plan 01 chess_algorithm.py
</output>
