---
phase: 01-skeleton-smoke
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chess_engine/engine/v7/__init__.py
  - src/chess_engine/engine/v7/chess_algorithm.py
  - src/chess_engine/engine/v7/native_build.py
  - src/chess_engine/engine/v7/CMakeLists.txt
  - src/chess_engine/engine/v7/include/engine.hpp
  - src/chess_engine/engine/v7/src/python_bindings.cpp
  - src/chess_engine/engine/v7/src/uci_main.cpp
  - tests/test_v7_bindings.py
  - tests/test_v7_engine.py
  - .gitignore
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-07
  - INT-09
user_setup:
  - tool: cmake
    why: "V7 native pybind11 module build (matches V6 requirement)"
    notes: "Required >=3.15; install Visual Studio Build Tools on Windows, gcc/clang on POSIX. Project already documents this for V6 — no new install for users who built V6."

must_haves:
  truths:
    - "V7 module exists at src/chess_engine/engine/v7/ mirroring V6's directory layout"
    - "V7 builds cross-platform via CMake FetchContent of pybind11 v2.12.0 (matches V6 pin exactly)"
    - "Python can import v7_engine and instantiate v7_engine.Engine() without exceptions"
    - "Engine.search() releases the GIL while running (FOUND-05) so a concurrent Python thread can call Engine.stop()"
    - "Engine.stop() flips an atomic flag observable from the C++ search thread without GIL acquisition"
    - "find_best_move(game_state, valid_moves, engine, search_info) adapter signature matches the project-wide engine adapter contract (FOUND-03)"
    - "V7 ships its own search_info pattern inside src/chess_engine/engine/v7/ — NO shared src/chess_engine/core/search_info.py is created (the file referenced in CONTEXT.md does not exist and per CLAUDE.md V1-V6 must not be modified). V7 honors the SearchInfo cancellation INTENT via the Engine.stop() atomic + adapter helper."
    - "v7_uci executable target is declared in CMakeLists.txt (stub uci_main.cpp accepted in this plan; full UCI loop completed in plan 06)"
  artifacts:
    - path: "src/chess_engine/engine/v7/__init__.py"
      provides: "Package marker"
    - path: "src/chess_engine/engine/v7/chess_algorithm.py"
      provides: "Loader, ensure_available(auto_build), find_best_move adapter, stop_engine helper, new_game helper, module-level _engine singleton"
      exports: ["V7_AVAILABLE", "_LOAD_ERROR", "V7UnavailableError", "reload_engine", "ensure_available", "find_best_move", "stop_engine", "new_game"]
    - path: "src/chess_engine/engine/v7/native_build.py"
      provides: "CMake driver mirroring v6/native_build.py"
      exports: ["V7BuildError", "V7BuildResult", "build_v7_native", "find_packaged_module"]
    - path: "src/chess_engine/engine/v7/CMakeLists.txt"
      provides: "FetchContent pybind11 v2.12.0; v7_engine MODULE target; v7_uci executable target; coeffs codegen and Fathom hooks reserved as TODO comments for plans 04/05"
      contains: "project(v7_engine LANGUAGES CXX C)"
    - path: "src/chess_engine/engine/v7/include/engine.hpp"
      provides: "v7::Engine class with atomic stop_flag_, tbhits_, nodes_; methods: set_syzygy_path, new_game, search, stop, tbhits, nodes"
    - path: "src/chess_engine/engine/v7/src/python_bindings.cpp"
      provides: "PYBIND11_MODULE(v7_engine, ...) with py::class_<Engine>; py::call_guard<py::gil_scoped_release>() on search/perft/set_syzygy_path"
    - path: "src/chess_engine/engine/v7/src/uci_main.cpp"
      provides: "Stub main() entry point — full UCI loop in plan 06"
    - path: "tests/test_v7_bindings.py"
      provides: "test_v7_layout, test_v7_build, test_v7_import, test_find_best_move_signature, test_gil_released, test_cancellation_latency, test_uci_binary_exists, test_auto_build"
    - path: "tests/test_v7_engine.py"
      provides: "Module-scoped v7_native_engine fixture (auto_build=True, skip on unavailable)"
  key_links:
    - from: "src/chess_engine/engine/v7/chess_algorithm.py"
      to: "v7_engine (compiled pybind11 module)"
      via: "import v7_engine inside _load_v7_engine() with .pyd/.so/.dylib glob fallback"
      pattern: "import v7_engine"
    - from: "src/chess_engine/engine/v7/src/python_bindings.cpp"
      to: "v7::Engine::search"
      via: "py::class_ binding with py::call_guard<py::gil_scoped_release>()"
      pattern: "py::call_guard<py::gil_scoped_release>"
    - from: "src/chess_engine/engine/v7/chess_algorithm.py::stop_engine"
      to: "v7::Engine::stop"
      via: "_engine.stop() invocation on module-level singleton"
      pattern: "_engine\\.stop\\(\\)"
---

<objective>
Establish the V7 skeleton: fork V6's directory shape into src/chess_engine/engine/v7/, write the CMakeLists.txt + native_build.py + Python adapter, and define the new stateful pybind11 binding pattern that closes V6's ignored-SearchInfo bug (FOUND-04) and wraps search in py::call_guard<py::gil_scoped_release>() from day one (FOUND-05). Plan 02 then ports the board/movegen/magic/zobrist/tt C++ files into this scaffold; plans 03/04/05 land search/eval/syzygy in parallel against the same skeleton.

Purpose: Lock the binding contract (Engine class with atomic stop, GIL-released search) and the build pipeline (CMake + pybind11 v2.12.0 + v7_uci executable target) BEFORE any C++ subsystem work. This avoids retrofitting the GIL release pattern later (PITFALLS #11) and gives plans 03/04/05 an Engine class to compile against.

Output: V7 directory tree with skeleton files; CMakeLists that configures successfully even if some sources are stubs; Python adapter that mirrors V6's chess_algorithm.py shape (loader + ensure_available + find_best_move + stop_engine) but binds the new stateful Engine class; pytest fixture file that other plans append tests to.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-skeleton-smoke/01-CONTEXT.md
@.planning/phases/01-skeleton-smoke/01-RESEARCH.md
@.planning/phases/01-skeleton-smoke/01-PATTERNS.md

# V6 analogs the executor will fork:
@src/chess_engine/engine/v6/__init__.py
@src/chess_engine/engine/v6/chess_algorithm.py
@src/chess_engine/engine/v6/native_build.py
@src/chess_engine/engine/v6/CMakeLists.txt
@src/chess_engine/engine/v6/src/python_bindings.cpp
@src/chess_engine/engine/v6/include/search.hpp
@src/chess_engine/engine/v5/search.py
@tests/test_v6_native.py

<interfaces>
<!-- V7 will EXPOSE these from Python (consumed by GameManager, tests, plan 06 INT-01). -->
<!-- Match these names exactly; later plans depend on them. -->

# src/chess_engine/engine/v7/chess_algorithm.py (Python adapter)
V7_AVAILABLE: bool
_LOAD_ERROR: Optional[Exception]
class V7UnavailableError(RuntimeError): ...
def reload_engine() -> Optional[Any]: ...
def ensure_available(auto_build: bool = False) -> Any: ...
def find_best_move(game_state, valid_moves, engine, search_info=None) -> Tuple[Move, Dict[str, Any]]: ...
def stop_engine() -> None: ...
def new_game() -> None: ...
_engine: Optional[Any]  # module-level v7_engine.Engine singleton

# src/chess_engine/engine/v7/native_build.py
class V7BuildError(RuntimeError): ...
class V7BuildResult: module_path: Path; built: bool
def build_v7_native(force: bool = False) -> V7BuildResult: ...
def find_packaged_module() -> Optional[Path]: ...

# src/chess_engine/engine/v7/include/engine.hpp (C++)
namespace v7 {
class Engine {
 public:
    Engine();
    void set_syzygy_path(const std::string& path);   // implementation deferred to plan 05; plan 01 ships a no-op that logs the D-08 "no path configured" line when called with ""
    void new_game();                                  // resets TT + repetition stack; plans 02/03 implement bodies
    SearchResult search(const std::string& fen, int depth, int time_ms);  // plan 03 implements full body; plan 01 ships a stub that returns SearchResult{} with best_move=MOVE_NONE
    void stop();                                      // sets stop_flag_; defined inline
    uint64_t tbhits() const;
    uint64_t nodes() const;
 private:
    std::atomic<bool> stop_flag_{false};
    std::atomic<uint64_t> tbhits_{0};
    std::atomic<uint64_t> nodes_{0};
    // TT tt_;            // populated in plan 02
    // Board board_;      // populated in plan 02
    // SyzygyState syzygy_;  // populated in plan 05
};
struct SearchResult { /* mirror v6 search.hpp shape — populated in plan 03 */ };
} // namespace v7

# src/chess_engine/engine/v7/src/python_bindings.cpp (binding signature)
PYBIND11_MODULE(v7_engine, m) {
    py::class_<v7::Engine>(m, "Engine")
        .def(py::init<>())
        .def("set_syzygy_path", &v7::Engine::set_syzygy_path,
             py::arg("path"), py::call_guard<py::gil_scoped_release>())
        .def("new_game",       &v7::Engine::new_game)
        .def("search",         &v7::Engine::search,
             py::arg("fen"), py::arg("depth") = 6, py::arg("time_ms") = 5000,
             py::call_guard<py::gil_scoped_release>())
        .def("stop",   &v7::Engine::stop)
        .def("tbhits", &v7::Engine::tbhits)
        .def("nodes",  &v7::Engine::nodes);
    m.def("perft", &v7::perft_entry, py::arg("fen"), py::arg("depth"),
          py::call_guard<py::gil_scoped_release>());  // plan 02 implements perft_entry
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fork V6 scaffold (package + adapter + native_build + CMakeLists) and lock the directory layout</name>
  <files>
    src/chess_engine/engine/v7/__init__.py,
    src/chess_engine/engine/v7/chess_algorithm.py,
    src/chess_engine/engine/v7/native_build.py,
    src/chess_engine/engine/v7/CMakeLists.txt,
    .gitignore,
    tests/test_v7_engine.py
  </files>
  <read_first>
    src/chess_engine/engine/v6/__init__.py,
    src/chess_engine/engine/v6/chess_algorithm.py,
    src/chess_engine/engine/v6/native_build.py,
    src/chess_engine/engine/v6/CMakeLists.txt,
    tests/test_v6_native.py,
    .gitignore,
    .planning/phases/01-skeleton-smoke/01-PATTERNS.md
  </read_first>
  <behavior>
    - Importing chess_engine.engine.v7.chess_algorithm exposes V7_AVAILABLE, reload_engine, ensure_available, find_best_move, stop_engine, new_game, V7UnavailableError without raising
    - chess_algorithm.find_best_move signature is (game_state, valid_moves, engine, search_info=None) — verified by inspect.signature
    - native_build.build_v7_native exists and is callable; build_v7_native(force=False) returns a V7BuildResult dataclass-shaped object whose .module_path is a Path
    - CMakeLists.txt declares project(v7_engine LANGUAGES CXX C), FetchContent_Declare(... GIT_TAG v2.12.0), and pybind11_add_module(v7_engine MODULE ...)
    - .gitignore excludes src/chess_engine/engine/v7/src/coeffs.cpp, src/chess_engine/engine/v7/build/, v7_engine*.{so,pyd,dylib}, v7_uci, v7_uci.exe (per D-12 + research §V7 Project Structure)
    - tests/test_v7_engine.py exposes a module-scoped v7_native_engine fixture that calls v7.ensure_available(auto_build=True) and pytest.skips on failure
  </behavior>
  <action>
    Create the V7 directory skeleton by mirroring V6's package shape (NOT by copying files via shell — perform each Write tool call deliberately so the executor reads V6 first, then renders the V7 file with v6→v7 / V6→V7 renames applied).

    Files to create:
    1. src/chess_engine/engine/v7/__init__.py — single-line package marker (mirror V6 line 1: change "V6" to "V7")
    2. src/chess_engine/engine/v7/chess_algorithm.py — fork V6 chess_algorithm.py (referenced in PATTERNS.md lines 60-194). Required deltas:
       - rename v6_engine→v7_engine, V6→V7, V6BuildError→V7BuildError, V6UnavailableError→V7UnavailableError
       - REPLACE the V6 free-function call `engine_module.find_best_move(fen, time, threads)` with stateful singleton: declare module-level `_engine = None` and lazy-init it inside find_best_move via `_engine = v7_engine.Engine()` on first call; then call `_engine.search(fen, depth=6, time_ms=DEFAULT_TIME_LIMIT)` and adapt the return shape to the V6 stats dict {'depth','nodes','time','score','nps','pv'}
       - ADD top-level helpers `stop_engine()` (calls `_engine.stop()` if _engine is not None — safe to call without holding GIL) and `new_game()` (calls `_engine.new_game()` if available)
       - DROP the OPENING_BOOK lookup for V7 — V7 declines the book in phase 1 (research §Pattern note); leave a TODO comment referencing future re-enablement
       - In ensure_available, on auto_build path import from `chess_engine.engine.v7.native_build` (NOT v6); on V7BuildError raise V7UnavailableError with message that mentions `node cli/bin/chess-engine.js build v7`
       - Module-load smoke print: `[V7] C++ Engine loaded successfully!` on success, or warning print with _LOAD_ERROR on failure (mirror V6 lines 93-96)
    3. src/chess_engine/engine/v7/native_build.py — fork V6 native_build.py (PATTERNS.md lines 200-249). Required deltas:
       - rename V6_DIR→V7_DIR, MODULE_PREFIX="v6_engine"→"v7_engine", V6BuildError→V7BuildError, V6BuildResult→V7BuildResult, build_v6_native→build_v7_native
       - Error message in V7BuildError when submodule missing: add explicit note `"Run 'git submodule update --init --recursive' first to fetch Fathom (extern/fathom/src/tbprobe.c)."` (per INT-06; Fathom submodule landed in plan 05, but the build driver should be ready to surface the diagnostic when the submodule is missing)
    4. src/chess_engine/engine/v7/CMakeLists.txt — fork V6 CMakeLists.txt. Required deltas:
       - project name v6_engine→v7_engine; add C to LANGUAGES (Fathom is C99) per PATTERNS.md line 260
       - sources list: include only files that exist at end of plan 01 (python_bindings.cpp, uci_main.cpp). Leave a clearly-marked `# TODO(plan-02): add board.cpp, movegen.cpp, magic.cpp, tt.cpp here` block and `# TODO(plan-03): add search.cpp`, `# TODO(plan-04): add eval.cpp + generated src/coeffs.cpp`, `# TODO(plan-05): add syzygy.cpp + Fathom subdirectory`
       - DEFINE the `pybind11_add_module(v7_engine MODULE ${V7_SOURCES})` target and `add_executable(v7_uci src/uci_main.cpp)` target. The v7_uci target in plan 01 links only uci_main.cpp (stub); plans 03/04/05 will add the rest of the sources to v7_uci via target_sources()
       - Keep OpenMP optional block verbatim from V6 (research §A1: not required Phase 1 but link if found)
       - DO NOT add Fathom subdirectory or coeffs custom_command in this plan — they belong to plans 05 and 04 respectively
    5. .gitignore — append exact paths listed in research §V7 Project Structure "Files added to .gitignore for V7": coeffs.cpp, build/, v7_engine*.so/.pyd/.dylib, v7_uci, v7_uci.exe. All paths prefixed with `src/chess_engine/engine/v7/`. Use a `# V7 build artifacts` comment header
    6. tests/test_v7_engine.py — create the file with imports + module-scoped fixture (mirror V6 lines 6-11 of test_v6_native.py). Adds NO test functions yet; plan 06 appends the smoke + perft tests, this plan only owns the fixture so plan 02/03/04/05/06 can append against a stable base

    Use the V6 file as the structural template for each new file — exact-shape mirror, just with v7 renames and the build-source-list TODOs in CMakeLists.

    Decision per known_correction: V7 ships its own SearchInfo pattern inside src/chess_engine/engine/v7/ (option a). DO NOT create src/chess_engine/core/search_info.py — the file does not exist and CLAUDE.md forbids V1-V6 modification. The Python-side SearchInfo argument on find_best_move is accepted for API compatibility but cancellation is delivered exclusively through Engine.stop() (wired from GameManager in plan 06). Add a docstring on find_best_move documenting this.
  </action>
  <verify>
    <automated>python3 -c "from chess_engine.engine.v7 import chess_algorithm as v7; import inspect; sig = inspect.signature(v7.find_best_move); assert list(sig.parameters) == ['game_state','valid_moves','engine','search_info'], sig; assert hasattr(v7, 'V7UnavailableError'); assert hasattr(v7, 'stop_engine'); assert hasattr(v7, 'new_game'); print('OK')"</automated>
    <automated>python3 -m pytest tests/test_v7_engine.py --collect-only -q</automated>
    <automated>grep -n 'project(v7_engine LANGUAGES CXX C)' src/chess_engine/engine/v7/CMakeLists.txt</automated>
    <automated>grep -n 'GIT_TAG v2.12.0' src/chess_engine/engine/v7/CMakeLists.txt</automated>
    <automated>grep -n 'v7_engine\*\.\(so\|pyd\|dylib\)\|src/chess_engine/engine/v7/src/coeffs\.cpp\|src/chess_engine/engine/v7/build/\|v7_uci' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - File src/chess_engine/engine/v7/__init__.py exists with the single-line `# V7 Python package marker` comment
    - File src/chess_engine/engine/v7/chess_algorithm.py exists; importing it does not raise (the compiled module being absent is acceptable — V7_AVAILABLE flips to False, _LOAD_ERROR is set, but import succeeds)
    - Verify command 1 (signature inspection) prints OK
    - chess_algorithm.py contains a `_engine` module-level binding initialized to None and lazy-instantiated inside find_best_move
    - chess_algorithm.py defines `stop_engine()` and `new_game()` top-level functions that are no-ops when `_engine is None`
    - chess_algorithm.py docstring on find_best_move documents: "search_info is accepted for adapter-contract compatibility; cancellation for V7 is delivered via stop_engine() which flips the C++ atomic — see plan 01 known_correction in PLAN.md"
    - File src/chess_engine/engine/v7/native_build.py exists; `from chess_engine.engine.v7.native_build import build_v7_native, V7BuildError, V7BuildResult, find_packaged_module` succeeds
    - V7BuildError message when submodule is missing contains the literal string `git submodule update --init --recursive`
    - File src/chess_engine/engine/v7/CMakeLists.txt declares `project(v7_engine LANGUAGES CXX C)` and contains `GIT_TAG v2.12.0`
    - CMakeLists.txt contains the four `# TODO(plan-NN):` marker comments described in the action
    - CMakeLists.txt declares both `pybind11_add_module(v7_engine MODULE ...)` and `add_executable(v7_uci ...)` targets
    - .gitignore appended block contains literal entries: `src/chess_engine/engine/v7/src/coeffs.cpp`, `src/chess_engine/engine/v7/build/`, `src/chess_engine/engine/v7/v7_engine*.so`, `src/chess_engine/engine/v7/v7_engine*.pyd`, `src/chess_engine/engine/v7/v7_engine*.dylib`, `src/chess_engine/engine/v7/v7_uci`, `src/chess_engine/engine/v7/v7_uci.exe`
    - File tests/test_v7_engine.py exists with a module-scoped pytest fixture named `v7_native_engine` that calls `v7.ensure_available(auto_build=True)` and `pytest.skip` on V7UnavailableError
    - pytest --collect-only on tests/test_v7_engine.py exits 0 (collects zero tests is fine; file must be importable)
    - NO file is created at src/chess_engine/core/search_info.py (verify with `test ! -f src/chess_engine/core/search_info.py`)
    - V1-V6 engine directories under src/chess_engine/engine/v{1,2,3,4,4b,4c,5,5b,5c,5d,6}/ are unchanged (verify with `git diff --name-only -- src/chess_engine/engine/ | grep -v '^src/chess_engine/engine/v7/'` returns empty)
  </acceptance_criteria>
  <done>V7 package directory exists with __init__.py, chess_algorithm.py, native_build.py, CMakeLists.txt; .gitignore extended; tests/test_v7_engine.py fixture-only file present; importing chess_engine.engine.v7.chess_algorithm succeeds; signature check passes; V1-V6 untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the stateful Engine class skeleton + REWRITTEN pybind11 binding with GIL release + uci_main.cpp stub + binding tests</name>
  <files>
    src/chess_engine/engine/v7/include/engine.hpp,
    src/chess_engine/engine/v7/src/python_bindings.cpp,
    src/chess_engine/engine/v7/src/uci_main.cpp,
    tests/test_v7_bindings.py
  </files>
  <read_first>
    src/chess_engine/engine/v6/src/python_bindings.cpp,
    src/chess_engine/engine/v6/include/search.hpp,
    src/chess_engine/engine/v7/CMakeLists.txt,
    src/chess_engine/engine/v7/chess_algorithm.py,
    .planning/phases/01-skeleton-smoke/01-RESEARCH.md,
    .planning/phases/01-skeleton-smoke/01-PATTERNS.md
  </read_first>
  <behavior>
    - Build (CMake configure + compile) succeeds on the executor's platform; produces v7_engine.{pyd,so,dylib} loadable from Python AND v7_uci executable
    - import v7_engine succeeds in a fresh Python interpreter; v7_engine.Engine() instantiates; e.tbhits() == 0 and e.nodes() == 0 immediately after construction
    - Engine.search("startpos-fen", depth=1, time_ms=100) returns a SearchResult-shaped object (best_move may be MOVE_NONE while plan 03 is pending; the call must not crash and must complete in < 200 ms wall clock for the stub)
    - Engine.stop() returns None and is callable from a different Python thread while Engine.search is executing (proves GIL was released)
    - When stop() is called from a worker thread mid-search, the search returns within 50 ms (verified via concurrent test; depends on plan 03's per-node polling, but the binding-level GIL release is testable in plan 01 by measuring that stop() does not block while search holds CPU)
    - v7_uci executable accepts the literal string `uci\n` on stdin and writes a line containing `id name V7` to stdout, then exits cleanly on `quit\n` (plan 06 expands; plan 01 ships the minimal stub sufficient for the FOUND-07 existence check)
  </behavior>
  <action>
    Write three files plus the bindings test:

    1. src/chess_engine/engine/v7/include/engine.hpp — declare v7::Engine and v7::SearchResult per the `<interfaces>` block in PLAN context. Engine member layout per RESEARCH.md §A1 "Engine class minimal shape" (lines 256-272). Use `std::atomic<bool>` for stop_flag_, `std::atomic<uint64_t>` for tbhits_ and nodes_. Include `<atomic>`, `<string>`, `<cstdint>`. Forward-declare TT/Board/SyzygyState types as opaque (`class TT; class Board; struct SyzygyState;` — full definitions land in plans 02/05). Use `#pragma once`.

       Engine method bodies in plan 01 are stubs:
       - Engine() = default constructor; initializes atomics to 0 via in-class initializers
       - set_syzygy_path: if path empty, write `[v7] syzygy: no path configured; tbhits will be 0\n` to std::cerr (verbatim per D-08) and return; otherwise also write `[v7] syzygy: path not found: <path>; tbhits will be 0\n` to std::cerr — plan 05 replaces with real filesystem checks + tb_init
       - new_game: reset stop_flag_, tbhits_, nodes_ to 0; plan 02/03 will also clear TT and rep stack
       - search: return SearchResult{} with best_move=MOVE_NONE; bumps nodes_ by 1 so the GIL-release test has observable work. Plan 03 replaces.
       - stop: inline `stop_flag_.store(true, std::memory_order_relaxed)`
       - tbhits/nodes: inline returns of atomic .load()

       SearchResult struct: define a minimal struct with `int best_move = 0` (will become v7::Move in plan 02), `int score = 0`, `int depth = 0`, `uint64_t nodes = 0`, `int nps = 0`. Plan 03 expands; plan 02 may replace `int best_move` with `Move best_move` once movegen.hpp lands.

       Add `MOVE_NONE = 0` constant in the v7 namespace as a temporary placeholder (plan 02 replaces with the real Move type).

    2. src/chess_engine/engine/v7/src/python_bindings.cpp — REWRITE per the `<interfaces>` PYBIND11_MODULE block above. Reference RESEARCH.md §A1 "V7 binding pattern" (lines 226-251) and PATTERNS.md "V7 FIX SHAPE" (lines 580-615). Critical bindings:
       - `.def("search", ..., py::call_guard<py::gil_scoped_release>())` — FOUND-05, Pitfall #11
       - `.def("set_syzygy_path", ..., py::call_guard<py::gil_scoped_release>())` — file I/O blocks
       - `.def("stop", &v7::Engine::stop)` — NO call_guard; atomic write is fast and stop_engine() must be callable while holding the GIL from another Python thread
       - `m.def("perft", &v7::perft_entry, ..., py::call_guard<py::gil_scoped_release>())` — declare `static uint64_t perft_entry(const std::string& fen, int depth) { return 0; /* plan 02 implements */ }` as a free function above PYBIND11_MODULE

       Include `<pybind11/pybind11.h>`, `<pybind11/stl.h>`, `"engine.hpp"`. Add `namespace py = pybind11;`. Module docstring: `"V7 Chess Engine - C++17 pybind11 with SearchInfo wiring + GIL release"`.

       Bind SearchResult as a py::class_ with read-only properties for best_move, score, depth, nodes, nps so chess_algorithm.py can unpack the result.

    3. src/chess_engine/engine/v7/src/uci_main.cpp — minimal stub for FOUND-07 existence. Implement a main() loop that:
       - reads stdin line by line
       - on line `uci\n`: writes `id name V7\nid author Chess-Engine V7 milestone\nuciok\n` to stdout, flushes
       - on line `isready\n`: writes `readyok\n` to stdout
       - on line `quit\n`: returns 0
       - all other lines: write `info string V7 phase 1 stub - command ignored\n` (plan 06 implements `position`, `go depth N`, `stop`)

       Use std::getline, std::cin, std::cout. No engine integration in plan 01; the stub proves the CMake target builds.

    4. tests/test_v7_bindings.py — create with 6 tests against the v7_native_engine fixture from tests/test_v7_engine.py. Use `from tests.test_v7_engine import v7_native_engine` or restructure as conftest if needed (V6 pattern is per-test-file fixture, so mirror that and define the fixture locally too). Tests:
       - test_v7_layout: assert Path("src/chess_engine/engine/v7/include/engine.hpp").exists(), same for python_bindings.cpp, CMakeLists.txt, chess_algorithm.py, native_build.py
       - test_v7_build (slow mark): invoke v7.ensure_available(auto_build=True); assert returned module is not None; assert hasattr(module, "Engine") and hasattr(module, "perft")
       - test_v7_import: import v7_engine module from the fixture; assert Engine() instantiates and e.tbhits() == 0 and e.nodes() == 0
       - test_find_best_move_signature: same inspect.signature check as Task 1's verify, plus assert default for search_info is None
       - test_gil_released: spawn a Python thread that runs `_engine.search(STARTPOS_FEN, depth=8, time_ms=3000)`; main thread sleeps 50ms, calls `_engine.stop()`, joins worker; assert elapsed wall time < 500 ms. (Note: with plan 01's stub, search returns immediately — this test will pass trivially. Plan 03's real implementation makes this test meaningful; do NOT weaken the bound now.)
       - test_cancellation_latency: similar to test_gil_released but assert worker thread completes within 200 ms of stop() being called (uses time.monotonic() per RESEARCH.md gotchas)
       - test_uci_binary_exists: locate the v7_uci executable next to the build directory (use native_build's find_packaged_module pattern adapted for the binary); if missing, pytest.skip; if present, subprocess.run with stdin=b"uci\nquit\n" and assert "id name V7" appears in stdout
       - test_auto_build: assert v7.ensure_available(auto_build=False) raises V7UnavailableError when the module was not previously built; then assert auto_build=True succeeds

       STARTPOS_FEN constant: `"rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1"`.

       Mark slow tests with `@pytest.mark.slow` if the project's pytest is configured for markers; otherwise inline-comment for future opt-in (research §A2 gotcha).
  </action>
  <verify>
    <automated>cmake --version && python3 -m uv run --extra build python -m chess_engine.engine.v7.native_build 2>&1 | tail -40</automated>
    <automated>python3 -c "from chess_engine.engine.v7 import chess_algorithm as v7; m = v7.ensure_available(auto_build=True); e = m.Engine(); assert e.tbhits() == 0 and e.nodes() == 0; print('OK')"</automated>
    <automated>python3 -m pytest tests/test_v7_bindings.py -q --tb=short</automated>
    <automated>grep -n 'py::call_guard<py::gil_scoped_release>' src/chess_engine/engine/v7/src/python_bindings.cpp | wc -l</automated>
    <automated>grep -n '\.def("stop", &v7::Engine::stop)' src/chess_engine/engine/v7/src/python_bindings.cpp</automated>
  </verify>
  <acceptance_criteria>
    - File src/chess_engine/engine/v7/include/engine.hpp exists and contains `class Engine` inside `namespace v7`
    - engine.hpp contains `std::atomic<bool> stop_flag_{false}`, `std::atomic<uint64_t> tbhits_{0}`, `std::atomic<uint64_t> nodes_{0}`
    - engine.hpp declares `void set_syzygy_path(const std::string&)`, `void new_game()`, `SearchResult search(const std::string&, int, int)`, `void stop()`, `uint64_t tbhits() const`, `uint64_t nodes() const`
    - python_bindings.cpp contains exactly 3 occurrences of `py::call_guard<py::gil_scoped_release>` (search, set_syzygy_path, perft) — verify with grep count
    - python_bindings.cpp `.def("stop", ...)` line has NO `call_guard` argument (verify with the focused grep above)
    - python_bindings.cpp `PYBIND11_MODULE(v7_engine, m)` block declares `Engine`, `perft`; module docstring contains "V7 Chess Engine"
    - uci_main.cpp main() reads stdin and on `uci` writes a line containing `id name V7` to stdout
    - CMake configure + build succeeds; produces a loadable v7_engine.{pyd,so,dylib} under src/chess_engine/engine/v7/ (verify by importing in Python — verify command 2)
    - CMake build produces v7_uci or v7_uci.exe under the v7/build/ directory (test_uci_binary_exists finds and exercises it)
    - All tests in tests/test_v7_bindings.py pass (verify command 3)
    - test_gil_released completes in < 500 ms total wall time (proves GIL released)
    - test_cancellation_latency: stop()-to-thread-exit elapsed < 200 ms (binding-level proof; plan 03 makes the latency meaningful for real searches)
    - V1-V6 directories untouched (re-check `git diff --name-only -- src/chess_engine/engine/ | grep -v '^src/chess_engine/engine/v7/'` returns empty)
  </acceptance_criteria>
  <done>v7_engine module compiles and loads in Python; Engine class binding matches the contract; GIL release verified by concurrent stop() test; v7_uci binary builds and accepts the minimal UCI handshake; all bindings tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → C++ (pybind11 binding) | Python caller controls fen string and integer depth/time_ms passed across the GIL release boundary. Untrusted only via the React UI's text inputs (e.g., custom FEN if exposed); GameManager constructs FENs from internal board state. |
| stdin → v7_uci | UCI commands arrive from fastchess or a local user; treat as untrusted text input on a process-level boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Engine.search(fen) C++ string parsing | mitigate | v7::Board::from_fen (ported in plan 02) must reject malformed FENs; for plan 01 the search stub does not parse FEN, so the surface is dormant until plan 02 lands. Document as a plan-02 follow-up. |
| T-01-02 | Denial of Service | Engine.search with attacker-controlled high depth | accept | Adapter caps depth at 6 (D-04 fixed depth); time_ms parameter has a default of 5000. Higher values from CLI users are accepted — V7 is a local desktop engine, not a network service. |
| T-01-03 | Denial of Service | uci_main.cpp stdin parsing | accept | Stub only reads line-by-line; no command in plan 01 allocates unbounded memory. Plan 06's `position` command will allocate per-move history; revisit at plan 06. |
| T-01-04 | Information Disclosure | std::cerr log lines (D-08 verbatim strings) | accept | Logs include filesystem paths the user already controls; no secrets in scope for this milestone. |
| T-01-05 | Elevation of Privilege | CMake FetchContent fetching pybind11 from GitHub | mitigate | Pinned to GIT_TAG v2.12.0 verbatim from V6 — same supply-chain surface as V6 (no new exposure). FetchContent verifies tag; no `--depth 1` shortcut. |
</threat_model>

<verification>
- CMake configure + build succeeds on the executor's host (Windows MSVC, Linux gcc/clang, or macOS clang) producing v7_engine.{pyd,so,dylib} and v7_uci{,.exe}
- Python can `from chess_engine.engine.v7 import chess_algorithm as v7; m = v7.ensure_available(auto_build=True); m.Engine().tbhits()`
- tests/test_v7_bindings.py is fully green (all 8 tests pass)
- tests/test_v7_engine.py collects without error
- find_best_move signature matches `(game_state, valid_moves, engine, search_info)`
- git status shows changes ONLY under src/chess_engine/engine/v7/, tests/test_v7_*.py, and .gitignore — no V1-V6 modifications
- File src/chess_engine/core/search_info.py is NOT created (CONTEXT.md known correction honored)
</verification>

<success_criteria>
- FOUND-01: V7 directory exists at src/chess_engine/engine/v7/ with __init__.py, chess_algorithm.py, native_build.py, CMakeLists.txt, include/engine.hpp, src/python_bindings.cpp, src/uci_main.cpp
- FOUND-02: CMake build via FetchContent_Declare pybind11 GIT_TAG v2.12.0 succeeds on the executor's platform
- FOUND-03: chess_algorithm.find_best_move signature is `(game_state, valid_moves, engine, search_info=None)`, matching the adapter contract
- FOUND-04: Engine class exposes stop() method that flips an atomic; adapter exposes stop_engine() helper; PYTHON binding signature accepts cancellation (full wiring to GameManager lands plan 06)
- FOUND-05: All binding methods that do C++ work (search, set_syzygy_path, perft) wrap with py::call_guard<py::gil_scoped_release>(); the Engine.stop binding does NOT (intentional — fast atomic write)
- FOUND-07: add_executable(v7_uci ...) target in CMakeLists; binary builds; accepts `uci` command and emits `id name V7`
- INT-09: ensure_available(auto_build=True) triggers native_build.build_v7_native on first import miss; pattern mirrors V6's chess_algorithm.py:61-90
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton-smoke/01-01-SUMMARY.md` documenting:
- V7 directory layout established and committed
- Engine class contract locked (atomic stop, GIL-released search)
- Decision: V7 ships its own SearchInfo pattern; src/chess_engine/core/search_info.py NOT created (per known_correction); GameManager wiring of stop_search→stop_engine deferred to plan 06
- What plans 02/03/04/05/06 should consume (Engine class header, CMakeLists TODO markers, fixture file)
- Any build platform notes (especially MSVC C99 issues if encountered with the stub uci_main.cpp)
</output>
