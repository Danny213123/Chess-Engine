---
phase: 01-skeleton-smoke
plan: 04
type: execute
wave: 4
depends_on: [02, 03]
files_modified:
  - src/chess_engine/engine/v7/coeffs.json
  - src/chess_engine/engine/v7/tools/gen_coeffs.py
  - src/chess_engine/engine/v7/include/coeffs.hpp
  - src/chess_engine/engine/v7/include/eval.hpp
  - src/chess_engine/engine/v7/src/eval.cpp
  - src/chess_engine/engine/v7/CMakeLists.txt
  - tests/test_v7_eval.py
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
  - EVAL-03
  - EVAL-04
  - EVAL-05
  - EVAL-06
  - EVAL-07
  - EVAL-08
  - EVAL-09
  - EVAL-10
  - EVAL-11

must_haves:
  truths:
    - "All 11 EVAL-* terms are structurally present in coeffs.json — material (mg+eg), PSTs (mg+eg, 12 tables of 64), phase weights, mobility (knight/bishop/rook/queen mg+eg), bishop pair (mg+eg), rook on (semi-)open file, pawn structure (doubled/isolated/backward/passed-by-rank), king attack table (100 entries), threats (3 named pairs), tempo (mg+eg)"
    - "coeffs.json _meta.source cites the Pesto chessprogramming.org URL verbatim AND mentions Zurichess as the Phase 4 tuning corpus (D-01 specifics)"
    - "tools/gen_coeffs.py produces deterministic output (sorted keys, LF line endings, fixed indent) — running twice on the same input produces byte-identical output (D-13)"
    - "CMake custom command regenerates src/coeffs.cpp from coeffs.json on every build when coeffs.json mtime is newer (D-11) — no manual codegen step required"
    - "src/coeffs.cpp is .gitignored and never committed (D-12) — generated at build time only"
    - "include/coeffs.hpp declares extern arrays/constants matching every key in coeffs.json — schema is locked between codegen output and header declarations"
    - "src/eval.cpp reads ALL weights via v7::coeffs::* extern references — no hardcoded const int weights remain in eval.cpp (EVAL-10)"
    - "eval(startpos) returns a score in roughly [-50, +50] cp (tempo bonus may push small positive); a large white-favored or black-favored score from startpos with Pesto baseline indicates a sign error — early sanity check"
    - "EVAL-11 tapered combine formula `eval = (mg*phase + eg*(24-phase)) / 24` is present in eval.cpp"
  artifacts:
    - path: "src/chess_engine/engine/v7/coeffs.json"
      provides: "Single source of truth for all V7 eval coefficients; Pesto baseline values for material+PSTs+phase, hand-set initial values for mobility/king-safety/threats; cited in _meta.source"
      contains: "_meta\\|material_mg\\|material_eg\\|phase_weights\\|pst_mg\\|pst_eg\\|mobility_knight_mg\\|king_attack_table"
    - path: "src/chess_engine/engine/v7/tools/gen_coeffs.py"
      provides: "Codegen script: coeffs.json → src/coeffs.cpp; deterministic output (D-13); CLI: python gen_coeffs.py <json> <cpp>"
    - path: "src/chess_engine/engine/v7/include/coeffs.hpp"
      provides: "Forward-declares every extern const int / extern const int[] symbol that gen_coeffs.py emits into src/coeffs.cpp; lives in namespace v7::coeffs"
    - path: "src/chess_engine/engine/v7/include/eval.hpp"
      provides: "v7::evaluate(const Board&) → int (centipawns); v7::evaluate_entry (binding entry)"
    - path: "src/chess_engine/engine/v7/src/eval.cpp"
      provides: "Tapered MG/EG eval reading from v7::coeffs::* extern arrays; implements all 11 EVAL-* terms; emits final score via EVAL-11 formula"
    - path: "tests/test_v7_eval.py"
      provides: "test_coeffs_json_schema (every EVAL-* key present), test_coeffs_codegen_deterministic, test_coeffs_cpp_gitignored, test_eval_startpos_balanced, test_eval_terms_use_coeffs (no hardcoded const int in eval.cpp), test_eval_returns_int, test_codegen_regenerates_on_change"
  key_links:
    - from: "src/chess_engine/engine/v7/CMakeLists.txt"
      to: "tools/gen_coeffs.py"
      via: "add_custom_command(OUTPUT src/coeffs.cpp COMMAND ${Python3_EXECUTABLE} tools/gen_coeffs.py coeffs.json src/coeffs.cpp DEPENDS coeffs.json tools/gen_coeffs.py)"
      pattern: "gen_coeffs\\.py"
    - from: "src/chess_engine/engine/v7/src/eval.cpp"
      to: "v7::coeffs::material_mg_P, ..., v7::coeffs::king_attack_table"
      via: "include coeffs.hpp; reference v7::coeffs::<name> in eval body"
      pattern: "v7::coeffs::"
    - from: "src/chess_engine/engine/v7/include/eval.hpp"
      to: "v7::coeffs (namespace)"
      via: "consumed transitively by eval.cpp; eval.hpp itself stays free of coeff references for compilation speed"
      pattern: "namespace v7"
---

<objective>
Stand up the V7 eval infrastructure: a JSON-driven coefficient pipeline (coeffs.json → tools/gen_coeffs.py → src/coeffs.cpp via CMake custom command), Pesto-baseline initial values for all 11 EVAL-* terms (D-01), and an eval.cpp that reads every weight from the generated extern arrays (EVAL-10) so Phase 4 Texel tuning can mutate coefficients in-place. EVAL-11 tapered MG/EG combine formula is implemented in eval.cpp. The output is a working evaluate(board) function callable from Engine::search (consumed by plan 03's PVS).

Purpose: Lock the "single source of truth = coeffs.json" contract that Phase 4 Texel depends on. Phase 1's eval need not be strong — D-03 explicitly accepts that V7 may play weaker than V6 in smoke games because Pesto-baseline ≠ V6-tuned. What MUST be true in Phase 1 is that every term is named, structurally present, and reads from a tunable handle (EVAL-10/11). Phase 4 then re-runs gen_coeffs.py with tuned values.

Output: Working pipeline coeffs.json → src/coeffs.cpp (regenerated by CMake on every build per D-11); eval.cpp implements all 11 terms reading from v7::coeffs::*; tests assert structural completeness, codegen determinism, and gitignore correctness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-skeleton-smoke/01-CONTEXT.md
@.planning/phases/01-skeleton-smoke/01-RESEARCH.md
@.planning/phases/01-skeleton-smoke/01-PATTERNS.md
@.planning/phases/01-skeleton-smoke/01-02-SUMMARY.md

# V6 eval reference (V7's starting structure):
@src/chess_engine/engine/v6/include/eval.hpp
@src/chess_engine/engine/v6/src/eval.cpp

# Plan 02 outputs needed:
@src/chess_engine/engine/v7/include/board.hpp
@src/chess_engine/engine/v7/include/types.hpp
@src/chess_engine/engine/v7/CMakeLists.txt
@.gitignore

<interfaces>
<!-- coeffs.json schema (RESEARCH.md §B2 lines 444-481) — copy verbatim into the seed file -->
<!-- Every key listed below is a CONTRACT — include/coeffs.hpp must forward-declare matching extern symbols -->

{
  "_meta": {
    "source": "Pesto evaluation values (https://www.chessprogramming.org/PeSTO%27s_Evaluation_Function); non-PST terms are hand-set initial values to be tuned in Phase 4 against the Zurichess quiet-labeled.epd dataset (https://bitbucket.org/zurichess/tuner/...)",
    "version": 1,
    "generated_by": "tools/gen_coeffs.py"
  },
  "material_mg": {"P": 82, "N": 337, "B": 365, "R": 477, "Q": 1025, "K": 0},
  "material_eg": {"P": 94, "N": 281, "B": 297, "R": 512, "Q": 936, "K": 0},
  "phase_weights": {"P": 0, "N": 1, "B": 1, "R": 2, "Q": 4, "K": 0},
  "pst_mg": {"P": [64 ints], "N": [64 ints], "B": [64 ints], "R": [64 ints], "Q": [64 ints], "K": [64 ints]},
  "pst_eg": {"P": [64 ints], "N": [64 ints], "B": [64 ints], "R": [64 ints], "Q": [64 ints], "K": [64 ints]},
  "mobility_knight_mg": [9 ints],     "mobility_knight_eg": [9 ints],
  "mobility_bishop_mg": [14 ints],    "mobility_bishop_eg": [14 ints],
  "mobility_rook_mg":   [15 ints],    "mobility_rook_eg":   [15 ints],
  "mobility_queen_mg":  [28 ints],    "mobility_queen_eg":  [28 ints],
  "bishop_pair_mg": 30, "bishop_pair_eg": 50,
  "rook_open_file": 15, "rook_semi_open_file": 10,
  "doubled_pawn": -10, "isolated_pawn": -15, "backward_pawn": -5,
  "passed_pawn_by_rank": [0, 5, 10, 20, 40, 80, 160, 0],
  "king_attack_table": [100 ints],
  "threat_minor_by_pawn": -25,
  "threat_rook_by_minor": -20,
  "threat_queen_by_rook": -30,
  "tempo_mg": 10, "tempo_eg": 0
}

<!-- gen_coeffs.py output convention (RESEARCH.md §B2 lines 485-525) -->
<!-- - sorted(data.keys()); skip keys starting with "_"
     - dict values: emit "{key}_{subkey}" for each scalar/list inside
     - list values: emit "const int {name}[N] = { v1, v2, ... };"
     - explicit LF line endings: write_bytes(text.encode().replace(b"\r\n", b"\n"))
     - header comment cites source JSON path
     - namespace v7::coeffs { ... } wrapper -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write coeffs.json (Pesto baseline + hand-set non-PST), tools/gen_coeffs.py (deterministic), include/coeffs.hpp (extern declarations matching every emitted symbol), CMake custom command wiring</name>
  <files>
    src/chess_engine/engine/v7/coeffs.json,
    src/chess_engine/engine/v7/tools/gen_coeffs.py,
    src/chess_engine/engine/v7/include/coeffs.hpp,
    src/chess_engine/engine/v7/CMakeLists.txt
  </files>
  <read_first>
    .planning/phases/01-skeleton-smoke/01-RESEARCH.md,
    .planning/phases/01-skeleton-smoke/01-PATTERNS.md,
    src/chess_engine/engine/v7/CMakeLists.txt,
    .gitignore
  </read_first>
  <behavior>
    - python3 tools/gen_coeffs.py coeffs.json src/coeffs.cpp produces a valid C++ translation unit
    - Running the script twice on the same coeffs.json produces byte-identical output (D-13 determinism)
    - On Windows AND POSIX, the output file has LF line endings (no CRLF)
    - Every key in coeffs.json maps to a corresponding `extern const int ...` declaration in include/coeffs.hpp — mismatch causes a linker error at v7_engine build time
    - CMake build invokes gen_coeffs.py automatically when coeffs.json mtime > src/coeffs.cpp mtime; first build (when coeffs.cpp is absent) always runs it
    - src/coeffs.cpp is present in .gitignore under the V7 build artifacts block (already added in plan 01)
  </behavior>
  <action>
    Step 1 — Create src/chess_engine/engine/v7/coeffs.json:
    - Use the schema from the &lt;interfaces&gt; block above as the canonical structure
    - For Pesto-sourced values (material_mg, material_eg, phase_weights, pst_mg, pst_eg, tempo_mg=10), use the exact values from RESEARCH.md §B2 line 429 and the Pesto chessprogramming.org page. The 768 PST values (12 tables × 64) come from chessprogramming.org/PeSTO%27s_Evaluation_Function — RESEARCH.md §B2 lines 482-484 explicitly says "All 12 tables are listed verbatim on the chessprogramming.org PeSTO page. The Phase 1 implementer copies them directly. Do NOT hand-transcribe — copy from a programmatic source (e.g., open-source engine using Pesto, with attribution)."
    - For non-Pesto values (mobility tables, king_attack_table, threats), use the hand-set values from RESEARCH.md §B2 table:
      * mobility_knight_mg: [-62,-53,-12,-4,3,13,22,28,33] (9 values)
      * mobility_knight_eg: [-81,-56,-31,-16,5,11,17,20,25] (9 values)
      * mobility_bishop_mg/eg: 14 values each — use Stockfish-style published values OR set modest [-50,-30,-10,0,5,10,15,20,25,28,30,32,34,35] / similar; mark with `// initial; will be tuned Phase 4` semantically (JSON has no comments — use `_meta.tuning_status` array of key names OR an inline `_initial_terms` list, the latter cleaner for codegen which skips `_*` keys)
      * mobility_rook_mg/eg: 15 values each — similar
      * mobility_queen_mg/eg: 28 values each — similar
      * king_attack_table: 100 entries, monotonically increasing penalty cp; use Stockfish-style table starting [0,0,1,2,3,5,7,9,12,15,18,22,26,30,35,40,46,52,59,66,...] — research §B2 has the seed; complete to 100 entries with continued growth, capping around 500-1000 cp
      * passed_pawn_by_rank: [0, 5, 10, 20, 40, 80, 160, 0] exactly per RESEARCH.md (indexed from own side: rank 1 = no bonus, rank 7 = 160cp before promotion; ranks 0 and 7 are 0)
      * bishop_pair_mg=30, eg=50; rook_open_file=15, rook_semi_open_file=10; doubled=-10, isolated=-15, backward=-5; threats per RESEARCH.md
      * tempo_mg=10, tempo_eg=0
    - _meta.source MUST cite both Pesto URL (`https://www.chessprogramming.org/PeSTO%27s_Evaluation_Function`) AND Zurichess as the future tuning corpus (D-01 specifics + CLAUDE.md tuning data constraint)
    - The file must parse as valid JSON. Use a JSON validator (e.g., `python3 -c "import json; json.loads(open('...').read())"`) before committing
    - File MUST be committed (D-12: coeffs.json is the SoT, IS committed; coeffs.cpp is generated and is NOT committed)

    Step 2 — Create src/chess_engine/engine/v7/tools/gen_coeffs.py:
    - Implement per RESEARCH.md §B2 lines 485-525 exactly (paste-ready)
    - Critical determinism guarantees (D-13):
      * `sorted(data.keys())` — iterate top-level keys in sorted order
      * Skip any key starting with `_` (meta keys)
      * For dict values: iterate `sorted(val.keys())` and emit `{key}_{subkey}` symbols
      * For list values: emit `const int {name}[N] = { v1, v2, ... };` with sized array
      * Output bytes write with explicit LF: `dst.write_bytes(text.encode("utf-8").replace(b"\r\n", b"\n"))`
      * Header comment: `// GENERATED by tools/gen_coeffs.py — DO NOT EDIT` AND `// Source: <coeffs.json path>`
      * Namespace wrapper: `namespace v7::coeffs { ... } // namespace v7::coeffs`
      * #include "coeffs.hpp"
    - CLI: `python3 gen_coeffs.py <coeffs.json> <output_coeffs.cpp>` (sys.argv[1], sys.argv[2])
    - Add a `#!/usr/bin/env python3` shebang for direct execution
    - Use only stdlib (json, sys, pathlib) — no external deps (matches CLAUDE.md "no extra cost" for Python which is already a build-time dep per pybind11)

    Step 3 — Create src/chess_engine/engine/v7/include/coeffs.hpp:
    - `#pragma once`
    - `namespace v7::coeffs { ... }`
    - Forward-declare EVERY symbol that gen_coeffs.py will emit, matching the JSON schema:
      * Scalars in dict values (material_mg, material_eg, phase_weights): `extern const int material_mg_P; extern const int material_mg_N;` ... etc. for {P,N,B,R,Q,K} × {material_mg, material_eg, phase_weights} = 18 declarations
      * Lists in dict values (pst_mg, pst_eg): `extern const int pst_mg_P[64]; extern const int pst_mg_N[64];` ... 12 declarations
      * Top-level lists (mobility_*, passed_pawn_by_rank, king_attack_table): `extern const int mobility_knight_mg[9]; extern const int mobility_bishop_mg[14];` ... etc.
      * Top-level scalars (bishop_pair_mg/eg, rook_*, doubled_pawn, isolated_pawn, backward_pawn, threat_*, tempo_*): `extern const int bishop_pair_mg;` ... etc.
    - The set of declarations in coeffs.hpp MUST exactly match the set of symbols gen_coeffs.py emits. Mismatch → link error. Write a header comment listing the count of expected symbols per category so plan 04's task 2 (eval.cpp) can grep-verify.

    Step 4 — Update src/chess_engine/engine/v7/CMakeLists.txt:
    - Replace the `# TODO(plan-04): add eval.cpp + generated src/coeffs.cpp` comment with:
      * The add_custom_command per RESEARCH.md §B2 lines 540-549 (paste-ready):
        ```
        set(COEFFS_JSON ${CMAKE_CURRENT_SOURCE_DIR}/coeffs.json)
        set(COEFFS_CPP  ${CMAKE_CURRENT_SOURCE_DIR}/src/coeffs.cpp)
        add_custom_command(
            OUTPUT ${COEFFS_CPP}
            COMMAND ${Python3_EXECUTABLE} ${CMAKE_CURRENT_SOURCE_DIR}/tools/gen_coeffs.py
                    ${COEFFS_JSON} ${COEFFS_CPP}
            DEPENDS ${COEFFS_JSON} ${CMAKE_CURRENT_SOURCE_DIR}/tools/gen_coeffs.py
            COMMENT "Generating coeffs.cpp from coeffs.json"
            VERBATIM
        )
        ```
      * Add ${COEFFS_CPP} to the V7_SOURCES list (and to v7_uci target_sources)
      * Add src/eval.cpp to V7_SOURCES (plan 04 task 2 creates it; CMake listing it now is fine because the custom command makes coeffs.cpp a dependency)
    - The eval.hpp/cpp files are touched in task 2; declaring src/eval.cpp in CMakeLists in task 1 is acceptable because task 2 follows and creates the file before next build
  </action>
  <verify>
    <automated>python3 -c "import json; d = json.loads(open('src/chess_engine/engine/v7/coeffs.json').read()); keys = set(d.keys()) - {'_meta'}; expected = {'material_mg','material_eg','phase_weights','pst_mg','pst_eg','mobility_knight_mg','mobility_knight_eg','mobility_bishop_mg','mobility_bishop_eg','mobility_rook_mg','mobility_rook_eg','mobility_queen_mg','mobility_queen_eg','bishop_pair_mg','bishop_pair_eg','rook_open_file','rook_semi_open_file','doubled_pawn','isolated_pawn','backward_pawn','passed_pawn_by_rank','king_attack_table','threat_minor_by_pawn','threat_rook_by_minor','threat_queen_by_rook','tempo_mg','tempo_eg'}; missing = expected - keys; assert not missing, f'missing keys: {missing}'; print('schema OK; keys:', len(keys))"</automated>
    <automated>python3 src/chess_engine/engine/v7/tools/gen_coeffs.py src/chess_engine/engine/v7/coeffs.json /tmp/coeffs_run1.cpp && python3 src/chess_engine/engine/v7/tools/gen_coeffs.py src/chess_engine/engine/v7/coeffs.json /tmp/coeffs_run2.cpp && diff -q /tmp/coeffs_run1.cpp /tmp/coeffs_run2.cpp && echo "determinism OK"</automated>
    <automated>python3 -c "data = open('/tmp/coeffs_run1.cpp', 'rb').read(); assert b'\r\n' not in data, 'CRLF found in output'; print('LF only OK; bytes:', len(data))"</automated>
    <automated>grep -c 'extern const int' src/chess_engine/engine/v7/include/coeffs.hpp</automated>
    <automated>grep -n 'add_custom_command' src/chess_engine/engine/v7/CMakeLists.txt && grep -n 'gen_coeffs.py' src/chess_engine/engine/v7/CMakeLists.txt</automated>
    <automated>grep -n 'src/chess_engine/engine/v7/src/coeffs\.cpp' .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - File src/chess_engine/engine/v7/coeffs.json exists and parses as valid JSON
    - Verify command 1: all 27 expected top-level keys are present in coeffs.json
    - coeffs.json _meta.source string contains literal substrings `chessprogramming.org/PeSTO` AND `Zurichess`
    - coeffs.json pst_mg.P, pst_mg.N, pst_mg.B, pst_mg.R, pst_mg.Q, pst_mg.K are each 64-element arrays; same for pst_eg (verify with json.loads and len() checks programmatically — embedded in test_v7_eval.py task 2)
    - File src/chess_engine/engine/v7/tools/gen_coeffs.py exists with shebang `#!/usr/bin/env python3`
    - Verify command 2 prints `determinism OK` — running gen_coeffs.py twice produces byte-identical output
    - Verify command 3 prints `LF only OK` — output has no CRLF (D-13 explicit LF requirement)
    - Generated /tmp/coeffs_run1.cpp contains `namespace v7::coeffs {` opening and `} // namespace v7::coeffs` closing
    - Generated coeffs.cpp contains a `// GENERATED by tools/gen_coeffs.py — DO NOT EDIT` header
    - include/coeffs.hpp contains `#pragma once` and `namespace v7::coeffs {`
    - Verify command 4: extern const int declaration count in coeffs.hpp matches the symbol count gen_coeffs.py emits (count by reading /tmp/coeffs_run1.cpp; embed precise comparison in test_v7_eval.py task 2)
    - CMakeLists.txt contains the add_custom_command block with OUTPUT ${COEFFS_CPP}, COMMAND invoking gen_coeffs.py, DEPENDS coeffs.json and gen_coeffs.py
    - CMakeLists.txt V7_SOURCES list includes ${COEFFS_CPP} and src/eval.cpp
    - .gitignore already includes src/chess_engine/engine/v7/src/coeffs.cpp from plan 01 (verify command 6 finds it)
    - V1-V6 untouched
  </acceptance_criteria>
  <done>coeffs.json has all 27 EVAL-* keys with Pesto baselines + hand-set seeds; gen_coeffs.py produces deterministic LF output; coeffs.hpp declares matching externs; CMake custom command wired; ready for task 2's eval.cpp to consume v7::coeffs::* symbols.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement eval.cpp using v7::coeffs::* (all 11 EVAL terms + EVAL-11 tapered combine); expose evaluate to bindings; write test_v7_eval.py</name>
  <files>
    src/chess_engine/engine/v7/include/eval.hpp,
    src/chess_engine/engine/v7/src/eval.cpp,
    src/chess_engine/engine/v7/src/python_bindings.cpp,
    tests/test_v7_eval.py
  </files>
  <read_first>
    src/chess_engine/engine/v6/include/eval.hpp,
    src/chess_engine/engine/v6/src/eval.cpp,
    src/chess_engine/engine/v7/include/coeffs.hpp,
    src/chess_engine/engine/v7/coeffs.json,
    src/chess_engine/engine/v7/include/board.hpp,
    src/chess_engine/engine/v7/include/movegen.hpp,
    src/chess_engine/engine/v7/src/python_bindings.cpp,
    tests/test_v6_native.py
  </read_first>
  <behavior>
    - v7_engine compiles after eval.cpp + eval.hpp added; no link errors (proves coeffs.hpp declarations match gen_coeffs.py emissions)
    - v7_engine.evaluate(STARTPOS_FEN) returns an int in roughly [-50, +50] cp (small positive due to tempo_mg=10)
    - v7_engine.evaluate is symmetric under color flip approximately (evaluating a mirrored position with side-to-move flipped produces a near-negated score)
    - eval.cpp contains NO hardcoded integer weight constants (every weight reads via v7::coeffs::*); grep for `const int .*= ` in eval.cpp returns zero matches outside of local variables / loop indices
    - tapered combine formula `(mg * phase + eg * (24 - phase)) / 24` appears in eval.cpp (EVAL-11)
    - All 7 tests in tests/test_v7_eval.py pass
  </behavior>
  <action>
    Step 1 — Fork V6 eval headers as a starting structure:
    - src/chess_engine/engine/v6/include/eval.hpp → src/chess_engine/engine/v7/include/eval.hpp with v6→v7 / V6→V7 namespace renames (plan 02 fork mechanics)
    - eval.hpp exports `int v7::evaluate(const Board& board);` and any helper used by python_bindings.cpp

    Step 2 — Write src/chess_engine/engine/v7/src/eval.cpp:
    - Use V6 src/eval.cpp as structural template (PATTERNS.md classification: "exact + coeffs:: indirection")
    - Replace EVERY hardcoded integer weight in V6's eval.cpp with a reference to the corresponding v7::coeffs::* symbol:
      * V6 material values → v7::coeffs::material_mg_P, v7::coeffs::material_eg_P, etc.
      * V6 PSTs → v7::coeffs::pst_mg_P[sq], v7::coeffs::pst_eg_P[sq], etc.
      * Phase weights → v7::coeffs::phase_weights_P, etc.
      * Mobility tables → v7::coeffs::mobility_knight_mg[attack_count], etc.
      * King safety → v7::coeffs::king_attack_table[attack_units]
      * Pawn structure → v7::coeffs::doubled_pawn, isolated_pawn, backward_pawn, passed_pawn_by_rank[rank]
      * Bishop pair, rook on file → v7::coeffs::bishop_pair_mg/eg, rook_open_file, rook_semi_open_file
      * Threats → v7::coeffs::threat_minor_by_pawn, threat_rook_by_minor, threat_queen_by_rook
      * Tempo → v7::coeffs::tempo_mg, tempo_eg
    - Implement EVAL-11 tapered combine at the function end (RESEARCH.md §B2 EVAL-11 row):
      * phase = phase_weights_P * countP + phase_weights_N * countN + ... = sum of weights for each piece on the board (capped at 24)
      * `int final_score = (mg * phase + eg * (24 - phase)) / 24;`
      * Return from white's perspective; flip sign if board.side_to_move == BLACK to return STM-relative score (mirror V6 convention)
    - Implement ALL 11 EVAL-* terms. If V6 lacks a term (e.g., threats or king-attack-table), implement it from scratch using the v7::coeffs::* values. Specifically EVAL-04 (mobility per piece type) and EVAL-08 (king safety table) may need adding if V6 didn't have them in the same shape — V7 uses the coeffs::* structure as the authority.
    - For EVAL-08 (king safety): for each enemy piece attacking squares adjacent to the friendly king, accumulate "attack units" (typically: knight=2, bishop=2, rook=3, queen=5 per attacker, with multiplier for number of attackers); cap at 99 and index into king_attack_table[]
    - For EVAL-07 (threats): identify pieces attacked by lesser pieces (e.g., enemy pawn attacks our knight/bishop → penalty)
    - Pawn-hash table (EVAL-08 alt — RESEARCH.md note "Pawn hash table for incremental pawn-structure eval"): Phase 1 may skip the cache and recompute pawn structure every call. The CACHE is a Phase 4 NPS-recovery concern; the TERM (passed/isolated/doubled/backward) MUST be present. Use a simple non-cached implementation in Phase 1; mark with a comment `// TODO(phase-4): pawn hash table for incremental eval (EVAL-08 NPS optimization)`.
    - Add `#include "coeffs.hpp"` at the top of eval.cpp
    - Add `#include "board.hpp"`, `#include "movegen.hpp"` (for attack-set generation)

    Step 3 — Update src/chess_engine/engine/v7/src/python_bindings.cpp:
    - Add `m.def("evaluate", &v7::evaluate_entry, py::arg("fen"));` (test_v7_eval.py and the future Phase 4 tuner harness need this)
    - `evaluate_entry` is a free function: `static int evaluate_entry(const std::string& fen) { v7::Board b; b.from_fen(fen); return v7::evaluate(b); }` — declare above PYBIND11_MODULE
    - The existing perft/Engine bindings remain unchanged; this is a small additive edit

    Step 4 — Create tests/test_v7_eval.py with these 7 tests:

    1. test_coeffs_json_schema:
       - Load src/chess_engine/engine/v7/coeffs.json
       - Assert every expected key is present (use the 27-key set from task 1 verify command 1)
       - Assert pst_mg/pst_eg sub-dicts have exactly {P,N,B,R,Q,K} keys, each with 64-element list
       - Assert mobility_knight_mg/eg have 9 elements; bishop 14; rook 15; queen 28
       - Assert king_attack_table has 100 elements
       - Assert passed_pawn_by_rank has 8 elements
       - Assert _meta.source contains both "chessprogramming.org/PeSTO" and "Zurichess" substrings

    2. test_coeffs_codegen_deterministic:
       - Run tools/gen_coeffs.py twice into two temp files via subprocess; assert byte-identical
       - Assert output has no CRLF

    3. test_coeffs_cpp_gitignored:
       - subprocess.check_output(["git", "check-ignore", "src/chess_engine/engine/v7/src/coeffs.cpp"]) — exit 0 means gitignored
       - Assert the path is in .gitignore (read .gitignore, search for the literal line)

    4. test_eval_startpos_balanced(v7_native_engine):
       - score = v7_engine.evaluate("rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1")
       - assert -50 <= score <= 50, f"startpos eval too far from balanced: {score}"

    5. test_eval_terms_use_coeffs:
       - Read src/chess_engine/engine/v7/src/eval.cpp as text
       - assert "v7::coeffs::" in text, "eval.cpp must reference v7::coeffs:: extern symbols (EVAL-10)"
       - assert at least 5 distinct v7::coeffs::* references (rough heuristic that all term families are wired): count = len(set(re.findall(r"v7::coeffs::\w+", text))); assert count >= 10
       - assert no hardcoded weight constants: scan for `const int.*=.*-?\d+` (constant assignments to numeric literals) — must be ZERO outside of local scope; this is a heuristic, may need adjustment. Alternative: count `static const` declarations — should also be near zero. Best heuristic: count integer literals > 50 (most weight magnitudes); should be < 10 (allows for piece-count loops, etc.)

    6. test_eval_returns_int(v7_native_engine):
       - r = v7_engine.evaluate(STARTPOS_FEN); assert isinstance(r, int)

    7. test_codegen_regenerates_on_change:
       - Compute initial coeffs.cpp via subprocess
       - Modify a copy of coeffs.json (e.g., bump tempo_mg from 10 to 11)
       - Run codegen on modified JSON
       - Assert the two outputs differ (proves codegen actually reads JSON)

    Append validation map rows for EVAL-01 through EVAL-11 (one row per requirement, all pointing to the same task 2 since the tests are bundled in test_v7_eval.py). The EVAL-* IDs are structural — they're satisfied by the schema + the eval.cpp wiring. Specifically:
    - EVAL-01..03 (material/PST/phase) → test_coeffs_json_schema (presence) + test_eval_startpos_balanced (math)
    - EVAL-04..09 (mobility/pawn structure/king/threats/etc.) → test_coeffs_json_schema + test_eval_terms_use_coeffs
    - EVAL-10 (single source of truth) → test_eval_terms_use_coeffs (no hardcoded weights)
    - EVAL-11 (tapered combine) → test_coeffs_json_schema (phase_weights present) + grep for `mg * phase + eg * (24 - phase)` in eval.cpp (add this verify command)
  </action>
  <verify>
    <automated>python3 -m uv run --extra build python -m chess_engine.engine.v7.native_build 2>&1 | tail -20</automated>
    <automated>python3 -c "from chess_engine.engine.v7 import chess_algorithm as v7; m = v7.ensure_available(auto_build=True); s = m.evaluate('rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1'); print('startpos eval:', s); assert -50 <= s <= 50, s"</automated>
    <automated>python3 -m uv run --group dev pytest tests/test_v7_eval.py -q --tb=short</automated>
    <automated>grep -c 'v7::coeffs::' src/chess_engine/engine/v7/src/eval.cpp</automated>
    <automated>grep -E '\* phase\s*\+\s*\w+\s*\*\s*\(24\s*-\s*phase\)' src/chess_engine/engine/v7/src/eval.cpp</automated>
    <automated>grep -n 'm.def\("evaluate"' src/chess_engine/engine/v7/src/python_bindings.cpp</automated>
  </verify>
  <acceptance_criteria>
    - CMake build green; eval.cpp compiles and links against generated src/coeffs.cpp (proves coeffs.hpp matches gen_coeffs.py emissions)
    - Verify command 2: evaluate(STARTPOS_FEN) returns an int in [-50, +50]
    - Verify command 3: all 7 tests in tests/test_v7_eval.py pass
    - eval.cpp contains ≥10 distinct v7::coeffs::* references (verify command 4 returns ≥10)
    - eval.cpp contains the literal tapered combine pattern matching `\* phase \+ \w+ \* \(24 - phase\)` (verify command 5)
    - python_bindings.cpp exposes `m.def("evaluate", ...)` for FEN-based eval (verify command 6)
    - All 11 EVAL-* requirements addressed: material/PSTs/phase/mobility/pawn-structure/bishop-pair/rook-on-file/king-safety/threats/tempo/tapered-combine
    - tests/test_v7_eval.py contains exactly 7 test functions
    - Validation map rows appended to .planning/phases/01-skeleton-smoke/01-VALIDATION.md for EVAL-01..11
    - V1-V6 untouched
  </acceptance_criteria>
  <done>eval.cpp implements all 11 EVAL-* terms reading from v7::coeffs::*; v7_engine.evaluate(fen) callable; startpos eval ~balanced; codegen pipeline proven deterministic and connected; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build-time: coeffs.json → src/coeffs.cpp | gen_coeffs.py executes during CMake build with full local privileges; coeffs.json content is checked into the repo so review surface is the JSON file itself. |
| Runtime: v7::coeffs::* in eval.cpp | Read-only static memory in compiled binary; immutable after build. Phase 4 introduces set_coeff() runtime mutation; out of scope for Phase 1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | coeffs.json modification | accept | Same surface as any source file; PR review catches malicious changes. |
| T-04-02 | Tampering | gen_coeffs.py executing arbitrary code | accept | Script is committed; pure stdlib (json, sys, pathlib); no eval/exec; reviewed during plan 04 execution. |
| T-04-03 | Information Disclosure | coeffs.cpp accidentally committed | mitigate | .gitignore (plan 01) excludes src/chess_engine/engine/v7/src/coeffs.cpp; test_coeffs_cpp_gitignored asserts via `git check-ignore`. |
| T-04-04 | Tampering | Codegen non-determinism across machines (CRLF, dict ordering) | mitigate | gen_coeffs.py forces LF; sorted() iteration; test_coeffs_codegen_deterministic verifies. |
</threat_model>

<verification>
- coeffs.json has all 27 keys with proper shapes
- gen_coeffs.py runs deterministically; output has LF endings
- coeffs.hpp's externs match gen_coeffs.py's emitted symbols (proven by successful link)
- CMake regenerates coeffs.cpp on every coeffs.json change (custom command DEPENDS)
- src/coeffs.cpp is gitignored
- eval.cpp reads ALL weights from v7::coeffs::*
- v7_engine.evaluate works on FEN strings
- All 7 tests green
- V1-V6 untouched
</verification>

<success_criteria>
- EVAL-01: material_mg + material_eg with Pesto values present
- EVAL-02: pst_mg + pst_eg 12×64 tables present (Pesto)
- EVAL-03: phase_weights present (Pesto)
- EVAL-04: mobility_{knight,bishop,rook,queen}_{mg,eg} present
- EVAL-05: bishop_pair_mg/eg present
- EVAL-06: rook_open_file, rook_semi_open_file present
- EVAL-07: threat_minor_by_pawn, threat_rook_by_minor, threat_queen_by_rook present
- EVAL-08: king_attack_table[100] present; pawn-structure terms in eval.cpp (pawn hash cache deferred to Phase 4 NPS recovery)
- EVAL-09: tempo_mg/tempo_eg present
- EVAL-10: ALL weights read via v7::coeffs::*; no hardcoded const int weights in eval.cpp (test_eval_terms_use_coeffs)
- EVAL-11: tapered combine formula present in eval.cpp (verify command 5)
</success_criteria>

<output>
After completion, create `.planning/phases/01-skeleton-smoke/01-04-SUMMARY.md` documenting:
- coeffs.json schema and value sources (Pesto for material/PSTs/phase/tempo_mg; hand-set for mobility/king-safety/threats)
- gen_coeffs.py determinism contract
- Locked schema: any future addition to coeffs.json requires matching extern declaration in coeffs.hpp
- eval.cpp's tapered combine implementation
- Notes for Phase 4 Texel tuning: every coefficient is structurally tunable; set_coeff(name, val) runtime mutation is NOT implemented in Phase 1 (research §B2 implies it's a Phase 4 add); document this as "Phase 1 ships read-only coefficients; Phase 4 will add set_coeff() to support live tuning per CONTEXT D-10 / EVAL-10 spec"
- Pawn hash table cache (EVAL-08 NPS-recovery aspect) deferred to Phase 4 — TODO comment in eval.cpp
</output>
