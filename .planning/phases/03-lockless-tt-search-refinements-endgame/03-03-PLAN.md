---
phase: 03-lockless-tt-search-refinements-endgame
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/chess_engine/engine/v7/include/engine.hpp
  - src/chess_engine/engine/v7/include/search.hpp
  - src/chess_engine/engine/v7/src/engine.cpp
  - src/chess_engine/engine/v7/src/search.cpp
  - src/chess_engine/engine/v7/src/uci_main.cpp
  - tests/test_v7_history.py
  - tests/test_v7_singular_nps_ratio.py
autonomous: false
requirements: [SRCH-07, SRCH-08, SRCH-09, SRCH-10]
must_haves:
  truths:
    - "Continuation history (4-ply table) and capture history accumulate on beta-cutoff and age on Engine::new_search() per D-04 SRCH-07"
    - "Singular extensions fire only when depth >= 8 && move == tt_move && tt_entry.depth >= depth - 3 && tt_entry.flag == TT_BETA && |tt_entry.score| < MATE_IN_MAX_PLY && !is_root (RESEARCH.md Pitfall 2 + Code Examples §SE — SRCH-08)"
    - "Singular verification re-search uses excluded_move[ply] to skip the TT move; the TT probe at top of alpha_beta is SKIPPED when excluded_move[ply] != MOVE_NONE (RESEARCH.md Code Examples §SE — critical correctness invariant)"
    - "Multi-cut (SRCH-09) at cut nodes (depth >= 8): when ≥3 of the first 6 generated moves fail high above beta in a reduced search at depth/2, return beta directly"
    - "ProbCut (SRCH-10) at depth >= 5 with margin: zero-window search of selected captures at (beta + probcut_margin) using reduced depth; on fail-high, return beta. Defaults ON but the Plan 03-03 mini-gauntlet may flip default OFF if it regresses"
    - "Each refinement exposes a UCI option default ON via the v7_uci setoption parser (D-06): UseSingular, UseMultiCut, UseProbCut"
    - "Singular-on vs singular-off NPS ratio is in [0.9, 1.1] on the bench position (RESEARCH.md Pitfall 2 + Success Criterion #4) — measured by tests/test_v7_singular_nps_ratio.py under RUN_BENCHMARKS=1"
    - "After this plan + Plan 03-02 merges, a blocking 200-game mini-gauntlet vs baseline-phase3/summary.json shows lower-bound Elo > -10 (D-05); if ProbCut fails the bisection, its default flips to OFF per D-04 SRCH-10"
  artifacts:
    - path: "src/chess_engine/engine/v7/src/search.cpp"
      provides: "Singular extension + multi-cut + ProbCut blocks; continuation/capture history reads and updates"
    - path: "src/chess_engine/engine/v7/include/engine.hpp"
      provides: "continuation_history_ and capture_history_ Engine members"
    - path: "src/chess_engine/engine/v7/include/search.hpp"
      provides: "EngineOptions extended with UseSingular, UseMultiCut, UseProbCut; SearchInfo non-owning pointers for continuation/capture history"
    - path: "tests/test_v7_history.py"
      provides: "Implemented unit tests for continuation/capture history accumulation and aging, plus counter-move side-of-mover sanity test (RESEARCH.md Pitfall 7)"
    - path: "tests/test_v7_singular_nps_ratio.py"
      provides: "Implemented RUN_BENCHMARKS=1 sentinel asserting |NPS(singular_on) - NPS(singular_off)| / NPS_off < 0.10 (Success Criterion #4)"
  key_links:
    - from: "src/chess_engine/engine/v7/src/search.cpp singular block"
      to: "info.search_stack->excluded_move[ply]"
      via: "set/unset around the verification re-search; consulted at move-loop start to skip excluded move; consulted at top of alpha_beta to SKIP TT probe"
      pattern: "excluded_move\\[ply\\]"
    - from: "src/chess_engine/engine/v7/src/search.cpp move loop"
      to: "info.continuation_history / info.capture_history"
      via: "updated on beta-cutoff alongside killers/history; read by move picker for scoring"
      pattern: "continuation_history|capture_history"
    - from: "tests/test_v7_singular_nps_ratio.py"
      to: "v7_uci setoption UseSingular true/false"
      via: "subprocess invocation using _find_uci_binary fixture (conftest.py:166-198)"
      pattern: "UseSingular"
---

<objective>
Land the Tier-2 (aggressive) search refinement stack per CONTEXT D-04: SRCH-07 (continuation + capture history), SRCH-08 (singular extensions, depth ≥ 8 with verification re-search), SRCH-09 (multi-cut at cut nodes), SRCH-10 (ProbCut — gauntlet-gated, may revert to OFF). All three new requirements are wired behind UCI toggles (D-06) defaulting ON.

Purpose: These are the "high-risk, high-reward" search techniques. Singular extensions especially risk search explosion (RESEARCH.md Pitfall 2 / Success Criterion #4) — the singular NPS ratio sentinel is mandatory evidence.

Output: alpha_beta gains singular/multi-cut/probcut blocks layered on top of Plan 03-02's tier-1 stack; a 200-game mini-gauntlet checkpoint confirms tier-2 non-regression vs baseline; if ProbCut shows regression, its default flips to OFF.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/03-CONTEXT.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/03-RESEARCH.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/03-PATTERNS.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/03-01-SUMMARY.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/03-02-SUMMARY.md
@src/chess_engine/engine/v7/include/engine.hpp
@src/chess_engine/engine/v7/include/search.hpp
@src/chess_engine/engine/v7/src/search.cpp
@src/chess_engine/engine/v7/src/uci_main.cpp
@tests/conftest.py
@tests/test_nps_regression.py

<interfaces>
Singular extensions (RESEARCH.md Code Examples §"Singular Extensions" — replaces the move loop's extension calculation in search.cpp around line 284-372):
  // Inside the move loop, AFTER move generation, BEFORE making move m:
  int extension = 0;
  if (info.options->UseSingular && depth >= 8 && m == tt_move
      && tt_entry.depth >= depth - 3
      && tt_entry.flag == TT_BETA
      && std::abs(tt_entry.score) < MATE_IN_MAX_PLY
      && !is_root) {

      int singular_beta  = tt_entry.score - 2 * depth;
      int singular_depth = (depth - 1) / 2;

      // Verification re-search — EXCLUDING the TT move:
      info.search_stack->excluded_move[ply] = tt_move;
      int singular_score = alpha_beta(board, singular_depth,
                                       singular_beta - 1, singular_beta,
                                       info, ply, /*do_null=*/false);
      info.search_stack->excluded_move[ply] = MOVE_NONE;

      if (singular_score < singular_beta) {
          extension = 1;   // TT move is singularly best
      } else if (info.options->UseMultiCut && singular_beta >= beta) {
          // Multi-cut piggyback (SRCH-09): a non-TT move ALSO failed high above beta
          return singular_beta;
      }
  }
  // child_depth = depth - 1 + extension;

  Critical invariants (RESEARCH.md Code Examples §SE final paragraphs):
  1. Move loop start: `if (m == info.search_stack->excluded_move[ply]) continue;`
  2. Top of alpha_beta: skip the TT probe entirely when `info.search_stack->excluded_move[ply] != MOVE_NONE` — otherwise the un-excluded score is returned and the singular test is meaningless.
  3. Mate scores stored in TT must remain ply-adjusted via score_to_tt/score_from_tt (preserved from Plan 03-01 — do not bypass).

Multi-cut (SRCH-09, RESEARCH.md D2 §3.7 — implemented as a piggyback in the singular block above; full standalone form below if planner prefers separate code path):
  if (info.options->UseMultiCut && !is_pv && depth >= 8 && !in_check) {
      int cuts = 0, M = 6, C = 3;
      int reduced_depth = depth / 2;
      for (int i = 0; i < M && i < num_moves; ++i) {
          Move m = ordered_moves[i];
          // make move, reduced zero-window search
          int s = -alpha_beta(board, reduced_depth - 1, -beta - 1, -beta, info, ply + 1, false);
          // unmake
          if (s >= beta) { if (++cuts >= C) return beta; }
      }
  }
  Either form is acceptable. Recommend the standalone form because it runs at every cut node, not just on TT-move singular tests.

ProbCut (SRCH-10, RESEARCH.md D2 §3.8 — runs near the top of alpha_beta, after TT probe and RFP, before main move generation):
  if (info.options->UseProbCut && !is_pv && !in_check && depth >= 5
      && std::abs(beta) < MATE_IN_MAX_PLY) {
      int probcut_beta  = beta + probcut_margin;       // probcut_margin ≈ 200 (tunable)
      int probcut_depth = depth - 3;
      MovePicker capture_picker(/* TT move */, /* captures only, SEE >= probcut_beta - static_eval */);
      while (Move m = capture_picker.next(board)) {
          // make move
          int s = -qsearch(board, -probcut_beta, -probcut_beta + 1, info, ply + 1);
          if (s >= probcut_beta) {
              s = -alpha_beta(board, probcut_depth, -probcut_beta, -probcut_beta + 1, info, ply + 1, false);
          }
          // unmake
          if (s >= probcut_beta) return s;
      }
  }
  D-04 explicitly notes ProbCut may revert to OFF if Tier-2 mini-gauntlet shows regression. The Plan 03-03 Task 4 checkpoint must run two mini-gauntlets in sequence: (a) full tier-2 stack as-is, (b) if (a) regresses, isolate ProbCut by re-running with UseProbCut=false. If isolation shows ProbCut is the regressor, change the default in EngineOptions from `true` to `false` and commit the change as part of this plan; otherwise leave default ON.

Continuation history (SRCH-07, RESEARCH.md D2 §3.5 — 4-ply table):
  // In Engine class:
  int continuation_history_[2][6][64][2][6][64] = {};
    // continuation_history_[stm][piece(prev)][to(prev)][stm_now][piece(curr)][to(curr)]
  int capture_history_[2][6][64][6] = {};
    // capture_history_[stm][piece(curr)][to(curr)][captured_piece]
  Update on beta-cutoff alongside main history (Plan 03-02 Task 2 site):
    continuation_history_[prev_stm][prev_piece][prev_to][stm][piece][to] += depth * depth;
    if (is_capture(m))
        capture_history_[stm][piece][to][captured] += depth * depth;
  Age on new_search() by right-shift 1 (matches main history aging in Plan 03-01 Engine::age_history).

  Dimension note: 2*6*64*2*6*64 = 589,824 ints (~2.3 MB). Acceptable per-engine memory; consider a smaller dim (e.g., 4-ply table) only if profile shows cache pressure. Planner discretion to pick exact shape; RESEARCH.md D2 §3.5 says "4-ply continuation table dims" which can be interpreted as either 1-ply-back (this form) or 2-ply-back. Recommend 1-ply-back for simplicity; cite the dimension choice and rationale in the implementation comment.

SearchInfo additions (extending Plan 03-01/03-02 pattern):
  int (*continuation_history)[6][64][2][6][64] = nullptr;
  int (*capture_history)[6][64][6] = nullptr;
  Wired in Engine::search alongside info.history and info.counter_moves.

UCI toggle additions (D-06, lifts to 11 of 12; Plan 03-04 adds the 12th):
  EngineOptions extends with: bool UseSingular = true; bool UseMultiCut = true; bool UseProbCut = true;
  (ProbCut default may flip to false at Task 4 checkpoint if gauntlet shows regression — see ProbCut block above.)
  uci_main.cpp uci handshake adds 3 more `option name Use<X> type check default true` lines.

Singular NPS sentinel (Pitfall 2 + Success Criterion #4 — tests/test_v7_singular_nps_ratio.py):
  Reuses the _assert_nps_ratio helper pattern from tests/test_nps_regression.py:81-113 and _find_uci_binary fixture from tests/conftest.py:166-198.
  Skeleton:
    @pytest.mark.benchmark
    @pytest.mark.skipif(os.environ.get("RUN_BENCHMARKS") != "1", reason="...")
    def test_singular_nps_ratio_within_10_percent(v7_native_engine):
        nps_on  = _bench_with_uci_option(uci_bin, "UseSingular", "true",  bench_position, depth=10)
        nps_off = _bench_with_uci_option(uci_bin, "UseSingular", "false", bench_position, depth=10)
        ratio = abs(nps_on - nps_off) / nps_off
        assert ratio < 0.10, f"Singular extensions changed NPS by {ratio:.2%} (> 10% — Pitfall 2 search-explosion)"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Continuation + capture history (SRCH-07)</name>
  <files>
    src/chess_engine/engine/v7/include/engine.hpp,
    src/chess_engine/engine/v7/include/search.hpp,
    src/chess_engine/engine/v7/src/engine.cpp,
    src/chess_engine/engine/v7/src/search.cpp,
    tests/test_v7_history.py
  </files>
  <read_first>
    src/chess_engine/engine/v7/include/engine.hpp,
    src/chess_engine/engine/v7/include/search.hpp,
    src/chess_engine/engine/v7/src/engine.cpp,
    src/chess_engine/engine/v7/src/search.cpp,
    .planning/phases/03-lockless-tt-search-refinements-endgame/03-RESEARCH.md,
    .planning/phases/03-lockless-tt-search-refinements-endgame/03-02-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1 (test_history_accumulates_on_beta_cutoff): after a search where move m caused a beta-cutoff at depth 6, history[side][from][to] increases by 36 (depth^2) — exact value depends on update formula but must be > 0 and monotonically up
    - Test 2 (test_history_aged_on_new_search): seed history with value 100 at one entry, call engine.new_game() (which calls new_search internally), and verify the entry is now ~50 (right-shift 1) — D-04 SRCH-07 aging contract
    - Test 3 (test_continuation_history_indexed_by_prev_move): after a sequence e2e4, then black responds with e7e5 causing a cutoff at depth 4, continuation_history[BLACK][PAWN][e5][BLACK][...] reflects that update (sanity check that the table is indexed by previous-mover not current-mover)
    - Test 4 (test_capture_history_only_on_captures): a quiet move beta-cutoff does NOT touch capture_history (the entry remains 0); a capture beta-cutoff DOES touch capture_history
    - Test 5 (test_counter_move_indexed_by_side_that_moved): re-verify Plan 03-02's counter-move pattern with the same convention — counter_moves[WHITE][e2][e4] gets populated when after WHITE plays e2e4 BLACK's reply causes a beta-cutoff (RESEARCH.md Pitfall 7)
  </behavior>
  <action>
    Step A — Add `int continuation_history_[2][6][64][2][6][64] = {};` and `int capture_history_[2][6][64][6] = {};` as Engine members in engine.hpp. Document the dimension shape choice in a comment (cite the <interfaces> block's dimension note). Total memory ~2.4 MB.

    Step B — Extend SearchInfo (search.hpp) with the two non-owning pointer fields mirroring info.history pattern (Plan 03-01). Update SearchInfo::reset() comment to enumerate the new pointers in the preserved-fields list.

    Step C — Extend Engine::new_game (engine.cpp) to memset both tables to zero alongside the existing history_/counter_moves_ resets. Extend Engine::age_history (added in Plan 03-01) to right-shift continuation_history_ and capture_history_ entries by 1.

    Step D — Wire info.continuation_history and info.capture_history in Engine::search alongside the existing pointer wiring.

    Step E — In search.cpp, at the beta-cutoff site that already updates main history and killers (Plan 03-02 Task 2), add updates to continuation_history (always on quiet beta-cutoff with `+= depth * depth`) and capture_history (only on capture beta-cutoff with `+= depth * depth`). The previous-move plumbing required for continuation_history indexing — prev_piece, prev_to, prev_stm — comes from the existing SearchStack ply state (prev_capture_sq was added in Plan 03-02 Task 2; expand it to also track prev_piece and prev_stm via either new SearchStack arrays or a per-ply `Move prev_move[MAX_PLY]` field, planner discretion).

    Step F — Update the move picker (added in Plan 03-02 Task 2) to read continuation_history in the quiets-scoring pass. Quiet move score = main_history + continuation_history (add the two reads; preserve killer/counter ordering on top of history-sort).

    Step G — Implement the 5 test_v7_history.py tests per Behavior. Use the v7_native_engine fixture; for the aging test, exercise a programmatic seed (likely via a debug introspection hook on Engine — if no such hook exists, add a minimal `int Engine::peek_history(int side, int from, int to) const` method behind a `#ifndef NDEBUG` or unconditionally with a comment that it is a test-only accessor).
  </action>
  <verify>
    <automated>python3 -m uv run --group dev pytest tests/test_v7_history.py tests/test_v7_search.py tests/test_v7_engine.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "continuation_history_\[2\]\[6\]\[64\]\[2\]\[6\]\[64\]" src/chess_engine/engine/v7/include/engine.hpp` returns exactly one match.
    - `grep -nE "capture_history_\[2\]\[6\]\[64\]\[6\]" src/chess_engine/engine/v7/include/engine.hpp` returns exactly one match.
    - `grep -nc "continuation_history\|capture_history" src/chess_engine/engine/v7/src/search.cpp` returns ≥ 2 (read at scoring + write at cutoff).
    - All 5 history tests pass: `pytest tests/test_v7_history.py -q -x` exits 0.
    - Phase 1 + Plan 03-01 + Plan 03-02 tests stay green.
  </acceptance_criteria>
  <done>SRCH-07 implemented; continuation + capture history tables persist on Engine, accumulate on cutoff, age on new_search; move picker consults both for quiet scoring; all 5 history tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Singular extensions + multi-cut + ProbCut (SRCH-08/09/10) + UCI toggles</name>
  <files>
    src/chess_engine/engine/v7/include/engine.hpp,
    src/chess_engine/engine/v7/include/search.hpp,
    src/chess_engine/engine/v7/src/engine.cpp,
    src/chess_engine/engine/v7/src/search.cpp,
    src/chess_engine/engine/v7/src/uci_main.cpp,
    tests/test_v7_search_refinements.py,
    tests/test_v7_singular_nps_ratio.py
  </files>
  <read_first>
    src/chess_engine/engine/v7/src/search.cpp,
    src/chess_engine/engine/v7/include/search.hpp,
    src/chess_engine/engine/v7/include/engine.hpp,
    src/chess_engine/engine/v7/src/uci_main.cpp,
    tests/conftest.py,
    tests/test_nps_regression.py,
    .planning/phases/03-lockless-tt-search-refinements-endgame/03-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1 (test_multicut_threshold — already stubbed by Plan 03-01 Task 2): on a cut-node position with depth >= 8, if 3 of the first 6 moves return scores >= beta at reduced depth, alpha_beta returns beta (multi-cut fires)
    - Test 2 (test_singular_extension_fires_on_tt_beta — new): in a constructed position where the TT entry has flag=BETA, depth>=depth_now-3, and singular-search returns < singular_beta, the TT move's child search depth equals depth - 1 + 1 = depth (extension fires)
    - Test 3 (test_singular_skips_tt_probe_when_excluded — new): when excluded_move[ply] is set, the TT probe at top of alpha_beta is bypassed (verifiable via instrumentation: a counter increment in the TT probe should NOT advance for the verification re-search)
    - Test 4 (test_singular_nps_ratio_within_10_percent — RUN_BENCHMARKS=1 gated): two bench runs (UseSingular=true vs false) on STARTPOS_FEN at depth=12 produce NPS values within 10% of each other (Pitfall 2 / Success Criterion #4)
    - Test 5 (test_probcut_returns_early_on_capture_failhigh — new): on a tactical position with a winning capture, ProbCut returns within fewer nodes than ProbCut=off (verifiable via node count)
  </behavior>
  <action>
    Step A — Extend EngineOptions struct (added by Plan 03-02 Task 3) with three new booleans defaulting true: UseSingular, UseMultiCut, UseProbCut. Document that ProbCut's default may flip to false at Task 3 checkpoint depending on gauntlet result.

    Step B — Implement the singular extension block per the <interfaces> block, INSIDE the move loop, BEFORE the make-move call. Critical invariants:
      1. Move-loop entry: `if (m == info.search_stack->excluded_move[ply]) continue;` (skip the excluded move)
      2. Top of alpha_beta: at the very start, before TT probe — `if (info.search_stack->excluded_move[ply] != MOVE_NONE) { /* skip TT probe block */ }`. The excluded value must be cleared before the recursive verification call returns to the caller's frame — set in step "Verification re-search" of the singular block, clear immediately after the alpha_beta returns. Use RAII or explicit set/clear pair; either way, the invariant is "excluded_move[ply] is non-NONE only for the duration of the singular verification call".
      3. Mate-score wrappers (score_to_tt / score_from_tt) preserve their existing call shape — singular block does NOT touch them.

    Step C — Implement multi-cut. Planner discretion: piggyback inside the singular block (RESEARCH.md Code Examples §SE final clause) OR implement as a standalone block at cut nodes (depth >= 8) per the <interfaces> standalone form. Both are documented; recommend the standalone form (runs at every cut node, not just TT-move tests). Document the choice in the implementation comment.

    Step D — Implement ProbCut per the <interfaces> block. Position: near the top of alpha_beta, after TT probe and after RFP (added in Plan 03-02), before the main move-generation step. Reuses the existing MovePicker (Plan 03-02 Task 2) with a "captures-only" mode and a SEE-margin filter. Initial probcut_margin = 200 (tunable; document Phase 4 TUNE-09 re-scaling note in comment).

    Step E — Extend uci_main.cpp uci handshake response to add 3 more lines: `option name UseSingular type check default true`, `option name UseMultiCut type check default true`, `option name UseProbCut type check default true`. Order alphabetically within the existing options list.

    Step F — Extend Engine::set_option dispatcher (added in Plan 03-02 Task 3) to handle the 3 new option names. Pattern is identical to the existing 8.

    Step G — Implement test_v7_search_refinements.py::test_multicut_threshold (was stubbed by Plan 03-01); add 2 new tests `test_singular_extension_fires_on_tt_beta` and `test_singular_skips_tt_probe_when_excluded` to the same file. The "skips TT probe" assertion may require a debug hook on TT::probe (counter increment) — add a minimal `uint64_t v7::TT::probe_count() const` (Plan 03-01 added `hit_count`/`miss_count`; reuse those) and assert hit_count + miss_count does NOT advance across the verification re-search. The ProbCut test goes in the same file as test_probcut_returns_early_on_capture_failhigh.

    Step H — Implement tests/test_v7_singular_nps_ratio.py (stubbed by Plan 03-01) using the _find_uci_binary fixture from conftest.py:166-198 and the _assert_nps_ratio helper pattern from tests/test_nps_regression.py:81-113. The test invokes v7_uci twice via subprocess: first with `setoption name UseSingular value true; position startpos; go depth 12`, second with `setoption name UseSingular value false; ...`. Parse NPS from the final `info ... nps <N>` line in each subprocess output; assert `|nps_on - nps_off| / nps_off < 0.10`. Skip when RUN_BENCHMARKS != "1" (skipif decorator already on the stub).
  </action>
  <verify>
    <automated>python3 -m uv run --group dev pytest tests/test_v7_search_refinements.py tests/test_v7_search.py tests/test_v7_engine.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "excluded_move\[ply\]" src/chess_engine/engine/v7/src/search.cpp` returns ≥ 3 matches (set, clear, consume at move-loop start; plus the TT-probe-skip guard).
    - `grep -nE "if \(.*excluded_move\[ply\]\s*!=\s*MOVE_NONE\)" src/chess_engine/engine/v7/src/search.cpp` returns ≥ 1 (the TT-probe skip — RESEARCH.md critical correctness invariant).
    - `grep -cE "UseSingular|UseMultiCut|UseProbCut" src/chess_engine/engine/v7/include/engine.hpp src/chess_engine/engine/v7/include/search/options.hpp 2>/dev/null` returns ≥ 3.
    - `grep -cE "option name (UseSingular|UseMultiCut|UseProbCut)" src/chess_engine/engine/v7/src/uci_main.cpp` returns ≥ 3.
    - `pytest tests/test_v7_search_refinements.py::test_multicut_threshold tests/test_v7_search_refinements.py::test_singular_extension_fires_on_tt_beta tests/test_v7_search_refinements.py::test_singular_skips_tt_probe_when_excluded -q -x` exits 0.
    - Phase 1 + Plan 03-01 + Plan 03-02 + Task 1 tests stay green.
    - Singular NPS sentinel runnable: `RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest tests/test_v7_singular_nps_ratio.py -q -x` exits 0 on the build host (deferred per Pattern 5; unit-test path with RUN_BENCHMARKS unset must collect and skip cleanly).
  </acceptance_criteria>
  <done>SRCH-08, SRCH-09, SRCH-10 implemented and toggle-gated; the three critical singular correctness invariants (move-loop skip, TT-probe skip when excluded, mate-score wrappers preserved) verified by tests; singular NPS sentinel implemented and runnable.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Tier-2 mini-gauntlet + singular NPS gate + ProbCut bisection (D-04/D-05)</name>
  <what-built>
    Plan 03-03 has landed continuation/capture history (SRCH-07), singular extensions with verification (SRCH-08), multi-cut (SRCH-09), and ProbCut (SRCH-10) behind 3 new UCI toggles. The full D-06 search-refinement option surface is now 11 of 12 toggles (Plan 03-04 adds UseFortressEval).

    The next gates are: (a) tier-2 mini-gauntlet vs baseline-phase3 with lower-bound Elo > -10 (D-05); (b) singular-on vs singular-off NPS ratio < 10% (Pitfall 2 + Success Criterion #4); (c) ProbCut bisection if (a) regresses.
  </what-built>
  <how-to-verify>
    Build host:
      1. Rebuild V7: `chess-engine build v7`.
      2. Run singular NPS sentinel: `RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest tests/test_v7_singular_nps_ratio.py -q -x`. MUST exit 0. If it fails, the singular gate (depth, margin, or TT-condition) is wrong — fix before proceeding (likely candidates per RESEARCH.md Pitfall 2: lower the depth gate, increase the margin, or tighten the TT-condition to require `tt_entry.depth >= depth - 2` instead of `- 3`).
      3. Run 200-game tier-2 mini-gauntlet: `python tools/gauntlet.py run --challenger v7 --baseline-summary .planning/gauntlets/baseline-phase3/summary.json --games 200 --tc 10+0.1 --book tools/8moves_v3.pgn --concurrency 1 --out .planning/gauntlets/tier2/`.
      4. If lower-bound Elo > -10: PASS, proceed to commit and approve.
      5. If lower-bound Elo <= -10: bisect via UCI toggle per D-06. Run mini-gauntlets with each Tier-2 option disabled in turn (`-engine ... option.UseProbCut=false`, then `option.UseMultiCut=false`, then `option.UseSingular=false`). Identify the regressing refinement.
        - If ProbCut is the regressor: flip its default in EngineOptions from `true` to `false`, re-run the tier-2 gauntlet, expect PASS, commit the default change. This is the path D-04 SRCH-10 explicitly anticipates.
        - If Singular or MultiCut is the regressor: do NOT ship as default-OFF — these are too valuable. Bisect further (likely a singular invariant bug — re-read RESEARCH.md Code Examples §SE final paragraphs, especially the TT-probe-skip invariant).
      6. Commit: `git add .planning/gauntlets/tier2/ src/chess_engine/engine/v7/include/engine.hpp && git commit -m "phase(03-03): tier-2 mini-gauntlet passes; ProbCut default = <true|false>"`.

    Windows dev host: SKIP.

    Report: (a) singular NPS ratio measured, (b) tier-2 Elo lower-bound, (c) final ProbCut default value and rationale.
  </how-to-verify>
  <resume-signal>
    Approve when (a) singular NPS sentinel passes and (b) tier-2 mini-gauntlet passes (either as-is or after ProbCut default flip).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Singular verification re-search ↔ TT probe | Recursive alpha_beta call with excluded_move set; if excluded_move not consulted in TT probe, the singular test silently no-ops (correctness bug — Pitfall 2 amplified) |
| Search loop ↔ probcut margin / depth gate | Mis-tuned margin causes search explosion or strength loss |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | DoS (UI thread starvation) — singular search explosion | Singular extension block in search.cpp | mitigate | Pitfall 2 + Success Criterion #4 — tests/test_v7_singular_nps_ratio.py asserts NPS ratio < 10% (RUN_BENCHMARKS=1 gated, build-host). Depth gate `>= 8` and TT-condition `flag == TT_BETA && depth >= depth_now - 3` minimize trigger frequency. Margin `2 * depth` is canonical. |
| T-03-X3 | Tampering (silent correctness) | Singular TT-probe-skip invariant | mitigate | Test test_singular_skips_tt_probe_when_excluded explicitly verifies that the TT hit/miss counter does NOT advance across the verification re-search — invariant has a behavioral check, not just a code-review check. |
| T-03-X4 | Repudiation — flaky ProbCut | ProbCut margin/depth gate | mitigate | D-04 SRCH-10 explicitly anticipates ProbCut may default OFF. Task 3 checkpoint bisects and flips default if needed; the option remains in the surface either way (gauntlet can flip it on for ablation studies). |
</threat_model>

<verification>
- All 5 history tests pass (Task 1).
- All 3 new search-refinement tests pass (multicut, singular, excluded-move guard).
- Singular NPS sentinel runnable and passes on build host (RUN_BENCHMARKS=1).
- Tier-2 mini-gauntlet passes (Elo lower-bound > -10) OR ProbCut default flipped and re-passes.
- 11 of 12 D-06 toggles wired (UseFortressEval added in Plan 03-04).
</verification>

<success_criteria>
- SRCH-07, SRCH-08, SRCH-09, SRCH-10 all implemented and toggle-gated.
- Tier-2 mini-gauntlet checkpoint passes (D-05 non-regression).
- Singular NPS ratio < 10% (Success Criterion #4 measurable evidence).
- ProbCut default decision documented (kept ON or flipped to OFF, with gauntlet evidence).
</success_criteria>

<output>
After completion, create `.planning/phases/03-lockless-tt-search-refinements-endgame/03-03-SUMMARY.md` recording: each of the 4 SRCH requirements with implementing file/line, the 3 new UCI toggle defaults (especially ProbCut's final decision), the tier-2 mini-gauntlet Elo result, the singular NPS ratio measured, and the singular block's exact gate constants (depth_gate, tt_depth_margin, singular_margin formula) used.
</output>
