---
phase: 03-lockless-tt-search-refinements-endgame
plan: 06
type: execute
wave: 3
depends_on: [03-02, 03-03, 03-04, 03-05]
files_modified:
  - .planning/gauntlets/ship-sprt-phase3/
  - .planning/gauntlets/full-tsan-rerun/
  - .planning/RETROSPECTIVE.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "Ship SPRT vs baseline-phase3 (frozen by Plan 03-01) demonstrates V7 Phase 3 ≥ +30 Elo with 95% confidence — the canonical strength gate per CONTEXT D-02 + ROADMAP Phase 3 goal"
    - "All 4 Wave 2 plans (03-02 tier-1 search, 03-03 tier-2 search, 03-04 endgame, 03-05 lockless TT) have shipped before this gate runs"
    - "scripts/tt_tsan_stress.sh is re-run as a final confirmation pass after all Phase 3 code lands (catches integration-level races that single-plan TSan runs might miss)"
    - "Singular-NPS sentinel from Plan 03-03 (tests/test_v7_singular_nps_ratio.py) is re-confirmed with all Wave 2 features ENABLED (default UCI options as shipped) — confirms the ≤10% NPS budget held under the full stack (D-04 acceptance)"
    - "LMR-depth sentinel from Plan 03-02 (tests/test_v7_lmr_depth.py) is re-confirmed with all Wave 2 features ENABLED"
    - "Cumulative Elo gain from Plans 03-02/03/04/05 ≥ +30 Elo with 95% confidence over baseline-phase3 (Goal validation — no shipping criterion overrides this per PROJECT.md Core Value)"
    - "All Plan 03-01..05 SUMMARYs exist and their stated acceptance criteria have green check-marks"
    - "REQUIREMENTS.md is annotated with the disposition for each of the 18 phase requirement IDs (PAR-01..03, SRCH-03..12, ENDG-01..05): SHIPPED, SHIPPED-BUT-DISABLED (UseFortressEval if validation gauntlet regressed per D-11), or DEFERRED with rationale"
    - "RETROSPECTIVE.md gains a Phase 3 entry per the get-shit-done milestone-retrospective template (lessons learned, patterns to follow/avoid for Phase 4)"
  artifacts:
    - path: ".planning/gauntlets/ship-sprt-phase3/summary.json"
      provides: "Ship SPRT result: V7-Phase3 vs baseline-phase3 with default UCI, ≥30 Elo lower bound at 95% confidence"
      contains: "elo_lower_bound"
    - path: ".planning/gauntlets/full-tsan-rerun/log.txt"
      provides: "Confirmation pass of scripts/tt_tsan_stress.sh after all Phase 3 code merged; PASS log captured"
      contains: "PASS"
    - path: ".planning/RETROSPECTIVE.md"
      provides: "Phase 3 retrospective entry per get-shit-done milestone-retrospective template"
      contains: "Phase 3"
  key_links:
    - from: ".planning/gauntlets/ship-sprt-phase3/summary.json"
      to: ".planning/gauntlets/baseline-phase3/summary.json"
      via: "tools/gauntlet.py --baseline-summary flag — same opponent set + book + TC, head-to-head V7-Phase3 vs the frozen Plan 03-01 baseline"
      pattern: "baseline-phase3"
    - from: ".planning/RETROSPECTIVE.md"
      to: ".planning/phases/03-lockless-tt-search-refinements-endgame/03-*-SUMMARY.md"
      via: "Phase 3 retrospective synthesizes lessons across all 6 plan summaries"
      pattern: "03-0[1-6]"
---

<objective>
Run the Phase 3 ship SPRT gate — the canonical strength validation per CONTEXT D-02 and ROADMAP Phase 3 goal — plus the final TSan re-run, NPS sentinel re-confirmation, and Phase 3 retrospective. This plan adds NO new code; it is the gauntlet-and-paperwork gate that closes Phase 3 and unblocks Phase 4 (Lazy SMP).

Purpose: PROJECT.md Core Value — "V7 must play stronger chess than V6 in head-to-head gauntlets" — extends to the per-phase contract. Phase 3 ships only if ship-SPRT vs baseline-phase3 shows ≥30 Elo with 95% confidence under default UCI. This is a hard gate; no Phase 4 work begins until this passes.

Output: ship-SPRT summary.json + full-TSan-rerun log + Phase 3 retrospective entry + REQUIREMENTS.md annotations for the 18 phase requirement IDs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/RETROSPECTIVE.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/03-CONTEXT.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/03-RESEARCH.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/03-PATTERNS.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/03-VALIDATION.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/03-01-SUMMARY.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/03-02-SUMMARY.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/03-03-SUMMARY.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/03-04-SUMMARY.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/03-05-SUMMARY.md
@.planning/gauntlets/baseline-phase3/summary.json
@tools/gauntlet.py
@scripts/tt_tsan_stress.sh
@tests/test_v7_singular_nps_ratio.py
@tests/test_v7_lmr_depth.py

<interfaces>
Ship-SPRT command shape (D-02 + Phase 2 gauntlet harness convention — mirror baseline-phase3 invocation from Plan 03-01 Task 3):
  python tools/gauntlet.py run \
    --challenger v7 \
    --baseline-summary .planning/gauntlets/baseline-phase3/summary.json \
    --games 2000 \
    --tc 10+0.1 \
    --book tools/8moves_v3.pgn \
    --concurrency 1 \
    --sprt \
    --sprt-elo0 0 \
    --sprt-elo1 30 \
    --sprt-alpha 0.05 \
    --sprt-beta 0.05 \
    --out .planning/gauntlets/ship-sprt-phase3/

  Note: SPRT auto-terminates when either H0 (≤0 Elo) or H1 (≥30 Elo) is reached. Expected runtime: variable depending on cumulative gain — Phase 3 stacked features should land well above 30 Elo, so SPRT typically resolves H1 in 200-800 games.

Pass criterion (D-02):
  - elo_lower_bound (LLR-based 95% CI lower bound) >= 30 Elo
  - sprt_result == "H1_accepted"
  - games_completed < games budget (early stop = healthy)
  - Both crash counts (challenger + baseline) == 0

Fail handling (per CONTEXT specifics + RESEARCH.md "Don't Hand-Roll"):
  If SPRT lands H0 (≤0 Elo) or stays inconclusive past the budget:
    1. Halt — do NOT push to main.
    2. Open a Phase 3 follow-up plan (bisect via per-toggle gauntlet: disable D-06 toggles one at a time, find the offender).
    3. The most likely culprits (in priority order): (a) ProbCut default-on (D-04 explicitly allows flipping to default-off at Plan 03-03 checkpoint), (b) UseFortressEval=true if it slipped through despite Plan 03-04's separate validation, (c) singular-extension search-explosion (RESEARCH.md Pitfall 4 — check NPS ratio sentinel first).

Full-TSan-rerun (after all Phase 3 code merged):
  bash scripts/tt_tsan_stress.sh 2>&1 | tee .planning/gauntlets/full-tsan-rerun/log.txt
  Acceptance: log file contains "PASS: TSan clean over 16 threads x 60s"; no "WARNING: ThreadSanitizer" anywhere in the log.

  Why re-run here (not just trust Plan 03-05's run): Plans 03-02/03/04 may have changed search.cpp / endgame.cpp in ways that interact with TT access patterns (more probes/stores from refinements). The re-run on the integrated code is the canonical final check before Phase 4 Lazy SMP work begins.

NPS sentinel re-confirmation (D-04 acceptance, with all features ON):
  RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest \
    tests/test_v7_singular_nps_ratio.py \
    tests/test_v7_lmr_depth.py \
    tests/test_nps_regression.py \
    -q -x
  Acceptance: both new Phase 3 sentinels pass; the Phase 2 NPS regression sentinel stays within its existing floor (Phase 2 contract — no regression from the cumulative Phase 3 stack).

REQUIREMENTS.md annotation pattern (Phase 1 / Phase 2 convention — annotate in place):
  - PAR-01..03: SHIPPED (link to 03-05-SUMMARY.md)
  - SRCH-03/04/05/06/11/12: SHIPPED (link to 03-02-SUMMARY.md)
  - SRCH-07/08/09/10: SHIPPED, or SHIPPED-BUT-DISABLED for ProbCut if Plan 03-03's tier-2 mini-gauntlet flipped its default to OFF (D-04)
  - ENDG-01..04: SHIPPED (link to 03-04-SUMMARY.md)
  - ENDG-05: SHIPPED or SHIPPED-BUT-DISABLED depending on Plan 03-04's fortress validation gauntlet outcome (D-11)

RETROSPECTIVE.md template (per get-shit-done milestone-retrospective format — read existing Phase 1/2 entries for style):
  ## Phase 3 — Lockless TT + Search Refinements + Endgame  (closed YYYY-MM-DD)
  ### What Worked
  ### What Was Inefficient
  ### Patterns Established
  ### Key Lessons (Phase 4 hand-off)
  ### Cost Patterns (model selection, agent strategy for Phase 4)
  ### Acceptance Summary
  - 18/18 requirements: <SHIPPED count> / <SHIPPED-BUT-DISABLED count> / <DEFERRED count>
  - Ship-SPRT Elo gain: <X> [95% CI: <lower>, <upper>]
  - TSan: clean on <build host + compiler + date>
  - NPS budget: held / regressed by <X>%

Decision logging:
  - If ship-SPRT lands H1 cleanly: commit the summary.json + log + REQUIREMENTS.md annotations + RETROSPECTIVE.md entry; tag the commit "phase-03-shipped".
  - If ship-SPRT lands H0 or inconclusive: do NOT tag; create .planning/phases/03-lockless-tt-search-refinements-endgame/.continue-here.md documenting the bisection plan.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Ship SPRT vs baseline-phase3 — the Phase 3 strength gate (D-02 / Phase 3 goal)</name>
  <what-built>
    All Phase 3 code has merged: Plan 03-01 (substrate refactor + 11 Wave 0 test stubs + baseline-phase3 gauntlet), Plan 03-02 (tier-1 search refinements — null-move/LMR/RFP/futility/LMP/move-ordering), Plan 03-03 (tier-2 — continuation+capture history/singular/multi-cut/ProbCut), Plan 03-04 (endgame — KPK/opposition/wrong-bishop/fortress/0..256 phase blend), Plan 03-05 (lockless Hyatt-Mann XOR TT + TSan stress harness).

    All 12 UCI toggles (D-06) are wired. Default UCI as shipped by the four Wave 2 plans is what the ship SPRT exercises — any per-toggle flips from the Wave 2 checkpoint decisions (ProbCut default per D-04 / Plan 03-03 checkpoint, UseFortressEval default per D-11 / Plan 03-04 checkpoint) are already in place.
  </what-built>
  <how-to-verify>
    Build host:
      1. Pull the latest main and rebuild V7: `git pull origin main && chess-engine build v7`.
      2. Confirm all Plan 03-01..05 SUMMARYs exist:
         `ls .planning/phases/03-lockless-tt-search-refinements-endgame/03-0{1,2,3,4,5}-SUMMARY.md` — must list 5 files.
      3. Run the ship SPRT (the canonical Phase 3 strength gate per D-02):
         `python tools/gauntlet.py run --challenger v7 --baseline-summary .planning/gauntlets/baseline-phase3/summary.json --games 2000 --tc 10+0.1 --book tools/8moves_v3.pgn --concurrency 1 --sprt --sprt-elo0 0 --sprt-elo1 30 --sprt-alpha 0.05 --sprt-beta 0.05 --out .planning/gauntlets/ship-sprt-phase3/`
         Expected runtime: 30 min - 6 hours depending on how decisive the cumulative gain is (SPRT terminates early on either H0 or H1).
      4. Open `.planning/gauntlets/ship-sprt-phase3/summary.json` and verify:
         - `sprt_result == "H1_accepted"` (the +30 Elo hypothesis was accepted)
         - `elo_lower_bound >= 30`
         - Both crash counts are 0
         - `games_completed < 2000` (early-stop is the expected outcome of a healthy phase)
      5. Re-run the integration TSan check (catches any TT-access regression from the Wave 2 search refinements):
         `bash scripts/tt_tsan_stress.sh 2>&1 | tee .planning/gauntlets/full-tsan-rerun/log.txt`
         Must end with "PASS: TSan clean over 16 threads x 60s".
      6. Re-run the Phase 3 NPS sentinels with all features enabled (default UCI):
         `RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest tests/test_v7_singular_nps_ratio.py tests/test_v7_lmr_depth.py tests/test_nps_regression.py -q -x`
         All three must exit 0.
      7. If ship-SPRT FAILS (H0_accepted or inconclusive at 2000 games):
         - Do NOT proceed to Task 2.
         - Create `.planning/phases/03-lockless-tt-search-refinements-endgame/.continue-here.md` documenting the per-toggle bisection plan from the <interfaces> Fail handling block.
         - Open a Phase 3 follow-up plan (`/gsd-plan-phase 03 --gaps`).
         - Report the failure with summary.json contents.

    Windows dev host: SKIP. SPRT is build-host-only (long runtime + chess UCI binary discovery).

    Report: (a) ship-SPRT summary.json contents (sprt_result, elo lower_bound, games_completed, crash counts), (b) full-TSan-rerun log tail (PASS/FAIL), (c) NPS sentinel results, (d) which UCI defaults were active during the SPRT (especially ProbCut + UseFortressEval — confirm matches D-04 / D-11 disposition).
  </how-to-verify>
  <resume-signal>
    Approve when ship-SPRT shows sprt_result == H1_accepted with elo_lower_bound >= 30, AND full-TSan-rerun PASSES, AND all 3 NPS sentinels exit 0. Block on any failure — Phase 4 cannot begin until Phase 3 strength is confirmed.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Annotate REQUIREMENTS.md + write RETROSPECTIVE.md Phase 3 entry + tag commit</name>
  <files>
    .planning/REQUIREMENTS.md,
    .planning/RETROSPECTIVE.md
  </files>
  <read_first>
    .planning/REQUIREMENTS.md,
    .planning/RETROSPECTIVE.md,
    .planning/phases/03-lockless-tt-search-refinements-endgame/03-01-SUMMARY.md,
    .planning/phases/03-lockless-tt-search-refinements-endgame/03-02-SUMMARY.md,
    .planning/phases/03-lockless-tt-search-refinements-endgame/03-03-SUMMARY.md,
    .planning/phases/03-lockless-tt-search-refinements-endgame/03-04-SUMMARY.md,
    .planning/phases/03-lockless-tt-search-refinements-endgame/03-05-SUMMARY.md,
    .planning/gauntlets/ship-sprt-phase3/summary.json
  </read_first>
  <action>
    Step A — Annotate each of the 18 Phase 3 requirement IDs in `.planning/REQUIREMENTS.md` per the disposition pattern in the <interfaces> block. Read each plan's SUMMARY to source the implementing file/function. Use the in-place annotation style established by Phases 1 and 2 (grep `SHIPPED|DEFERRED|SHIPPED-BUT-DISABLED` in the existing file to confirm format).
      - PAR-01, PAR-02, PAR-03 — from 03-05-SUMMARY
      - SRCH-03, SRCH-04, SRCH-05, SRCH-06, SRCH-11, SRCH-12 — from 03-02-SUMMARY
      - SRCH-07, SRCH-08, SRCH-09, SRCH-10 — from 03-03-SUMMARY (note: SRCH-09 ProbCut may be SHIPPED-BUT-DISABLED if D-04 flipped default to OFF at the 03-03 checkpoint)
      - ENDG-01, ENDG-02, ENDG-03, ENDG-04 — from 03-04-SUMMARY
      - ENDG-05 — from 03-04-SUMMARY (SHIPPED-BUT-DISABLED if D-11 fortress validation regressed)

    Step B — Append a Phase 3 entry to `.planning/RETROSPECTIVE.md` per the template in the <interfaces> block. Synthesize lessons from all 5 plan SUMMARYs. Required sections: What Worked, What Was Inefficient, Patterns Established, Key Lessons (Phase 4 hand-off), Cost Patterns (model selection / agent strategy hint for Phase 4 — Phase 4 is Lazy SMP, so flag the TSan harness as the canonical CI gate), Acceptance Summary (18/18 disposition counts + ship-SPRT result + TSan status + NPS budget verdict).

    Step C — Sanity-check: confirm every requirement ID in the Phase 3 frontmatter of every PLAN (03-01..05) appears in REQUIREMENTS.md with a non-blank disposition. Cross-reference:
      `grep -E "(PAR-0[123]|SRCH-(0[3-9]|1[012])|ENDG-0[1-5])" .planning/REQUIREMENTS.md` should return at least 18 lines, each ending with SHIPPED / SHIPPED-BUT-DISABLED / DEFERRED.

    Step D — Commit with a tag-worthy message:
      `git add .planning/REQUIREMENTS.md .planning/RETROSPECTIVE.md .planning/gauntlets/ship-sprt-phase3/ .planning/gauntlets/full-tsan-rerun/ && git commit -m "phase(03): ship — +<X> Elo over baseline-phase3, TSan clean, 18/18 requirements dispositioned"`
      Then tag: `git tag phase-03-shipped` (per the <interfaces> Decision logging — only if SPRT passed in Task 1).

    Step E — Update `.planning/STATE.md`'s phase pointer to Phase 4 (Lazy SMP) and mark Phase 3 as CLOSED.
  </action>
  <verify>
    <automated>grep -cE "(PAR-0[123]|SRCH-(0[3-9]|1[012])|ENDG-0[1-5]).*(SHIPPED|DEFERRED)" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - The `grep -cE "(PAR-0[123]|SRCH-(0[3-9]|1[012])|ENDG-0[1-5]).*(SHIPPED|DEFERRED)" .planning/REQUIREMENTS.md` command returns >= 18 (every Phase 3 requirement ID dispositioned).
    - `grep -nE "^## Phase 3" .planning/RETROSPECTIVE.md` returns exactly 1 new match (the Phase 3 entry was appended).
    - `grep -nE "Acceptance Summary" .planning/RETROSPECTIVE.md | tail -1` references the Phase 3 entry (template section present).
    - `git tag --list phase-03-shipped` returns the tag name (commit tagged).
    - `.planning/STATE.md` no longer lists Phase 3 as the active phase (closed / advanced to Phase 4).
    - `.planning/gauntlets/ship-sprt-phase3/summary.json` exists and is tracked by git.
    - `.planning/gauntlets/full-tsan-rerun/log.txt` exists and contains "PASS".
  </acceptance_criteria>
  <done>Phase 3 dispositioned in REQUIREMENTS.md, retrospective written, commit tagged phase-03-shipped, STATE.md advanced to Phase 4. Phase 3 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ship-SPRT result → "Phase 3 done" decision | Single gauntlet outcome decides whether Phase 4 starts; the SPRT methodology itself is the trust anchor |
| Integration TSan rerun → race-freedom claim across full stack | Wave 2 may have changed TT access patterns; re-run confirms no integration-level race |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-X8 | Repudiation — ship-SPRT result not reproducible (different opponent, different host) | tools/gauntlet.py invocation | mitigate | --baseline-summary pins the opponent set + book + TC verbatim from baseline-phase3; summary.json records git SHA of both V7 and baseline binaries. Phase 4 work re-runs ship-SPRT on a fresh build to confirm continuity. |
| T-03-X9 | Tampering — UCI default flips between Wave 2 checkpoints and ship-SPRT | EngineOptions defaults | mitigate | Task 1 step 7d explicitly reports the active UCI defaults during the SPRT; the SUMMARYs from Plans 03-03 (ProbCut) and 03-04 (UseFortressEval) record the agreed default. Mismatch = block and re-run. |
| T-03-X10 | DoS — ship-SPRT runs to 2000-game budget without termination | SPRT methodology | accept | Inconclusive result is a legitimate outcome (cumulative Phase 3 gain may be ≥0 but <30); triggers the per-toggle bisection follow-up plan. No data corruption. |
| T-03-X11 | Information disclosure — RETROSPECTIVE.md may leak coefficient values or future-phase strategy | RETROSPECTIVE.md | accept | Repo is private milestone work; RETROSPECTIVE.md is a normal in-tree artifact. No PII or secrets. |
</threat_model>

<verification>
- Ship-SPRT: sprt_result == H1_accepted, elo_lower_bound >= 30, both crash counts == 0.
- Full-TSan-rerun: "PASS: TSan clean over 16 threads x 60s" in the log.
- NPS sentinels (singular-NPS-ratio, LMR-depth, Phase 2 NPS regression) all pass with default UCI.
- REQUIREMENTS.md: 18 Phase 3 IDs all dispositioned (SHIPPED / SHIPPED-BUT-DISABLED / DEFERRED).
- RETROSPECTIVE.md: Phase 3 entry exists with all template sections.
- Commit tagged phase-03-shipped (only if SPRT passed).
- STATE.md advanced to Phase 4.
</verification>

<success_criteria>
- V7 Phase 3 is ≥ +30 Elo over baseline-phase3 with 95% confidence — the canonical Phase 3 goal achieved.
- Lockless TT is race-free on the integrated codebase (full TSan re-run clean).
- NPS budget held under the full Wave 2 stack.
- Phase 3 closed: requirements dispositioned, retrospective written, commit tagged, state advanced.
- Phase 4 (Lazy SMP) unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/03-lockless-tt-search-refinements-endgame/03-06-SUMMARY.md` recording: ship-SPRT result (sprt_result, elo bounds, games played, crash counts), full-TSan-rerun verdict + build host + compiler + date, NPS sentinel results with the cumulative-feature ratio numbers, the 18 requirement dispositions, key lessons for Phase 4, and the commit + tag SHA of phase-03-shipped.
</output>
