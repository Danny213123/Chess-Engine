---
phase: 03-lockless-tt-search-refinements-endgame
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chess_engine/engine/v7/include/engine.hpp
  - src/chess_engine/engine/v7/include/search.hpp
  - src/chess_engine/engine/v7/include/tt.hpp
  - src/chess_engine/engine/v7/include/board.hpp
  - src/chess_engine/engine/v7/include/search/move_picker.hpp
  - src/chess_engine/engine/v7/include/search/options.hpp
  - src/chess_engine/engine/v7/src/engine.cpp
  - src/chess_engine/engine/v7/src/search.cpp
  - src/chess_engine/engine/v7/src/board.cpp
  - src/chess_engine/engine/v7/src/search/move_picker.cpp
  - tests/test_v7_tt_lockless.py
  - tests/test_v7_search_refinements.py
  - tests/test_v7_move_picker.py
  - tests/test_v7_history.py
  - tests/test_v7_lmr_depth.py
  - tests/test_v7_singular_nps_ratio.py
  - tests/test_v7_kpk_bitbase.py
  - tests/test_v7_endgame.py
  - tests/test_v7_phase_blend.py
  - scripts/tt_tsan_stress.sh
  - src/chess_engine/engine/v7/tt_tsan_stress.cpp
autonomous: false
requirements: []
must_haves:
  truths:
    - "All 3 known Phase 1 perf bugs are fixed inside Plan 03-01 — no separate gap-closure milestone (D-01)"
    - "Phase 1 perf bug #1 (info.depth overwrite at engine.cpp:99-103 vs search.cpp:398) is fixed by splitting info.max_depth from info.depth (per D-03)"
    - "Phase 1 perf bug #2 (per-call stack-allocated killers/history at search.cpp:270-273) is fixed by relocating to Engine member state (per D-03)"
    - "Phase 1 perf bug #3 (std::vector<Move> pv allocations at search.cpp:241/317/319/361/391) is fixed by triangular Move pv[MAX_PLY][MAX_PLY] on SearchStack with MAX_PLY = 128 (per D-03)"
    - "g_tt global is migrated to Engine::tt_ via SearchInfo::tt non-owning pointer at every callsite (search.cpp:199, 366, 375, 492) so Plan 03-05's lockless rewrite has zero search-side dependencies on the global"
    - "11 Wave 0 test files exist and import cleanly (stubs allowed; pytest collects them without ImportError) so Plans 03-02..05 can add behavior tests against existing skeletons"
    - "Bench NPS ≥ 200,000 on the standard position after this plan merges (D-03 sanity floor for Phase 3 baseline measurement)"
    - "A blocking human checkpoint after this plan persists a 500-game V7-vs-V6 gauntlet summary.json at .planning/gauntlets/baseline-phase3/summary.json (per D-02), which becomes the canonical reference for Success Criterion #2"
    - "Plan 03-01 lifts the cross-Wave-2 SCAFFOLD (board.non_pawn_material helper, staged MovePicker stub, EngineOptions struct with all 12 D-06 toggles, and Engine::set_option dispatcher) so Plans 03-02/03-03/03-04 truly run in parallel in Wave 2 — none of them re-introduce shared types or shared search-side helpers"
  artifacts:
    - path: "src/chess_engine/engine/v7/include/search.hpp"
      provides: "SearchStack struct with pv[MAX_PLY][MAX_PLY], pv_length[MAX_PLY], killers[MAX_PLY][2], excluded_move[MAX_PLY]; MAX_PLY constant = 128; SearchInfo additions for max_depth, tt, search_stack, history, counter_moves non-owning pointers"
      contains: "constexpr int MAX_PLY = 128"
    - path: "src/chess_engine/engine/v7/include/engine.hpp"
      provides: "Engine members search_stack_, history_[2][64][64], counter_moves_[2][64][64]"
      contains: "SearchStack search_stack_"
    - path: "src/chess_engine/engine/v7/src/search.cpp"
      provides: "Refactored alpha_beta and iterative_deepening using triangular PV, persistent killers/history via SearchInfo pointers, and info.max_depth contract"
    - path: "tests/test_v7_tt_lockless.py"
      provides: "Test stubs for PAR-01/02 probe/store roundtrip + pack/unpack invariant (skeleton with pytest.skip OK; Plan 03-05 implements)"
    - path: "tests/test_v7_search_refinements.py"
      provides: "Test stubs for SRCH-03/05/09/11/12 boundary tests (skeleton)"
    - path: "tests/test_v7_move_picker.py"
      provides: "Test stub for SRCH-06 staged ordering (skeleton)"
    - path: "tests/test_v7_history.py"
      provides: "Test stub for SRCH-07 history/counter aging (skeleton)"
    - path: "tests/test_v7_lmr_depth.py"
      provides: "Test stub for SRCH-04 LMR depth-at-fixed-time benchmark (RUN_BENCHMARKS=1 gated skeleton)"
    - path: "tests/test_v7_singular_nps_ratio.py"
      provides: "Test stub for SRCH-08 Success Criterion #4 NPS ratio (RUN_BENCHMARKS=1 gated skeleton)"
    - path: "tests/test_v7_kpk_bitbase.py"
      provides: "Test stub for ENDG-01 KPK vs Fathom (skeleton)"
    - path: "tests/test_v7_endgame.py"
      provides: "Test stubs for ENDG-02/03/05 (skeleton)"
    - path: "tests/test_v7_phase_blend.py"
      provides: "Test stub for ENDG-04 monotonicity (skeleton)"
    - path: "scripts/tt_tsan_stress.sh"
      provides: "Shell harness stub for PAR-03 (skeleton with usage message; Plan 03-05 implements full TSan run)"
    - path: "src/chess_engine/engine/v7/tt_tsan_stress.cpp"
      provides: "Standalone C++ harness stub (skeleton main() returning 0; Plan 03-05 implements multi-thread stress)"
    - path: "src/chess_engine/engine/v7/include/board.hpp"
      provides: "int Board::non_pawn_material(Color c) const declaration (consumed by Plans 03-02 null-move zugzwang guard and 03-04 phase blend)"
      contains: "non_pawn_material"
    - path: "src/chess_engine/engine/v7/src/board.cpp"
      provides: "non_pawn_material implementation (popcount * piece-value sum using v7::coeffs externs; mirrors v6 helper if present)"
    - path: "src/chess_engine/engine/v7/include/search/options.hpp"
      provides: "struct EngineOptions with all 12 D-06 toggles at canonical defaults — UseNullMove/UseLMR/UseRFP/UseFutility/UseLMP/UseIIR/UseCheckExt/UseRecaptureExt/UseSingular/UseMultiCut/UseProbCut=true, UseFortressEval=false; Engine::set_option(name,value) declaration"
      contains: "struct EngineOptions"
    - path: "src/chess_engine/engine/v7/include/search/move_picker.hpp"
      provides: "MovePicker class skeleton with Stage enum (S_TT/S_GEN_CAPTURES/S_GOOD_CAPTURES/S_KILLERS/S_COUNTER/S_GEN_QUIETS/S_QUIETS/S_BAD_CAPTURES/S_DONE), constructor signature, and Move next(Board&) stub returning MOVE_NONE (Plan 03-02 Task 2 fills in the body)"
      contains: "class MovePicker"
    - path: "src/chess_engine/engine/v7/src/search/move_picker.cpp"
      provides: "MovePicker::next stub returning MOVE_NONE — body added in Plan 03-02 Task 2"
  key_links:
    - from: "src/chess_engine/engine/v7/src/engine.cpp"
      to: "src/chess_engine/engine/v7/include/search.hpp SearchInfo::tt"
      via: "info.tt = &tt_ wiring (mirrors info.external_stop = &stop_flag_ at engine.cpp:84)"
      pattern: "info\\.tt\\s*=\\s*&tt_"
    - from: "src/chess_engine/engine/v7/src/search.cpp"
      to: "info.tt"
      via: "all four g_tt callsites (probe @199, store @366, store @375, new_search @492) replaced with info.tt->...; no g_tt references remain in v7 source"
      pattern: "info\\.tt->(probe|store|new_search)"
    - from: "src/chess_engine/engine/v7/src/engine.cpp"
      to: "info.max_depth"
      via: "info.max_depth = max_depth; info.depth = 0; (replaces info.depth = max_depth at engine.cpp:99-103 per D-03)"
      pattern: "info\\.max_depth\\s*="
    - from: "src/chess_engine/engine/v7/include/search.hpp"
      to: "include/search/options.hpp EngineOptions"
      via: "SearchInfo gains const EngineOptions* options = nullptr field mirroring info.tt pointer pattern; Engine::search wires info.options = &options_"
      pattern: "info\\.options\\s*=\\s*&options_"
    - from: "src/chess_engine/engine/v7/src/engine.cpp"
      to: "Engine::set_option dispatcher (12 D-06 toggle names)"
      via: "dispatcher recognizes all 12 D-06 toggle names case-insensitively; logs info string Unknown option on miss; no throw"
      pattern: "Engine::set_option"
---

<objective>
Land the production scaffold for all of Phase 3 — fix the three Phase 1 perf bugs (D-01, D-03), relocate killers/history/PV to persistent Engine state (D-03), migrate g_tt → Engine::tt_ via SearchInfo (so Plan 03-05's lockless rewrite is file-disjoint from search), and create the 11 Wave 0 test-file stubs identified in 03-VALIDATION.md. After this plan merges, a blocking human checkpoint runs a 500-game V7-vs-V6 baseline gauntlet whose summary.json becomes the canonical reference for Success Criterion #2 (≥30 Elo gain in Plan 03-06's ship SPRT).

Purpose: Plans 03-02/03/04/05 all extend the SearchStack/info.tt/persistent-history substrate created here. Without this scaffold, every search refinement layered on top inherits broken move ordering and PV allocator pressure, and per-tier mini-gauntlets will show false regressions that bisect to the wrong commit (RESEARCH.md primary recommendation).

Output: a measurable NPS jump from ~3k to ≥200k on bench, a clean migration path for the lockless TT rewrite, and a frozen baseline summary.json for all downstream Phase 3 gauntlet comparisons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/03-CONTEXT.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/03-RESEARCH.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/03-PATTERNS.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/03-VALIDATION.md
@src/chess_engine/engine/v7/include/engine.hpp
@src/chess_engine/engine/v7/include/search.hpp
@src/chess_engine/engine/v7/include/tt.hpp
@src/chess_engine/engine/v7/src/engine.cpp
@src/chess_engine/engine/v7/src/search.cpp
@tests/test_v7_search.py
@tests/test_v7_engine.py
@tests/conftest.py

<interfaces>
<!-- Key contracts the executor must preserve. Extracted from PATTERNS.md and direct file reads. -->
<!-- Use these directly — no codebase exploration needed. -->

Bug #1 — engine.cpp:99-103 (current):
    int max_depth = std::min(std::max(depth, 1), 64);
    info.depth = max_depth;       // bug: intended upper bound, gets overwritten
    tt_.new_search();
    iterative_deepening(board_, info, false);

  ...and search.cpp:395-398 (current):
    int max_depth = (info.depth > 0) ? std::min(info.depth, 64) : 64;
    for (int depth = 1; depth <= max_depth && !info.stopped; ++depth) {
        info.depth = depth;       // clobbers the caller's max_depth bound

  Fix per D-03: SearchInfo gains a new field `int max_depth = 0;`. engine.cpp writes info.max_depth = max_depth and info.depth = 0. iterative_deepening reads info.max_depth as the cap and writes info.depth as the per-iteration counter only.

Bug #2 — search.cpp:270-273 (current):
    std::array<Move, 64> killers = {};                  // Simplified (V6 parity) — RESET EVERY CALL
    std::array<std::array<int, 64>, 12> history = {};   // Simplified (V6 parity) — RESET EVERY CALL
    int move_scores[256];
    score_moves(board, moves, tt_move, killers, history, move_scores);

  Fix per D-03: killers move to SearchStack::killers[MAX_PLY][2]; history moves to Engine::history_[2][64][64]; counter_moves moves to Engine::counter_moves_[2][64][64]. score_moves() signature changes to take per-ply killers + engine-owned history (planner's discretion: extend SearchInfo with non-owning pointers `int (*history)[64][64]` and `Move (*counter_moves)[64][64]` mirroring the rep_stack pattern at search.hpp:83-86, or pass directly).

Bug #3 — std::vector<Move> pv allocations in current V7 search.cpp:
  - line 241: std::vector<Move> null_pv;
  - line 317: std::vector<Move> child_pv;
  - line 319/361: pv.clear(); pv.push_back(m); pv.insert(pv.end(), child_pv.begin(), child_pv.end());
  - line 391: std::vector<Move> pv; (iterative_deepening top)

  Fix per D-03 + RESEARCH.md Pattern 2:
    constexpr int MAX_PLY = 128;
    struct SearchStack {
        Move pv[MAX_PLY][MAX_PLY];
        int  pv_length[MAX_PLY];
        Move killers[MAX_PLY][2];
        Move excluded_move[MAX_PLY] = {};   // Plan 03-03 singular extension
    };
    // On new best move at this ply:
    ss.pv[ply][ply] = best_move;
    for (int i = ply + 1; i < ss.pv_length[ply + 1]; ++i)
        ss.pv[ply][i] = ss.pv[ply + 1][i];
    ss.pv_length[ply] = ss.pv_length[ply + 1];

g_tt migration (RESEARCH.md Open Question 1 — pre-D1 mechanical move):
  Current sites in v7/src/search.cpp:
    line 199: g_tt.probe(board.hash, tt_entry)
    line 366: g_tt.store(...)
    line 375: g_tt.store(...)
    line 492: g_tt.new_search()
  Migrate all four to info.tt-> (where info.tt is a new SearchInfo non-owning pointer of type v7::TT*, mirroring info.external_stop at search.hpp:83-86). Wire in engine.cpp Engine::search:
    info.tt = &tt_;                          // NEW
    info.search_stack = &search_stack_;      // NEW
    info.history = &history_;                // NEW (pointer to int[2][64][64])
    info.counter_moves = &counter_moves_;    // NEW
  After migration, delete `extern TT g_tt;` from v7/include/tt.hpp:77. Verify no remaining references with grep.

SearchInfo::reset preservation contract (search.hpp:94 comment style):
  Document that reset() does NOT clobber external_stop, rep_stack, tt, search_stack, history, counter_moves — they are wired by Engine::search per call and must survive the reset.

Engine::new_game additions (engine.cpp:38-52 extension):
  std::memset(history_, 0, sizeof(history_));
  std::memset(counter_moves_, 0, sizeof(counter_moves_));
  // SearchStack pv/pv_length/killers/excluded reset is a no-op
  // (stack-owned, reset by search entry).

Wave 0 test-file stub skeleton (copy from tests/test_v7_search.py:17-71):
  from __future__ import annotations
  import pytest
  from chess_engine.engine.v7 import chess_algorithm as v7

  STARTPOS_FEN = "rnbqkbnr/pppppppp/8/8/8/8/PPPPPPPP/RNBQKBNR w KQkq - 0 1"

  @pytest.fixture(scope="module")
  def v7_native_engine():
      try:
          return v7.ensure_available(auto_build=True)
      except v7.V7UnavailableError as error:
          pytest.skip(f"V7 native engine unavailable: {error}")
      except Exception as error:
          pytest.skip(f"V7 native engine unavailable: {error}")

  @pytest.mark.skip(reason="Plan 03-NN will implement; this is a Wave 0 stub")
  def test_<placeholder>(v7_native_engine):
      pass

Benchmark sentinel skipif decorator (copy from tests/test_v7_engine.py:108-117):
  @pytest.mark.benchmark
  @pytest.mark.skipif(
      os.environ.get("RUN_BENCHMARKS") != "1",
      reason="NPS sentinel; set RUN_BENCHMARKS=1 to execute.",
  )
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Search-substrate refactor — SearchStack + persistent killers/history + info.max_depth + g_tt migration</name>
  <files>
    src/chess_engine/engine/v7/include/search.hpp,
    src/chess_engine/engine/v7/include/engine.hpp,
    src/chess_engine/engine/v7/include/tt.hpp,
    src/chess_engine/engine/v7/src/engine.cpp,
    src/chess_engine/engine/v7/src/search.cpp
  </files>
  <read_first>
    src/chess_engine/engine/v7/include/search.hpp,
    src/chess_engine/engine/v7/include/engine.hpp,
    src/chess_engine/engine/v7/include/tt.hpp,
    src/chess_engine/engine/v7/src/engine.cpp,
    src/chess_engine/engine/v7/src/search.cpp,
    src/chess_engine/engine/v6/src/search.cpp,
    src/chess_engine/engine/v6/include/search.hpp,
    .planning/phases/03-lockless-tt-search-refinements-endgame/03-PATTERNS.md,
    .planning/phases/03-lockless-tt-search-refinements-endgame/03-CONTEXT.md,
    memory/project_phase1_known_perf_bugs.md
  </read_first>
  <behavior>
    - Test 1: After refactor, killers populated at ply N during alpha_beta survive to the next alpha_beta call at the same ply (heuristic actually accumulates — Bug #2 fixed)
    - Test 2: After refactor, history table values at Engine::history_[side][from][to] persist across search() calls and are halved by new_search() (RESEARCH.md A11 aging)
    - Test 3: info.max_depth and info.depth are independent fields; iterative_deepening writes info.depth as iteration counter without touching info.max_depth (Bug #1 fixed)
    - Test 4: No std::vector<Move> declared inside alpha_beta, qsearch, or iterative_deepening (Bug #3 fixed — verifiable via grep)
    - Test 5: No reference to symbol `g_tt` remains anywhere under src/chess_engine/engine/v7/ (g_tt migration complete)
    - Test 6: After Engine::search() with depth=10 on STARTPOS_FEN, the returned best move is legal in the starting position (functional non-regression)
  </behavior>
  <action>
    Refactor the V7 search substrate per CONTEXT D-01 and D-03 in one atomic change so downstream plans extend a single coherent surface.

    Step A — search.hpp additions (extend existing RepStack pattern at lines 41-48 and SearchInfo at lines 75-95):
      1. Add `constexpr int MAX_PLY = 128;` near the existing search constants.
      2. Add `struct SearchStack { Move pv[MAX_PLY][MAX_PLY]; int pv_length[MAX_PLY]; Move killers[MAX_PLY][2]; Move excluded_move[MAX_PLY] = {}; };` — excluded_move is reserved for Plan 03-03 singular extensions but lands here so the stack shape is final.
      3. Extend SearchInfo with five new non-owning pointer fields (mirroring rep_stack at search.hpp:83-86): `int max_depth = 0; TT* tt = nullptr; SearchStack* search_stack = nullptr; int (*history)[64][64] = nullptr; Move (*counter_moves)[64][64] = nullptr;` — exact field types are at planner discretion provided the access shape is `info.tt->probe(...)`, `info.search_stack->pv[ply][i]`, `(*info.history)[side][from][to]`, and `(*info.counter_moves)[side][from][to]`.
      4. Update SearchInfo::reset() body and its comment to enumerate the preserved fields: external_stop, soft_deadline_ms, hard_deadline_ms, rep_stack, max_depth, tt, search_stack, history, counter_moves. ONLY the iteration counters (depth, nodes, stopped, pv-related) are reset.

    Step B — engine.hpp additions (extend members below rep_stack_ at lines 71-81):
      Add `SearchStack search_stack_;`, `int history_[2][64][64] = {};`, `Move counter_moves_[2][64][64] = {};` and a `void age_history();` method declaration.

    Step C — engine.cpp Engine::new_game body extension:
      After existing rep_stack_/tt_ clears, add `std::memset(history_, 0, sizeof(history_));` and `std::memset(counter_moves_, 0, sizeof(counter_moves_));`. Add a comment block matching the V7 commenting style explaining that SearchStack reset is a no-op (stack-owned, reset by search entry) and that `std::memset(0)` on Move arrays is valid because MOVE_NONE == 0.

    Step D — engine.cpp Engine::search bug-1 fix and SearchInfo wiring:
      Replace the current `info.depth = max_depth;` line at engine.cpp:99-103 with two lines: `info.max_depth = max_depth; info.depth = 0;`. Below the existing `info.external_stop = &stop_flag_;` line, wire the four new pointers:
        info.tt = &tt_;
        info.search_stack = &search_stack_;
        info.history = &history_;
        info.counter_moves = &counter_moves_;
      Call `age_history();` before `iterative_deepening(...)` so per-search aging happens once. Implement `Engine::age_history()` to right-shift every history_[s][f][t] by 1 (canonical Stockfish-style decay; RESEARCH.md A11).

    Step E — search.cpp refactor:
      1. Replace every `std::vector<Move>` PV declaration (lines 241, 317, 391; and the .clear/.push_back/.insert at 319/361) with the triangular-PV pattern documented in the <interfaces> block. Drop the `pv` parameter from alpha_beta/qsearch signatures in favor of `info.search_stack->pv[ply]` access.
      2. Delete the per-call `std::array<Move,64> killers = {};` and `std::array<std::array<int,64>,12> history = {};` at search.cpp:270-273. Replace with reads of `info.search_stack->killers[ply]` and `(*info.history)[side][from][to]`. Update score_moves and its callsite signature accordingly; counter_moves is wired but not consumed until Plan 03-02 (score_moves takes the pointer but does not yet score by counter — leave a TODO comment referencing Plan 03-02).
      3. Replace iterative_deepening's `int max_depth = (info.depth > 0) ? std::min(info.depth, 64) : 64;` (search.cpp:395) with `int max_depth = (info.max_depth > 0) ? std::min(info.max_depth, MAX_PLY - 1) : MAX_PLY - 1;`. The loop body retains `info.depth = depth;` (now safe — it's the iteration counter only).
      4. Replace all four g_tt callsites (search.cpp:199, 366, 375, 492) with `info.tt->...` equivalents. Preserve every score_to_tt / score_from_tt wrapper exactly — they remain on the search side per RESEARCH.md PATTERN 8 and PATTERNS.md Shared Pattern 8.

    Step F — tt.hpp cleanup:
      Delete the `extern TT g_tt;` declaration at tt.hpp:77 (and any corresponding `TT g_tt(...)` definition in tt.cpp). Run `grep -rn "g_tt" src/chess_engine/engine/v7/` to confirm zero remaining references before declaring the migration complete.

    Comment every behavior change with the source decision tag (e.g., `// D-03: persistent killers — see 03-CONTEXT.md`).
  </action>
  <verify>
    <automated>python3 -m uv run --group dev pytest tests/test_v7_search.py tests/test_v7_engine.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "g_tt" src/chess_engine/engine/v7/` returns zero matches.
    - `grep -n "std::vector<Move>" src/chess_engine/engine/v7/src/search.cpp` returns zero matches.
    - `grep -n "constexpr int MAX_PLY = 128" src/chess_engine/engine/v7/include/search.hpp` returns exactly one match.
    - `grep -n "SearchStack search_stack_" src/chess_engine/engine/v7/include/engine.hpp` returns exactly one match.
    - `grep -nE "info\.max_depth\s*=" src/chess_engine/engine/v7/src/engine.cpp` returns at least one match; the same grep against search.cpp returns zero matches in iterative_deepening (i.e., info.max_depth is never written from inside the search loop).
    - `python3 -m uv run --group dev pytest tests/test_v7_engine.py tests/test_v7_search.py -q -x` exits 0; the Phase 1 perft and smoke tests continue to pass.
    - Bench NPS gate (advisory at this task — final gate at Task 4): `RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest tests/test_v7_engine.py::test_nps_sentinel -q -x` either passes the existing floor or reports an NPS ≥ 200,000 in its captured output.
  </acceptance_criteria>
  <done>Three Phase 1 perf bugs fixed (info.max_depth split, persistent killers/history on Engine, triangular PV on SearchStack); g_tt fully migrated to info.tt with zero residual references; SearchInfo carries the four new non-owning pointers wired in Engine::search; SearchInfo::reset() preserves them; Phase 1 pytest suite still green.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-Wave-2 scaffold — non_pawn_material helper + MovePicker stub + EngineOptions struct + Engine::set_option dispatcher</name>
  <files>
    src/chess_engine/engine/v7/include/board.hpp,
    src/chess_engine/engine/v7/src/board.cpp,
    src/chess_engine/engine/v7/include/search/options.hpp,
    src/chess_engine/engine/v7/include/search/move_picker.hpp,
    src/chess_engine/engine/v7/src/search/move_picker.cpp,
    src/chess_engine/engine/v7/include/search.hpp,
    src/chess_engine/engine/v7/include/engine.hpp,
    src/chess_engine/engine/v7/src/engine.cpp
  </files>
  <read_first>
    src/chess_engine/engine/v7/include/board.hpp,
    src/chess_engine/engine/v7/src/board.cpp,
    src/chess_engine/engine/v6/include/board.hpp,
    src/chess_engine/engine/v6/src/board.cpp,
    src/chess_engine/engine/v7/include/search.hpp,
    src/chess_engine/engine/v7/include/engine.hpp,
    src/chess_engine/engine/v7/src/engine.cpp,
    .planning/phases/03-lockless-tt-search-refinements-endgame/03-CONTEXT.md,
    .planning/phases/03-lockless-tt-search-refinements-endgame/03-RESEARCH.md
  </read_first>
  <action>
    Land the cross-Wave-2 scaffold so Plans 03-02/03-03/03-04 can run truly in parallel in Wave 2 (no plan introduces shared types or shared search-side helpers that another Wave 2 plan also needs to introduce). All scaffold pieces are declared here and given minimal stubs; Wave 2 plans fill in bodies and consume.

    Step A — board.non_pawn_material helper (consumed by Plan 03-02 null-move zugzwang guard AND Plan 03-04 phase blend).
      1. Grep v6/include/board.hpp and v6/src/board.cpp first; if a helper of this shape already exists in v6, mirror its name and signature verbatim (Shared Pattern 2 — "// V6 parity").
      2. If absent, add to v7/include/board.hpp: `int non_pawn_material(Color c) const;` and implement in v7/src/board.cpp as the sum `popcount(knights(c)) * KNIGHT_VAL + popcount(bishops(c)) * BISHOP_VAL + popcount(rooks(c)) * ROOK_VAL + popcount(queens(c)) * QUEEN_VAL`. Piece values must come from v7::coeffs externs (single source of truth per EVAL-10) — do NOT hardcode 325/325/500/975.
      3. Add comment: `// D-01 + D-03 scaffold; consumed by Plan 03-02 null-move zugzwang guard (RESEARCH.md Pitfall 4) and Plan 03-04 0..256 phase blend (ENDG-04).`

    Step B — struct EngineOptions with all 12 D-06 toggles at canonical defaults.
      1. Create new header src/chess_engine/engine/v7/include/search/options.hpp (NEW directory `include/search/` — `mkdir -p` if it does not exist).
      2. File contents:
         ```
         #pragma once
         namespace v7 {
           // D-06: 12 per-refinement UCI toggles. Default ON for Tier-1 + Tier-2;
           // UseFortressEval defaults OFF per D-11 (separate validation gauntlet decides ship-default).
           struct EngineOptions {
             // Tier-1 (Plan 03-02 consumers)
             bool UseNullMove     = true;
             bool UseLMR          = true;
             bool UseRFP          = true;
             bool UseFutility     = true;
             bool UseLMP          = true;
             bool UseIIR          = true;
             bool UseCheckExt     = true;
             bool UseRecaptureExt = true;
             // Tier-2 (Plan 03-03 consumers)
             bool UseSingular     = true;
             bool UseMultiCut     = true;
             bool UseProbCut      = true;  // may flip to false at Plan 03-03 Task 3 checkpoint
             // Endgame (Plan 03-04 consumer)
             bool UseFortressEval = false;  // D-11
           };
         }
         ```
      3. The exact location is at planner discretion (could equivalently live in engine.hpp); recommendation is the new sibling header for clean ownership and minimal include surface in search.cpp consumers.

    Step C — Engine::set_option dispatcher recognizing all 12 names.
      1. In engine.hpp, add `#include "search/options.hpp"`, add `EngineOptions options_;` member, and declare `void set_option(const std::string& name, const std::string& value);`.
      2. In engine.cpp, implement `Engine::set_option` with case-insensitive name compare and value parse:
         - On unknown name: emit `std::cout << "info string Unknown option: " << name << std::endl;` and return without throwing (matches Phase 2 uci_main convention at uci_main.cpp:247-269).
         - On known name with value matching `true`/`True`/`TRUE` (case-insensitive): write `true` into the corresponding options_ field.
         - On known name with value matching `false`/`False`/`FALSE`: write `false`.
         - On known name with malformed value: emit `std::cout << "info string Invalid value for " << name << ": " << value << std::endl;` and leave options_ unchanged.
         All 12 names must be recognized: UseNullMove, UseLMR, UseRFP, UseFutility, UseLMP, UseIIR, UseCheckExt, UseRecaptureExt, UseSingular, UseMultiCut, UseProbCut, UseFortressEval.
      3. Comment cites D-06.

    Step D — SearchInfo::options pointer + Engine::search wiring.
      1. In search.hpp SearchInfo, add `const v7::EngineOptions* options = nullptr;` adjacent to the four pointers added in Task 1.
      2. In engine.cpp Engine::search, add `info.options = &options_;` alongside the other Task 1 pointer-wiring lines.
      3. Update the SearchInfo::reset() comment to enumerate `options` among the preserved fields.

    Step E — MovePicker class skeleton (consumed by Plan 03-02 Task 2; not used yet by this plan).
      1. Create `include/search/move_picker.hpp` with:
         ```
         #pragma once
         #include "../board.hpp"
         #include "../search.hpp"
         namespace v7 {
           class MovePicker {
            public:
             enum Stage { S_TT, S_GEN_CAPTURES, S_GOOD_CAPTURES, S_KILLERS, S_COUNTER,
                          S_GEN_QUIETS, S_QUIETS, S_BAD_CAPTURES, S_DONE };
             MovePicker(const Board& board, Move tt_move, const SearchInfo& info, int ply);
             Move next(const Board& board);
            private:
             // Plan 03-02 Task 2 fills in private state (stage_, ply_, move buffers, history pointer, etc.)
             Stage stage_ = S_TT;
           };
         }
         ```
      2. Create `src/search/move_picker.cpp` with the ctor body initializing `stage_ = S_TT` and a `Move MovePicker::next(const Board&)` body returning `MOVE_NONE` (stub). Add a header comment: `// Stub created by Plan 03-01 cross-Wave-2 scaffold; Plan 03-02 Task 2 fills in body per RESEARCH.md Code Examples §"Staged Move Picker".`
      3. Wire `src/search/move_picker.cpp` into the V7 CMakeLists.txt V7_SOURCES list (preserve glob if globbing; explicit add if explicit). The stub MUST compile and link — the v7_engine.{pyd,so} build is part of acceptance for this task.

    Comment every scaffold piece with the consuming Wave 2 plan ID so future readers see the contract (e.g., `// Consumed by Plan 03-02 Task 2 — staged move picker body`).
  </action>
  <verify>
    <automated>python3 -m uv run --group dev pytest tests/test_v7_search.py tests/test_v7_engine.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "non_pawn_material" src/chess_engine/engine/v7/include/board.hpp src/chess_engine/engine/v7/src/board.cpp` returns at least 2 matches (declaration + definition).
    - `ls src/chess_engine/engine/v7/include/search/options.hpp src/chess_engine/engine/v7/include/search/move_picker.hpp src/chess_engine/engine/v7/src/search/move_picker.cpp` all succeed.
    - `grep -cE "Use(NullMove|LMR|RFP|Futility|LMP|IIR|CheckExt|RecaptureExt|Singular|MultiCut|ProbCut|FortressEval)" src/chess_engine/engine/v7/include/search/options.hpp` returns exactly 12 (all D-06 toggles declared).
    - `grep -nE "UseFortressEval\s*=\s*false" src/chess_engine/engine/v7/include/search/options.hpp` returns exactly 1 match (D-11 default).
    - `grep -cE "Use(NullMove|LMR|RFP|Futility|LMP|IIR|CheckExt|RecaptureExt|Singular|MultiCut|ProbCut|FortressEval)" src/chess_engine/engine/v7/src/engine.cpp` returns at least 12 (set_option dispatcher recognizes all 12 names).
    - `grep -nE "info\.options\s*=\s*&options_" src/chess_engine/engine/v7/src/engine.cpp` returns exactly 1 match (Engine::search wires the pointer).
    - `grep -n "class MovePicker" src/chess_engine/engine/v7/include/search/move_picker.hpp` returns exactly 1 match.
    - Build host: `chess-engine build v7` succeeds (the new files compile and link into v7_engine.{pyd,so}). Windows dev host may defer the build per Shared Pattern 5; in that case the grep acceptance criteria suffice.
    - Phase 1 pytest suite stays green.
  </acceptance_criteria>
  <done>Cross-Wave-2 scaffold landed: board.non_pawn_material helper, EngineOptions struct with all 12 D-06 toggles (UseFortressEval default false per D-11), Engine::set_option dispatcher recognizing all 12 names, MovePicker class skeleton + stub. Plans 03-02/03-03/03-04 consume this scaffold; none of them re-introduce shared types or shared search-side helpers.</done>
</task>

<task type="auto">
  <name>Task 3: Create 11 Wave 0 test-file stubs (PAR/SRCH/ENDG/TSan-harness skeletons)</name>
  <files>
    tests/test_v7_tt_lockless.py,
    tests/test_v7_search_refinements.py,
    tests/test_v7_move_picker.py,
    tests/test_v7_history.py,
    tests/test_v7_lmr_depth.py,
    tests/test_v7_singular_nps_ratio.py,
    tests/test_v7_kpk_bitbase.py,
    tests/test_v7_endgame.py,
    tests/test_v7_phase_blend.py,
    scripts/tt_tsan_stress.sh,
    src/chess_engine/engine/v7/tt_tsan_stress.cpp
  </files>
  <read_first>
    tests/test_v7_search.py,
    tests/test_v7_engine.py,
    tests/conftest.py,
    .planning/phases/03-lockless-tt-search-refinements-endgame/03-VALIDATION.md,
    .planning/phases/03-lockless-tt-search-refinements-endgame/03-PATTERNS.md
  </read_first>
  <action>
    Create the 11 Wave 0 stubs enumerated in 03-VALIDATION.md so Plans 03-02..05 add behavior tests against existing skeletons (rather than creating files mid-plan, which makes parallel execution conflict-prone).

    Each stub follows the exact skeleton in the <interfaces> block of this plan's context — module-scope v7_native_engine fixture, STARTPOS_FEN constant, and one or more pytest functions decorated `@pytest.mark.skip(reason="Plan 03-NN will implement; this is a Wave 0 stub")`.

    Per-file contents:

    tests/test_v7_tt_lockless.py — PAR-01/02 (Plan 03-05 implements):
      - test_probe_store_roundtrip — skip
      - test_pack_unpack_invariant — skip
      - test_replacement_age_then_depth — skip
      Reference comment cites D-07, D-09, RESEARCH.md Pattern 1.

    tests/test_v7_search_refinements.py — SRCH-03/05/09/11/12 (Plans 03-02/03 implement):
      - test_null_move_skipped_in_kp_endgame (SRCH-03 zugzwang guard) — skip
      - test_rfp_prunes_above_margin (SRCH-05) — skip
      - test_lmp_late_move_pruning (SRCH-05) — skip
      - test_multicut_threshold (SRCH-09) — skip
      - test_iir_reduces_no_tt_move (SRCH-11) — skip
      - test_recapture_extension (SRCH-12) — skip

    tests/test_v7_move_picker.py — SRCH-06 (Plan 03-02 implements):
      - test_picker_yields_tt_first — skip
      - test_picker_good_captures_before_killers — skip
      - test_picker_quiets_history_sorted — skip

    tests/test_v7_history.py — SRCH-07 (Plan 03-03 implements):
      - test_history_accumulates_on_beta_cutoff — skip
      - test_history_aged_on_new_search — skip
      - test_counter_move_indexed_by_side_that_moved (cross-ref RESEARCH.md Pitfall 7 — note in docstring) — skip

    tests/test_v7_lmr_depth.py — SRCH-04 (Plan 03-02 implements):
      Use the benchmark-sentinel skipif skeleton (RUN_BENCHMARKS=1).
      - test_lmr_depth_advantage — skip with both RUN_BENCHMARKS gate AND "Plan 03-02 will implement" reason

    tests/test_v7_singular_nps_ratio.py — SRCH-08 / Success Criterion #4 / Pitfall 5 (Plan 03-03 implements):
      Use the benchmark-sentinel skipif skeleton.
      - test_singular_nps_ratio_within_10_percent — skip with both gates

    tests/test_v7_kpk_bitbase.py — ENDG-01 (Plan 03-04 implements):
      - test_all_positions_match_fathom — skip with "slow; Plan 03-04 will implement" reason
      - test_kpk_cpp_gitignored (mirrors test_v7_eval.py::test_coeffs_cpp_gitignored) — skip

    tests/test_v7_endgame.py — ENDG-02/03/05 (Plan 03-04 implements):
      - test_opposition_white_to_move — skip
      - test_wrong_bishop_rook_pawn_draw — skip
      - test_fortress_detection (default-OFF per D-11) — skip

    tests/test_v7_phase_blend.py — ENDG-04 (Plan 03-04 implements):
      - test_monotonic_phase_across_captures (RESEARCH.md Pitfall 6) — skip

    scripts/tt_tsan_stress.sh — PAR-03 stub (Plan 03-05 implements):
      Create `scripts/` directory if it does not exist. Stub script:
        #!/usr/bin/env bash
        # PAR-03 TSan stress harness for V7 lockless TT.
        # Stub created by Plan 03-01; full implementation in Plan 03-05 per CONTEXT D-08.
        echo "tt_tsan_stress.sh: stub — Plan 03-05 will implement" >&2
        exit 78  # EX_CONFIG — distinguishable from real failures
      chmod a+x the file.

    src/chess_engine/engine/v7/tt_tsan_stress.cpp — Plan 03-05 implements:
      Minimal stub: empty `int main() { return 0; }` with a comment stub block citing CONTEXT D-08 and stating that Plan 03-05 will fill in the 16-thread × 60-second probe/store driver per RESEARCH.md Open Question 4.

    All Python stubs must collect under pytest without ImportError (every import resolvable today; native engine fixture allowed to skip).
  </action>
  <verify>
    <automated>python3 -m uv run --group dev pytest tests/test_v7_tt_lockless.py tests/test_v7_search_refinements.py tests/test_v7_move_picker.py tests/test_v7_history.py tests/test_v7_lmr_depth.py tests/test_v7_singular_nps_ratio.py tests/test_v7_kpk_bitbase.py tests/test_v7_endgame.py tests/test_v7_phase_blend.py --collect-only -q</automated>
  </verify>
  <acceptance_criteria>
    - All 9 Python test files exist under tests/ and `pytest --collect-only -q` against them exits 0 with zero collection errors (all individual tests skipped, none erroring).
    - `ls scripts/tt_tsan_stress.sh` succeeds and the file is executable (`test -x scripts/tt_tsan_stress.sh` returns 0 on POSIX; presence + shebang acceptable on Windows).
    - `ls src/chess_engine/engine/v7/tt_tsan_stress.cpp` succeeds; the file contains the string "int main" and a citation comment referencing CONTEXT D-08.
    - Every Python stub references the future implementing plan ID in its skip reason (grep `Plan 03-` in each file returns ≥1 match).
    - `grep -l "v7_native_engine" tests/test_v7_*.py | wc -l` returns ≥9 (every new stub uses the standard fixture pattern).
  </acceptance_criteria>
  <done>11 Wave 0 stubs created and pytest-collectable; Plans 03-02..05 can land their behavior tests by un-skipping and filling in bodies, with zero new-file conflict risk between parallel plans.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Baseline gauntlet checkpoint — persist baseline-phase3/summary.json (D-02)</name>
  <what-built>
    Plan 03-01 has merged the search-substrate refactor (Task 1), the cross-Wave-2 scaffold (Task 2 — non_pawn_material/EngineOptions/set_option/MovePicker stub), and the 11 Wave 0 test stubs (Task 3). The bench NPS should now be ≥ 200,000 (D-03 sanity floor) on the standard position.

    The next blocking gate is the baseline measurement that Success Criterion #2 compares against: a 500-game V7-vs-V6 gauntlet whose summary.json becomes .planning/gauntlets/baseline-phase3/summary.json — the canonical reference for the Plan 03-06 ship SPRT.
  </what-built>
  <how-to-verify>
    Build host (Linux/macOS/WSL — same host class as Phase 2 deferred gates):
      1. Rebuild V7: `python3 -m uv run --group build python -m chess_engine.engine.v7.native_build` (or `chess-engine build v7`).
      2. Verify bench NPS ≥ 200,000: `RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest tests/test_v7_engine.py::test_nps_sentinel -q -x` — capture the printed NPS number.
      3. Run the 500-game gauntlet (using Phase 2 tools/gauntlet.py): exact command shape per Phase 2 D-08/D-11 (TC=10+0.1, concurrency=1, book=8moves_v3.pgn). Suggested: `python tools/gauntlet.py run --challenger v7 --baseline v6 --games 500 --tc 10+0.1 --book tools/8moves_v3.pgn --concurrency 1 --out .planning/gauntlets/baseline-phase3/`.
      4. Verify the run produced .planning/gauntlets/baseline-phase3/summary.json with the exact fastchess command line embedded (GAUNT-03 invariant).
      5. Commit the new gauntlets/ directory: `git add .planning/gauntlets/baseline-phase3/ && git commit -m "phase(03-01): freeze Phase 3 baseline gauntlet (D-02)"`.

    Windows dev host: SKIP — no build toolchain per .planning/phases/01-skeleton-smoke/.continue-here.md and Shared Pattern 5 in 03-PATTERNS.md. Update Phase 3 .continue-here.md to document the deferred step.

    Report back here with: (a) bench NPS number, (b) baseline gauntlet Elo and 95% CI, (c) summary.json path confirmed committed.
  </how-to-verify>
  <resume-signal>
    Approve to continue to Wave 2 (Plans 03-02, 03-03, 03-04, 03-05 in parallel). If bench NPS < 200,000, do NOT approve — revisit Task 1 for missed refactor.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python ↔ pybind11 boundary | SearchInfo pointers wired in C++ Engine::search; no Python-side mutation during search (GIL released per FOUND-05) |
| Search loop ↔ TT API | info.tt non-owning pointer; lifetime guaranteed by Engine ownership of tt_ member |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering (memory-safety) | Engine::tt_ via SearchInfo::tt pointer | mitigate | Plan 03-05 replaces the underlying TT with Hyatt-Mann lockless; this plan only refactors the access path. No concurrent writes possible until Phase 4 Lazy SMP. Documented in SearchInfo::reset comment that tt pointer is preserved across resets. |
| T-03-02 | DoS (UI thread starvation) | Recursive alpha_beta with new SearchStack stack frame | accept | MAX_PLY = 128 fixed at compile time; stack frame size of SearchStack (~98 KB: 128*128*sizeof(Move) PV + 128*2*sizeof(Move) killers + 128*sizeof(Move) excluded + 128*sizeof(int) pv_length) lives on Engine member, not call stack. Max call-stack depth bounded by MAX_PLY - 1. No DoS surface. |
</threat_model>

<verification>
- Phase 1 pytest suite (tests/test_v7_*.py from Phase 1, excluding the new Wave 0 stubs) continues to pass with zero regressions.
- `grep -rn "g_tt" src/chess_engine/engine/v7/` returns zero matches (migration complete).
- `grep -n "std::vector<Move>" src/chess_engine/engine/v7/src/search.cpp` returns zero matches (Bug #3 fixed).
- Engine::search bench NPS ≥ 200,000 on the standard position (D-03 sanity floor).
- All 11 Wave 0 test files exist and collect cleanly.
- After Task 4 checkpoint: .planning/gauntlets/baseline-phase3/summary.json committed to git.
</verification>

<success_criteria>
- All three Phase 1 perf bugs fixed: info.max_depth split, persistent killers/history on Engine, triangular PV on SearchStack.
- g_tt fully migrated to Engine::tt_ via info.tt with zero residual references.
- 11 Wave 0 test files created and pytest-collectable.
- Bench NPS recovers from ~3k to ≥ 200,000 (≥66× improvement; concrete proof that Plan 03-01's scaffold work landed).
- Baseline gauntlet summary.json persisted as the canonical Phase 3 reference (D-02).
</success_criteria>

<output>
After completion, create `.planning/phases/03-lockless-tt-search-refinements-endgame/03-01-SUMMARY.md` recording: the three bug-fix patches applied (with before/after line references), the g_tt migration sweep result, the bench NPS measured post-refactor, the path of the persisted baseline gauntlet summary.json, and a brief note on which Wave 0 stubs each downstream plan owns (03-02 fills in move_picker body + search_refinements/lmr_depth; 03-03 implements history/singular_nps_ratio + extends EngineOptions defaults if ProbCut bisection requires; 03-04 implements kpk_bitbase/endgame/phase_blend + consumes UseFortressEval; 03-05 implements tt_lockless + tt_tsan_stress). Task 2 scaffold (non_pawn_material, EngineOptions struct with all 12 D-06 toggles, Engine::set_option dispatcher, MovePicker skeleton) lifted from Wave 2 plans into this Wave 1 plan to eliminate cross-plan introduction conflicts.
</output>
