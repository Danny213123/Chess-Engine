---
phase: 02-gauntlet-harness-early
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chess_engine/engine/v6/CMakeLists.txt
  - src/chess_engine/engine/v6/src/uci_main.cpp
  - tests/test_uci_binaries_v6.py
autonomous: true
requirements:
  - GAUNT-02
user_setup: []

must_haves:
  truths:
    - "v6_uci standalone executable target exists in V6 CMakeLists.txt and builds alongside the existing v6_engine pybind11 module"
    - "v6_uci speaks the minimal UCI surface (uci / isready / ucinewgame / position startpos|fen / go depth|movetime|wtime+btime / stop / quit) sufficient for fastchess"
    - "v6_uci accepts setoption name <X> value <Y> silently (Hash setter is accept-and-ignore per A4 — V6 TT is construction-time only)"
    - "Existing v6_engine pybind11 module continues to build (INT-08 from Phase 1 invariant)"
  artifacts:
    - path: "src/chess_engine/engine/v6/CMakeLists.txt"
      provides: "add_executable(v6_uci ...) target additive to v6_engine pybind11 module"
      contains: "v6_uci"
    - path: "src/chess_engine/engine/v6/src/uci_main.cpp"
      provides: "Minimal UCI loop calling v6::search free function (NOT engine class)"
      min_lines: 180
    - path: "tests/test_uci_binaries_v6.py"
      provides: "Subprocess UCI handshake tests for v6_uci"
      contains: "test_v6_uci_smoke"
  key_links:
    - from: "src/chess_engine/engine/v6/src/uci_main.cpp"
      to: "v6::search"
      via: "free-function call v6::search(board, time_ms, false) — NOT engine.search(...)"
      pattern: "v6::search"
    - from: "src/chess_engine/engine/v6/CMakeLists.txt"
      to: "build/v6_uci{,.exe}"
      via: "add_executable target with V6 sources (excluding python_bindings.cpp + bench.cpp)"
      pattern: "add_executable\\(v6_uci"
---

<objective>
Add the missing `v6_uci` standalone executable so fastchess can drive V6 as a subprocess in Phase 2 gauntlets.

Purpose: V6 currently ships only the `v6_engine` pybind11 module (CMakeLists lines 39-58) — fastchess requires a process to exec, not a Python module. This is gap #1 from project_v6_v7_uci_gaps.md and the **blocking** prerequisite for the V6-vs-V6 sanity probe (GAUNT-04) and any future V7-vs-V6 SPRT (D-09). The CMake target mirrors Phase 1's `v7_uci` block; the uci_main.cpp mirrors V7's uci_main.cpp loop with one critical adaptation — V6 exposes `v6::search(board, time_ms, verbose)` as a free function, not an `Engine` class (verified in v6/src/python_bindings.cpp line 36).

Output: a new `v6_uci` executable, a new uci_main.cpp under V6, and subprocess unit tests proving the UCI handshake works.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gauntlet-harness-early/02-CONTEXT.md
@.planning/phases/02-gauntlet-harness-early/02-RESEARCH.md
@.planning/phases/02-gauntlet-harness-early/02-PATTERNS.md
@src/chess_engine/engine/v6/CMakeLists.txt
@src/chess_engine/engine/v6/src/python_bindings.cpp
@src/chess_engine/engine/v6/include/search.hpp
@src/chess_engine/engine/v6/include/board.hpp
@src/chess_engine/engine/v6/include/types.hpp
@src/chess_engine/engine/v7/src/uci_main.cpp
@src/chess_engine/engine/v7/CMakeLists.txt
@tests/test_v7_bindings.py
</context>

<interfaces>
<!-- Critical: PATTERNS §"src/chess_engine/engine/v6/src/uci_main.cpp (NEW)" warns that V6 differs from V7 in two ways executors must NOT miss: -->
<!-- 1. V6 has NO engine.hpp / Engine class — `v6::search(board, time_ms, verbose)` is a free function (per v6/src/python_bindings.cpp line 36). -->
<!-- 2. V6 has a global TT singleton at `v6::TT` per v6/include/tt.hpp:75 — `ucinewgame` calls `v6::TT.clear()` instead of V7's `engine.new_game()`. -->
<!-- 3. V6 sources to include in v6_uci: board.cpp eval.cpp magic.cpp movegen.cpp search.cpp tt.cpp — EXCLUDE python_bindings.cpp (pybind11 symbols won't link) and bench.cpp (has its own main()). perft.cpp may be includable if no main() — verify before adding. -->
<!-- 4. The existing pybind11_add_module(v6_engine ...) block (V6 CMakeLists lines 39-58) MUST stay untouched — additive target discipline per PATTERNS §"Shared Patterns → CMake additive-target discipline" preserves INT-08. -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add v6_uci CMake target additive to the v6_engine pybind11 module</name>
  <files>src/chess_engine/engine/v6/CMakeLists.txt</files>
  <read_first>
    - src/chess_engine/engine/v6/CMakeLists.txt (FULL — lines 39-58 are the existing pybind11_add_module(v6_engine ...) block which MUST NOT be modified)
    - src/chess_engine/engine/v7/CMakeLists.txt lines 105-130 (the canonical add_executable(v7_uci ...) block PATTERNS says to copy verbatim with substitutions)
    - .planning/phases/02-gauntlet-harness-early/02-PATTERNS.md §"src/chess_engine/engine/v6/CMakeLists.txt (MOD)" (exact source list substitution — V6 has no engine.cpp/perft.cpp/syzygy.cpp/coeffs.cpp)
    - src/chess_engine/engine/v6/src/ directory listing (verify which .cpp files actually exist — board.cpp eval.cpp magic.cpp movegen.cpp search.cpp tt.cpp expected; python_bindings.cpp + bench.cpp must be excluded)
  </read_first>
  <action>
    Append a new `add_executable(v6_uci ...)` block AFTER the existing pybind11_add_module block (do not reorder or modify pre-existing lines — additive only, per PATTERNS §"CMake additive-target discipline" which preserves Phase 1 INT-08).

    Use PATTERNS §"src/chess_engine/engine/v6/CMakeLists.txt (MOD)" as the literal template. Source list MUST be the intersection of (what V6 has) and (what v6_uci.cpp will reference):
      - src/uci_main.cpp (created in Task 2 below)
      - src/board.cpp
      - src/eval.cpp
      - src/magic.cpp
      - src/movegen.cpp
      - src/search.cpp
      - src/tt.cpp

    EXCLUDE src/python_bindings.cpp (pybind11 symbols would fail to link without the Python interpreter), bench.cpp (has its own main()), and perft.cpp (verify whether it defines main() before deciding — if it does, exclude; if it's a free function the executor MAY include it but must confirm). Mirror V7's `target_include_directories(v6_uci PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/include)` and the `if (OpenMP_CXX_FOUND) target_link_libraries(v6_uci PRIVATE OpenMP::OpenMP_CXX) endif()` block. Use the same C++17 standard the existing v6_engine target uses (verify via the existing CMakeLists; do not regress).

    Add a header comment block above the new target mirroring V7's style, citing Phase 2 GAUNT-02 and noting that this satisfies project_v6_v7_uci_gaps.md gap #1.

    Do NOT modify the existing v6_engine target. Do NOT alter the project()/cmake_minimum_required directives. Do NOT add new global compile flags — scope all flags to the new target.
  </action>
  <verify>
    <automated>cmake -S src/chess_engine/engine/v6 -B src/chess_engine/engine/v6/build && cmake --build src/chess_engine/engine/v6/build --target v6_uci 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "add_executable(v6_uci" src/chess_engine/engine/v6/CMakeLists.txt` returns 1
    - The pre-existing `pybind11_add_module(v6_engine` line is unchanged (`git diff` shows no edits in lines 39-58)
    - `cmake --build .../v6/build --target v6_uci` completes with exit 0
    - The binary src/chess_engine/engine/v6/build/v6_uci{,.exe} (or under build/Release on multi-config generators) exists after build
    - The existing v6_engine target still builds (`cmake --build src/chess_engine/engine/v6/build --target v6_engine` exit 0) — INT-08 invariant
  </acceptance_criteria>
  <done>v6_uci executable target is declared, builds, and the v6_engine pybind11 module still builds independently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write src/chess_engine/engine/v6/src/uci_main.cpp modeled on v7_uci with free-function v6::search</name>
  <files>src/chess_engine/engine/v6/src/uci_main.cpp</files>
  <read_first>
    - src/chess_engine/engine/v7/src/uci_main.cpp (FULL — 213-line canonical loop; copy verbatim with the adaptations below)
    - .planning/phases/02-gauntlet-harness-early/02-PATTERNS.md §"src/chess_engine/engine/v6/src/uci_main.cpp (NEW)" (the four critical V6 vs V7 differences listed in <interfaces> above are spelled out here in detail with line refs)
    - src/chess_engine/engine/v6/src/python_bindings.cpp lines 22-41 (canonical v6::search call site: `v6::search(board, time_limit_ms, true)` — free function returning v6::SearchResult with .best_move, .score, .depth, .nodes, .nps())
    - src/chess_engine/engine/v6/include/board.hpp (verify Board::from_fen / to_fen / make_move and the side-to-move accessor; do NOT assume names match V7)
    - src/chess_engine/engine/v6/include/types.hpp (verify v6::Move, v6::Square, v6::PROMOTION naming — used in apply_uci_move)
    - src/chess_engine/engine/v6/include/movegen.hpp (verify v6::generate_legal_moves signature and v6::MoveList shape)
    - src/chess_engine/engine/v6/include/tt.hpp (line ~75 — confirm v6::TT global singleton and the clear() method; this is what `ucinewgame` calls in lieu of V7's engine.new_game())
    - src/chess_engine/engine/v6/include/search.hpp (verify the v6::search signature `(Board&, int time_ms, bool verbose) -> SearchResult` and check for any cancellation primitive; if absent, `stop` is a no-op comment)
    - src/chess_engine/engine/v6/include/magic.hpp (V6's init_magics() — must be called once at program start; V7 does this inside Engine ctor, V6 does it lazily via ensure_init() in python_bindings.cpp — replicate that here)
  </read_first>
  <behavior>
    - `printf 'uci\nquit\n' | ./v6_uci` outputs a line containing `id name V6` (or similar identifier) AND outputs `uciok` and exits 0
    - `printf 'uci\nisready\nquit\n' | ./v6_uci` outputs `readyok` between `uciok` and exit
    - `printf 'position startpos\ngo movetime 200\nquit\n' | ./v6_uci` outputs a `bestmove <move>` line where `<move>` is a legal UCI move from the starting position (e.g., e2e4, g1f3) within 1 s wall clock
    - `printf 'position startpos moves e2e4 e7e5\ngo movetime 200\nquit\n' | ./v6_uci` outputs `bestmove` for a legal Black-side move
    - `printf 'setoption name Hash value 64\nisready\nquit\n' | ./v6_uci` outputs `readyok` with NO `Unknown command` or error in any stream (A4: V6 Hash setter is accept-and-ignore — V6 TT is construction-time only)
    - `printf 'go wtime 1000 btime 1000 winc 0 binc 0\n' | ./v6_uci` returns a bestmove within 1.5 s (proves wtime parsing wins over any fallback)
    - `printf 'ucinewgame\nposition startpos\ngo depth 3\nquit\n' | ./v6_uci` returns a bestmove (depth-fixed back-compat)
  </behavior>
  <action>
    Create src/chess_engine/engine/v6/src/uci_main.cpp by copying src/chess_engine/engine/v7/src/uci_main.cpp verbatim then applying these surgical adaptations per PATTERNS §"src/chess_engine/engine/v6/src/uci_main.cpp (NEW)":

    1. Substitute every `v7::` namespace prefix with `v6::` throughout the file (board, MoveList, Move, Square, PROMOTION, KNIGHT/BISHOP/ROOK/QUEEN, move_from, move_to, move_type, promo_piece, move_to_string, string_to_square, generate_legal_moves, NO_SQUARE).

    2. Replace the V7 include `#include "engine.hpp"` with `#include "search.hpp"` (V6 has NO engine.hpp — verified in PATTERNS). Keep board.hpp, movegen.hpp, types.hpp includes. Add `#include "magic.hpp"` and `#include "tt.hpp"` so the new branches can call init_magics()/TT.clear().

    3. Replace V7's `v7::Engine engine;` instantiation (near the top of main()) with a one-shot call to `v6::init_magics();` (mirroring v6/python_bindings.cpp::ensure_init()). There is no V6 Engine class to instantiate.

    4. In the `go` branch, replace `v7::SearchResult r = engine.search(fen, depth, time_ms);` with the V6 free-function form `v6::SearchResult r = v6::search(board, time_ms, false);` (note: depth is NOT an arg in V6's signature per v6/python_bindings.cpp:36 — V6 uses time-only iterative deepening; document this with a one-line comment that depth-fixed mode falls back to UCI_MAX_TIME_MS to keep depth as a soft signal). Pass `board` directly — V6's search takes `Board&`, not a FEN string. Use r.best_move / move_to_string for the output.

    5. Replace V7's `engine.new_game();` (in the ucinewgame branch) with `v6::TT.clear();` (V6's global TT singleton — confirm the symbol via tt.hpp; if it's named differently, use the actual identifier).

    6. Replace V7's `engine.stop();` (in the stop branch) with a no-op + a code comment citing "V6 has no in-search cancellation primitive — stop is best-effort per PATTERNS §critical-finding. Phase 4 may add an atomic flag." Do NOT introduce a new V6 cancellation API in this plan.

    7. Apply BOTH extensions from Plan 01 Task 1 to this V6 loop: the `setoption` branch (silent accept) AND the `wtime`/`btime`/`winc`/`binc` parsing in the `go` branch with the same `(my_time / 30) + my_inc` budget heuristic. The setoption branch in V6 MUST silently accept `Hash` even though V6 has no resize API (A4 — document the asymmetry with a code comment citing RESEARCH §10 Q4). V6's side-to-move accessor may have a different name than V7's; use whatever Board exposes.

    8. Change the `id name` line in the `uci` branch from `id name V7 ...` to `id name V6` (and `id author` to match V6's pre-existing attribution if any; otherwise mirror V7's author line).

    Preserve the rest of the V7 loop structure verbatim: tokenize() helper, apply_uci_move() helper, set_startpos() helper, the position-parser token walk, ios_base buffering disable, the main while-loop reading std::getline. The file should be ~210 lines after substitutions.

    Do NOT add V7-specific features V6 doesn't have (no Syzygy, no coeffs.json). Do NOT touch any other V6 source file. Do NOT modify python_bindings.cpp.
  </action>
  <verify>
    <automated>cmake --build src/chess_engine/engine/v6/build --target v6_uci 2>&1 | tail -20 && printf 'uci\nquit\n' | src/chess_engine/engine/v6/build/v6_uci | head -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists: src/chess_engine/engine/v6/src/uci_main.cpp
    - File compiles into the v6_uci target with zero warnings
    - `grep -cE "^#include" src/chess_engine/engine/v6/src/uci_main.cpp` shows includes for board.hpp, movegen.hpp, search.hpp, types.hpp, magic.hpp, tt.hpp (six total V6 headers — NOT engine.hpp)
    - `grep -v '^[[:space:]]*//' src/chess_engine/engine/v6/src/uci_main.cpp | grep -c "v6::search(board" >= 1` — confirms free-function call form
    - `grep -v '^[[:space:]]*//' src/chess_engine/engine/v6/src/uci_main.cpp | grep -c "setoption" >= 1` — setoption branch present
    - `grep -v '^[[:space:]]*//' src/chess_engine/engine/v6/src/uci_main.cpp | grep -c "wtime" >= 1` — wtime parsing present
    - `printf 'uci\nquit\n' | ./v6_uci` exits 0 and stdout contains "uciok"
    - `printf 'position startpos\ngo movetime 200\nquit\n' | ./v6_uci` exits 0 within 2 s and stdout contains "bestmove "
  </acceptance_criteria>
  <done>v6_uci binary builds and answers a complete UCI handshake (uci/isready/setoption/position/go movetime/wtime+btime/quit) without errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Subprocess tests for v6_uci handshake, setoption, wtime/btime, and movetime back-compat</name>
  <files>tests/test_uci_binaries_v6.py</files>
  <read_first>
    - tests/test_v7_bindings.py lines 163-201 (`_find_v7_uci_binary` and `test_uci_binary_exists` patterns — the canonical skip-on-missing-binary subprocess shape)
    - tests/test_uci_binaries.py (created in Plan 01 Task 2 — use the same helper / fixture style for consistency; this V6 file mirrors that V7 file)
    - .planning/phases/02-gauntlet-harness-early/02-PATTERNS.md §"tests/test_uci_binaries.py (NEW; RESEARCH §9 GAUNT-02 coverage)" (three test cases listed: setoption, smoke/position-go, wtime/btime)
    - src/chess_engine/engine/v6/src/uci_main.cpp (Task 2 output — read post-edit to confirm assertions match the actual `id name` line)
  </read_first>
  <behavior>
    - test_v6_uci_smoke: subprocess receives `uci\nposition startpos\ngo movetime 200\nquit\n` and stdout contains `bestmove ` followed by a 4-or-5-char UCI move within a 5 s timeout
    - test_v6_uci_id_name: subprocess receives `uci\nquit\n` and stdout contains `id name V6` and `uciok`
    - test_v6_uci_setoption_silent: subprocess receives `uci\nsetoption name Hash value 64\nisready\nquit\n` and stdout contains `readyok`; combined stdout+stderr contains no "Unknown" (case-insensitive) and stderr contains no "error" (case-insensitive)
    - test_v6_uci_threads_setoption: same shape with `Threads value 1`
    - test_v6_uci_unknown_setoption: same shape with `setoption name FooBar value 1` — silent accept
    - test_v6_uci_wtime_btime: subprocess receives `position startpos\ngo wtime 1000 btime 1000 winc 0 binc 0\n` and returns a bestmove within 1.5 s (wtime parsing — not 5000ms fallback)
    - test_v6_uci_movetime_backcompat: `position startpos\ngo movetime 200\n` returns a bestmove (movetime path still works)
    - test_v6_engine_module_still_loads: import chess_engine.engine.v6 succeeds (INT-08 invariant — pybind11 module not broken by the CMake addition)
  </behavior>
  <action>
    Create tests/test_uci_binaries_v6.py mirroring tests/test_uci_binaries.py (created in Plan 01 Task 2). Per GAUNT-02 / RESEARCH §9 / PATTERNS:

    - Define `_find_v6_uci_binary()` as a near-copy of `_find_v7_uci_binary()` from tests/test_v7_bindings.py, swapping V7_DIR for `Path(__file__).parents[1] / "src/chess_engine/engine/v6"` and BUILD_DIR accordingly. Candidate names: ("v6_uci.exe", "v6_uci"); candidate dirs: Release / RelWithDebInfo / Debug / build root / engine dir.
    - Reuse the same `_run_uci(binary, script, timeout=5.0)` helper shape from Plan 01 Task 2 (copy or extract into a small helper at the top of this file — do NOT create a conftest.py in this plan; that's Plan 05's job).
    - Implement all eight test functions from `<behavior>`. Each subprocess test skips with `pytest.skip("v6_uci binary not found — CMake may not have produced it on this platform")` when `_find_v6_uci_binary()` returns None.
    - `test_v6_engine_module_still_loads` does NOT need v6_uci — it loads the pybind11 module to prove INT-08 from Phase 1 is preserved. Mirror the pattern in tests/test_v6_native.py (module-scope fixture). If the import fails, this test should FAIL (not skip) — a regression in v6_engine is a hard error.
    - For the wtime/movetime timing assertions: wrap subprocess.run in time.perf_counter() and assert wall-clock under 1.5 s.
    - For "no error printed" assertions: case-insensitive substring check across stdout+stderr — same predicate as the V7 tests.
    - Test runtime budget: < 15 s total (each subprocess capped at 5 s timeout, 8 tests, most fast paths).
  </action>
  <verify>
    <automated>python3 -m uv run --group dev pytest -q tests/test_uci_binaries_v6.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: tests/test_uci_binaries_v6.py
    - `grep -c "def test_" tests/test_uci_binaries_v6.py` returns 8
    - All 8 tests PASS on a host where v6_uci is built; subprocess-dependent tests skip cleanly when the binary is absent; `test_v6_engine_module_still_loads` PASSes (must NOT skip — INT-08 hard invariant)
    - `python3 -m uv run --group dev pytest -q` (full suite) shows no regression in any existing v6 test
    - Test runtime ≤ 15 s for this file alone
  </acceptance_criteria>
  <done>tests/test_uci_binaries_v6.py PASSes (or skips subprocess tests gracefully when v6_uci is unbuilt). v6_engine pybind11 module continues to import — INT-08 preserved.</done>
</task>

</tasks>

<verification>
- v6_uci builds without breaking v6_engine pybind11 module (INT-08 invariant)
- 8 V6 subprocess tests pass on a built host; skip when binary absent
- Full pytest suite still green (`python3 -m uv run --group dev pytest -q`)
</verification>

<success_criteria>
- src/chess_engine/engine/v6/build/v6_uci (or v6_uci.exe) exists and answers `uci\nquit\n` with `uciok`
- v6_uci accepts setoption silently and parses wtime/btime correctly
- v6_engine pybind11 module continues to build and import (Phase 1 INT-08 invariant preserved)
</success_criteria>

<output>
After completion, create `.planning/phases/02-gauntlet-harness-early/02-02-SUMMARY.md` recording: V6 sources included in v6_uci, deviations from V7's uci_main.cpp (free-function vs Engine class + TT clear instead of new_game + no-op stop), test pass/skip counts, and confirmation of INT-08 invariant.
</output>
