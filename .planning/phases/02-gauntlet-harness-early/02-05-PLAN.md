---
phase: 02-gauntlet-harness-early
plan: 05
type: execute
wave: 3
depends_on: ["02-04"]
files_modified:
  - tests/test_nps_regression.py
  - tests/conftest.py
  - tests/test_gauntlet_sanity.py
autonomous: false
requirements:
  - GAUNT-04
  - GAUNT-08
user_setup: []

must_haves:
  truths:
    - "RUN_BENCHMARKS=1 pytest tests/test_nps_regression.py reads the latest .planning/gauntlets/<ISO>/summary.json and PASSes when summary.nps.v7.bench_nps >= 0.8 * summary.nps.v6.bench_nps (D-13, GAUNT-08)"
    - "RUN_BENCHMARKS=1 pytest tests/test_nps_regression.py FAILs with a clear message when the ratio drops below 0.8"
    - "Default pytest (without RUN_BENCHMARKS=1) SKIPs the sentinel cleanly — no false failures in normal CI"
    - "The sentinel reads the LATEST summary.json by lexical-sort over ISO timestamps under .planning/gauntlets/ (deterministic discovery)"
    - "When no .planning/gauntlets/<ISO>/summary.json exists, the sentinel skips with a clear 'no gauntlet runs found — run tools/gauntlet.py sanity first' message"
    - "tests/conftest.py provides a shared synthetic-summary.json factory + tmp .planning/gauntlets/ root fixture so test_nps_regression.py and test_gauntlet_sanity.py can be exercised offline"
    - "A human-verify checkpoint runs the actual V6-vs-V6 sanity probe (GAUNT-04) at the end of the phase — confirms the harness ACTUALLY produces an Elo within ±15 over 200 games"
  artifacts:
    - path: "tests/test_nps_regression.py"
      provides: "Benchmark-gated pytest sentinel that extends Phase 1 test_nps_sentinel and reads summary.json"
      contains: "test_nps_regression"
    - path: "tests/conftest.py"
      provides: "Shared fixtures: gauntlet_root (tmp .planning/gauntlets/), summary_json_factory (synthesizes a summary.json with configurable bench_nps), _find_uci_binary helper"
      contains: "summary_json_factory"
    - path: "tests/test_gauntlet_sanity.py"
      provides: "Integration tests for the latest-summary-discovery + ratio assertions + skip-on-empty-gauntlets-dir + the V6-vs-V6 manual instructions"
      contains: "test_latest_summary"
  key_links:
    - from: "tests/test_nps_regression.py"
      to: ".planning/gauntlets/<ISO>/summary.json"
      via: "sorted(OUTPUT_ROOT.iterdir())[-1] / 'summary.json' — lexical sort over ISO names"
      pattern: "summary.json"
    - from: "tests/test_nps_regression.py"
      to: "Phase 1 test_nps_sentinel"
      via: "extends the same benchmark gate + assertion floor; substitutes summary.json source for inline find_best_move call"
      pattern: "RUN_BENCHMARKS"
---

<objective>
Close the validation loop: implement the NPS regression sentinel (D-13 / GAUNT-08) and the V6-vs-V6 sanity probe wiring (D-10 / GAUNT-04) that together give Phase 2 its "complete" signal per D-09.

Purpose: GAUNT-08's "build fails if V7 NPS drops more than 20% vs V6" is operationalized as a pytest sentinel that reads the latest summary.json's bench_nps fields (populated by Plan 04's gauntlet.py). GAUNT-04's "V6-vs-V6 returns approximately 0 Elo" is operationalized as `tools/gauntlet.py sanity --games 200` producing a summary.json with |elo| <= 15 — Plan 04 already implements the verdict computation; this plan's checkpoint requests the actual execution. Together these two artifacts close the phase: when both pass, the harness has demonstrated it can produce trustworthy measurements.

Output: tests/test_nps_regression.py + tests/conftest.py + tests/test_gauntlet_sanity.py + a `checkpoint:human-verify` task that runs the actual ~30-60 min V6-vs-V6 probe on a built host.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gauntlet-harness-early/02-CONTEXT.md
@.planning/phases/02-gauntlet-harness-early/02-RESEARCH.md
@.planning/phases/02-gauntlet-harness-early/02-PATTERNS.md
@.planning/phases/02-gauntlet-harness-early/02-04-SUMMARY.md
@tools/gauntlet.py
@tests/test_v7_engine.py
@tests/test_v7_bindings.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/conftest.py — shared fixtures (gauntlet_root, summary_json_factory, _find_uci_binary helper)</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (if it exists, read in full — DO NOT clobber existing fixtures; append rather than overwrite. If it does not exist, create from scratch.)
    - tests/test_v7_bindings.py lines 163-184 (the _find_v7_uci_binary helper — generalize to _find_uci_binary(engine: str) supporting both v6 and v7 per PATTERNS module-scoped fixture analog)
    - tests/test_v7_bindings.py lines 40-51 (module-scoped v7_native_engine fixture — model the shared fixture scope here)
    - tools/gauntlet.py (Plan 04 output — the summary.json schema this factory must mirror)
    - .planning/phases/02-gauntlet-harness-early/02-PATTERNS.md sections "Shared Patterns → Module-scoped native-engine fixture" and "tests/test_nps_regression.py"
    - .planning/phases/02-gauntlet-harness-early/02-VALIDATION.md Wave-0 list (conftest.py fixture listed as a deliverable)
  </read_first>
  <action>
    Two cases:

    Case A — tests/conftest.py exists: READ the file fully, identify existing fixtures, and APPEND the new fixtures listed below without renaming or shadowing any existing fixture. If a fixture name collides, suffix with `_gauntlet` (e.g., `gauntlet_root_gauntlet` if `gauntlet_root` already exists from another phase).

    Case B — tests/conftest.py does NOT exist: create it from scratch with the imports + fixtures listed below.

    Fixtures to provide (per PATTERNS + RESEARCH §9):

    1. _find_uci_binary(engine: str) -> Path | None — generalized binary discovery for both v6 and v7. Move the candidate-dir + candidate-name pattern from tests/test_v7_bindings.py here so test_uci_binaries.py (Plan 01), test_uci_binaries_v6.py (Plan 02), and test_gauntlet_sanity.py (this plan Task 3) all share one helper. Signature: engine in ("v6", "v7"); for each, candidate_dirs = (engine_dir / "build" / "Release", .../RelWithDebInfo, .../Debug, engine_dir / "build", engine_dir); candidate_names = (f"{engine}_uci.exe", f"{engine}_uci"). Return absolute Path or None.

    2. gauntlet_root(tmp_path) — pytest fixture returning a tmp_path subdirectory simulating .planning/gauntlets/. Used by every test that needs to read or write summary.json files without touching the real .planning/gauntlets/ tree. Scope: function (default).

    3. summary_json_factory(gauntlet_root) — pytest fixture returning a callable. The callable signature: `make(timestamp="2026-05-16T21-00-00Z", v7_bench_nps=500_000, v6_bench_nps=600_000, verdict="H1", elo=13.87, **overrides) -> Path`. Builds the full summary.json on the Plan 04 schema and writes it under `gauntlet_root / timestamp / "summary.json"`. Returns the path to the written file. Overrides shallow-merge into the dict so individual tests can tweak any field.

    4. (Optional helper) latest_summary_dir(gauntlet_root) — utility that returns `sorted(gauntlet_root.iterdir())[-1]` (lexical sort over ISO names — D-13's discovery rule).

    Use pytest @pytest.fixture decorators. Provide concise docstrings citing D-13 / GAUNT-08 / RESEARCH §9. Do NOT introduce session-scope fixtures (per PATTERNS Shared analog: V6/V7 native engine fixtures use module scope, but these gauntlet fixtures should be function-scoped because each test needs an isolated gauntlet_root).

    Imports needed: json, os, platform, pytest, pathlib (Path). Stdlib + pytest only.
  </action>
  <verify>
    <automated>python3 -m uv run --group dev pytest -q --collect-only tests/conftest.py 2>&1 | tail -10 && python3 -c "import ast, sys; tree = ast.parse(open('tests/conftest.py').read()); fixtures = [n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef) and any((isinstance(d, ast.Attribute) and d.attr == 'fixture') or (isinstance(d, ast.Name) and d.id == 'fixture') or (isinstance(d, ast.Call) and ((isinstance(d.func, ast.Attribute) and d.func.attr == 'fixture') or (isinstance(d.func, ast.Name) and d.func.id == 'fixture'))) for d in n.decorator_list)]; print('fixtures:', fixtures); sys.exit(0 if ('gauntlet_root' in fixtures and 'summary_json_factory' in fixtures) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py exists; if pre-existing, NO pre-existing fixture name is shadowed or renamed
    - The three new fixtures (`gauntlet_root`, `summary_json_factory`, `_find_uci_binary` helper) are defined
    - `_find_uci_binary("v7")` and `_find_uci_binary("v6")` both work (return Path or None — never raise)
    - `summary_json_factory()` produces a JSON file whose top-level keys match the Plan 04 schema (schema_version, timestamp_utc, engines, tc, concurrency, openings, sprt, fastchess_command, fastchess_version, host, result, nps)
    - `python3 -m uv run --group dev pytest -q --collect-only tests/` exits 0 (conftest loads cleanly across the whole test suite)
    - All pre-existing tests still PASS (no regression from the conftest addition)
  </acceptance_criteria>
  <done>tests/conftest.py provides the three shared fixtures; the full pytest suite still collects and runs cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_nps_regression.py + tests/test_gauntlet_sanity.py — sentinel + latest-summary discovery + ratio assertions</name>
  <files>tests/test_nps_regression.py, tests/test_gauntlet_sanity.py</files>
  <read_first>
    - tests/test_v7_engine.py lines 108-172 (CANONICAL test_nps_sentinel — copy the @pytest.mark.benchmark + @pytest.mark.skipif(RUN_BENCHMARKS != "1") decorator block VERBATIM per PATTERNS section "tests/test_nps_regression.py")
    - tests/conftest.py (Task 1 output — the fixtures this test file consumes)
    - tools/gauntlet.py (Plan 04 output — verify summary.json key names; the test reads summary.nps.v7.bench_nps and summary.nps.v6.bench_nps per D-13 / RESEARCH §8 Option B)
    - .planning/phases/02-gauntlet-harness-early/02-PATTERNS.md section "tests/test_nps_regression.py" (EXACT extension of Phase 1 sentinel — copy decorator block verbatim, copy assertion floor verbatim, swap inline find_best_move call for summary.json read)
    - .planning/phases/02-gauntlet-harness-early/02-RESEARCH.md §9 Phase-Requirements-to-Test-Map rows for GAUNT-04 (V6-vs-V6 sanity over 200 games yields Elo in [-15, +15]; tagged manual / slow / RUN_SLOW_GAUNTLET) and GAUNT-08 (the two rows for the sentinel + latest-summary discovery)
    - .planning/phases/02-gauntlet-harness-early/02-RESEARCH.md §9 "Validation guardrails" (NEVER run the slow sanity in default CI; gate behind RUN_SLOW_GAUNTLET=1 or `-m slow` marker)
  </read_first>
  <behavior>
    tests/test_nps_regression.py:
    - test_nps_regression_passes_at_ratio_above_floor: uses summary_json_factory(v7_bench_nps=500_000, v6_bench_nps=600_000) → ratio 0.833 → test PASSes (above 0.8 floor)
    - test_nps_regression_fails_at_ratio_below_floor: uses summary_json_factory(v7_bench_nps=400_000, v6_bench_nps=600_000) → ratio 0.667 → test_nps_regression FAILs with a clear AssertionError mentioning the floor (0.8) and the actual ratio
    - test_nps_regression_skips_without_run_benchmarks: when RUN_BENCHMARKS != "1", the test SKIPs cleanly (does not run, does not fail)
    - test_nps_regression_skips_on_empty_gauntlets_dir: when no summary.json exists under .planning/gauntlets/, the test SKIPs with a clear message "no gauntlet runs found — run tools/gauntlet.py sanity first"
    - test_nps_regression_reads_latest_summary: write three summary.json files at three different timestamps; the test reads from the LATEST (lexically-last ISO timestamp) — assert by setting different bench_nps values per timestamp and verifying which one the assertion sees

    tests/test_gauntlet_sanity.py:
    - test_latest_summary_discovery: write 3 summary.json files at different timestamps, assert latest_summary_dir() returns the lexically-greatest name
    - test_sanity_verdict_pass_when_elo_within_tolerance: build a synthetic sanity-mode summary with elo=5.0; assert result.verdict == "PASS" (D-10 tolerance ±15)
    - test_sanity_verdict_fail_when_elo_exceeds_tolerance: synthetic summary with elo=20.0; assert verdict == "FAIL"
    - test_sanity_verdict_exact_boundary: synthetic summary with elo=15.0; assert verdict == "PASS" (boundary inclusive)
    - test_sanity_summary_omits_sprt: a sanity-mode summary.json has top-level "sprt" == null (per Plan 04 write_summary contract)
  </behavior>
  <action>
    Two files. Both consume the conftest.py fixtures from Task 1.

    A. tests/test_nps_regression.py — the GAUNT-08 sentinel per D-13. Copy the benchmark decorator + RUN_BENCHMARKS skipif block VERBATIM from tests/test_v7_engine.py lines 108-117. Apply to a single test_nps_regression() function that:
      1. Discovers the latest summary.json under .planning/gauntlets/ (use the latest_summary_dir helper from conftest.py or replicate the sort logic; per D-13 read the REAL .planning/gauntlets/ not the tmp gauntlet_root — production sentinel reads production artifacts).
      2. If no summary.json exists → pytest.skip("no gauntlet runs found — run tools/gauntlet.py sanity first").
      3. Loads JSON, reads `summary["nps"]["v7"]["bench_nps"]` and `summary["nps"]["v6"]["bench_nps"]`.
      4. Computes `ratio = v7_nps / v6_nps`; asserts `ratio >= 0.8` with the assertion message copied VERBATIM from PATTERNS (Phase 1 sentinel line 168-172 wording: "V7 NPS {v7_nps:.0f} / V6 NPS {v6_nps:.0f} ratio {ratio:.2f} below 0.8 floor (CLAUDE.md ~20% NPS constraint). Re-run on a quiet host before treating as a regression.").

    For the unit-test entries (test_nps_regression_passes_at_ratio_above_floor and friends), parameterize / monkeypatch the discovery function so the test reads from gauntlet_root (the conftest fixture) rather than the production .planning/gauntlets/. The cleanest path: split the sentinel into (a) a `_load_latest_summary(root=PRODUCTION_ROOT)` helper at module top, then (b) the test_nps_regression function calls _load_latest_summary() with the default arg, and (c) the unit tests pass `root=gauntlet_root` directly to assertion logic factored into `_assert_nps_ratio(summary)`. This keeps the production sentinel pure while the unit tests exercise the assertion logic on synthetic summaries built via summary_json_factory.

    DO NOT add `@pytest.mark.benchmark` to the unit tests — only the real production sentinel `test_nps_regression()` gets the RUN_BENCHMARKS gate.

    B. tests/test_gauntlet_sanity.py — covers latest-summary discovery + sanity-verdict computation + manual instructions for the slow probe. All tests are pure-unit (consume summary_json_factory) — none triggers an actual gauntlet run.

    For test_sanity_verdict_pass_when_elo_within_tolerance and friends: build a synthetic sanity-mode summary using summary_json_factory(verdict computed by Plan 04's write_summary). The verdict for sanity mode comes from Plan 04's write_summary contract (D-10): "PASS" if abs(elo) <= 15 else "FAIL". If Plan 04 instead stores the raw elo and lets the reader compute, adapt the test to call gauntlet.compute_sanity_verdict(elo) directly. Read Plan 04's code post-Task-1 to verify which factoring was chosen, and align this test accordingly.

    NO test in either file launches a real fastchess subprocess. NO test takes longer than 1 s. The actual V6-vs-V6 sanity probe lives in Task 3 (a checkpoint).
  </action>
  <verify>
    <automated>python3 -m uv run --group dev pytest -q tests/test_nps_regression.py tests/test_gauntlet_sanity.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist: tests/test_nps_regression.py, tests/test_gauntlet_sanity.py
    - `grep -c "def test_" tests/test_nps_regression.py` returns at least 5
    - `grep -c "def test_" tests/test_gauntlet_sanity.py` returns at least 5
    - Default pytest invocation (no RUN_BENCHMARKS): the production `test_nps_regression` SKIPs cleanly; all other tests PASS
    - `RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest -q tests/test_nps_regression.py -k "passes_at_ratio_above_floor"` PASSes
    - `RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest -q tests/test_nps_regression.py -k "fails_at_ratio_below_floor"` FAILs (red is expected — this test asserts the assertion floor works)
    - The PATTERNS-cited verbatim assertion message text appears in tests/test_nps_regression.py: `grep -F "0.8 floor (CLAUDE.md ~20% NPS constraint)" tests/test_nps_regression.py` returns at least one match
    - Total runtime ≤ 5 s for both files combined (no subprocess, no SPRT)
    - No test depends on built v6_uci / v7_uci / fastchess binaries — pure unit
  </acceptance_criteria>
  <done>Sentinel and sanity-verdict unit tests pass; production sentinel skips by default and is callable via RUN_BENCHMARKS=1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — run the actual V6-vs-V6 sanity probe (GAUNT-04, D-10)</name>
  <what-built>
    Across Plans 01-04: v7_uci with setoption + wtime/btime parsing; v6_uci standalone binary; tools/fetch_fastchess.py (idempotent fastchess fetcher); tools/books/8moves_v3.pgn (vendored opening book); tools/gauntlet.py with run + sanity subcommands; full unit test suite covering the parser, command builder, summary.json schema, and NPS regression sentinel.

    Plan 05 Task 2 added the NPS sentinel that reads .planning/gauntlets/<ISO>/summary.json — but no summary.json exists yet. This checkpoint produces the first one by running the harness for real, satisfying Phase 2's "complete" signal per D-09 (V6-vs-V6 sanity probe green, with V7-vs-V6 SPRT deferred until Phase 1 perf bugs land).
  </what-built>
  <how-to-verify>
    Run on a host where v6_uci is built and fastchess can be downloaded (Phase 2 requires C++17 + CMake ≥3.15 + network access on first run — same toolchain gate as Phase 1 verification per RESEARCH §"Environment Availability").

    Step 1 — Confirm preconditions (each command should exit 0):
      - `ls -l src/chess_engine/engine/v6/build/v6_uci{,.exe} 2>/dev/null` shows the binary exists (built in Plan 02)
      - `python3 tools/fetch_fastchess.py 2>&1` prints a path under tools/.cache/ and exits 0 (downloads on first run, idempotent on subsequent)
      - `printf 'uci\nquit\n' | src/chess_engine/engine/v6/build/v6_uci | grep -q uciok` exits 0

    Step 2 — Run the V6-vs-V6 sanity probe at full scale (D-10 specifies 200 games — wall clock ~30-60 minutes on a quiet host):
      `python3 -m uv run --group dev python tools/gauntlet.py sanity --games 200`

    Step 3 — Inspect the produced artifacts:
      - A new directory exists at .planning/gauntlets/<ISO-timestamp>/
      - That directory contains exactly three files: games.pgn, summary.json, fastchess.log
      - `python3 -c "import json; s=json.load(open(sorted(__import__('pathlib').Path('.planning/gauntlets').iterdir())[-1] / 'summary.json')); print('elo:', s['result']['elo'], 'verdict:', s['result']['verdict'], 'n:', s['result']['games']['n'])"` prints:
        - elo: a float in the range [-15.0, +15.0] (D-10 tolerance)
        - verdict: "PASS"
        - n: 200 (or 199-201 if a game was dropped via `-recover`)
      - summary.json contains a non-null "fastchess_command" (re-runnable per GAUNT-03)
      - summary.json contains "nps.v6.bench_nps" as an integer (D-13 prerequisite)
      - summary.json's "time_forfeits.v6" tally is 0 (or, if any forfeit occurred, INVESTIGATE — GAUNT-07 says a forfeit triggers a manual-investigation flag, not silent counting)

    Step 4 — Confirm the NPS sentinel can now READ that summary:
      - `RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest -q tests/test_nps_regression.py::test_nps_regression` SKIPs (because V7 NPS isn't in a V6-vs-V6 summary — the production sentinel needs the FIRST V7-vs-V6 run, which is deferred per D-09); document this in the resume note. Alternatively, if the executor designed the sentinel to also handle V6-vs-V6 (both engines = v6, so v7 key absent), it should SKIP cleanly with a "needs v7 entry in nps block" message rather than KeyError.

    Step 5 — Confirm the full pytest suite is green (no regressions across Phases 1 + 2):
      - `python3 -m uv run --group dev pytest -q` exits 0

    Expected outcome: the harness has produced a real summary.json with |elo| <= 15 over 200 games of V6 vs itself, proving the harness measures correctly (no color-assignment bug, no TC asymmetry, no book-pairing bug). Phase 2 is then complete per D-09 — the first V7-vs-V6 SPRT verdict run is intentionally deferred until Phase 1 gap-closure lands the 3 perf bugs from memory/project_phase1_known_perf_bugs.md.

    If Step 2 returns elo OUTSIDE [-15, +15]: do NOT mark this checkpoint complete. Re-run on a quieter host or with more games to rule out noise. If a second probe also fails the tolerance, the harness has a measurement bug — file findings as a gap and route to /gsd-plan-phase 2 --gaps.

    If the wall clock for Step 2 is unacceptable on the local host (laptop, throttled), the user may reduce --games to 50 for a faster preliminary check — but the OFFICIAL Phase 2 completion signal requires the full 200-game run per D-10.
  </how-to-verify>
  <resume-signal>
    Type "approved" with the summary.json path and the measured Elo (e.g. "approved — .planning/gauntlets/2026-05-17T03-30-00Z/summary.json — elo 3.2 over 200 games") to confirm Phase 2 complete. Type "failed — <details>" to escalate to gap-closure.
  </resume-signal>
</task>

</tasks>

<verification>
- `python3 -m uv run --group dev pytest -q tests/test_nps_regression.py tests/test_gauntlet_sanity.py` passes (default — sentinel skips)
- `RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest -q tests/test_nps_regression.py -k "passes_at_ratio"` passes
- Full suite `python3 -m uv run --group dev pytest -q` passes (no Phase 1 regression — INT-08 invariant)
- Manual V6-vs-V6 sanity probe (Task 3) produces a real summary.json with |elo| <= 15
</verification>

<success_criteria>
- NPS sentinel (D-13 / GAUNT-08) is wired to .planning/gauntlets/ summary.json reads
- conftest.py shared fixtures support all Phase 2 test files
- V6-vs-V6 sanity probe (D-10 / GAUNT-04) demonstrably PASSes on a real host — that's Phase 2 "complete" per D-09
- First V7-vs-V6 SPRT is correctly deferred (no test or task in this plan attempts it — D-09 honored)
</success_criteria>

<output>
After completion, create `.planning/phases/02-gauntlet-harness-early/02-05-SUMMARY.md` recording: sentinel design (split into _load_latest_summary + _assert_nps_ratio for unit-testability), conftest.py fixtures added (and whether the file pre-existed), test pass counts, and — most importantly — the human-verify checkpoint result: timestamped path to the first real summary.json, the measured Elo, the verdict, and a confirmation that the harness is now ready to validate V7-vs-V6 SPRT once Phase 1 perf bugs land.
</output>
