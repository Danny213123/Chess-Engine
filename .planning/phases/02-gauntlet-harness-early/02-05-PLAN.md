---
phase: 02-gauntlet-harness-early
plan: 05
type: execute
wave: 3
depends_on: ["02-04b"]
files_modified:
  - tests/test_nps_regression.py
  - tests/conftest.py
  - tests/test_gauntlet_sanity.py
autonomous: false
requirements:
  - GAUNT-04
  - GAUNT-08
user_setup: []

must_haves:
  truths:
    - "RUN_BENCHMARKS=1 pytest tests/test_nps_regression.py reads the latest .planning/gauntlets/<ISO>/summary.json and PASSes when summary.nps.v7.bench_nps >= 0.8 * summary.nps.v6.bench_nps (D-13, GAUNT-08)"
    - "RUN_BENCHMARKS=1 pytest tests/test_nps_regression.py FAILs with a clear message when the ratio drops below 0.8"
    - "Default pytest (without RUN_BENCHMARKS=1) SKIPs the sentinel cleanly — no false failures in normal CI"
    - "The sentinel reads the LATEST summary.json by lexical-sort over ISO timestamps under .planning/gauntlets/ (deterministic discovery)"
    - "When no .planning/gauntlets/<ISO>/summary.json exists, the sentinel skips with a clear 'no gauntlet runs found — run tools/gauntlet.py sanity first' message"
    - "tests/conftest.py provides a shared synthetic-summary.json factory + tmp .planning/gauntlets/ root fixture so test_nps_regression.py and test_gauntlet_sanity.py can be exercised offline"
    - "A human-verify checkpoint runs the actual V6-vs-V6 sanity probe (GAUNT-04) at the end of the phase — confirms the harness ACTUALLY produces an Elo within ±15 over 200 games (D-10) AND surfaces investigation_required=true in summary.json as a hard checkpoint-block (GAUNT-07)"
  artifacts:
    - path: "tests/test_nps_regression.py"
      provides: "Benchmark-gated pytest sentinel that extends Phase 1 test_nps_sentinel and reads summary.json"
      contains: "test_nps_regression"
    - path: "tests/conftest.py"
      provides: "Shared fixtures: gauntlet_root (tmp .planning/gauntlets/), summary_json_factory (synthesizes a summary.json with configurable bench_nps), _find_uci_binary helper"
      contains: "summary_json_factory"
    - path: "tests/test_gauntlet_sanity.py"
      provides: "Integration tests for the latest-summary-discovery + ratio assertions + skip-on-empty-gauntlets-dir + the V6-vs-V6 manual instructions"
      contains: "test_latest_summary"
  key_links:
    - from: "tests/test_nps_regression.py"
      to: ".planning/gauntlets/<ISO>/summary.json"
      via: "sorted(OUTPUT_ROOT.iterdir())[-1] / 'summary.json' — lexical sort over ISO names"
      pattern: "summary.json"
    - from: "tests/test_nps_regression.py"
      to: "Phase 1 test_nps_sentinel"
      via: "extends the same benchmark gate + assertion floor; substitutes summary.json source for inline find_best_move call"
      pattern: "RUN_BENCHMARKS"
---

<objective>
Close the validation loop: implement the NPS regression sentinel (D-13 / GAUNT-08) and the V6-vs-V6 sanity probe wiring (D-10 / GAUNT-04) that together give Phase 2 its "complete" signal per D-09.

Purpose: GAUNT-08's "build fails if V7 NPS drops more than 20% vs V6" is operationalized as a pytest sentinel that reads the latest summary.json's bench_nps fields (populated by Plan 02-04b's gauntlet.py). GAUNT-04's "V6-vs-V6 returns approximately 0 Elo" is operationalized as `tools/gauntlet.py sanity --games 200` producing a summary.json with |elo| <= 15 — Plan 02-04a already implements the verdict computation; this plan's checkpoint requests the actual execution. Together these two artifacts close the phase: when both pass AND investigation_required is false, the harness has demonstrated it can produce trustworthy measurements.

Output: tests/test_nps_regression.py + tests/conftest.py + tests/test_gauntlet_sanity.py + a `checkpoint:human-verify` task that runs the actual ~30-60 min V6-vs-V6 probe on a built host.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gauntlet-harness-early/02-CONTEXT.md
@.planning/phases/02-gauntlet-harness-early/02-RESEARCH.md
@.planning/phases/02-gauntlet-harness-early/02-PATTERNS.md
@.planning/phases/02-gauntlet-harness-early/02-04a-SUMMARY.md
@.planning/phases/02-gauntlet-harness-early/02-04b-SUMMARY.md
@tools/gauntlet.py
@tools/gauntlet_core.py
@tests/test_v7_engine.py
@tests/test_v7_bindings.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/conftest.py — shared fixtures (gauntlet_root, summary_json_factory, _find_uci_binary helper)</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (if it exists, read in full — DO NOT clobber existing fixtures; append rather than overwrite. If it does not exist, create from scratch.)
    - tests/test_v7_bindings.py lines 163-184 (the _find_v7_uci_binary helper — generalize to _find_uci_binary(engine: str) supporting both v6 and v7 per PATTERNS module-scoped fixture analog)
    - tests/test_v7_bindings.py lines 40-51 (module-scoped v7_native_engine fixture — model the shared fixture scope here)
    - tools/gauntlet.py + tools/gauntlet_core.py (Plan 02-04a + 02-04b outputs — the summary.json schema this factory must mirror, including the investigation_required field)
    - .planning/phases/02-gauntlet-harness-early/02-PATTERNS.md sections "Shared Patterns → Module-scoped native-engine fixture" and "tests/test_nps_regression.py"
    - .planning/phases/02-gauntlet-harness-early/02-VALIDATION.md Wave-0 list (conftest.py fixture listed as a deliverable)
  </read_first>
  <action>
    Two cases:

    Case A — tests/conftest.py exists: READ the file fully, identify existing fixtures, and APPEND the new fixtures listed below without renaming or shadowing any existing fixture. If a fixture name collides, suffix with `_gauntlet`.

    Case B — tests/conftest.py does NOT exist: create it from scratch with the imports + fixtures listed below.

    Fixtures to provide (per PATTERNS + RESEARCH §9):

    1. _find_uci_binary(engine: str) -> Path | None — generalized binary discovery for both v6 and v7. Move the candidate-dir + candidate-name pattern from tests/test_v7_bindings.py here so test_uci_binaries.py (Plan 01), test_uci_binaries_v6.py (Plan 02), and test_gauntlet_sanity.py (this plan Task 3) all share one helper.

    2. gauntlet_root(tmp_path) — pytest fixture returning a tmp_path subdirectory simulating .planning/gauntlets/.

    3. summary_json_factory(gauntlet_root) — pytest fixture returning a callable: `make(timestamp="2026-05-16T21-00-00Z", v7_bench_nps=500_000, v6_bench_nps=600_000, verdict="H1", elo=13.87, investigation_required=False, **overrides) -> Path`. Builds the full summary.json on the Plan 02-04b schema (including the investigation_required field) and writes it under `gauntlet_root / timestamp / "summary.json"`. Returns the path. Overrides shallow-merge into the dict.

    4. (Optional) latest_summary_dir(gauntlet_root) — returns `sorted(gauntlet_root.iterdir())[-1]`.

    Function-scoped fixtures (each test needs isolated gauntlet_root). Imports: json, os, platform, pytest, pathlib.Path. Stdlib + pytest only.
  </action>
  <verify>
    <automated>python3 -m uv run --group dev pytest -q --collect-only tests/conftest.py 2>&1 | tail -5 && python3 -m uv run --group dev pytest -q tests/test_v7_bindings.py 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py exists; if pre-existing, NO pre-existing fixture name is shadowed or renamed
    - The three new fixtures (`gauntlet_root`, `summary_json_factory`, `_find_uci_binary` helper) are defined
    - `_find_uci_binary("v7")` and `_find_uci_binary("v6")` both work (return Path or None — never raise)
    - `summary_json_factory()` produces a JSON file whose top-level keys match the Plan 02-04b schema (schema_version, timestamp_utc, engines, tc, concurrency, openings, sprt, fastchess_command, fastchess_version, host, result, nps, investigation_required)
    - `python3 -m uv run --group dev pytest -q --collect-only tests/` exits 0 (conftest loads cleanly across the whole test suite)
    - `python3 -m uv run --group dev pytest -q tests/test_v7_bindings.py` PASSes — proves no regression in a pre-existing test file that consumes the same conftest scope
    - All pre-existing tests still PASS
  </acceptance_criteria>
  <done>tests/conftest.py provides the three shared fixtures; the full pytest suite still collects and runs cleanly; the existing test_v7_bindings.py still passes (proves no fixture-scope regression).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/test_nps_regression.py + tests/test_gauntlet_sanity.py — sentinel + latest-summary discovery + ratio assertions</name>
  <files>tests/test_nps_regression.py, tests/test_gauntlet_sanity.py</files>
  <read_first>
    - tests/test_v7_engine.py lines 108-172 (CANONICAL test_nps_sentinel — copy the @pytest.mark.benchmark + @pytest.mark.skipif(RUN_BENCHMARKS != "1") decorator block VERBATIM)
    - tests/conftest.py (Task 1 output)
    - tools/gauntlet.py + tools/gauntlet_core.py (Plan 02-04a + 02-04b outputs — verify summary.json key names; test reads summary.nps.v7.bench_nps and summary.nps.v6.bench_nps per D-13 / RESEARCH §8 Option B; also reads summary.investigation_required per GAUNT-07)
    - .planning/phases/02-gauntlet-harness-early/02-PATTERNS.md section "tests/test_nps_regression.py"
    - .planning/phases/02-gauntlet-harness-early/02-RESEARCH.md §9 Phase-Requirements-to-Test-Map rows for GAUNT-04 + GAUNT-08
    - .planning/phases/02-gauntlet-harness-early/02-RESEARCH.md §9 "Validation guardrails" (NEVER run slow sanity in default CI; gate behind RUN_SLOW_GAUNTLET=1)
  </read_first>
  <behavior>
    tests/test_nps_regression.py:
    - test_nps_regression_passes_at_ratio_above_floor: summary_json_factory(v7_bench_nps=500_000, v6_bench_nps=600_000) → ratio 0.833 → PASS
    - test_nps_regression_fails_at_ratio_below_floor: summary_json_factory(v7_bench_nps=400_000, v6_bench_nps=600_000) → ratio 0.667 → FAIL with clear AssertionError mentioning floor (0.8) and actual ratio
    - test_nps_regression_skips_without_run_benchmarks: when RUN_BENCHMARKS != "1", SKIP cleanly
    - test_nps_regression_skips_on_empty_gauntlets_dir: no summary.json under .planning/gauntlets/, SKIP with "no gauntlet runs found — run tools/gauntlet.py sanity first"
    - test_nps_regression_reads_latest_summary: write three summary.json files at three different timestamps; sentinel reads from the lexically-greatest

    tests/test_gauntlet_sanity.py:
    - test_latest_summary_discovery: write 3 summary.json files; assert latest_summary_dir() returns lexically-greatest
    - test_sanity_verdict_pass_when_elo_within_tolerance: synthetic summary with elo=5.0; assert verdict == "PASS" (D-10 tolerance ±15)
    - test_sanity_verdict_fail_when_elo_exceeds_tolerance: elo=20.0; verdict == "FAIL"
    - test_sanity_verdict_exact_boundary: elo=15.0; verdict == "PASS" (boundary inclusive)
    - test_sanity_summary_omits_sprt: sanity-mode summary.json has top-level "sprt" == null
  </behavior>
  <action>
    Two files. Both consume the conftest.py fixtures from Task 1.

    A. tests/test_nps_regression.py — GAUNT-08 sentinel per D-13. Copy the benchmark decorator + RUN_BENCHMARKS skipif block VERBATIM from tests/test_v7_engine.py lines 108-117. Apply to test_nps_regression():
      1. Discover latest summary.json under .planning/gauntlets/ (real production root, not gauntlet_root tmp).
      2. If none → pytest.skip("no gauntlet runs found — run tools/gauntlet.py sanity first").
      3. Load JSON, read `summary["nps"]["v7"]["bench_nps"]` and `summary["nps"]["v6"]["bench_nps"]`.
      4. Compute `ratio = v7_nps / v6_nps`; assert `ratio >= 0.8` with assertion message copied VERBATIM from PATTERNS: "V7 NPS {v7_nps:.0f} / V6 NPS {v6_nps:.0f} ratio {ratio:.2f} below 0.8 floor (CLAUDE.md ~20% NPS constraint). Re-run on a quiet host before treating as a regression."

    Split sentinel into (a) `_load_latest_summary(root=PRODUCTION_ROOT)` helper, (b) production test calls it with default arg, (c) unit tests pass `root=gauntlet_root`. Keeps production sentinel pure; unit tests exercise assertion logic on synthetic summaries via summary_json_factory.

    DO NOT add `@pytest.mark.benchmark` to unit tests — only the production sentinel gets the RUN_BENCHMARKS gate.

    B. tests/test_gauntlet_sanity.py — covers latest-summary discovery + sanity-verdict computation. All tests are pure-unit (consume summary_json_factory) — none triggers an actual gauntlet run.

    For sanity-verdict tests: call `gauntlet_core.compute_sanity_verdict(elo)` directly (Plan 02-04a's pure function, boundary-inclusive at ±15 per D-10).

    NO test launches a real fastchess subprocess. NO test takes longer than 1 s. Actual V6-vs-V6 sanity probe lives in Task 3.
  </action>
  <verify>
    <automated>python3 -m uv run --group dev pytest -q tests/test_nps_regression.py tests/test_gauntlet_sanity.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist
    - `grep -c "def test_" tests/test_nps_regression.py` returns at least 5
    - `grep -c "def test_" tests/test_gauntlet_sanity.py` returns at least 5
    - Default pytest (no RUN_BENCHMARKS): production `test_nps_regression` SKIPs; all other tests PASS
    - `RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest -q tests/test_nps_regression.py -k "passes_at_ratio_above_floor"` PASSes
    - `RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest -q tests/test_nps_regression.py -k "fails_at_ratio_below_floor"` FAILs (red is expected)
    - `grep -F "0.8 floor (CLAUDE.md ~20% NPS constraint)" tests/test_nps_regression.py` returns at least one match
    - Total runtime ≤ 5 s combined
    - No test depends on built v6_uci / v7_uci / fastchess binaries
  </acceptance_criteria>
  <done>Sentinel and sanity-verdict unit tests pass; production sentinel skips by default and is callable via RUN_BENCHMARKS=1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — run the actual V6-vs-V6 sanity probe (GAUNT-04, D-10) + verify investigation_required is false</name>
  <what-built>
    Across Plans 01-04b: v7_uci with setoption + wtime/btime parsing; v6_uci standalone binary; tools/fetch_fastchess.py (idempotent fastchess fetcher); tools/books/8moves_v3.pgn OR tools/books/.fetch_fallback.json (opening book provisioning); tools/gauntlet_core.py (pure functions) + tools/gauntlet.py (I/O); full unit test suite covering parser, command builder, summary.json schema, and NPS regression sentinel.

    Plan 02-05 Task 2 added the NPS sentinel that reads .planning/gauntlets/<ISO>/summary.json — but no summary.json exists yet. This checkpoint produces the first one by running the harness for real, satisfying Phase 2's "complete" signal per D-09 (V6-vs-V6 sanity probe green AND investigation_required=false, with V7-vs-V6 SPRT deferred until Phase 1 perf bugs land).
  </what-built>
  <how-to-verify>
    Run on a host where v6_uci is built and fastchess can be downloaded.

    Step 1 — Confirm preconditions (each command should exit 0):
      - `ls -l src/chess_engine/engine/v6/build/v6_uci{,.exe} 2>/dev/null` shows the binary exists (built in Plan 02-02)
      - `python3 tools/fetch_fastchess.py 2>&1` prints a path under tools/.cache/ and exits 0
      - `printf 'uci\nquit\n' | src/chess_engine/engine/v6/build/v6_uci | grep -q uciok` exits 0

    Step 2 — Run the V6-vs-V6 sanity probe at full scale (D-10: 200 games, wall clock ~30-60 min on a quiet host):
      `python3 -m uv run --group dev python tools/gauntlet.py sanity --games 200`

    Step 3 — Inspect the produced artifacts:
      - New directory at .planning/gauntlets/<ISO-timestamp>/
      - Contains exactly three files: games.pgn, summary.json, fastchess.log
      - `python3 -c "import json; s=json.load(open(sorted(__import__('pathlib').Path('.planning/gauntlets').iterdir())[-1] / 'summary.json')); print('elo:', s['result']['elo'], 'verdict:', s['result']['verdict'], 'n:', s['result']['games']['n'], 'investigation_required:', s['investigation_required'])"` prints:
        - elo: a float in [-15.0, +15.0] (D-10 tolerance)
        - verdict: "PASS"
        - n: 200 (or 199-201 if a game dropped via `-recover`)
        - investigation_required: False
      - summary.json contains non-null "fastchess_command" (re-runnable per GAUNT-03)
      - summary.json contains "nps.v6.bench_nps" as an integer (D-13 prerequisite)

    Step 4 — investigation_required gate (GAUNT-07, mandatory hard block):
      - If `summary.investigation_required == True`: this checkpoint is NOT auto-approved regardless of the Elo result. Read summary.json's `time_forfeits` and `unrecognized_terminations` fields, surface them in the checkpoint resume-signal as the reason for hold, and DO NOT type "approved" until either (a) the forfeits/unrecognized terminations are explained and explicitly accepted, or (b) a re-run produces investigation_required=False.
      - Specifically: gauntlet_core.parse_pgn_terminations() over-flags unknown terminations conservatively (per RESEARCH §10 RESOLVED A2). A first run hitting investigation_required=True does NOT automatically mean the harness is broken — it means a termination pattern wasn't in TIME_FORFEIT_PATTERNS or in the recognized list. The human's job here is to inspect the unrecognized termination strings, decide whether they represent real forfeits or merely unmodeled-but-benign endings, and either patch gauntlet_core.TIME_FORFEIT_PATTERNS / the recognized-list constants and re-run, or accept the flag and route to /gsd-plan-phase 2 --gaps.

    Step 5 — Confirm the NPS sentinel can READ the summary:
      - `RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest -q tests/test_nps_regression.py::test_nps_regression` SKIPs (V7 NPS isn't in a V6-vs-V6 summary; sentinel needs the FIRST V7-vs-V6 run, deferred per D-09). Document in resume note. If the sentinel was designed to also handle V6-vs-V6 (both engines = v6, v7 key absent), it should SKIP cleanly with "needs v7 entry in nps block" rather than KeyError.

    Step 6 — Confirm full pytest suite is green (no Phase 1 + 2 regressions):
      - `python3 -m uv run --group dev pytest -q` exits 0

    Expected outcome: harness produced a real summary.json with |elo| <= 15 over 200 games of V6 vs itself, investigation_required=False, proving the harness measures correctly. Phase 2 is then complete per D-09 — the first V7-vs-V6 SPRT verdict is intentionally deferred until Phase 1 gap-closure lands the 3 perf bugs.

    If Step 3 returns elo OUTSIDE [-15, +15] OR Step 4 detects investigation_required=True without an accepted explanation: do NOT mark this checkpoint complete. Re-run on a quieter host or patch the unrecognized-termination handling. If a second probe also fails, route to /gsd-plan-phase 2 --gaps.

    If wall clock for Step 2 is unacceptable on the local host, the user may reduce --games to 50 for a preliminary check — but the OFFICIAL Phase 2 completion signal requires the full 200-game run per D-10.
  </how-to-verify>
  <resume-signal>
    Type "approved — <summary.json path> — elo <value> — investigation_required=false" to confirm Phase 2 complete (e.g. "approved — .planning/gauntlets/2026-05-17T03-30-00Z/summary.json — elo 3.2 — investigation_required=false"). Type "held — investigation_required=true: <unrecognized-terminations or forfeit details>" to hold for triage. Type "failed — <details>" to escalate to gap-closure.
  </resume-signal>
</task>

</tasks>

<verification>
- `python3 -m uv run --group dev pytest -q tests/test_nps_regression.py tests/test_gauntlet_sanity.py` passes (default — sentinel skips)
- `RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest -q tests/test_nps_regression.py -k "passes_at_ratio"` passes
- Full suite `python3 -m uv run --group dev pytest -q` passes (no Phase 1 regression — INT-08 invariant)
- Manual V6-vs-V6 sanity probe (Task 3) produces a real summary.json with |elo| <= 15 AND investigation_required=false
</verification>

<success_criteria>
- NPS sentinel (D-13 / GAUNT-08) is wired to .planning/gauntlets/ summary.json reads
- conftest.py shared fixtures support all Phase 2 test files
- V6-vs-V6 sanity probe (D-10 / GAUNT-04) demonstrably PASSes on a real host with investigation_required=false — that's Phase 2 "complete" per D-09
- First V7-vs-V6 SPRT is correctly deferred (no test or task in this plan attempts it — D-09 honored)
- GAUNT-07 enforcement: the checkpoint blocks on investigation_required=true regardless of Elo verdict
</success_criteria>

<output>
After completion, create `.planning/phases/02-gauntlet-harness-early/02-05-SUMMARY.md` recording: sentinel design (split into _load_latest_summary + _assert_nps_ratio for unit-testability), conftest.py fixtures added (and whether the file pre-existed), test pass counts, and the human-verify checkpoint result: timestamped path to the first real summary.json, the measured Elo, the verdict, investigation_required state, and confirmation that the harness is ready to validate V7-vs-V6 SPRT once Phase 1 perf bugs land.
</output>
</output>
