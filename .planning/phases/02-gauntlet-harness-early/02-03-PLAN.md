---
phase: 02-gauntlet-harness-early
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/fetch_fastchess.py
  - tools/books/8moves_v3.pgn
  - tools/books/SOURCES.md
  - tests/test_fetch_fastchess.py
  - .gitignore
autonomous: true
requirements:
  - GAUNT-01
  - GAUNT-05
user_setup: []

must_haves:
  truths:
    - "Running tools/fetch_fastchess.py on a clean cache downloads the pinned fastchess release, verifies its SHA256, and places the binary at tools/.cache/fastchess{.exe,} executable"
    - "Re-running tools/fetch_fastchess.py against an already-cached + checksum-valid binary is a no-op (idempotent fast path; exits 0 in under 1 s without a network call)"
    - "The script picks the correct asset for Windows / Linux / macOS based on platform.system()"
    - "Mismatched SHA256 raises SystemExit and removes the corrupt cached file (D-02 non-bypassable gate)"
    - "tools/books/8moves_v3.pgn exists as a vendored PGN ready for fastchess `-openings file=...`"
    - "tools/.cache/, .planning/gauntlets/, v6_uci{,.exe} are gitignored — no binaries enter the repo"
  artifacts:
    - path: "tools/fetch_fastchess.py"
      provides: "Stdlib-only idempotent fetch+checksum+cache module for the fastchess binary"
      contains: "ensure_fastchess"
      min_lines: 60
    - path: "tools/books/8moves_v3.pgn"
      provides: "Vendored opening book seed (~34,700 positions, 16 plies deep)"
    - path: "tools/books/SOURCES.md"
      provides: "Provenance record: upstream URL + commit SHA + license confirmation + vendor date"
      contains: "official-stockfish/books"
    - path: "tests/test_fetch_fastchess.py"
      provides: "Unit tests for idempotency, checksum mismatch, OS routing (network mocked via monkeypatch)"
      contains: "test_idempotent_when_cached"
    - path: ".gitignore"
      provides: "Phase 2 ignores: tools/.cache/, .planning/gauntlets/, v6_uci binaries"
      contains: "tools/.cache/"
  key_links:
    - from: "tools/fetch_fastchess.py"
      to: "tools/.cache/fastchess{,.exe}"
      via: "ensure_fastchess() returns the executable Path; downloads + verifies on cache miss"
      pattern: "ensure_fastchess"
    - from: "tools/fetch_fastchess.py"
      to: "GitHub releases (Disservin/fastchess)"
      via: "urllib.request.urlopen — only on cache miss / checksum mismatch"
      pattern: "urllib.request"
---

<objective>
Stand up the fastchess binary provisioning module and vendor the opening book seed used by every Phase 2 gauntlet.

Purpose: D-01..D-04 require an idempotent, OS-aware, SHA256-gated fetcher modeled on the Syzygy download pattern (which, per RESEARCH §6, doesn't actually exist as a Python module in the repo — this plan creates the first one). D-11 + RESEARCH §5 recommend vendoring `8moves_v3.pgn` (small enough that ~1-2 MB once outweighs a second network dependency per gauntlet run). D-03 mandates `tools/.cache/` is gitignored — the .gitignore change here also preemptively covers the v6_uci binary from Plan 02 to keep that plan's `.gitignore` touchpoint zero (avoids wave-bumping it).

Output: `tools/fetch_fastchess.py`, `tools/books/8moves_v3.pgn` + `SOURCES.md`, unit tests, and updated `.gitignore`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gauntlet-harness-early/02-CONTEXT.md
@.planning/phases/02-gauntlet-harness-early/02-RESEARCH.md
@.planning/phases/02-gauntlet-harness-early/02-PATTERNS.md
@tools/gen_coeffs.py
@cli/src/index.js
@tests/test_v7_bindings.py
@.gitignore
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement tools/fetch_fastchess.py (stdlib-only idempotent fetch+checksum) and vendor 8moves_v3.pgn</name>
  <files>tools/fetch_fastchess.py, tools/books/8moves_v3.pgn, tools/books/SOURCES.md, .gitignore</files>
  <read_first>
    - .planning/phases/02-gauntlet-harness-early/02-RESEARCH.md §3 (pinned release + checksum strategy — the planner picks a tag at plan time, downloads each per-OS asset manually once, records SHA256s in the script as a hardcoded dict — same trust model pip uses)
    - .planning/phases/02-gauntlet-harness-early/02-RESEARCH.md §6 (CANONICAL recommended shape — copy the `ensure_fastchess()` skeleton verbatim as the starting point)
    - .planning/phases/02-gauntlet-harness-early/02-RESEARCH.md §5 (8moves_v3.pgn vendor strategy — upstream URL, license check requirement, SOURCES.md provenance)
    - .planning/phases/02-gauntlet-harness-early/02-PATTERNS.md §"tools/fetch_fastchess.py (downloader, file-I/O + network)" (WARNING: D-01's "modeled on existing Syzygy script" is aspirational — no Python analog exists; design from scratch using RESEARCH §6 shape; emit progress to stderr, success path to stdout; raise SystemExit on checksum mismatch, do not return False/print-and-continue)
    - .planning/phases/02-gauntlet-harness-early/02-PATTERNS.md §"tools/books/8moves_v3.pgn (vendored asset)" (SOURCES.md provenance template — modeled on V7 CMakeLists comment discipline)
    - .planning/phases/02-gauntlet-harness-early/02-PATTERNS.md §".gitignore (MOD)" (exact additive section format mirroring existing lines 67-81)
    - tools/gen_coeffs.py lines 113-131 (stdlib-only CLI structure — main(argv) returns int, sys.stderr for progress, sys.stdout for the resolved path, non-zero exit on failure)
    - .gitignore (FULL — current file is 81 lines; new section appends after line 81; do NOT reorder existing lines per PATTERNS §"Shared Patterns → .gitignore add-not-rewrite")
  </read_first>
  <action>
    Three sub-artifacts in one task (they share a single semantic concern — "make a clean clone runnable"):

    **A. tools/fetch_fastchess.py** — stdlib-only Python module (no third-party deps; pip-install-free; uv group-dev not required to run it):

    - Module docstring citing D-01/D-02/D-03/D-04 + GAUNT-01.
    - Constants block at top:
      - `FASTCHESS_RELEASE` — pin to a specific tag (executor MUST query the GitHub releases API per RESEARCH §3 at execution time and pick the latest stable; record the chosen tag in 02-03-SUMMARY.md).
      - `FASTCHESS_ASSETS` — dict keyed by `platform.system()` return values ("Windows", "Linux", "Darwin"); value is `{"asset": "<filename>", "sha256": "<64-hex>"}`. Executor MUST: (1) query GitHub releases API to discover the exact asset filename for each OS at the pinned tag, (2) download each asset once via browser/curl, (3) compute sha256sum locally, (4) hardcode the resulting SHA256s in this dict. Do NOT use a placeholder string — every entry must be a real 64-character hex hash on commit.
      - `CACHE_DIR = Path(__file__).resolve().parent / ".cache"`.
    - Function `_asset_for_host() -> tuple[str, str]`: returns (asset filename, sha256) for `platform.system()`; raises `SystemExit(f"Unsupported OS: {platform.system()}")` on unknown OS (D-04).
    - Function `_sha256(path: Path) -> str`: hashlib.sha256 over 64KB chunks (RESEARCH §6 skeleton).
    - Function `ensure_fastchess() -> Path` (the public API per GAUNT-01):
      1. Compute `dest = CACHE_DIR / ("fastchess.exe" if platform.system() == "Windows" else "fastchess")`.
      2. Idempotent fast path: `if dest.exists() and _sha256(dest) == want_sha: return dest` (D-03).
      3. mkdir CACHE_DIR with `parents=True, exist_ok=True`.
      4. Build URL `https://github.com/Disservin/fastchess/releases/download/{FASTCHESS_RELEASE}/{asset}`; print to stderr `"[fetch_fastchess] downloading {url}"`.
      5. `urllib.request.urlopen(url)` → write bytes to dest atomically (write to `dest.with_suffix(dest.suffix + ".part")` then rename, so a partial download never poisons the cache).
      6. Re-checksum; if mismatch, unlink the partial file and `raise SystemExit(f"checksum mismatch: want {want_sha}, got {got}")` (D-02 non-bypassable gate per PATTERNS).
      7. On POSIX (`platform.system() != "Windows"`), chmod +x via `dest.chmod(dest.stat().st_mode | stat.S_IXUSR | stat.S_IXGRP | stat.S_IXOTH)`.
      8. Return `dest`.
    - `if __name__ == "__main__":` block that prints the resolved path to stdout (so `gauntlet.py` can capture it via subprocess if it ever needs to). Exit code from main(argv).
    - Use ONLY stdlib: hashlib, os, platform, stat, sys, urllib.request, pathlib. No requests, no click, no rich.
    - No global mutable state. No log file writes. Progress to stderr only.

    **B. tools/books/8moves_v3.pgn + tools/books/SOURCES.md** — vendored asset + provenance:

    - Download `8moves_v3.pgn.zip` from `https://github.com/official-stockfish/books/blob/master/8moves_v3.pgn.zip` (the canonical source per RESEARCH §5 — VERIFIED).
    - Unzip it; commit ONLY the unzipped `8moves_v3.pgn` (~1-2 MB) at `tools/books/8moves_v3.pgn`. Do NOT commit the .zip.
    - Verify the upstream repo's LICENSE before commit (RESEARCH §10 Q3 — ASSUMED GPL-compatible but unverified). If the LICENSE is GPL-3.0 and incompatible with this repo, ABORT the vendoring sub-task and surface the issue in 02-03-SUMMARY.md as a follow-up needing user decision (fall back to fetch-on-demand per D-11). Do not commit the file silently if licensing is unclear.
    - Create `tools/books/SOURCES.md` with sections: Upstream URL (with the specific commit SHA pinned via the upstream repo's commit log at vendor time), Vendor Date, License (with explicit text quoted from upstream LICENSE), Position Count (~34,700), Ply Depth (16). Modeled on V7 CMakeLists comment discipline per PATTERNS §"tools/books/8moves_v3.pgn".
    - Do NOT add tools/books/ to .gitignore — vendored asset is intentionally committed.

    **C. .gitignore** — additive new section after line 81 per PATTERNS §".gitignore (MOD)":

    Append (exact format mirroring lines 67-81):
    ```
    # Phase 2: gauntlet artifacts (downloaded binaries + run results)
    tools/.cache/
    .planning/gauntlets/
    src/chess_engine/engine/v6/v6_uci
    src/chess_engine/engine/v6/v6_uci.exe
    ```

    Do NOT reorder pre-existing lines. Do NOT remove existing entries. Pre-emptively including v6_uci ignores here (even though the v6_uci binary is built in Plan 02) keeps Plan 02 from touching `.gitignore`, preserving Wave 1 file disjointness so Plans 01/02/03 truly run in parallel.

    Do NOT touch pyproject.toml. Do NOT add a new dep group. The fetcher is stdlib-only by design.
  </action>
  <verify>
    <automated>python3 tools/fetch_fastchess.py 2>&1 | tail -3 && python3 tools/fetch_fastchess.py 2>&1 | tail -3 && test -f tools/books/8moves_v3.pgn && test -f tools/books/SOURCES.md && grep -q "tools/.cache/" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/fetch_fastchess.py` on a clean cache downloads the binary, verifies SHA256, places it at `tools/.cache/fastchess` (or `.exe` on Windows), and prints the cache path on stdout
    - A second `python3 tools/fetch_fastchess.py` invocation exits 0 within 1 s with NO network call (idempotent — verifiable by running offline after first fetch)
    - `tools/.cache/fastchess` (or `.exe`) is executable on POSIX (`-x` bit set; `ls -l` shows the bit)
    - Corrupting the cached file (`echo bad > tools/.cache/fastchess` then re-run) triggers a re-download (idempotent fast-path skipped because checksum no longer matches)
    - `tools/books/8moves_v3.pgn` exists, is a valid PGN (first line starts with `[Event "..."]`), file size between 500 KB and 5 MB
    - `tools/books/SOURCES.md` exists and contains "official-stockfish/books" + a commit SHA + a license declaration
    - `.gitignore` contains the four new lines: `tools/.cache/`, `.planning/gauntlets/`, `src/chess_engine/engine/v6/v6_uci`, `src/chess_engine/engine/v6/v6_uci.exe`
    - `grep -v '^#' .gitignore | grep -c "tools/.cache/"` returns 1
    - All hashes in FASTCHESS_ASSETS are 64-character hex strings (no placeholders, no `"..."`)
    - `python3 -c "import tools.fetch_fastchess; print(tools.fetch_fastchess.ensure_fastchess())"` returns a Path object (importable as a module)
  </acceptance_criteria>
  <done>fetch_fastchess.py is a working idempotent fetcher; 8moves_v3.pgn is vendored with provenance; .gitignore covers all Phase 2 transient artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for fetch_fastchess.py — idempotency, checksum mismatch, OS routing</name>
  <files>tests/test_fetch_fastchess.py</files>
  <read_first>
    - tools/fetch_fastchess.py (Task 1 output — read post-edit; tests assert against the actual constant names and function signatures the executor chose)
    - .planning/phases/02-gauntlet-harness-early/02-PATTERNS.md §"tests/test_fetch_fastchess.py (NEW)" (three test cases listed: test_idempotent_when_cached, test_checksum_mismatch_raises, test_asset_for_host — synthesize from gen_coeffs.py discipline + test_v7_bindings.py pytest idioms; no exact analog exists)
    - .planning/phases/02-gauntlet-harness-early/02-RESEARCH.md §9 "Phase Requirements → Test Map" rows for GAUNT-01 (three sub-cases listed)
    - tests/test_v7_bindings.py (pytest idioms: tmp_path, monkeypatch, module-scope fixtures)
  </read_first>
  <behavior>
    - test_idempotent_when_cached: monkeypatch CACHE_DIR → tmp_path, monkeypatch FASTCHESS_ASSETS so the "want_sha" matches a pre-staged file in tmp_path; monkeypatch urllib.request.urlopen to raise RuntimeError("no network call expected"); call ensure_fastchess(); assert returned Path == staged file (no network attempt → urlopen never called)
    - test_checksum_mismatch_raises: monkeypatch urlopen to return wrong bytes (BytesIO with content whose sha256 doesn't match FASTCHESS_ASSETS[host]); assert SystemExit raised; assert the corrupt file at CACHE_DIR/fastchess{.exe,} no longer exists after the failure (cleanup happened)
    - test_asset_for_host_windows: monkeypatch platform.system → "Windows"; assert _asset_for_host() returns the Windows tuple
    - test_asset_for_host_linux: monkeypatch platform.system → "Linux"; assert _asset_for_host() returns the Linux tuple
    - test_asset_for_host_darwin: monkeypatch platform.system → "Darwin"; assert _asset_for_host() returns the macOS tuple
    - test_asset_for_host_unsupported: monkeypatch platform.system → "FreeBSD" (or any unknown); assert SystemExit raised with a message containing the OS name
    - test_executable_bit_set_on_posix: skip on Windows; after a fresh fetch (synthetic, monkeypatched urlopen returning valid bytes), assert the cached file has the user-execute bit set
    - test_atomic_write_does_not_leak_partial: monkeypatch urlopen so the response stream raises mid-read; assert no `*.part` file is left behind in CACHE_DIR
  </behavior>
  <action>
    Create tests/test_fetch_fastchess.py covering the three sub-cases from RESEARCH §9 Wave-0-Gaps + the additional defensive tests for executable-bit setting and atomic-write behavior:

    - Import the module under test as `from tools import fetch_fastchess as ff` (ensure tools/ is importable — add `tools/__init__.py` if not already present; the existing `tools/gen_coeffs.py` already establishes tools/ as a package).
    - Use pytest fixtures: `tmp_path` (for synthetic CACHE_DIR), `monkeypatch` (for platform.system, FASTCHESS_ASSETS, urllib.request.urlopen, ff.CACHE_DIR).
    - For the network-mock tests, monkeypatch `urllib.request.urlopen` with a function returning a context-manager object exposing `.read()`. Do NOT actually hit the network in any test — every test must work offline.
    - For test_idempotent_when_cached: stage a file at the expected dest, monkeypatch FASTCHESS_ASSETS[host] to set "sha256" = _sha256(staged_file); assert ensure_fastchess() returns the staged Path and that the monkeypatched urlopen was NEVER called (use a counter via a mutable closure or assert by raising on call).
    - For test_checksum_mismatch_raises: monkeypatch urlopen to return bytes whose sha256 doesn't match the host's want_sha; assert SystemExit raised with message containing "checksum mismatch"; assert the dest file doesn't exist after the call (cleanup happened).
    - For test_executable_bit_set_on_posix: use `@pytest.mark.skipif(platform.system() == "Windows", reason="chmod is POSIX-only")`; after a synthetic successful fetch, assert `dest.stat().st_mode & stat.S_IXUSR`.
    - For test_atomic_write_does_not_leak_partial: monkeypatch urlopen to raise after returning a few bytes (use a custom class with a `.read()` that raises); assert SystemExit propagates AND `list(CACHE_DIR.glob("*.part"))` is empty.
    - Use `@pytest.mark.parametrize` where it shortens the OS-routing tests but keep individual assertions clear.
    - Test runtime budget: < 5 s total (no network, all tmp filesystem).
  </action>
  <verify>
    <automated>python3 -m uv run --group dev pytest -q tests/test_fetch_fastchess.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: tests/test_fetch_fastchess.py
    - `grep -c "def test_" tests/test_fetch_fastchess.py` returns at least 8
    - All tests PASS on a host without network connectivity (monkeypatched urlopen — every test offline)
    - Total test runtime ≤ 5 s
    - No test ACTUALLY downloads anything (assertable by monkeypatching urlopen to raise; if a test fails because urlopen was called, the test surface is wrong)
    - Tests pass on both POSIX (executable-bit test runs) and Windows (executable-bit test skips cleanly)
    - `tools/__init__.py` exists (created if missing) so the test's `from tools import fetch_fastchess` works
  </acceptance_criteria>
  <done>All eight unit tests pass offline; the fetcher's behavior contract is fully captured.</done>
</task>

</tasks>

<verification>
- `python3 tools/fetch_fastchess.py` downloads + checksum-verifies on first run; second run is a < 1 s no-op
- `tools/books/8moves_v3.pgn` is committed and is a valid PGN
- `tools/books/SOURCES.md` records provenance (URL, commit SHA, license, date)
- `.gitignore` covers `tools/.cache/`, `.planning/gauntlets/`, `v6_uci` binaries
- pytest tests/test_fetch_fastchess.py passes offline with no real network calls
</verification>

<success_criteria>
- fetch_fastchess.py is idempotent, OS-aware, SHA256-gated, stdlib-only
- 8moves_v3.pgn + SOURCES.md vendored under tools/books/
- Tests cover idempotency, checksum mismatch, OS routing, exec-bit, atomic-write
- .gitignore is additive (no reordering, no removal)
</success_criteria>

<output>
After completion, create `.planning/phases/02-gauntlet-harness-early/02-03-SUMMARY.md` recording: chosen FASTCHESS_RELEASE tag + asset filenames + SHA256 hashes per OS (for audit), 8moves_v3.pgn upstream commit SHA + license result, test pass count, and any license-related decisions (if 8moves_v3.pgn vendoring was deferred to runtime-fetch, document the reason).
</output>
