---
phase: 02-gauntlet-harness-early
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tools/fetch_fastchess.py
  - tools/books/8moves_v3.pgn
  - tools/books/SOURCES.md
  - tools/books/.fetch_fallback.json
  - tests/test_fetch_fastchess.py
  - .gitignore
autonomous: false
requirements:
  - GAUNT-01
  - GAUNT-05
user_setup: []

must_haves:
  truths:
    - "Running tools/fetch_fastchess.py on a clean cache downloads the pinned fastchess release, verifies its SHA256, and places the binary at tools/.cache/fastchess{.exe,} executable"
    - "Re-running tools/fetch_fastchess.py against an already-cached + checksum-valid binary is a no-op (idempotent fast path; exits 0 in under 1 s without a network call)"
    - "The script picks the correct asset for Windows / Linux / macOS based on platform.system()"
    - "Mismatched SHA256 raises SystemExit and removes the corrupt cached file (D-02 non-bypassable gate)"
    - "Opening-book provisioning produces EITHER tools/books/8moves_v3.pgn (vendored on LICENSE-compatible outcome) OR tools/books/.fetch_fallback.json (manifest with url + sha256 if vendoring is blocked by license incompatibility) — Plan 02-04b's resolve_opening_book() handles both paths transparently"
    - "Human checkpoint records the chosen FASTCHESS_RELEASE tag, the per-OS asset filenames, and the locally-computed SHA256s the executor pastes into the constants block — no asset hashes are auto-discovered without human verification (D-02 trust gate)"
    - "tools/.cache/, .planning/gauntlets/, v6_uci{,.exe} are gitignored — no binaries enter the repo"
  artifacts:
    - path: "tools/fetch_fastchess.py"
      provides: "Stdlib-only idempotent fetch+checksum+cache module for the fastchess binary"
      contains: "ensure_fastchess"
      min_lines: 60
    - path: "tools/books/8moves_v3.pgn"
      provides: "Vendored opening book seed (~34,700 positions, 16 plies deep) — produced ONLY when upstream license is compatible"
    - path: "tools/books/.fetch_fallback.json"
      provides: "Fallback manifest with {url, sha256} for fetch-on-demand when vendoring is blocked by license incompatibility (D-11 fallback path) — produced INSTEAD OF 8moves_v3.pgn in the license-blocked case; never both"
      contains: "sha256"
    - path: "tools/books/SOURCES.md"
      provides: "Provenance record: upstream URL + commit SHA + license confirmation + vendor date + final provisioning mode (vendored vs fallback-manifest)"
      contains: "official-stockfish/books"
    - path: "tests/test_fetch_fastchess.py"
      provides: "Unit tests for idempotency, checksum mismatch, OS routing (network mocked via monkeypatch)"
      contains: "test_idempotent_when_cached"
    - path: ".gitignore"
      provides: "Phase 2 ignores: tools/.cache/, .planning/gauntlets/, v6_uci binaries"
      contains: "tools/.cache/"
  key_links:
    - from: "tools/fetch_fastchess.py"
      to: "tools/.cache/fastchess{,.exe}"
      via: "ensure_fastchess() returns the executable Path; downloads + verifies on cache miss"
      pattern: "ensure_fastchess"
    - from: "tools/fetch_fastchess.py"
      to: "GitHub releases (Disservin/fastchess)"
      via: "urllib.request.urlopen — only on cache miss / checksum mismatch"
      pattern: "urllib.request"
    - from: "tools/books/{8moves_v3.pgn | .fetch_fallback.json}"
      to: "tools/gauntlet.py (Plan 02-04b resolve_opening_book)"
      via: "Plan 02-04b reads vendored PGN if present, else reads manifest and runs the same SHA256-gated fetch-and-cache flow"
      pattern: "resolve_opening_book"
---

<objective>
Stand up the fastchess binary provisioning module and vendor the opening book seed used by every Phase 2 gauntlet.

Purpose: D-01..D-04 require an idempotent, OS-aware, SHA256-gated fetcher modeled on the Syzygy download pattern (which, per RESEARCH §6, doesn't actually exist as a Python module in the repo — this plan creates the first one). D-11 + RESEARCH §5 recommend vendoring `8moves_v3.pgn` (small enough that ~1-2 MB once outweighs a second network dependency per gauntlet run); when upstream license blocks vendoring, fall back to a `.fetch_fallback.json` manifest that Plan 02-04b consumes transparently. D-03 mandates `tools/.cache/` is gitignored — the .gitignore change here also preemptively covers the v6_uci binary from Plan 02 to keep that plan's `.gitignore` touchpoint zero (avoids wave-bumping it).

Output: `tools/fetch_fastchess.py`, EITHER `tools/books/8moves_v3.pgn` OR `tools/books/.fetch_fallback.json`, `tools/books/SOURCES.md`, unit tests, and updated `.gitignore`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-gauntlet-harness-early/02-CONTEXT.md
@.planning/phases/02-gauntlet-harness-early/02-RESEARCH.md
@.planning/phases/02-gauntlet-harness-early/02-PATTERNS.md
@tools/gen_coeffs.py
@cli/src/index.js
@tests/test_v7_bindings.py
@.gitignore
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Acquire pinned fastchess release metadata + per-OS asset SHA256s (preparation for the human-checkpoint paste)</name>
  <files>tools/fetch_fastchess.py (constants-only skeleton, no values yet)</files>
  <read_first>
    - .planning/phases/02-gauntlet-harness-early/02-RESEARCH.md §3 (pinned release + checksum strategy — the planner picks a tag at plan time, downloads each per-OS asset manually once, records SHA256s in the script as a hardcoded dict — same trust model pip uses)
    - .planning/phases/02-gauntlet-harness-early/02-RESEARCH.md §6 (CANONICAL recommended shape — copy the `ensure_fastchess()` skeleton verbatim as the starting point)
    - tools/gen_coeffs.py lines 113-131 (stdlib-only CLI structure)
  </read_first>
  <action>
    Create tools/fetch_fastchess.py with the FULL module body (docstring, imports, helper functions, ensure_fastchess(), main()) per RESEARCH §6 — but leave the FASTCHESS_RELEASE tag and the three FASTCHESS_ASSETS sha256 fields as the SENTINEL string "PENDING_HUMAN_CHECKPOINT" (a literal that intentionally fails any regex check for hex 64). The constants block exists; the values are intentionally invalid until Task 2 (the human checkpoint) lands them.

    Acquire-and-stage workflow (executor performs these steps before Task 2, recording outputs in a scratch buffer for the human to validate):
      1. `curl -fsSL https://api.github.com/repos/Disservin/fastchess/releases/latest | python3 -c "import json,sys; r=json.load(sys.stdin); print('tag:', r['tag_name']); [print(a['name'], a['browser_download_url']) for a in r['assets']]"` — records the latest stable tag + asset filenames for all three OSes.
      2. For EACH per-OS asset, download via curl: `curl -fsSL -o /tmp/fc-<os>.<ext> <browser_download_url>` and compute `sha256sum /tmp/fc-<os>.<ext>` — record (asset_filename, sha256) pairs in a scratch buffer.
      3. DO NOT paste those SHA256s into the script yet. Surface the (tag, [(os, asset, sha256)]) tuple to the user via Task 2's checkpoint — the human re-runs sha256sum locally and pastes the verified hashes.

    The module shape per RESEARCH §6:
    - Module docstring citing D-01/D-02/D-03/D-04 + GAUNT-01.
    - `FASTCHESS_RELEASE = "PENDING_HUMAN_CHECKPOINT"`.
    - `FASTCHESS_ASSETS = {"Windows": {"asset": "PENDING_HUMAN_CHECKPOINT", "sha256": "PENDING_HUMAN_CHECKPOINT"}, "Linux": ..., "Darwin": ...}`.
    - `CACHE_DIR = Path(__file__).resolve().parent / ".cache"`.
    - `_asset_for_host()`, `_sha256()`, `ensure_fastchess()`, `main()` per the inlined skeleton at RESEARCH §6.
    - `ensure_fastchess()` MUST refuse to run if any constant is still "PENDING_HUMAN_CHECKPOINT" — `raise SystemExit("fetch_fastchess: human checkpoint not yet applied — see Plan 02-03 Task 2")`.
    - Atomic-write pattern: download to `dest.with_suffix(dest.suffix + ".part")` then `os.replace()` rename; checksum-mismatch unlinks the .part file and SystemExits.
    - POSIX `chmod +x` after successful checksum.
    - Use ONLY stdlib: hashlib, os, platform, stat, sys, urllib.request, pathlib.
  </action>
  <verify>
    <automated>test -f tools/fetch_fastchess.py && python3 -c "import ast; t=ast.parse(open('tools/fetch_fastchess.py').read()); names=[n.name for n in ast.walk(t) if isinstance(n,ast.FunctionDef)]; print('functions:', names); assert 'ensure_fastchess' in names and '_asset_for_host' in names and '_sha256' in names" && grep -q "PENDING_HUMAN_CHECKPOINT" tools/fetch_fastchess.py</automated>
  </verify>
  <acceptance_criteria>
    - tools/fetch_fastchess.py exists with the full module body
    - All four PENDING_HUMAN_CHECKPOINT sentinels are present (1 release tag + 3 OS sha256s — or 3 OS asset filenames + 3 sha256s = 7 sentinels if executor chose to gate asset filenames too)
    - ensure_fastchess() raises SystemExit immediately if any sentinel is present (PROOF: `python3 -c "from tools.fetch_fastchess import ensure_fastchess; ensure_fastchess()"` exits non-zero with the expected message)
    - The scratch buffer from steps 1-2 above is recorded for the next task's checkpoint
  </acceptance_criteria>
  <done>The fetcher skeleton is committed, intentionally refuses to run, and the executor holds (tag, asset filenames, sha256s) ready for human validation in Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: HUMAN CHECKPOINT — verify fastchess release + per-OS SHA256s, paste into constants block</name>
  <what-built>
    Task 1 created tools/fetch_fastchess.py with FASTCHESS_RELEASE + FASTCHESS_ASSETS[host].sha256 fields set to the sentinel literal "PENDING_HUMAN_CHECKPOINT". The executor scratched the latest release tag + per-OS asset filenames + per-OS SHA256s from `api.github.com/repos/Disservin/fastchess/releases/latest` and `curl -fsSL` + local `sha256sum`. Per D-02, those hashes MUST NOT enter the codebase on the say-so of the executor alone — they constitute the non-bypassable trust gate for every future binary download from this release. The human must independently verify the hashes via a second-path download (GitHub UI in a browser, with TLS chain visible) and locally re-compute sha256sum before pasting.
  </what-built>
  <how-to-verify>
    Step 1 — Confirm the release tag the executor picked. Open https://github.com/Disservin/fastchess/releases/latest in a browser. Verify the visible tag matches what the executor recorded. If a newer release dropped between scratch and now, decide whether to pin to the older recorded tag or update the executor's scratch.

    Step 2 — For EACH of the three per-OS assets (Windows .exe, Linux binary, macOS binary):
      - Click the asset link on the release page (this is the second-path download — browser TLS, not curl).
      - Save it to a known local path.
      - Run `sha256sum <path-to-asset>` on Linux/macOS, or `Get-FileHash -Algorithm SHA256 <path>` on Windows.
      - Compare the locally-computed SHA256 to the one the executor recorded in the scratch buffer.
      - They MUST match. If they don't, STOP — the asset may have been tampered with in transit or in the executor's curl, and the trust gate has detected real divergence. Surface as a blocking issue, do not paste any hash.

    Step 3 — Paste the verified values into tools/fetch_fastchess.py:
      - Replace `FASTCHESS_RELEASE = "PENDING_HUMAN_CHECKPOINT"` with the verified tag string (e.g. `FASTCHESS_RELEASE = "v0.10.0"`).
      - For each of the three OS keys in FASTCHESS_ASSETS, replace BOTH the `asset` filename (if it was sentinel) and the `sha256` value with the verified strings.
      - Save the file.

    Step 4 — Verify the fetcher now runs end-to-end on the local host:
      - Delete any prior cache: `rm -rf tools/.cache/`
      - Run: `python3 tools/fetch_fastchess.py`
      - It MUST print a path under tools/.cache/ and exit 0 (downloads the asset for THIS host's OS, verifies checksum against the value you just pasted, places the binary).
      - Run it a second time: it MUST exit 0 in under 1 second with NO new download (idempotent fast path).
      - On POSIX: `ls -l tools/.cache/fastchess` shows the executable bit.

    Step 5 — Acceptance:
      - `grep -c PENDING_HUMAN_CHECKPOINT tools/fetch_fastchess.py` returns 0 (no sentinels remain).
      - All sha256 values in FASTCHESS_ASSETS match `^[a-f0-9]{64}$`.
      - The fetcher's first invocation succeeds; the second is a sub-1-second no-op.
  </how-to-verify>
  <resume-signal>
    Type "approved — tag=<release-tag>" with the chosen tag (e.g. "approved — tag=v0.10.0") after pasting and verifying. If a hash mismatch was found in Step 2, type "halt — asset mismatch <details>" to escalate.
  </resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Vendor 8moves_v3.pgn (license-permitting) OR write .fetch_fallback.json + always write SOURCES.md + update .gitignore</name>
  <files>tools/books/8moves_v3.pgn, tools/books/.fetch_fallback.json, tools/books/SOURCES.md, .gitignore</files>
  <read_first>
    - .planning/phases/02-gauntlet-harness-early/02-RESEARCH.md §5 (8moves_v3.pgn vendor strategy — upstream URL, license check requirement, SOURCES.md provenance)
    - .planning/phases/02-gauntlet-harness-early/02-RESEARCH.md §10 RESOLVED (fetch-fallback manifest schema — Plan 02-04b's resolve_opening_book reads either the vendored PGN at tools/books/8moves_v3.pgn OR the manifest at tools/books/.fetch_fallback.json with shape `{"url": "...", "sha256": "..."}`; never both must exist simultaneously)
    - .planning/phases/02-gauntlet-harness-early/02-PATTERNS.md §"tools/books/8moves_v3.pgn (vendored asset)" (SOURCES.md provenance template)
    - .planning/phases/02-gauntlet-harness-early/02-PATTERNS.md §".gitignore (MOD)" (exact additive section format mirroring existing lines 67-81)
    - .gitignore (FULL — current file is 81 lines; new section appends after line 81; do NOT reorder existing lines)
  </read_first>
  <action>
    Three independent sub-artifacts. Sub-A and sub-B are mutually exclusive (exactly one runs depending on the license outcome); sub-C always runs.

    **Sub-A. License-compatible path (preferred): vendor tools/books/8moves_v3.pgn**

    - Download `8moves_v3.pgn.zip` from `https://github.com/official-stockfish/books/blob/master/8moves_v3.pgn.zip` (canonical source per RESEARCH §5).
    - Open the upstream repo's LICENSE file (https://github.com/official-stockfish/books/blob/master/LICENSE). Read the full text — do NOT just check the file name.
    - The license MUST be confirmed compatible with this repo's license (this repo has no LICENSE file at root as of plan time; default-permissive — confirm with a `find . -maxdepth 2 -iname LICENSE\*` first). Acceptable upstream licenses: MIT, BSD-2/3, Apache-2.0, CC0, public-domain. NOT acceptable without explicit user opt-in: GPL-3.0 (copyleft on derivative repo distribution), AGPL.
    - If compatible: unzip and commit the PGN ONLY (`tools/books/8moves_v3.pgn`, ~1-2 MB); DO NOT commit the .zip; DO NOT create `.fetch_fallback.json` (its absence signals "vendored").
    - If incompatible: skip sub-A entirely, fall through to sub-B.

    **Sub-B. License-blocked path (fallback): write tools/books/.fetch_fallback.json**

    - Compute the upstream URL + sha256: `curl -fsSL https://github.com/official-stockfish/books/raw/master/8moves_v3.pgn.zip -o /tmp/book.zip && sha256sum /tmp/book.zip`.
    - Write `tools/books/.fetch_fallback.json` with shape:
      ```
      {
        "url": "https://github.com/official-stockfish/books/raw/master/8moves_v3.pgn.zip",
        "sha256": "<64-hex>",
        "unzip_entry": "8moves_v3.pgn",
        "reason": "vendoring blocked — upstream LICENSE <name> incompatible with this repo (see SOURCES.md)"
      }
      ```
    - This file is intentionally checked into the repo (small JSON, no binary content, ~200 bytes); Plan 02-04b's resolve_opening_book() reads it on cache miss and runs the same SHA256-gated download flow that the fetcher uses for fastchess. The manifest is the contract between this plan and Plan 02-04b.

    **Sub-C. Always: tools/books/SOURCES.md provenance + .gitignore additions**

    Create tools/books/SOURCES.md with sections:
      - Upstream URL (with specific commit SHA pinned at vendor time via the upstream repo's commit log)
      - Vendor Date
      - License (full quoted excerpt from upstream LICENSE, NOT just the name)
      - License Compatibility Decision (one of: "VENDORED — compatible", "FALLBACK MANIFEST — incompatible: <details>")
      - Position Count (~34,700)
      - Ply Depth (16)
      - Final Provisioning Mode ("vendored at tools/books/8moves_v3.pgn" OR "fetch-fallback at tools/books/.fetch_fallback.json")

    Append to .gitignore (after line 81, mirroring lines 67-81 format):
    ```
    # Phase 2: gauntlet artifacts (downloaded binaries + run results)
    tools/.cache/
    .planning/gauntlets/
    src/chess_engine/engine/v6/v6_uci
    src/chess_engine/engine/v6/v6_uci.exe
    ```

    Do NOT add tools/books/ to .gitignore (the vendored PGN OR the manifest must enter the repo).
    Do NOT reorder pre-existing .gitignore lines. Do NOT remove existing entries.
    Do NOT touch pyproject.toml. Do NOT add a new dep group.
  </action>
  <verify>
    <automated>test -f tools/books/SOURCES.md && grep -q "official-stockfish/books" tools/books/SOURCES.md && grep -E -q '^(VENDORED|FALLBACK MANIFEST|vendored|fetch-fallback)' tools/books/SOURCES.md && (test -f tools/books/8moves_v3.pgn || test -f tools/books/.fetch_fallback.json) && ! (test -f tools/books/8moves_v3.pgn && test -f tools/books/.fetch_fallback.json) && grep -v '^#' .gitignore | grep -c "tools/.cache/" | grep -qx 1</automated>
  </verify>
  <acceptance_criteria>
    - tools/books/SOURCES.md exists; contains "official-stockfish/books", a commit SHA, and the License Compatibility Decision line
    - EXACTLY one of {tools/books/8moves_v3.pgn, tools/books/.fetch_fallback.json} exists; the other does NOT
    - If 8moves_v3.pgn exists: first line starts with `[Event "..."]`; file size 500 KB to 5 MB
    - If .fetch_fallback.json exists: valid JSON with keys {url, sha256, unzip_entry, reason}; sha256 matches `^[a-f0-9]{64}$`
    - .gitignore contains the four new lines (tools/.cache/, .planning/gauntlets/, v6_uci, v6_uci.exe) — appended AFTER existing line 81, no reordering
    - `grep -v '^#' .gitignore | grep -c "tools/.cache/"` returns exactly 1 (de-duplicated, no comment confusion)
  </acceptance_criteria>
  <done>Opening-book provisioning is complete in EITHER vendored or fallback-manifest mode; SOURCES.md records the decision; .gitignore covers all Phase 2 transient artifacts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Unit tests for fetch_fastchess.py — idempotency, checksum mismatch, OS routing</name>
  <files>tests/test_fetch_fastchess.py</files>
  <read_first>
    - tools/fetch_fastchess.py (post-Task-2 — sentinels replaced with real values; tests reference the actual constant names and function signatures)
    - .planning/phases/02-gauntlet-harness-early/02-PATTERNS.md §"tests/test_fetch_fastchess.py (NEW)" (three test cases listed: test_idempotent_when_cached, test_checksum_mismatch_raises, test_asset_for_host)
    - .planning/phases/02-gauntlet-harness-early/02-RESEARCH.md §9 "Phase Requirements → Test Map" rows for GAUNT-01
    - tests/test_v7_bindings.py (pytest idioms: tmp_path, monkeypatch, module-scope fixtures)
  </read_first>
  <behavior>
    - test_idempotent_when_cached: monkeypatch CACHE_DIR → tmp_path, monkeypatch FASTCHESS_ASSETS so "want_sha" matches a pre-staged file; monkeypatch urllib.request.urlopen to raise RuntimeError("no network call expected"); call ensure_fastchess(); assert returned Path == staged file
    - test_checksum_mismatch_raises: monkeypatch urlopen to return wrong bytes; assert SystemExit raised; assert the corrupt file at CACHE_DIR/fastchess{.exe,} no longer exists
    - test_asset_for_host_windows / _linux / _darwin: monkeypatch platform.system; assert correct tuple returned
    - test_asset_for_host_unsupported: monkeypatch platform.system → "FreeBSD"; assert SystemExit raised with message containing OS name
    - test_executable_bit_set_on_posix: skip on Windows; after fresh fetch, assert user-execute bit set
    - test_atomic_write_does_not_leak_partial: monkeypatch urlopen so response stream raises mid-read; assert no `*.part` file is left behind
    - test_constants_no_sentinels: assert FASTCHESS_RELEASE != "PENDING_HUMAN_CHECKPOINT" and every FASTCHESS_ASSETS[k]["sha256"] matches `^[a-f0-9]{64}$` (proves Task 2 checkpoint was applied)
  </behavior>
  <action>
    Create tests/test_fetch_fastchess.py per the behavior block above:
    - Import as `from tools import fetch_fastchess as ff` (ensure tools/__init__.py exists; tools/gen_coeffs.py already establishes tools/ as a package).
    - Use pytest fixtures `tmp_path` and `monkeypatch`. Stage synthetic CACHE_DIRs; monkeypatch `urllib.request.urlopen` with a function returning a context-manager exposing `.read()`. NO actual network in any test.
    - For test_idempotent_when_cached: stage a file at the expected dest, monkeypatch FASTCHESS_ASSETS[host]["sha256"] = _sha256(staged); assert urlopen was NEVER called.
    - For test_checksum_mismatch_raises: monkeypatch urlopen to return wrong bytes; assert SystemExit with "checksum mismatch"; assert dest file unlinked.
    - For test_executable_bit_set_on_posix: `@pytest.mark.skipif(platform.system() == "Windows", reason="chmod is POSIX-only")`; after synthetic fetch, assert `dest.stat().st_mode & stat.S_IXUSR`.
    - For test_atomic_write_does_not_leak_partial: monkeypatch urlopen to raise mid-read; assert SystemExit AND `list(CACHE_DIR.glob("*.part"))` is empty.
    - For test_constants_no_sentinels: import the module; assert no constant equals "PENDING_HUMAN_CHECKPOINT" — this test PINS the Task 2 checkpoint outcome.
    - Runtime budget: < 5 s total.
  </action>
  <verify>
    <automated>python3 -m uv run --group dev pytest -q tests/test_fetch_fastchess.py -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: tests/test_fetch_fastchess.py
    - `grep -c "def test_" tests/test_fetch_fastchess.py` returns at least 8
    - All tests PASS offline (monkeypatched urlopen)
    - Total runtime ≤ 5 s
    - No test actually downloads anything
    - Tests pass on POSIX and Windows (exec-bit test skips cleanly on Windows)
    - tools/__init__.py exists
  </acceptance_criteria>
  <done>All unit tests pass offline; the fetcher's behavior contract is fully captured; the constants-no-sentinels test pins the human-checkpoint outcome.</done>
</task>

</tasks>

<verification>
- `python3 tools/fetch_fastchess.py` downloads + checksum-verifies on first run; second run is a < 1 s no-op (after Task 2 checkpoint)
- EITHER `tools/books/8moves_v3.pgn` OR `tools/books/.fetch_fallback.json` is committed (never both)
- `tools/books/SOURCES.md` records provenance (URL, commit SHA, license, date, provisioning-mode decision)
- `.gitignore` covers `tools/.cache/`, `.planning/gauntlets/`, `v6_uci` binaries
- pytest tests/test_fetch_fastchess.py passes offline with no real network calls
</verification>

<success_criteria>
- fetch_fastchess.py is idempotent, OS-aware, SHA256-gated, stdlib-only
- Human checkpoint validated the per-OS SHA256s via second-path download (D-02 trust gate)
- Opening book provisioned via vendored PGN OR fallback manifest — Plan 02-04b consumes either path transparently
- Tests cover idempotency, checksum mismatch, OS routing, exec-bit, atomic-write, no-sentinels-remaining
- .gitignore is additive (no reordering, no removal)
</success_criteria>

<output>
After completion, create `.planning/phases/02-gauntlet-harness-early/02-03-SUMMARY.md` recording: chosen FASTCHESS_RELEASE tag + asset filenames + SHA256 hashes per OS (for audit), 8moves_v3.pgn provisioning mode (vendored vs fallback-manifest) + upstream commit SHA + license decision, test pass count, and the human-checkpoint resume-signal (so the audit trail captures who validated the hashes and when).
</output>
</output>
