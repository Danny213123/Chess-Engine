---
phase: 04-lazy-smp-texel-tuning
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tools/gauntlet.py
  - tools/gauntlet_core.py
  - tests/test_gauntlet_core.py
  - tests/test_gauntlet_io.py
  - .planning/phases/04-lazy-smp-texel-tuning/.continue-here.md
autonomous: false
requirements: [PAR-09]
preconditions:
  - id: PLAN-04-01-COMPLETE
    description: "Plan 04-01 merged AND PAR-07 + PAR-08 build-host gates marked PASSED in .planning/phases/04-lazy-smp-texel-tuning/.continue-here.md. PAR-09 measures the same V7 binary in 4t vs 1t self-play — meaningless without a working Lazy SMP binary."
    blocking: true
must_haves:
  truths:
    - "`tools/gauntlet.py run` accepts self-play V7-vs-V7 with different `Threads=` per side and forwards each side's setoption sequence to fastchess (the existing D-09 hard-deferral is lifted for the self-play case)"
    - "Per-side option forwarding (--engine-a-options Threads=4 / --engine-b-options Threads=1) is parsed in pure functions under tools/gauntlet_core.py so it is unit-testable without subprocess"
    - "PAR-09 gauntlet recipe is encoded as a bash one-liner inside .planning/phases/04-lazy-smp-texel-tuning/.continue-here.md (Shape B — matches Phase 1/3 precedent; no new YAML loader)"
    - "Recipe pins Phase 2 D-08/11/12 SPRT params: TC=10+0.1, c=1, 8moves_v3.pgn book, pentanomial SPRT elo0=0 elo1=10 alpha=0.05 beta=0.05, Hash=64 (matches Phase 2 Hash choice per RESEARCH Open Question 4)"
    - "Recipe specifies ≥500 games (D-10 floor; run 1000 for safety per CONTEXT D-10 + RESEARCH Plan 04-02 §Tasks)"
    - "After build-host run completes, Plan 04-02 summary records SPRT verdict (H1/H0), lower-bound Elo, game count, time-forfeit count"
    - "On PAR-09 PASS (lower-bound Elo >= 0 and verdict H1, meeting the >=40 Elo gain over the SPRT bounds): Plan 04-03 is unblocked"
    - "On PAR-09 FAIL: Bisect per Pitfall 5 (re-run Threads=2 vs Threads=1 first; if 2t-vs-1t also fails, suspect shared state from Pitfall 1 + re-open Plan 04-01)"
  artifacts:
    - path: "tools/gauntlet.py"
      provides: "Self-play V7-vs-V7 invocation with per-side Threads setoption — D-09 hard-deferral lifted for this case"
      contains: "--engine-a-options"
    - path: "tools/gauntlet_core.py"
      provides: "Pure parser/builder for per-side options (testable without subprocess)"
      contains: "parse_engine_options"
    - path: ".planning/phases/04-lazy-smp-texel-tuning/.continue-here.md"
      provides: "PAR-09 build-host gate recipe + result block"
      contains: "PAR-09"
  key_links:
    - from: "tools/gauntlet.py CLI"
      to: "tools/gauntlet_core.py pure-function parser"
      via: "argparse → parse_engine_options(arg_value) → list of (name, value) tuples"
      pattern: "parse_engine_options"
    - from: ".planning/phases/04-lazy-smp-texel-tuning/.continue-here.md PAR-09 recipe"
      to: "tools/gauntlet.py run subcommand"
      via: "bash one-liner invoking the CLI with the 1000-game self-play setup"
      pattern: "python3 tools/gauntlet.py run"
---

<objective>
Plan 04-02 lifts the existing D-09 hard-deferral in `tools/gauntlet.py run` for the self-play V7-vs-V7 case so PAR-09 (4-thread vs 1-thread same-binary gauntlet, ≥40 Elo via pentanomial SPRT) can run on the build host. The lift is narrow: add per-side `--engine-a-options Threads=N` / `--engine-b-options Threads=M` parsing (and any other per-side option forwarding required) so the same V7 binary plays itself with different thread counts. The PAR-09 recipe lands as a bash one-liner inside the Phase 4 `.continue-here.md` (Shape B per PATTERNS — matches Phase 1/3 precedent; no new YAML loader).

Purpose: PAR-09 isolates the SMP contribution. A failure here is an SMP bug — insufficient divergence, lockstep, shared mutable state, TT contention — and not a tuning bug. Per CONTEXT D-10, this gauntlet runs BEFORE Plan 04-03 (Texel pipeline) so any Elo delta in SC#6 attributes cleanly to the tune.

Output: an executable PAR-09 gauntlet recipe + pure-function plumbing in gauntlet_core.py that the executor can run on a build host and a human can review by approving the resulting summary.json + lower-bound Elo paste-back into `.continue-here.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-lazy-smp-texel-tuning/04-CONTEXT.md
@.planning/phases/04-lazy-smp-texel-tuning/04-RESEARCH.md
@.planning/phases/04-lazy-smp-texel-tuning/04-PATTERNS.md
@.planning/research/PITFALLS.md
@CLAUDE.md

# Phase 2 outputs Plan 04-02 builds on
@tools/gauntlet.py
@tools/gauntlet_core.py
@tests/test_gauntlet_core.py
@tests/test_gauntlet_io.py
@.planning/phases/02-gauntlet-harness-early/02-CONTEXT.md

# Phase 1/3 .continue-here.md format references
@.planning/phases/01-skeleton-smoke/.continue-here.md
@.planning/phases/03-lockless-tt-search-refinements-endgame/.continue-here.md

# Plan 04-01 .continue-here.md (file Plan 04-02 EXTENDS, not creates)
@.planning/phases/04-lazy-smp-texel-tuning/.continue-here.md

<interfaces>
<!-- The existing tools/gauntlet.py D-09 hard-deferral block — line range is the lift target. -->
<!-- Verified analog refs from PATTERNS line 41-43. -->

From tools/gauntlet.py:
  # Lines ~14-22: existing module-level defer flag.
  # Line ~100: D9_MESSAGE literal — "self-play V7-vs-V7 is deferred ..."
  # The `run` subcommand currently raises SystemExit with D9_MESSAGE for self-play invocations.
  # Plan 04-02 narrows the defer: allow self-play when both binaries are v7_uci
  # AND --engine-a-options / --engine-b-options are provided.

From tools/gauntlet_core.py (Phase 2 02-04a pure functions):
  # build_fastchess_command(...) → list[str]: builds the fastchess argv.
  # parse_stdout(...) / parse_pgn(...): result extractors.
  # ADD: parse_engine_options(opts_str: str) -> list[tuple[str, str]]
  #   accepts "Threads=4;Hash=64" or "Threads=4,Hash=64" — pick ONE separator; document.
  # ADD: build_fastchess_command must accept per-side options dicts and emit them as
  #      `-engine cmd=<bin> name=<n> option.Threads=4 option.Hash=64` per fastchess CLI.

From tools/fetch_fastchess.py:
  # Pinned fastchess release is the binary tools/gauntlet.py shells to; no change in Plan 04-02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lift D-09 self-play defer + add per-side options parsing (pure functions + I/O wiring + unit tests)</name>
  <files>tools/gauntlet.py, tools/gauntlet_core.py, tests/test_gauntlet_core.py, tests/test_gauntlet_io.py</files>
  <read_first>
    - tools/gauntlet.py (FULL FILE — locate the D9_MESSAGE literal + the defer block at lines 14-22; map every existing self-play guard so the lift is narrow and surgical)
    - tools/gauntlet_core.py (FULL FILE — read existing pure-function shapes: build_fastchess_command, parse_stdout, parse_pgn, sanity-verdict — mirror their style for the new parse_engine_options + per-side option wiring)
    - tests/test_gauntlet_core.py (existing pure-function unit test idiom — mirror exactly for the new parse_engine_options tests)
    - tests/test_gauntlet_io.py (existing subprocess/I-O test idiom — mirror for the new --engine-a-options / --engine-b-options CLI test)
    - .planning/phases/02-gauntlet-harness-early/02-CONTEXT.md (D-08/11/12 SPRT param spec — TC=10+0.1, c=1, 8moves_v3.pgn, pentanomial elo0=0 elo1=10 alpha=0.05 beta=0.05 — these are the EXACT values the new self-play case must emit)
    - .planning/phases/04-lazy-smp-texel-tuning/04-RESEARCH.md (§Plan 04-02 §Tasks T1/T2; Open Question 4 — Hash=64 for apples-to-apples)
    - .planning/phases/04-lazy-smp-texel-tuning/04-PATTERNS.md (assignment for tools/gauntlet.py — the narrow lift; assignment for gauntlets/*.yaml documents the Shape A vs Shape B choice — Plan 04-02 picks Shape B)
  </read_first>
  <behavior>
    - parse_engine_options("Threads=4;Hash=64") returns [("Threads", "4"), ("Hash", "64")] in order.
    - parse_engine_options("") returns [].
    - parse_engine_options("Threads=4") returns [("Threads", "4")].
    - parse_engine_options("Threads=;Hash=64") raises ValueError (empty value).
    - parse_engine_options("=4") raises ValueError (empty name).
    - parse_engine_options("invalid") raises ValueError (no `=` separator).
    - build_fastchess_command for self-play V7-vs-V7 with engine_a_options=[("Threads","4"),("Hash","64")] and engine_b_options=[("Threads","1"),("Hash","64")] emits exactly two `-engine` blocks with `option.Threads=4 option.Hash=64` and `option.Threads=1 option.Hash=64` respectively.
    - tools/gauntlet.py CLI parses `--engine-a-options "Threads=4;Hash=64"` and `--engine-b-options "Threads=1;Hash=64"` correctly.
    - tools/gauntlet.py CLI `run` no longer raises D-09 deferral when both --binary-a and --binary-b point at the same path AND both --engine-a-options and --engine-b-options are provided (narrow lift; the defer still fires for V6-vs-V7 cross-binary cases unless explicitly enabled elsewhere — out of scope for Plan 04-02).
    - SPRT defaults match Phase 2: --tc defaults to "10+0.1", --book defaults to tools/books/8moves_v3.pgn, --sprt-elo0 defaults to 0, --sprt-elo1 defaults to 10, --sprt-alpha defaults to 0.05, --sprt-beta defaults to 0.05, --pentanomial defaults to True (or these are passed through verbatim from CLI; either way they appear in the constructed fastchess command).
  </behavior>
  <action>
    (a) **Add `parse_engine_options(opts_str: str) -> list[tuple[str, str]]` to tools/gauntlet_core.py.** Pure function; document the separator choice (`;` recommended — `,` reads ambiguously with multi-value options). Raise `ValueError` with descriptive message on malformed input. Place alphabetically among existing helpers.

    (b) **Extend `build_fastchess_command` in tools/gauntlet_core.py** to accept `engine_a_options: list[tuple[str, str]] | None = None` and `engine_b_options: list[tuple[str, str]] | None = None`. For each provided option, append `option.<name>=<value>` to the corresponding `-engine` block in the fastchess argv. Keep existing signatures backwards-compatible — None defaults preserve current behavior so V7-vs-V6 Phase 2 invocations continue to work unchanged.

    (c) **Lift the D-09 hard-defer in tools/gauntlet.py for the self-play case** (narrow lift). Locate the existing defer block (around lines 14-22) + the D9_MESSAGE literal (around line 100). Refactor: the defer FIRES only when `binary_a == binary_b AND (engine_a_options is None OR engine_b_options is None)` — i.e., self-play is still deferred unless the caller has explicitly differentiated the two sides via per-side options. Add `--engine-a-options` and `--engine-b-options` argparse args to the `run` subcommand. Wire them through to gauntlet_core.parse_engine_options + build_fastchess_command. Leave V6-vs-V7 and V6-vs-V6 paths untouched.

    (d) **Add unit tests in tests/test_gauntlet_core.py** for every `<behavior>` bullet on parse_engine_options + build_fastchess_command per-side option wiring. Mirror existing test idiom.

    (e) **Add I/O test in tests/test_gauntlet_io.py** — invoke `python tools/gauntlet.py run --binary-a <fake>/v7_uci --binary-b <fake>/v7_uci --engine-a-options "Threads=4" --engine-b-options "Threads=1" --dry-run --games 10` (add a `--dry-run` flag if not already present — emits the constructed fastchess command to stdout and exits 0). Assert stdout contains both `option.Threads=4` and `option.Threads=1`.
  </action>
  <verify>
    <automated>python3 -m uv run --group dev pytest tests/test_gauntlet_core.py tests/test_gauntlet_io.py -x -q --tb=line 2>&amp;1 | tail -10 &amp;&amp; grep -c "parse_engine_options" tools/gauntlet_core.py &amp;&amp; grep -c "engine-a-options\|engine_a_options" tools/gauntlet.py</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_gauntlet_core.py tests/test_gauntlet_io.py -x -q` exits 0.
    - `grep -c "parse_engine_options" tools/gauntlet_core.py` returns >= 2 (function definition + at least one call/export).
    - `grep -c "engine_a_options\|engine-a-options" tools/gauntlet.py` returns >= 1 (CLI arg added).
    - The existing Phase 2 V6-vs-V7 and V6-vs-V6 invocations still construct the same fastchess command as before (backwards-compat — verify with at least one regression test in tests/test_gauntlet_core.py that the existing call path with engine_a_options=None produces the pre-Plan-04-02 output).
    - D9_MESSAGE defer block still fires for self-play WITHOUT per-side options (verify with a test_gauntlet_io.py case that asserts SystemExit + D9_MESSAGE on `--binary-a x --binary-b x` with no `--engine-a-options`).
  </acceptance_criteria>
  <done>parse_engine_options is testable in isolation; CLI accepts per-side option flags; D-09 defer narrows correctly; backwards-compat preserved; all unit + I/O tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Add PAR-09 gauntlet recipe + result block to Phase 4 .continue-here.md</name>
  <files>.planning/phases/04-lazy-smp-texel-tuning/.continue-here.md</files>
  <read_first>
    - .planning/phases/04-lazy-smp-texel-tuning/.continue-here.md (existing file from Plan 04-01 Task 3 — Plan 04-02 APPENDS the PAR-09 gate to it, does NOT recreate)
    - .planning/phases/01-skeleton-smoke/.continue-here.md (per-gate body pattern source)
    - .planning/phases/03-lockless-tt-search-refinements-endgame/.continue-here.md (single-gate format the new gate mirrors verbatim — Status / Why deferred / Reproduction / Pass criterion / Fail behavior subsections)
    - .planning/phases/04-lazy-smp-texel-tuning/04-PATTERNS.md (Shared Pattern 6; assignment for .continue-here.md — 4 checkpoints to land across Plan 04-02 + 04-04)
    - .planning/phases/02-gauntlet-harness-early/02-CONTEXT.md (D-08/11/12 SPRT param spec — verbatim source for the recipe params)
  </read_first>
  <behavior>
    - .continue-here.md gains a new section `## Plan 04-02 — PAR-09 self-play 4t-vs-1t SPRT gauntlet (PAR-09)` immediately after the Plan 04-01 PAR-08 gate.
    - Section contains exactly five subsections in this order: Status (DEFERRED on Windows dev host), Why this is deferred (no v7_uci binary on dev host + ≥500-game SPRT takes hours), Reproduction (bash block), Pass criterion (literal pass string), Fail behavior (literal fail string + bisection hint per Pitfall 5).
    - Reproduction bash block is a single command using the new --engine-a-options / --engine-b-options CLI from Task 1 with all Phase 2 SPRT params pinned + Hash=64 (apples-to-apples per RESEARCH Open Question 4) + games=1000 (safety over 500 floor) + output_dir=.planning/gauntlets/phase4-par09.
    - Pass criterion specifies: SPRT verdict H1 (≥10 Elo lower bound under elo1=10) AND lower-bound Elo >= 0 (4t is strictly not worse than 1t) — verdict bar is the SPRT contract, not a separate ≥40 Elo gate. The CONTEXT D-10 / ROADMAP SC#3 ≥40 Elo target is the point estimate target documented for the human reviewer; the SPRT bound is the formal gate.
    - Fail behavior includes the Pitfall 5 bisection hint: re-run Threads=2 vs Threads=1 first; if 2t-vs-1t also fails, suspect Pitfall 1 (shared mutable per-thread state) and re-open Plan 04-01.
    - Section has a Result subsection (empty initially; build-host human fills in after the run): SPRT verdict, games, point estimate Elo, lower-bound Elo, upper-bound Elo, time-forfeit count.
  </behavior>
  <action>
    APPEND (do not rewrite) the PAR-09 section to `.planning/phases/04-lazy-smp-texel-tuning/.continue-here.md`. Use Edit tool. Front-matter and existing PAR-07/PAR-08 sections from Plan 04-01 stay intact. The new section follows the verbatim Phase 3 per-gate pattern (Status / Why this is deferred / Reproduction / Pass criterion / Fail behavior). Bash reproduction block (Shape B per PATTERNS Layout Note 2):

    Command outline (executor materializes the actual command with current binary paths + the constants below):
    - Binary: build/v7_uci (the build-host artifact)
    - --binary-a build/v7_uci --binary-b build/v7_uci
    - --engine-a-options "Threads=4;Hash=64"
    - --engine-b-options "Threads=1;Hash=64"
    - --tc 10+0.1
    - --concurrency 1
    - --book tools/books/8moves_v3.pgn
    - --games 1000
    - --sprt-elo0 0 --sprt-elo1 10 --sprt-alpha 0.05 --sprt-beta 0.05
    - --pentanomial
    - --output-dir .planning/gauntlets/phase4-par09

    Pass criterion literal: `SPRT verdict: H1 accepted` AND lower-bound Elo printed in summary.json is >= 0; document the expected ≥40 Elo point estimate target in prose alongside.

    Fail behavior literal: `SPRT verdict: H0 accepted` OR lower-bound Elo < 0 → re-run with Threads=2 vs Threads=1 (same other params) to bisect; if 2t-vs-1t also FAILS, suspect Pitfall 1 (shared state) and re-open Plan 04-01 Task 2 (c) Worker lift; if 2t-vs-1t PASSES but 4t-vs-1t fails, suspect TT contention (Plan 04-01 PATTERNS open question — revisit Phase 3 D-07 TT sizing per Pitfall 5).
  </action>
  <verify>
    <automated>grep -c "## Plan 04-02 — PAR-09" .planning/phases/04-lazy-smp-texel-tuning/.continue-here.md &amp;&amp; grep -c "engine-a-options" .planning/phases/04-lazy-smp-texel-tuning/.continue-here.md &amp;&amp; grep -E "(--sprt-elo0 0|elo0=0)" .planning/phases/04-lazy-smp-texel-tuning/.continue-here.md &amp;&amp; grep -E "(--sprt-elo1 10|elo1=10)" .planning/phases/04-lazy-smp-texel-tuning/.continue-here.md &amp;&amp; grep -c "8moves_v3.pgn" .planning/phases/04-lazy-smp-texel-tuning/.continue-here.md &amp;&amp; grep -c "10+0.1" .planning/phases/04-lazy-smp-texel-tuning/.continue-here.md &amp;&amp; grep -c "1000" .planning/phases/04-lazy-smp-texel-tuning/.continue-here.md</automated>
  </verify>
  <acceptance_criteria>
    - File has exactly one `## Plan 04-02 — PAR-09` section header.
    - Reproduction block grep-confirms: `engine-a-options`, `engine-b-options`, `Threads=4`, `Threads=1`, `Hash=64`, `--tc 10+0.1`, `--book tools/books/8moves_v3.pgn`, `--games 1000`, `--sprt-elo0 0`, `--sprt-elo1 10`, `--sprt-alpha 0.05`, `--sprt-beta 0.05`, `--pentanomial`, `--output-dir .planning/gauntlets/phase4-par09`.
    - Pass criterion block contains the literal `SPRT verdict: H1`.
    - Fail behavior block contains the literal `Pitfall 5` AND mentions the Threads=2 bisection step.
    - Result subsection placeholder exists (empty rows for SPRT verdict / games / point Elo / lower-bound Elo / upper-bound Elo / time-forfeit count).
    - Plan 04-01's PAR-07 + PAR-08 sections are unchanged (no accidental rewrite).
  </acceptance_criteria>
  <done>PAR-09 recipe is human-runnable as a copy-paste bash one-liner; pass/fail literals + bisection guidance are in place for the build-host human reviewer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Build-host PAR-09 SPRT run + Result block paste-back</name>
  <files>.planning/phases/04-lazy-smp-texel-tuning/.continue-here.md, .planning/gauntlets/phase4-par09/summary.json</files>
  <read_first>
    - .planning/phases/04-lazy-smp-texel-tuning/.continue-here.md (Plan 04-02 PAR-09 section just added)
    - tools/gauntlet.py (verify --engine-a-options / --engine-b-options support is in place from Task 1)
    - .planning/phases/04-lazy-smp-texel-tuning/04-RESEARCH.md (Pitfall 5 bisection hint — what to do on fail)
  </read_first>
  <what-built>
    Plan 04-02 Tasks 1+2 are committed: tools/gauntlet.py + gauntlet_core.py support per-side options; the PAR-09 recipe lives in `.planning/phases/04-lazy-smp-texel-tuning/.continue-here.md`. The actual SPRT run (≥500-game pentanomial, hours of wall-clock) requires a quiet build host with v7_uci binary and fastchess, neither available on the Windows dev host.

    The developer runs the recipe on a build host, reviews `.planning/gauntlets/phase4-par09/summary.json`, and pastes the SPRT verdict / Elo bounds / time-forfeit count back into the Result subsection of the .continue-here.md PAR-09 section.
  </what-built>
  <how-to-verify>
    1. Verify the developer ran the EXACT bash command from the Reproduction block (no per-option drift; same TC, same book, same SPRT params, same games count).
    2. Verify `.planning/gauntlets/phase4-par09/summary.json` exists and is committed to git.
    3. Verify the PAR-09 Result subsection in .continue-here.md has been filled in with: SPRT verdict (must be `H1 accepted`), games played (must be > 0), point estimate Elo (target ≥40), lower-bound Elo (gate: >= 0), upper-bound Elo, time-forfeit count (sanity: 0 expected; any forfeit triggers manual investigation per GAUNT-07).
    4. If SPRT verdict is `H0 accepted` OR lower-bound Elo < 0: BLOCK the approval; the developer must perform the Pitfall 5 bisection (re-run Threads=2 vs Threads=1) and report results before this gate can pass. On bisection failure (2t-vs-1t also fails), Plan 04-02 is REJECTED → re-open Plan 04-01 Task 2 (c) per Fail behavior literal.
    5. Approve when SPRT verdict is H1 AND lower-bound Elo >= 0 AND time-forfeit count is 0.
  </how-to-verify>
  <resume-signal>Type "approved" with the SPRT verdict + Elo bounds + game count + time-forfeit count, OR describe the failure (verdict, bounds, bisection results, suspected pitfall). On failure, surface the bisection results so the next action (re-open 04-01 vs revisit TT sizing) is unambiguous.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → tools/gauntlet.py → subprocess fastchess | --engine-a-options / --engine-b-options strings are forwarded as fastchess `option.X=Y` args. Malformed input could mis-construct the fastchess command. |
| build-host human → committed summary.json | Result paste-back into .continue-here.md is human-curated; relies on the developer to faithfully transcribe summary.json fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | parse_engine_options(opts_str) input validation | mitigate | Pure function in gauntlet_core.py with explicit ValueError on empty name/value, missing `=`, or empty input. Task 1 unit tests verify each malformed-input case. |
| T-04-08 | Denial of Service | fastchess subprocess argv construction with attacker-controlled option strings | mitigate | Per-side options are forwarded as discrete argv tokens (NEVER concatenated into a shell string); subprocess.run uses list-form argv. Even if a malicious value is passed, it cannot escape into a shell context. Matches Phase 2 PATTERNS §Subprocess invocation discipline. |
| T-04-09 | Repudiation | summary.json result paste-back | accept | Build-host developer is the trust anchor (per Phase 1/2/3 .continue-here.md precedent). The committed summary.json is the audit trail; the .continue-here.md Result subsection is a human-readable digest. If the developer mis-transcribes, the git-committed summary.json wins per D-13 pattern. |

</threat_model>

<verification>
**Per-task commit (quick suite, no benchmark, no gauntlet):**

```bash
python3 -m uv run --group dev pytest tests/test_gauntlet_core.py tests/test_gauntlet_io.py -q
```

**Full Phase 1-3 + Plan 04-01 regression (per-plan merge):**

```bash
python3 -m uv run --group dev pytest -q -m "not benchmark and not gauntlet"
```

**Build-host gate (Plan 04-02 Task 3 — only place gauntlet runs):**

The bash one-liner in `.planning/phases/04-lazy-smp-texel-tuning/.continue-here.md` PAR-09 section.

**Phase 4 precondition (CHECK BEFORE STARTING ANY WORK):**

```bash
grep -E "Status:\s*(PASSED|GREEN)" .planning/phases/04-lazy-smp-texel-tuning/.continue-here.md | wc -l
# Must return >= 2 — both Plan 04-01 PAR-07 + PAR-08 gates PASSED
```
</verification>

<success_criteria>
Plan 04-02 is complete when:
- Plan 04-01 PAR-07 + PAR-08 gates are PASSED on build host (precondition)
- tools/gauntlet.py + gauntlet_core.py support per-side options (`--engine-a-options` / `--engine-b-options`) with unit + I/O test coverage
- The D-09 defer narrows correctly: self-play deferral fires only when per-side options are absent; backwards-compat preserved for Phase 2 invocation paths
- `.planning/phases/04-lazy-smp-texel-tuning/.continue-here.md` PAR-09 section has Status / Why deferred / Reproduction / Pass criterion / Fail behavior / Result subsections
- Build-host human ran the PAR-09 SPRT with the exact recipe params (Phase 2 D-08 alignment + Hash=64 + 1000 games) — `.planning/gauntlets/phase4-par09/summary.json` exists + is committed
- Result subsection filled in: SPRT verdict H1 + lower-bound Elo >= 0 + time-forfeit count 0 (or developer documented investigation per GAUNT-07)
</success_criteria>

<output>
After completion, create `.planning/phases/04-lazy-smp-texel-tuning/04-02-SUMMARY.md` capturing:
- The exact bash command run on the build host (verbatim from .continue-here.md Reproduction block)
- summary.json verdict + Elo bounds + games + time-forfeit count
- Whether bisection (Threads=2 vs Threads=1) was needed and its result
- Confirmation that Phase 2 V6-vs-V7 / V6-vs-V6 invocation paths still work (backwards-compat regression check)
- Path to the committed .planning/gauntlets/phase4-par09/summary.json
</output>
