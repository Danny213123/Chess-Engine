---
phase: 04-lazy-smp-texel-tuning
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - tools/texel-tuner/
  - tools/.cache/.texel-tuner.sha
  - tools/fetch_tuning_data.py
  - tools/filter_quiet_positions.py
  - tools/texel_tune.py
  - src/chess_engine/engine/v7/CMakeLists.txt
  - src/chess_engine/engine/v7/include/eval.hpp
  - src/chess_engine/engine/v7/coeffs.json
  - .planning/phases/04-lazy-smp-texel-tuning/.continue-here.md
  - tests/test_v7_texel_vendoring.py
  - tests/test_v7_fetch_tuning_data.py
  - tests/test_v7_quiet_filter.py
  - tests/test_v7_texel_k_fit.py
  - tests/test_v7_texel_pipeline.py
  - tests/test_v7_eval_quiet_parity.py
  - tests/test_v7_eval_static_link.py
autonomous: false
requirements: [TUNE-01, TUNE-02, TUNE-03, TUNE-04, TUNE-05, TUNE-06, TUNE-07, TUNE-08]
preconditions:
  - id: PLAN-04-02-PAR-09-PASSED
    description: "PAR-09 self-play gauntlet PASSED (SPRT H1 + lower-bound Elo >= 0) on build host per CONTEXT D-14 sequencing. SC#5 + SC#6 measure against the post-SMP V7 binary; tuning before SMP is verified wastes a tuning run on an unstable baseline."
    blocking: true
must_haves:
  truths:
    - "GediminasMasaitis/texel-tuner is vendored under tools/texel-tuner/ with the verified upstream SHA recorded in tools/.cache/.texel-tuner.sha (TUNE-01)"
    - "tools/fetch_tuning_data.py downloads Zurichess quiet-labeled.epd into tools/.cache/ idempotently with SHA256 verification mirroring tools/fetch_fastchess.py (TUNE-02)"
    - "tools/filter_quiet_positions.py emits tools/.cache/quiet-labeled.filtered.epd dropping in-check / qsearch!=eval / popcount<=TB_LARGEST positions (TUNE-03 / Pitfall 14 / D-08)"
    - "K-factor fit once via golden-section in K in [0.5, 2.0] tol=1e-4; persisted in coeffs.json `_meta.K` (TUNE-04 / D-09 / Pitfall 13)"
    - "Vendored tuner has sparse coefficient extraction enabled (TUNE-05; upstream feature verify-and-enable at Task 1)"
    - "Train/val split is deterministic 90/10 given seed; validation loss curve tracked alongside training (TUNE-06)"
    - "ADAM + L2 regularization wired with upstream defaults (b1=0.9, b2=0.999, lr=1.0, L2=1e-6 starting point) (TUNE-07)"
    - "Multi-seed driver tools/texel_tune.py runs Pesto-baseline + neutral-zeros seeds unconditionally; conditional 3rd perturbed seed iff post-tune coefficients diverge >5% between seeds 1+2 (TUNE-08 / D-09)"
    - "CMake v7_eval_static STATIC target bundles eval.cpp + coeffs.cpp + endgame.cpp + types.cpp (+ transitive eval deps verified at Task 1); v7_engine + v7_uci + vendored texel-tuner ALL link this single static lib (D-07; Pitfall 15 structurally impossible)"
    - "SC#5: eval_quiet(STARTPOS) returns bit-identical values from the Python tuner harness AND from v7_engine.evaluate() proves the codegen SSoT pipeline + v7_eval_static linkage have no name mismatches (Pitfall 15)"
  artifacts:
    - path: "tools/texel-tuner/"
      provides: "Vendored MIT-licensed C++ tuner source tree"
    - path: "tools/.cache/.texel-tuner.sha"
      provides: "Pinned upstream SHA for reproducibility"
      min_lines: 1
    - path: "tools/fetch_tuning_data.py"
      provides: "Zurichess EPD fetcher (mirrors tools/fetch_fastchess.py pattern)"
      min_lines: 100
    - path: "tools/filter_quiet_positions.py"
      provides: "Quietness filter (drops in-check / qsearch!=eval / TB-range positions)"
      min_lines: 60
    - path: "tools/texel_tune.py"
      provides: "Multi-seed driver wrapping vendored C++ tuner + K-fit harness"
      min_lines: 80
    - path: "src/chess_engine/engine/v7/CMakeLists.txt"
      provides: "v7_eval_static target + EXISTS-guarded add_subdirectory(tools/texel-tuner)"
      contains: "add_library(v7_eval_static STATIC"
    - path: "src/chess_engine/engine/v7/coeffs.json"
      provides: "_meta.K persisted after golden-section fit"
      contains: "\"K\":"
  key_links:
    - from: "tools/fetch_tuning_data.py"
      to: "tools/.cache/quiet-labeled.epd"
      via: "SHA256-verified atomic-rename download mirroring fetch_fastchess.py"
      pattern: "ZURICHESS_SHA256"
    - from: "tools/filter_quiet_positions.py"
      to: "tools/.cache/quiet-labeled.filtered.epd"
      via: "v7_engine.evaluate + qsearch parity check per Pitfall 14"
      pattern: "qsearch"
    - from: "tools/texel_tune.py"
      to: "tools/texel-tuner/build/texel-tuner (subprocess)"
      via: "per-seed subprocess invocation + loss-curve capture"
      pattern: "subprocess.run"
    - from: "src/chess_engine/engine/v7/CMakeLists.txt (v7_eval_static target)"
      to: "v7_engine + v7_uci + tools/texel-tuner/ all consumers"
      via: "target_link_libraries(<consumer> PRIVATE v7_eval_static)"
      pattern: "target_link_libraries.*v7_eval_static"
    - from: "tests/test_v7_eval_quiet_parity.py"
      to: "Python tuner harness AND v7_engine.evaluate(STARTPOS)"
      via: "side-by-side eval call with assertEquals SC#5 / Pitfall 15"
      pattern: "v7_engine.evaluate"
---

<objective>
Plan 04-03 lands the E2 Texel pipeline end-to-end: vendor the GediminasMasaitis/texel-tuner under `tools/`, refactor the V7 CMakeLists into a `v7_eval_static` STATIC target consumed by `v7_engine` + `v7_uci` + the vendored tuner (D-07 makes Pitfall 15 structurally impossible), add the Zurichess EPD fetcher + quiet-position filter + golden-section K-fit + multi-seed driver, persist `_meta.K` in `coeffs.json`, and verify the SC#5 eval parity test.

This is the largest plan in Phase 4 by surface area but the tuning RUN itself stays out of this plan the actual multi-seed tune happens on the build host and gets recorded in the Phase 4 .continue-here.md as a deferred gate (Plan 04-04 then handles the rescale + SC#6 + ship-checkpoint commit).

Purpose: produce all the plumbing the build-host human needs to run a reproducible Texel tune. CONTEXT D-06..D-09 lock the design; CMake architecture per D-07 / PATTERNS Shared Pattern 7; codegen pipeline (coeffs.json -> coeffs.cpp) already exists from Phase 1 D-10 and does not change.

Output: working `tools/texel_tune.py` that the build-host human invokes per-seed; a `v7_eval_static`-linked tuner binary that reads the same eval code as the engine binary; an SC#5 parity test that guarantees no name-mismatch drift between tuner-side and engine-side eval (Pitfall 15).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-lazy-smp-texel-tuning/04-CONTEXT.md
@.planning/phases/04-lazy-smp-texel-tuning/04-RESEARCH.md
@.planning/phases/04-lazy-smp-texel-tuning/04-PATTERNS.md
@.planning/research/PITFALLS.md
@CLAUDE.md

# Phase 1 outputs Plan 04-03 consumes
@src/chess_engine/engine/v7/coeffs.json
@src/chess_engine/engine/v7/tools/gen_coeffs.py
@src/chess_engine/engine/v7/CMakeLists.txt
@src/chess_engine/engine/v7/include/eval.hpp
@src/chess_engine/engine/v7/src/eval.cpp
@src/chess_engine/engine/v7/src/python_bindings.cpp

# Phase 2 patterns Plan 04-03 mirrors
@tools/fetch_fastchess.py
@tests/test_fetch_fastchess.py

# Plan 04-01 outputs (Lazy SMP binary is the tuning baseline)
@.planning/phases/04-lazy-smp-texel-tuning/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor texel-tuner + verify upstream layout + add v7_eval_static CMake target</name>
  <files>
    tools/texel-tuner/,
    tools/.cache/.texel-tuner.sha,
    src/chess_engine/engine/v7/CMakeLists.txt,
    src/chess_engine/engine/v7/include/eval.hpp,
    tests/test_v7_texel_vendoring.py,
    tests/test_v7_eval_static_link.py
  </files>
  <read_first>
    - .planning/phases/04-lazy-smp-texel-tuning/04-CONTEXT.md D-06 + D-07 (vendor under tools/texel-tuner/; v7_eval_static target name + bundling; pinned SHA in tools/.cache/.texel-tuner.sha)
    - .planning/phases/04-lazy-smp-texel-tuning/04-RESEARCH.md (Pattern 4 + Code Examples Pattern 4 full CMake sketch for v7_eval_static; Open Question 5 verify upstream layout first; Assumptions A1/A2/A3)
    - .planning/phases/04-lazy-smp-texel-tuning/04-PATTERNS.md (Shared Pattern 7 CMake additive-target discipline; assignment for CMakeLists.txt compose three existing patterns: coeffs add_custom_command at lines 43-70, v7_uci additive-target at lines 136-158, Fathom EXISTS-guard at lines 196-217)
    - src/chess_engine/engine/v7/CMakeLists.txt (FULL FILE locate V7_SOURCES at lines 99-116; locate the COEFFS_CPP add_custom_command at lines 43-70; locate the Fathom EXISTS-guard at lines 196-217 these are the verbatim templates)
    - src/chess_engine/engine/v7/include/eval.hpp (lines 36-46 existing evaluate() + evaluate_entry() declarations; add eval_quiet() alias)
    - src/chess_engine/engine/v7/src/eval.cpp (FULL FILE confirm what evaluate() does and verify eval_quiet aliasing is a one-line addition; identify transitive .cpp dependencies that must go into v7_eval_static eval.cpp, endgame.cpp, types.cpp, possibly board.cpp + movegen.cpp + magic.cpp if eval references attack tables)
    - tools/fetch_fastchess.py (Phase 2 02-03 SHA-pin pattern reference adapted: tuner SHA goes into a small text file, not inline in code, because tuner is a one-shot fetch on a build host)
    - GediminasMasaitis/texel-tuner upstream README (web fetch MANDATORY first step; verify MIT license, sparse extraction toggle name, eval-injection seam, CMake target name, default ADAM hyperparams closes Assumptions A1/A2/A3 + Open Question 5)
  </read_first>
  <action>
    (a) Verify upstream first (RESEARCH Open Question 5 close before vendoring). Use WebFetch on https://github.com/GediminasMasaitis/texel-tuner/blob/main/README.md (and the repository root) to confirm: (1) LICENSE is MIT, (2) the eval-injection seam exists (typically a `Eval.h` / `Eval.cpp` shim where the user's eval is plugged in), (3) sparse extraction is a build-time option or a default, (4) the CMake target name (likely `texel-tuner` per Assumption A3 record the exact name in tools/.cache/.texel-tuner.sha as a comment line), (5) ADAM defaults documented. Record findings in a brief comment block at the top of tools/.cache/.texel-tuner.sha.

    (b) Vendor the source tree. `git clone https://github.com/GediminasMasaitis/texel-tuner.git tools/texel-tuner` then `cd tools/texel-tuner && git checkout <verified-SHA> && rm -rf .git` so the tree is vendored as static source (not a submodule). Record the verified SHA + verification timestamp in `tools/.cache/.texel-tuner.sha` (format: one SHA per line + a comment line documenting the upstream URL + the verification timestamp + the upstream target name discovered in step (a)).

    (c) Refactor CMakeLists.txt for v7_eval_static. Compose the three existing patterns per PATTERNS Shared Pattern 7. Build order:
      - At the top of CMakeLists.txt (after the existing pybind11 + python find blocks, BEFORE pybind11_add_module(v7_engine ...)), add the `add_library(v7_eval_static STATIC ...)` target. Source list: eval.cpp + ${COEFFS_CPP} + endgame.cpp + KPK source + types.cpp + (verify at Task 1 by grep'ing eval.cpp includes) any of board.cpp / movegen.cpp / magic.cpp that eval.cpp transitively links via attack tables.
      - `target_include_directories(v7_eval_static PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/include)`.
      - `target_compile_features(v7_eval_static PUBLIC cxx_std_17)`.
      - `set_target_properties(v7_eval_static PROPERTIES POSITION_INDEPENDENT_CODE ON)` (REQUIRED pybind11 module links it, needs -fPIC on Linux).
      - Refactor V7_SOURCES (CMakeLists.txt:99-116) to REMOVE the files now in v7_eval_static (no duplicate compilation Pitfall 15 mitigation depends on a single .o per eval translation unit). KEEP non-eval sources (engine.cpp, search.cpp, thread_pool.cpp from Plan 04-01, board.cpp if not in static lib, perft.cpp, etc.).
      - `target_link_libraries(v7_engine PRIVATE v7_eval_static)` after the existing pybind11_add_module call.
      - `target_link_libraries(v7_uci PRIVATE v7_eval_static)` after the existing add_executable(v7_uci ...).
      - At end of CMakeLists.txt, add the EXISTS-guarded vendored tuner block per RESEARCH Pattern 4 sketch: `set(TEXEL_TUNER_DIR ${CMAKE_SOURCE_DIR}/tools/texel-tuner)` then `if(EXISTS ${TEXEL_TUNER_DIR}/CMakeLists.txt) add_subdirectory(${TEXEL_TUNER_DIR}); target_link_libraries(<verified-upstream-target-name> PRIVATE v7_eval_static) else() message(STATUS "[v7] texel-tuner not vendored; tuning binary disabled") endif()`. The upstream target name is whichever was verified in step (a).

    (d) Add eval_quiet alias in include/eval.hpp one-line `inline int eval_quiet(...) { return evaluate(...); }` or a simple re-export, depending on whether evaluate() already operates on quiet positions only (verify by reading eval.cpp if evaluate() runs qsearch internally, eval_quiet stays an alias; if not, eval_quiet calls the static-eval-only path documented in eval.cpp). Document the choice in a code comment.

    (e) Add tests/test_v7_texel_vendoring.py asserts: (1) tools/texel-tuner/ exists and contains LICENSE + CMakeLists.txt + a source tree, (2) tools/.cache/.texel-tuner.sha exists and contains a valid 40-char hex SHA (regex match `^[0-9a-f]{40}$` for the first non-comment line), (3) LICENSE contains the string "MIT".

    (f) Add tests/test_v7_eval_static_link.py runtime smoke: import v7_engine.evaluate(STARTPOS), assert it returns an int (proves v7_eval_static linked into the pybind11 module). Skip-on-missing-binary per Phase 2 PATTERNS line 444. If v7_uci binary is available on the test host, additionally subprocess `v7_uci position startpos\nisready\nquit` and assert exit code 0 + stdout contains `readyok` (proves v7_eval_static also linked into v7_uci with the same eval code).
  </action>
  <verify>
    <automated>test -d tools/texel-tuner && test -f tools/.cache/.texel-tuner.sha && grep -E "^[0-9a-f]{40}$" tools/.cache/.texel-tuner.sha && grep -c "add_library(v7_eval_static STATIC" src/chess_engine/engine/v7/CMakeLists.txt && grep -c "target_link_libraries(v7_engine PRIVATE v7_eval_static)" src/chess_engine/engine/v7/CMakeLists.txt && grep -c "target_link_libraries(v7_uci PRIVATE v7_eval_static)" src/chess_engine/engine/v7/CMakeLists.txt && grep -c "add_subdirectory(\${TEXEL_TUNER_DIR}" src/chess_engine/engine/v7/CMakeLists.txt && python3 -m uv run --group dev pytest tests/test_v7_texel_vendoring.py tests/test_v7_eval_static_link.py -q --tb=line 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tools/texel-tuner/ directory exists with LICENSE (containing "MIT") + CMakeLists.txt
    - tools/.cache/.texel-tuner.sha first non-comment line matches `^[0-9a-f]{40}$`
    - CMakeLists.txt grep counts: `add_library(v7_eval_static STATIC` >= 1, `target_link_libraries(v7_engine PRIVATE v7_eval_static)` == 1, `target_link_libraries(v7_uci PRIVATE v7_eval_static)` == 1, `add_subdirectory(${TEXEL_TUNER_DIR}` == 1
    - V7_SOURCES list in CMakeLists.txt no longer contains eval.cpp / endgame.cpp / types.cpp (those moved into v7_eval_static)
    - test_v7_texel_vendoring.py + test_v7_eval_static_link.py both PASS or properly skip-on-missing-binary
    - include/eval.hpp grep shows `eval_quiet` symbol exported
  </acceptance_criteria>
  <done>Texel-tuner vendored at pinned SHA; v7_eval_static target builds; v7_engine + v7_uci + vendored tuner all link the same eval object files; Pitfall 15 structurally impossible; vendoring + linkage tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Zurichess fetcher + quiet-position filter</name>
  <files>
    tools/fetch_tuning_data.py,
    tools/filter_quiet_positions.py,
    tests/test_v7_fetch_tuning_data.py,
    tests/test_v7_quiet_filter.py
  </files>
  <read_first>
    - .planning/phases/04-lazy-smp-texel-tuning/04-CONTEXT.md D-08 (filter_quiet_positions.py drops in-check / qsearch!=eval / popcount<=TB_LARGEST; idempotent; tuner reads filtered file directly)
    - .planning/phases/04-lazy-smp-texel-tuning/04-RESEARCH.md (Recommended Plan Shape Plan 04-03 task list; Code Examples on quietness filter)
    - .planning/phases/04-lazy-smp-texel-tuning/04-PATTERNS.md (Shared Pattern 5 atomic-rename SHA fetch reuse from fetch_fastchess.py; assignment for fetch_tuning_data.py)
    - .planning/research/PITFALLS.md Pitfall 14 (quietness definition: drop in-check + qsearch!=eval + TB-range)
    - tools/fetch_fastchess.py (FULL FILE direct template; reproduce atomic-rename + SHA256 verify; only differences are URL, SHA constant, destination filename)
    - tests/test_fetch_fastchess.py (FULL FILE direct template; reproduce idempotence + SHA mismatch error + atomic-rename assertions)
    - src/chess_engine/engine/v7/src/python_bindings.cpp (peek to confirm v7_engine.evaluate(fen) and v7_engine.qsearch(fen) bindings exist; if qsearch is not exposed, filter_quiet_positions.py implements the Stockfish-style check by comparing evaluate(fen) against a depth=0 captures-only search via the existing search binding)
  </read_first>
  <action>
    (a) Write tools/fetch_tuning_data.py: literal port of tools/fetch_fastchess.py with `ZURICHESS_URL`, `ZURICHESS_SHA256`, `DEST_FILENAME` constants swapped in. Source URL = the public Zurichess quiet-labeled.epd release (record actual URL + SHA via curl -sL <url> | sha256sum on first run; commit only the SHA constant + the URL constant). Destination = `tools/.cache/quiet-labeled.epd`. Idempotent: if dest exists and SHA matches, exit 0 silently. Otherwise download to `<dest>.tmp`, verify SHA256, atomic rename to dest. Mirror fetch_fastchess.py CLI surface (`--force` flag, exit codes, log format).

    (b) Write tools/filter_quiet_positions.py: read `tools/.cache/quiet-labeled.epd` line-by-line. For each EPD record parse FEN + result label (per Zurichess format). Drop position if: (1) `is_check(side_to_move)` (via v7_engine binding), (2) `popcount(occupied) <= TB_LARGEST` where TB_LARGEST is the value already used in src/chess_engine/engine/v7/src/syzygy.cpp (read at task start grep for `TB_LARGEST` to find the literal), (3) `qsearch(fen) != evaluate(fen)` per Pitfall 14 (Stockfish-style quietness via the v7_engine binding; if qsearch is not exposed as a Python binding, expose it as a one-line addition to python_bindings.cpp in the same task and document this in the file's header comment). Emit filtered records to `tools/.cache/quiet-labeled.filtered.epd` via atomic-rename. CLI: `--input <epd>`, `--output <epd>`, `--report` (emit drop-rate stats to stderr). Idempotent: if output exists AND newer than input AND filter-script unchanged (SHA of self embedded in output header), exit 0.

    (c) Write tests/test_v7_fetch_tuning_data.py: literal port of tests/test_fetch_fastchess.py asserting (1) idempotence (second call no-op), (2) SHA mismatch raises, (3) atomic rename (no half-written file on simulated failure). Use the same monkey-patching pattern that test_fetch_fastchess.py uses.

    (d) Write tests/test_v7_quiet_filter.py: 4 unit cases (small embedded EPD fixtures in the test file or under tests/fixtures/):
      - in-check position dropped
      - quiet position (qsearch == eval) kept
      - tactical position (qsearch != eval) dropped
      - TB-range position (e.g., KQK) dropped
      Use the v7_engine fixture pattern from Phase 1 (skip-on-missing-binary) per PATTERNS Shared Pattern 2.

    (e) Run both filter + fetch tests; verify quick suite remains under 30s budget (per VALIDATION.md sampling rate).
  </action>
  <verify>
    <automated>test -f tools/fetch_tuning_data.py && test -f tools/filter_quiet_positions.py && grep -c "ZURICHESS_SHA256" tools/fetch_tuning_data.py && grep -c "qsearch" tools/filter_quiet_positions.py && grep -c "TB_LARGEST\|popcount" tools/filter_quiet_positions.py && python3 -m uv run --group dev pytest tests/test_v7_fetch_tuning_data.py tests/test_v7_quiet_filter.py -q --tb=line 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tools/fetch_tuning_data.py mirrors fetch_fastchess.py structure (atomic rename + SHA256 verify + idempotence + --force flag)
    - tools/filter_quiet_positions.py drops in-check / qsearch!=eval / popcount<=TB_LARGEST per D-08; emits filtered EPD via atomic rename
    - Both unit test files PASS (or skip cleanly when v7_engine binary missing)
    - No test in the quick suite exceeds ~5 seconds (filter test uses small embedded fixtures, not the full 725k Zurichess set)
    - grep confirms `ZURICHESS_SHA256` constant in fetcher and `qsearch` + `TB_LARGEST|popcount` keywords in filter
  </acceptance_criteria>
  <done>Build-host human can run `python tools/fetch_tuning_data.py` then `python tools/filter_quiet_positions.py` to materialize `tools/.cache/quiet-labeled.filtered.epd` reproducibly; both tools idempotent; both have unit-test coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Golden-section K-fit + sparse extraction + ADAM/L2 wiring + train/val split</name>
  <files>
    tools/texel_tune.py,
    src/chess_engine/engine/v7/coeffs.json,
    tests/test_v7_texel_k_fit.py,
    tests/test_v7_texel_pipeline.py
  </files>
  <read_first>
    - .planning/phases/04-lazy-smp-texel-tuning/04-CONTEXT.md D-09 (K-fit golden-section in [0.5, 2.0] tol=1e-4 once; persisted in coeffs.json _meta.K; never refit per iteration per Pitfall 13)
    - .planning/phases/04-lazy-smp-texel-tuning/04-RESEARCH.md (Code Examples golden-section K-fit; sparse extraction is upstream feature toggle; train/val split deterministic 90/10; ADAM defaults b1=0.9 b2=0.999 lr=1.0 L2=1e-6)
    - .planning/phases/04-lazy-smp-texel-tuning/04-PATTERNS.md (Shared Pattern 8 subprocess discipline for invoking the C++ tuner binary)
    - .planning/research/PITFALLS.md (Pitfall 13 K refit; Pitfall 17 val loss is not strength)
    - src/chess_engine/engine/v7/coeffs.json (FULL FILE current schema; understand existing _meta.* keys if any; K goes under _meta.K)
    - src/chess_engine/engine/v7/tools/gen_coeffs.py (FULL FILE codegen pipeline coeffs.json -> coeffs.cpp; must round-trip after tuning)
    - tools/texel-tuner/ (vendored source from Task 1) inspect upstream main.cpp or main entry for CLI flags `--sparse`, `--lr`, `--l2`, `--seed`, `--train-val-split`, `--output-coeffs`; if names differ, adapt accordingly
  </read_first>
  <action>
    (a) Create tools/texel_tune.py the SINGLE-SEED entrypoint at this task (multi-seed driver wraps it in Task 4). CLI: `--seed-name <pesto|zeros|perturbed>`, `--input <filtered.epd>`, `--output-coeffs <coeffs.json>`, `--K <float>` (if omitted, runs golden-section fit first), `--iterations <N>`, `--lr <float>`, `--l2 <float>`, `--seed <int>`. Module-level constants: `K_RANGE = (0.5, 2.0)`, `K_TOL = 1e-4`, `TRAIN_VAL_SPLIT = 0.9`, `ADAM_B1 = 0.9`, `ADAM_B2 = 0.999`, `ADAM_LR = 1.0`, `L2_REG = 1e-6`.

    (b) Implement `fit_K(filtered_epd_path, eval_callable) -> float`: golden-section search in `K_RANGE` with `K_TOL` tolerance, loss = sigmoid(eval/K) MSE vs labels per the standard Texel formulation. Persist fitted K into `coeffs.json` under `_meta.K` (read existing coeffs.json, merge `_meta.K` + `_meta.K_fit_timestamp`, atomic-rename write). Per D-09 / Pitfall 13: this runs ONCE per coeffs.json baseline, NOT per iteration.

    (c) Implement deterministic 90/10 train/val split: `random.Random(seed).shuffle(positions)`, slice at 0.9*len. Both halves passed to the tuner subprocess; loss curves emitted for both. Pitfall 17 reminder: surface in the script header docstring that val loss alone is not strength val loss is a sanity gate, strength is the gauntlet.

    (d) Wire ADAM + L2 via subprocess invocation of the vendored tuner binary (built via Task 1's `add_subdirectory(tools/texel-tuner)`). Build texel_tune.py around `subprocess.run([texel_tuner_binary, "--input", filtered, "--seed", str(seed), "--lr", str(ADAM_LR), "--l2", str(L2_REG), "--sparse", "--train-val-split", "0.9", "--output-coeffs", out_json_path], check=True, capture_output=True, text=True)`. Adapt flag names to match what was verified in Task 1 step (a). Capture stdout (loss curves) into `tools/.cache/tune-results/<seed-name>-<run-id>/log.txt`.

    (e) Enable sparse extraction: confirm at Task 1 whether sparse mode is a build-time flag (rebuild via CMake `-DTUNER_SPARSE=ON`) or a runtime flag (`--sparse` CLI). If build-time, add the flag to the EXISTS-guarded `add_subdirectory` block from Task 1 and document in CMakeLists. If runtime, just pass `--sparse` from the Python driver.

    (f) Write tests/test_v7_texel_k_fit.py 3 cases: (1) `fit_K` on a tiny embedded EPD fixture converges within `K_TOL` of a known answer (verify by computing MSE manually at K*=1.0), (2) `fit_K` persists `_meta.K` into a tmp coeffs.json with atomic rename, (3) `fit_K` raises if input EPD path missing. Use the `tmp_coeffs_json` fixture from VALIDATION.md Wave 0 conftest.

    (g) Write tests/test_v7_texel_pipeline.py integration smoke: invoke `tools/texel_tune.py --seed-name zeros --input <tiny-fixture-epd> --iterations 5 --seed 42 --output-coeffs <tmp>` end-to-end (with the vendored tuner binary if available; SKIP otherwise). Assert: subprocess exit 0, output coeffs.json contains all original coefficient keys + `_meta.K`, training loss DECREASED between iter 0 and iter 5. Pitfall 17 reminder: this is a loss-decreased smoke, NOT a strength claim.

    (h) Document in tools/texel_tune.py header docstring the EXACT build-host invocation that Plan 04-04 calls (seed-1 Pesto, seed-2 zeros, conditional seed-3 perturbed) so Task 5 just copies the commands verbatim.
  </action>
  <verify>
    <automated>test -f tools/texel_tune.py && grep -c "K_RANGE = (0.5, 2.0)" tools/texel_tune.py && grep -c "K_TOL = 1e-4" tools/texel_tune.py && grep -c "TRAIN_VAL_SPLIT = 0.9" tools/texel_tune.py && grep -c "ADAM_B1" tools/texel_tune.py && grep -c "L2_REG" tools/texel_tune.py && grep -c "fit_K\|golden" tools/texel_tune.py && python3 -m uv run --group dev pytest tests/test_v7_texel_k_fit.py tests/test_v7_texel_pipeline.py -q --tb=line 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tools/texel_tune.py exists with all named constants (K_RANGE, K_TOL, TRAIN_VAL_SPLIT, ADAM_B1, ADAM_B2, ADAM_LR, L2_REG)
    - `fit_K` implements golden-section in K_RANGE with K_TOL tolerance and persists into coeffs.json `_meta.K`
    - Deterministic 90/10 train/val split via `random.Random(seed)` (TUNE-06)
    - ADAM + L2 wired via subprocess invocation of vendored tuner (TUNE-07)
    - Sparse extraction enabled (TUNE-05) build-flag or runtime flag, documented either way
    - test_v7_texel_k_fit.py + test_v7_texel_pipeline.py PASS (or skip-on-missing-tuner-binary cleanly)
    - coeffs.json after a tune run contains `_meta.K` key (verify via test 2)
    - Header docstring lists the exact build-host invocations Plan 04-04 will copy
  </acceptance_criteria>
  <done>Single-seed tune runs end-to-end on the build host; K persists in coeffs.json; sparse + ADAM + L2 + 90/10 split all wired; unit tests + integration smoke green.</done>
</task>

<task type="auto">
  <name>Task 4: Multi-seed driver wrapper + SC#5 eval_quiet parity test</name>
  <files>
    tools/texel_tune.py,
    tests/test_v7_eval_quiet_parity.py
  </files>
  <read_first>
    - .planning/phases/04-lazy-smp-texel-tuning/04-CONTEXT.md D-09 (multi-seed: Pesto + neutral-zeros unconditionally; conditional 3rd perturbed iff seeds 1+2 diverge >5% per-coeff); D-13 (TUNE-10 ship gate human checkpoint reviews summary.json from seed-decider)
    - .planning/phases/04-lazy-smp-texel-tuning/04-RESEARCH.md (Code Examples SC#5 parity test; multi-seed shape per_seed_outputs in tools/.cache/tune-results/<seed-name>/)
    - .planning/phases/04-lazy-smp-texel-tuning/04-PATTERNS.md (Shared Pattern 2 V7 native-engine fixture skip-on-missing)
    - .planning/research/PITFALLS.md Pitfall 15 (tuner-search name mismatch SC#5 is the structural test that catches drift even with v7_eval_static)
    - src/chess_engine/engine/v7/coeffs.json (default Pesto-baseline coefficients)
    - src/chess_engine/engine/v7/src/python_bindings.cpp (confirm v7_engine.evaluate(fen) returns the same value the C++ tuner sees via v7_eval_static)
    - tools/texel_tune.py (the single-seed entrypoint from Task 3 Task 4 ADDS a multi-seed `--multi-seed` mode that orchestrates 2-3 sequential single-seed runs)
  </read_first>
  <action>
    (a) Extend tools/texel_tune.py with a `--multi-seed` mode: when present, the script ignores `--seed-name` and instead runs:
      - Seed 1 = `pesto` (initialize from current `coeffs.json` Pesto baseline). Output dir: `tools/.cache/tune-results/seed-1-pesto/`. Subprocess call mirrors Task 3.
      - Seed 2 = `zeros` (initialize from a neutral-zeros coeffs.json synthesized on the fly: all PST values 0, mobility weights 1, king-safety weights 0). Output dir: `tools/.cache/tune-results/seed-2-zeros/`.
      - Conditional seed 3 = `perturbed` (gaussian noise sigma=10cp added to Pesto baseline). Run ONLY IF the divergence check between seeds 1 and 2 exceeds 5%. Output dir: `tools/.cache/tune-results/seed-3-perturbed/`.
      - Divergence metric: `max(abs(c1[k] - c2[k]) / max(abs(c1[k]), abs(c2[k]), 1.0))` across all coefficient keys `k`; if max > 0.05, trigger seed 3.

    (b) Emit a top-level `tools/.cache/tune-results/multi-seed-summary.json` after all seeds run, with per-seed: final train_loss, final val_loss, K_fit, divergence_pct vs seed 1, coeffs path. This file is what the Plan 04-04 seed-decider gauntlet ingests and what the human reviews per D-13.

    (c) Write tests/test_v7_eval_quiet_parity.py the SC#5 test (TUNE-05/Pitfall 15 structural mitigation):
      - For STARTPOS and 3-4 other quiet fixture positions (deterministic FEN list embedded in the test):
        - Call `v7_engine.evaluate(fen)` (the pybind11 binding linked against v7_eval_static)
        - Call a Python helper that loads the same `coeffs.json` and computes the same eval expression in pure Python (or, preferably, subprocess the vendored tuner with a `--print-eval <fen>` flag if upstream exposes one; otherwise call v7_engine.evaluate(fen) on a freshly-instantiated engine that loaded the SAME coeffs.json the tuner emitted, proving the round-trip)
        - Assert bit-identical results: `assert engine_val == tuner_val, f"eval_quiet drift on {fen}: engine={engine_val} tuner={tuner_val} (Pitfall 15 SC#5)"`
      - Skip-on-missing-tuner-binary per Phase 2 PATTERNS line 444.

    (d) Run the full quick suite to confirm everything is green (`python3 -m uv run --group dev pytest tests/ -q -m "not benchmark and not gauntlet"`).
  </action>
  <verify>
    <automated>grep -c "multi-seed\|--multi-seed" tools/texel_tune.py && grep -c "seed-1-pesto\|seed-2-zeros" tools/texel_tune.py && grep -c "0.05\|divergence" tools/texel_tune.py && grep -c "multi-seed-summary.json" tools/texel_tune.py && test -f tests/test_v7_eval_quiet_parity.py && grep -c "Pitfall 15\|SC#5\|eval_quiet" tests/test_v7_eval_quiet_parity.py && python3 -m uv run --group dev pytest tests/test_v7_eval_quiet_parity.py -q --tb=line 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - tools/texel_tune.py `--multi-seed` mode runs seed-1-pesto + seed-2-zeros unconditionally; seed-3-perturbed only if divergence >5%
    - tools/.cache/tune-results/multi-seed-summary.json schema documented in tools/texel_tune.py header docstring
    - tests/test_v7_eval_quiet_parity.py PASSES (or skip cleanly when v7_engine missing) on at least STARTPOS + 3 fixture FENs
    - Quick suite green: zero failures, zero unexpected skips
    - grep confirms divergence threshold `0.05` and `multi-seed-summary.json` artifact name in driver
  </acceptance_criteria>
  <done>Multi-seed driver ready for build-host invocation; SC#5 parity test proves engine and tuner read the same eval; full quick suite green; Plan 04-04 inherits a working tune pipeline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Build-host multi-seed tune run + commit to .continue-here.md (deferred gate)</name>
  <files>
    .planning/phases/04-lazy-smp-texel-tuning/.continue-here.md
  </files>
  <read_first>
    - .planning/phases/04-lazy-smp-texel-tuning/04-CONTEXT.md D-09 (multi-seed: Pesto + neutral-zeros unconditionally; conditional 3rd perturbed); D-13 (TUNE-10 ship gate human checkpoint Plan 04-04 commits winning coeffs.json)
    - .planning/phases/04-lazy-smp-texel-tuning/04-PATTERNS.md (Shared Pattern 6 build-host .continue-here.md deferred gate Shape B)
    - .planning/phases/03-lockless-tt-search-refinements-endgame/.continue-here.md (verbatim template: DEFERRED status + Why deferred + Reproduction bash block + Pass criterion)
    - tools/texel_tune.py header docstring (the exact invocations from Task 3 step (h))
  </read_first>
  <what-built>
    Tasks 1-4 landed the entire Texel pipeline plumbing: vendored tuner at pinned SHA, v7_eval_static CMake target (Pitfall 15 structurally impossible), Zurichess fetcher + quietness filter, single-seed K-fit + ADAM/L2/sparse/90-10-split harness, multi-seed driver, SC#5 parity test. The pipeline is fully exercisable on the build host but the actual multi-seed tune is hours-to-days of compute and a hard human-gate input for Plan 04-04 (D-13 ship-checkpoint).
  </what-built>
  <how-to-verify>
    This task adds the Plan 04-03 entry to `.planning/phases/04-lazy-smp-texel-tuning/.continue-here.md` describing the deferred build-host tune. It does NOT run the tune itself the orchestrator confirms the entry is committed and Plan 04-04 inherits the deferred-gate dependency.

    Append (or create, if Plan 04-02 Task 3 has not yet created the file) the following block to `.planning/phases/04-lazy-smp-texel-tuning/.continue-here.md`:

    ```markdown
    ## Plan 04-03 — Multi-seed Texel tune (DEFERRED to build host)

    **Status:** DEFERRED — requires build host with C++17 toolchain + Zurichess data

    **Why deferred:** Multi-seed Texel tune is ~hours-to-days of compute per seed on the build host; not feasible inside the GSD inner loop. Plan 04-04 (margin rescale + SC#6 gauntlet + ship-commit) consumes the `multi-seed-summary.json` + per-seed `coeffs.json` artifacts produced here.

    **Reproduction (build host):**

    ```bash
    # 1. Build v7 + texel-tuner + run vendoring + parity tests
    cmake -S src/chess_engine/engine/v7 -B build -DCMAKE_BUILD_TYPE=Release
    cmake --build build --parallel
    python3 -m uv run --group dev pytest tests/test_v7_texel_vendoring.py tests/test_v7_eval_static_link.py tests/test_v7_eval_quiet_parity.py -q

    # 2. Fetch + filter Zurichess
    python3 tools/fetch_tuning_data.py
    python3 tools/filter_quiet_positions.py --input tools/.cache/quiet-labeled.epd --output tools/.cache/quiet-labeled.filtered.epd --report

    # 3. Fit K once on baseline (Pesto coeffs.json) and persist to _meta.K
    python3 tools/texel_tune.py --seed-name pesto --input tools/.cache/quiet-labeled.filtered.epd --iterations 0 --seed 1

    # 4. Multi-seed tune (Pesto + neutral-zeros; conditional 3rd perturbed if seeds 1+2 diverge >5%)
    python3 tools/texel_tune.py --multi-seed --input tools/.cache/quiet-labeled.filtered.epd --iterations <N>
    ```

    **Pass criterion:**
    - `tools/.cache/tune-results/multi-seed-summary.json` exists with at least 2 seed entries
    - Each seed's `final_val_loss < initial_val_loss` (train + val both decreased then plateaued)
    - `coeffs.json` for each seed contains the same `_meta.K` value (K is fit once, shared across seeds)
    - If divergence >5% between seeds 1+2, summary.json has a `seed-3-perturbed` entry
    - SC#5 parity test (`tests/test_v7_eval_quiet_parity.py`) PASSED post-build on the build host

    **Artifact handoff to Plan 04-04:**
    - `tools/.cache/tune-results/seed-1-pesto/coeffs.json`
    - `tools/.cache/tune-results/seed-2-zeros/coeffs.json`
    - `tools/.cache/tune-results/seed-3-perturbed/coeffs.json` (if triggered)
    - `tools/.cache/tune-results/multi-seed-summary.json`

    **Reviewer:** Developer commits this block to `.continue-here.md`; Plan 04-04 reads it.
    ```

    After appending the block, commit the .continue-here.md change to git. Plan 04-04 frontmatter declares this as a hard precondition.
  </how-to-verify>
  <resume-signal>Type "approved" once .continue-here.md is appended + committed; or describe issues with the deferred-gate block.</resume-signal>
  <done>.continue-here.md contains the Plan 04-03 deferred-tune block with Reproduction bash + Pass criterion + Artifact handoff; committed to git; Plan 04-04's precondition is satisfied structurally (the actual build-host tune run happens out-of-band, not inside this plan).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream-github -> vendored-tuner | tools/texel-tuner/ source tree pulled from github at a pinned SHA; untrusted-network -> repo-source boundary |
| zurichess-mirror -> filter-input | tools/fetch_tuning_data.py downloads quiet-labeled.epd from a public mirror; untrusted-network -> training-data boundary |
| vendored-tuner-binary -> coeffs.json | C++ tuner subprocess emits coefficients consumed by the engine; subprocess-output -> evaluation-input boundary |
| build-host-developer -> coeffs.json commit | Plan 04-04 ship-checkpoint commits tuned coeffs to source; human-decision -> production-eval boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | tools/fetch_tuning_data.py (Zurichess EPD source) | mitigate | ZURICHESS_SHA256 constant + atomic-rename write per fetch_fastchess.py pattern (Phase 2 02-03); mismatch raises and refuses to overwrite cached file |
| T-04-11 | Tampering | tools/texel-tuner/ vendored source | mitigate | Pinned SHA in tools/.cache/.texel-tuner.sha; vendoring test asserts SHA matches regex `^[0-9a-f]{40}$`; LICENSE assertion confirms MIT (catches replacement with malicious fork) |
| T-04-12 | Spoofing | v7_engine.evaluate() vs vendored tuner eval (Pitfall 15) | mitigate | SC#5 parity test (tests/test_v7_eval_quiet_parity.py) asserts bit-identical evals across engine + tuner via v7_eval_static linkage; structural mitigation: same .o files in both consumers |
| T-04-13 | Information Disclosure | tools/.cache/ (training data, intermediate tunes, summary.json) | accept | tools/.cache/ is gitignored (existing convention from Phase 2 02-03); no PII in Zurichess set (public game records only); risk is low |
| T-04-14 | Tampering | src/chess_engine/engine/v7/coeffs.json post-tune | accept | TUNE-10 ship-checkpoint (Plan 04-04) is human-gated per D-13; developer reviews multi-seed-summary.json before committing; explicit acceptance: no automated coeff override path exists |
| T-04-15 | Denial of Service | Multi-seed tune subprocess wall-clock (hours-to-days) | accept | By design deferred to build host per D-09 + Pattern 6; NOT in inner loop; documented in .continue-here.md as DEFERRED status |
| T-04-16 | Repudiation | Per-seed tune provenance | mitigate | multi-seed-summary.json records seed name + final losses + divergence_pct + coeffs path; ties tuned coeffs back to a reproducible invocation |
</threat_model>

<verification>
## Plan-Level Verification

Run after Task 5 .continue-here.md commit:

```bash
# Wave 0 + structural sanity
python3 -m uv run --group dev pytest \
  tests/test_v7_texel_vendoring.py \
  tests/test_v7_eval_static_link.py \
  tests/test_v7_fetch_tuning_data.py \
  tests/test_v7_quiet_filter.py \
  tests/test_v7_texel_k_fit.py \
  tests/test_v7_texel_pipeline.py \
  tests/test_v7_eval_quiet_parity.py \
  -q --tb=line

# CMake structural assertions (no rebuild required)
grep -c "add_library(v7_eval_static STATIC" src/chess_engine/engine/v7/CMakeLists.txt
grep -c "target_link_libraries(v7_engine PRIVATE v7_eval_static)" src/chess_engine/engine/v7/CMakeLists.txt
grep -c "target_link_libraries(v7_uci PRIVATE v7_eval_static)" src/chess_engine/engine/v7/CMakeLists.txt
grep -c "add_subdirectory(\${TEXEL_TUNER_DIR}" src/chess_engine/engine/v7/CMakeLists.txt

# Vendoring SHA pin
grep -E "^[0-9a-f]{40}$" tools/.cache/.texel-tuner.sha

# .continue-here.md deferred-gate block landed
grep -c "Plan 04-03.*Multi-seed Texel tune" .planning/phases/04-lazy-smp-texel-tuning/.continue-here.md
```

All commands must exit 0 and grep counts must be >= 1.
</verification>

<success_criteria>
- TUNE-01 satisfied: texel-tuner vendored at pinned SHA under tools/texel-tuner/
- TUNE-02 satisfied: tools/fetch_tuning_data.py downloads + verifies Zurichess EPD
- TUNE-03 satisfied: tools/filter_quiet_positions.py drops in-check / qsearch!=eval / TB-range positions
- TUNE-04 satisfied: K fit once via golden-section, persisted in coeffs.json `_meta.K`
- TUNE-05 satisfied: sparse coefficient extraction enabled (build-flag or runtime flag)
- TUNE-06 satisfied: deterministic 90/10 train/val split via `random.Random(seed)`
- TUNE-07 satisfied: ADAM + L2 wired with documented defaults
- TUNE-08 satisfied: multi-seed driver runs Pesto + zeros + conditional perturbed
- SC#5 satisfied (structural): v7_eval_static + eval_quiet parity test makes Pitfall 15 detectable
- All 7 Wave 0 test files green or properly skip-on-missing-binary
- .continue-here.md contains the Plan 04-03 deferred-gate block, committed to git
- Plan 04-04 precondition (PLAN-04-03-DEFERRED-TUNE-DOCUMENTED) is structurally satisfiable
</success_criteria>

<output>
After completion, create `.planning/phases/04-lazy-smp-texel-tuning/04-03-SUMMARY.md` documenting:
- Verified upstream texel-tuner SHA + license + sparse-toggle name + ADAM defaults
- Final CMake structure (which .cpp files moved into v7_eval_static, which remained in V7_SOURCES)
- ZURICHESS_SHA256 constant + URL recorded
- TB_LARGEST value used by the quietness filter
- Per-seed output directory schema (`tools/.cache/tune-results/<seed-name>/`)
- multi-seed-summary.json schema (keys, types)
- Pass-through to Plan 04-04: the deferred .continue-here.md block + the artifact paths Plan 04-04 reads
- Any deviations from D-09 multi-seed shape (e.g., if upstream tuner CLI flag names differed)
</output>
