---
phase: 04-lazy-smp-texel-tuning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chess_engine/engine/v7/include/thread_pool.hpp
  - src/chess_engine/engine/v7/src/thread_pool.cpp
  - src/chess_engine/engine/v7/include/engine.hpp
  - src/chess_engine/engine/v7/src/engine.cpp
  - src/chess_engine/engine/v7/src/search.cpp
  - src/chess_engine/engine/v7/include/search.hpp
  - src/chess_engine/engine/v7/include/search/options.hpp
  - src/chess_engine/engine/v7/src/uci_main.cpp
  - src/chess_engine/engine/v7/CMakeLists.txt
  - tests/test_v7_lazy_smp.py
  - tests/test_v7_uci_threads.py
autonomous: false
requirements: [PAR-04, PAR-05, PAR-06, PAR-07, PAR-08]
preconditions:
  - id: PAR-03-TSAN-GATE
    description: "Phase 3 PAR-03 TSan 16t×60s stress gate cleared on build host (Gate 7 in .planning/phases/03-lockless-tt-search-refinements-endgame/.continue-here.md). Lazy SMP on a non-TSan-verified lockless TT = silent corruption (PITFALLS Pitfall 36). Executor MUST block at start of work until this gate's status in the Phase 3 .continue-here.md is marked passed."
    blocking: true
must_haves:
  truths:
    - "Engine::search internally distributes work across N std::thread workers when options_.Threads > 1, returns the same SearchResult shape as Threads=1"
    - "Each Worker owns value-typed copies of history / killers (via SearchStack) / counter_moves / continuation_history / capture_history (no shared mutable per-thread state — Pitfall 1)"
    - "Workers communicate only via the shared TT (Engine::tt_) and the shared atomic stop_flag_ (Engine::stop_flag_) — no other shared mutable state (PAR-05 literal)"
    - "Helper threads diverge from main via Berserk-style depth-stagger (skip_pattern + skip_phase arrays) using info.worker_id (PAR-06)"
    - "`setoption name Threads value N` clamps to [1,256], rejects non-integer with `info string`, resizes ThreadPool"
    - "Cancellation poll cadence is exactly 1024 nodes (not 4096) at both search.cpp poll sites (qsearch + alpha_beta) per Pitfall 10"
    - "Existing single-threaded tests (test_v7_engine.py, test_v7_search.py, test_v7_history.py, test_v7_tt_lockless.py) remain green — Threads=1 behavior bit-identical to pre-04-01"
    - "GIL release at python_bindings.cpp m.def site for search remains in place (FOUND-05 / Pitfall 11 — DO NOT regress)"
  artifacts:
    - path: "src/chess_engine/engine/v7/include/thread_pool.hpp"
      provides: "Worker struct + ThreadPool class (cv-wake state machine declaration)"
      contains: "struct Worker"
    - path: "src/chess_engine/engine/v7/src/thread_pool.cpp"
      provides: "ThreadPool::resize / start_search / wait_for_all / shutdown / helper_loop implementations"
      contains: "ThreadPool::start_search"
    - path: "src/chess_engine/engine/v7/include/engine.hpp"
      provides: "Engine grows `ThreadPool pool_` + `Worker worker0_`; per-thread tables lifted into Worker"
      contains: "ThreadPool pool_"
    - path: "src/chess_engine/engine/v7/include/search/options.hpp"
      provides: "EngineOptions.Threads field (int, default 1)"
      contains: "int Threads"
    - path: "tests/test_v7_lazy_smp.py"
      provides: "PAR-04 worker isolation + PAR-06 depth-stagger + PAR-07 NPS scaling + PAR-08 cancellation sentinels"
      min_lines: 80
    - path: "tests/test_v7_uci_threads.py"
      provides: "PAR-04 UCI setoption contract: Threads clamp, invalid-value warning, pool resize"
      min_lines: 30
  key_links:
    - from: "src/chess_engine/engine/v7/src/engine.cpp (Engine::search)"
      to: "src/chess_engine/engine/v7/src/thread_pool.cpp (ThreadPool::start_search)"
      via: "pool_.start_search(spec) then pool_.wait_for_all() then return worker0_ result"
      pattern: "pool_\\.start_search"
    - from: "src/chess_engine/engine/v7/src/search.cpp (iterative_deepening)"
      to: "Berserk SkipDepths/SkipPhases arrays"
      via: "info.worker_id indexes into per-helper skip table"
      pattern: "SkipDepths\\[.*worker_id"
    - from: "src/chess_engine/engine/v7/src/engine.cpp (Engine::set_option)"
      to: "ThreadPool::resize"
      via: "Threads case in dispatcher invokes pool_.resize(n)"
      pattern: "pool_\\.resize"
    - from: "src/chess_engine/engine/v7/src/search.cpp (qsearch + alpha_beta poll sites)"
      to: "1024-node poll cadence"
      via: "info.nodes % 1024 == 0 (was % 4096) — Pitfall 10"
      pattern: "% 1024"
---

<objective>
Plan 04-01 lifts V7's per-thread search state from `Engine` into a new `Worker` struct, adds a persistent cv-wake `ThreadPool` owned by `Engine`, wires Berserk-style depth-stagger divergence for helper threads, adds the `Threads` UCI/setoption surface (clamped to [1,256]), and tightens the cancellation poll cadence from 4096 → 1024 nodes at both search.cpp poll sites. PAR-07 NPS scaling + PAR-08 cancellation sentinels land alongside the code under `RUN_BENCHMARKS=1` gating.

Purpose: turn on the parallelism the codebase has been preparing for since Phase 3 03-01 D-03 (per-thread tables already declared on `Engine`). All primitives already exist (lockless Hyatt-Mann TT from 03-05; GIL release from FOUND-05; persistent search state from 03-01; setoption dispatcher from 03-01 Task 2). This plan is a structural lift, not a redesign.

Output: working Lazy SMP V7 binary where `setoption name Threads value 4` makes Engine::search spawn helpers that diverge via depth-stagger, share the lockless TT, and respect the existing cooperative cancellation contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-lazy-smp-texel-tuning/04-CONTEXT.md
@.planning/phases/04-lazy-smp-texel-tuning/04-RESEARCH.md
@.planning/phases/04-lazy-smp-texel-tuning/04-PATTERNS.md
@.planning/research/PITFALLS.md
@CLAUDE.md

# Phase 3 outputs Phase 4 consumes (HARD)
@src/chess_engine/engine/v7/include/engine.hpp
@src/chess_engine/engine/v7/src/engine.cpp
@src/chess_engine/engine/v7/include/search.hpp
@src/chess_engine/engine/v7/src/search.cpp
@src/chess_engine/engine/v7/include/search/options.hpp
@src/chess_engine/engine/v7/src/uci_main.cpp
@src/chess_engine/engine/v7/src/python_bindings.cpp

# Test pattern analogs (Phase 3 PATTERNS-canonical fixtures)
@tests/test_v7_engine.py
@tests/test_v7_tt_lockless.py
@tests/test_v7_history.py

# Hard precondition status
@.planning/phases/03-lockless-tt-search-refinements-endgame/.continue-here.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Use these directly — no codebase exploration required. -->

From src/chess_engine/engine/v7/include/engine.hpp (Phase 3 03-01 D-03 — the verbatim lift source):

  // Lines 113-148 — the per-thread state block that moves into Worker.
  // VALUE-TYPED COPIES, not references — Pitfall 1.
  TT          tt_{64};               // SHARED — stays on Engine (lockless XOR TT from 03-05)
  Board       board_;                // PER-WORKER — copy per worker as board_copy
  RepStack    rep_stack_;            // PER-WORKER (push/pop within tree; safer per-worker)
  SyzygyState syzygy_;               // SHARED — stays on Engine
  SearchStack search_stack_;         // PER-WORKER (includes triangular PV + killers)
  int  history_[2][64][64] = {};
  Move counter_moves_[2][64][64] = {};
  int  continuation_history_[2][6][64][2][6][64] = {};   // ~2.3 MB per worker
  int  capture_history_[2][6][64][6] = {};               // ~18 KB per worker
  std::atomic<bool>     stop_flag_{false};   // SHARED — stays on Engine
  std::atomic<uint64_t> nodes_{0};            // SHARED for reporting; per-worker counter also exists
  EngineOptions options_;                     // SHARED (read-only during search)

From src/chess_engine/engine/v7/include/search.hpp (the wiring contract; reset() preserves pointers):

  struct SearchInfo {
    std::atomic<bool>*  external_stop;
    RepStack*           rep_stack;
    TT*                 tt;
    SearchStack*        search_stack;
    int (*history)[64][64];                  // 3D, side-major
    Move (*counter_moves)[64][64];
    int (*continuation_history)[6][64][2][6][64];
    int (*capture_history)[6][64][6];
    const EngineOptions* options;
    int                 worker_id = 0;        // ADD if not present — used by depth-stagger
    int                 num_threads = 1;      // existing field (dead code today; repurpose OK)
    // ... other fields ...
    void reset();   // PRESERVES every non-owning pointer (search.hpp:208-212 contract)
  };

From src/chess_engine/engine/v7/src/search.cpp (current 4096-node poll sites — TIGHTEN TO 1024):

  // Line 79 (qsearch) and line 174 (alpha_beta) both have:
  //   if (info.stopped || (info.nodes % 4096 == 0 && info.check_time())) return 0;
  // Change both occurrences of `% 4096` → `% 1024` per Pitfall 10.

From src/chess_engine/engine/v7/src/uci_main.cpp:122-138 (option declaration block — alphabetical):

  // Add ONE line between "option name UseSingular ..." and "uciok":
  std::cout << "option name Threads type spin default 1 min 1 max 256\n";

From src/chess_engine/engine/v7/src/engine.cpp:245-343 (Engine::set_option dispatcher):

  // Add a new `else if (name_eq("Threads"))` case BEFORE the final "Unknown option" branch.
  // Pattern follows the existing 12-toggle dispatcher style (see UseNullMove case at line 267).

From src/chess_engine/engine/v7/src/python_bindings.cpp:46-54 (GIL release — DO NOT change):

  .def("search", &v7::Engine::search,
       py::arg("fen"),
       py::arg("depth")   = 64,
       py::arg("time_ms") = 5000,
       py::call_guard<py::gil_scoped_release>())   // FOUND-05 — MUST stay
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — test scaffolding (PAR-04/05/06 unit tests + PAR-07/PAR-08 sentinels + Threads UCI contract)</name>
  <files>tests/test_v7_lazy_smp.py, tests/test_v7_uci_threads.py</files>
  <read_first>
    - .planning/phases/04-lazy-smp-texel-tuning/04-PATTERNS.md (Shared Patterns 1 + 2 — RUN_BENCHMARKS skipif + V7 native-engine fixture, both canonical)
    - tests/test_v7_engine.py (lines 108-145 NPS sentinel skipif decorator; lines 44-105 threading + .stop pattern — verbatim copy targets)
    - tests/test_v7_tt_lockless.py (lines 20-50 import block + module-scoped v7_native_engine fixture — verbatim copy target)
    - tests/test_v7_history.py (peek_history accessor usage — confirms test surface stays via Engine accessors, not directly into Worker)
    - .planning/phases/04-lazy-smp-texel-tuning/04-RESEARCH.md (Code Examples §3 PAR-07 sentinel; §4 PAR-08 sentinel — full reference bodies)
  </read_first>
  <behavior>
    - Test test_pool_resizes_to_n: after `engine.set_option("Threads", "4")`, internal helper count is 3 (worker0 is main); after `set_option("Threads", "1")` helpers join. Use a public surface — e.g. an `engine.thread_count()` accessor that returns `options_.Threads` (add a one-line peek accessor in engine.hpp if needed).
    - Test test_threads_clamps_to_range: `set_option("Threads", "0")` and `set_option("Threads", "999")` and `set_option("Threads", "not-an-int")` each leave options.Threads unchanged AND emit `info string` on stderr/stdout (capture via capsys or v7_uci subprocess).
    - Test test_worker_state_isolation: search at Threads=4 + Threads=1 on same FEN, both return a legal best move; no crash; nodes counter monotone-increasing.
    - Test test_depth_stagger_table: with Threads=4, helpers do NOT all hit the same iteration count as main (proxy: collect per-worker depth-reached counters via a new `engine.peek_worker_depths()` accessor or via parsing UCI `info` lines — pick whichever is cheaper).
    - Test test_nps_scales_3x_4t_vs_1t (RUN_BENCHMARKS=1): nps ratio >= 3.0 on bench FEN STARTPOS at depth=12 / time_ms=5000.
    - Test test_cancellation_under_4_threads_under_50ms (RUN_BENCHMARKS=1): with Threads=4, deep search interrupted by engine.stop() returns within 50ms wall-clock.
    - Test test_existing_single_thread_tests_green (sanity): import test_v7_engine.test_nps_sentinel and run it as a smoke — proves Threads=1 default keeps behavior bit-identical.
  </behavior>
  <action>
    Create two NEW test files using the Shared Pattern 1 + 2 idioms (verbatim from existing canonical files, no invention). Both files MUST use module-scoped `v7_native_engine` fixture from PATTERNS Shared Pattern 2; NPS + cancellation tests MUST use the RUN_BENCHMARKS skipif decorator from PATTERNS Shared Pattern 1 (verbatim block). Each test name and assertion listed in `<behavior>` becomes one `def test_*` function. These tests are EXPECTED TO FAIL at Wave 0 (no ThreadPool yet) — that is the Nyquist scaffold; Tasks 2–6 turn them green.

    Add a one-line peek accessor `int thread_count() const { return options_.Threads; }` to engine.hpp public section (mirrors the existing peek_history / peek_continuation_history / peek_capture_history pattern at engine.hpp:158-172) so the unit tests have a public surface to query without poking at private members. Worker-depth peek is optional — if test_depth_stagger_table can be implemented via stdout UCI `info` parsing on a v7_uci subprocess, prefer that (no new C++ accessor needed); otherwise add a sibling peek accessor returning `worker0_.last_iteration_depth` plus a vector of helper depths.

    Register pytest markers `benchmark` and `gauntlet` in `pyproject.toml [tool.pytest.ini_options].markers` if not already present (VALIDATION.md sign-off requires it; check first with grep before editing).
  </action>
  <verify>
    <automated>python3 -m uv run --group dev pytest tests/test_v7_lazy_smp.py tests/test_v7_uci_threads.py --collect-only -q 2>&amp;1 | grep -E "test_(pool_resizes|threads_clamps|worker_state_isolation|depth_stagger_table|nps_scales|cancellation_under|existing_single)" | grep -v "^#" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `pytest --collect-only tests/test_v7_lazy_smp.py tests/test_v7_uci_threads.py` lists at minimum the 7 test names enumerated in `<behavior>`.
    - Each test file contains the verbatim `v7_native_engine` module-scoped fixture from PATTERNS Shared Pattern 2 (grep `scope="module"` + `ensure_available(auto_build=True)` succeeds in both files).
    - NPS + cancellation tests have the RUN_BENCHMARKS skipif decorator verbatim from PATTERNS Shared Pattern 1 (grep `RUN_BENCHMARKS` succeeds in test_v7_lazy_smp.py at least 2 times).
    - `pyproject.toml` declares markers `benchmark` and `gauntlet` under `[tool.pytest.ini_options].markers` (grep returns both).
    - All seven new tests are currently RED (expected — Wave 0). Verify by running quick suite and seeing the new collection items fail-or-error, not pass-by-accident.
  </acceptance_criteria>
  <done>Test files exist, import the v7_native_engine fixture verbatim, register pytest markers, and collect cleanly. RED state is correct.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ThreadPool + Worker primitives, lift per-thread tables, depth-stagger, Threads UCI option, tighten cancel poll</name>
  <files>
    src/chess_engine/engine/v7/include/thread_pool.hpp,
    src/chess_engine/engine/v7/src/thread_pool.cpp,
    src/chess_engine/engine/v7/include/engine.hpp,
    src/chess_engine/engine/v7/src/engine.cpp,
    src/chess_engine/engine/v7/src/search.cpp,
    src/chess_engine/engine/v7/include/search.hpp,
    src/chess_engine/engine/v7/include/search/options.hpp,
    src/chess_engine/engine/v7/src/uci_main.cpp,
    src/chess_engine/engine/v7/CMakeLists.txt
  </files>
  <read_first>
    - src/chess_engine/engine/v7/include/engine.hpp (lines 100-180 — full per-thread state block; the LIFT SOURCE — VERIFY field names + dimensions verbatim)
    - src/chess_engine/engine/v7/src/engine.cpp (lines 56-84 new_game memset block, lines 128-233 Engine::search body, lines 245-343 set_option dispatcher — the existing patterns to extend)
    - src/chess_engine/engine/v7/include/search.hpp (lines 36-38 forward-decls, lines 65-72 + 97-150 RepStack/SearchStack struct shape, lines 158-218 SearchInfo definition + reset() contract)
    - src/chess_engine/engine/v7/src/search.cpp (lines 79 qsearch poll, line 174 alpha_beta poll — the EXACT two callsites for % 4096 → % 1024)
    - src/chess_engine/engine/v7/include/search/options.hpp (lines 27-46 — existing 13-field EngineOptions struct; add Threads here)
    - src/chess_engine/engine/v7/src/uci_main.cpp (lines 119-139 — option declaration block; alphabetical order)
    - src/chess_engine/engine/v7/CMakeLists.txt (lines 99-116 V7_SOURCES list; thread_pool.cpp adds here; lines 175-185 tt_tsan_stress conditional target pattern as reference for build invariants)
    - src/chess_engine/engine/v7/src/python_bindings.cpp (lines 46-54 — VERIFY GIL release stays put; this task MUST NOT modify)
    - .planning/phases/04-lazy-smp-texel-tuning/04-PATTERNS.md (Shared Pattern 3 per-search SearchInfo wiring discipline; Shared Pattern 4 GIL release; Shared Pattern 7 CMake additive-target discipline)
    - .planning/phases/04-lazy-smp-texel-tuning/04-RESEARCH.md (Code Examples §1 Worker struct verbatim; §2 Threads UCI option; Pattern 1 cv-wake state machine sketch; Pattern 2 Berserk depth-stagger; Pattern 3 cancellation poll)
    - Berserk thread.c upstream (fetch via web — verify current SkipDepths/SkipPhases arrays; documented `[ASSUMED]` constants in RESEARCH Pattern 2 are starting points only)
  </read_first>
  <behavior>
    - After Task 2, test_pool_resizes_to_n passes: set_option("Threads", "4") creates 3 helper std::threads parked on cv_start_; set_option("Threads", "1") joins them.
    - test_threads_clamps_to_range passes: Threads=0, Threads=999, Threads="abc" each leave options.Threads at its prior value AND emit `info string Threads ...` to stdout.
    - test_worker_state_isolation passes: search at Threads=4 returns same SearchResult shape as Threads=1; per-worker history/counter_moves/continuation_history/capture_history are byte-distinct memory addresses (no aliasing).
    - test_depth_stagger_table passes: helper depths diverge from main depths by at least one iteration on a 5-second search.
    - PAR-07 (RUN_BENCHMARKS=1): nps ratio 4t/1t >= 3.0 on STARTPOS at depth=12 time_ms=5000.
    - PAR-08 (RUN_BENCHMARKS=1): cancellation latency < 50ms with Threads=4 on a deep stress FEN.
    - All Phase 1-3 single-threaded tests stay green at default Threads=1 (test_v7_engine.py, test_v7_search.py, test_v7_history.py, test_v7_tt_lockless.py).
  </behavior>
  <action>
    Implement the lift in five sub-steps, in this order (each step keeps the build green):

    (a) **Define Worker struct + ThreadPool API in `include/thread_pool.hpp`.** Mirror PATTERNS assignment for thread_pool.hpp: forward-declare `class TT; struct EngineOptions; struct SyzygyState; class Board;` inside `namespace v7`. Define `struct Worker` with value-typed fields exactly as RESEARCH Code Example §1: `Board board_copy; RepStack rep_stack; SearchStack search_stack; int history[2][64][64] = {}; Move counter_moves[2][64][64] = {}; int continuation_history[2][6][64][2][6][64] = {}; int capture_history[2][6][64][6] = {}; SearchInfo info; uint64_t nodes = 0; int worker_id = 0; TT* shared_tt = nullptr; std::atomic<bool>* shared_stop = nullptr; const EngineOptions* shared_options = nullptr; SyzygyState* shared_syzygy = nullptr;`. Define `class ThreadPool` with `resize(int n)`, `start_search(const SearchSpec&)`, `wait_for_all()`, `shutdown()` and private `helper_loop(Worker&)` — declare `SearchSpec` as a small POD holding the search request (fen / depth / time_ms / max_depth). Owner choice (per CONTEXT Claude's Discretion): keep state machine inline in ThreadPool — no separate `WorkerBarrier` split.

    (b) **Implement `src/thread_pool.cpp` cv-wake state machine.** Pure original code, NO copy from Stockfish (GPL). RAII locks via `std::lock_guard` / `std::unique_lock`. `resize(n)`: if growing, append new Worker + spawn std::thread(helper_loop, ref(*this), ref(worker)); if shrinking, set shutting_down_ + broadcast + join the excess + drop them. `start_search(spec)`: ++generation_; active_count_ = n_helpers; cv_start_.notify_all(); then run main worker (worker0_) inline. `helper_loop`: `for(;;) { unique_lock lk(mtx_); cv_start_.wait(lk, [&]{ return helper_gen_seen_[id] < generation_ || shutting_down_; }); if (shutting_down_) return; helper_gen_seen_[id] = generation_; lk.unlock(); run_search_on_worker(w, spec); { lock_guard l(mtx_); --active_count_; } cv_done_.notify_all(); }`. `wait_for_all()`: `cv_done_.wait(lk, [&]{ return active_count_ == 0; })`. `shutdown()`: shutting_down_ = true; cv_start_.notify_all(); join all helper threads. `~ThreadPool()`: calls shutdown(). Allow std::thread ctor failure (OS thread cap) to propagate — match the V7 "fail loud on OS resource exhaustion" convention.

    (c) **Lift per-thread tables from `Engine` into `Worker` in `include/engine.hpp` + `src/engine.cpp`.** In engine.hpp: remove the fields `board_`, `rep_stack_`, `search_stack_`, `history_`, `counter_moves_`, `continuation_history_`, `capture_history_` from the private block at lines 117-145 (KEEP `stop_flag_`, `nodes_`, `tt_`, `syzygy_`, `options_`). Add `ThreadPool pool_;` and `Worker worker0_;` as private members. Re-route the existing public peek accessors (`peek_history`, `peek_continuation_history`, `peek_capture_history` at lines 155-172) to read from `worker0_` instead of the removed engine fields — minimum-blast-radius refactor so test_v7_history.py keeps passing without edits. Add `int thread_count() const { return options_.Threads; }` accessor for Task 1's unit tests. In engine.cpp: `Engine::new_game()` at lines 56-84 — replace `std::memset(history_, 0, sizeof(history_))` (and the 3 sibling memsets) with a per-worker loop over `pool_` AND worker0_. `Engine::search` body (lines 128-233): set up shared spec (fen / depth / time_ms / iteration max), call `pool_.start_search(spec)` (which posts to helpers and runs main inline on worker0_), call `pool_.wait_for_all()`, then return worker0_'s SearchResult (the main thread's result is the canonical pick; helpers only seed the TT — standard Lazy SMP). Signature MUST NOT change (RESEARCH Open Question 3 RESOLVED). The per-worker SearchInfo wiring uses PATTERNS Shared Pattern 3 verbatim — `info.history = &w.history;` etc. (per-worker); `info.tt = &engine.tt_; info.external_stop = &engine.stop_flag_; info.options = &engine.options_;` (shared); then `info.reset()` (preserves all pointers per search.hpp:208-212 contract).

    (d) **Wire Berserk depth-stagger in `src/search.cpp` iterative_deepening + add `worker_id` to `SearchInfo`.** In search.hpp at the existing `int num_threads = 1;` (currently dead code), add a sibling `int worker_id = 0;`. In search.cpp `iterative_deepening`, add `static constexpr int SkipDepths[20] = {...}; static constexpr int SkipPhases[20] = {...};` (FETCH the current arrays from Berserk's upstream `thread.c` before committing — RESEARCH §Pattern 2 `[ASSUMED]` constants must be verified; if upstream Berserk is unreachable, use the canonical published arrays `{1,1,2,2,2,3,3,3,4,4,4,5,5,5,6,6,6,7,7,7}` and `{0,1,0,1,2,0,1,2,0,1,2,0,1,2,0,1,2,0,1,2}` from RESEARCH Code Example §2 and record the choice in a code comment). At the top of the iterative-deepening loop add the helper-skip gate from PATTERNS assignment for search.cpp: `if (info.worker_id > 0) { int idx = (info.worker_id - 1) % 20; if ((d + SkipPhases[idx]) % SkipDepths[idx] != 0) continue; }`.

    (e) **Tighten cancellation poll cadence + add Threads UCI surface.** In search.cpp: change `% 4096` → `% 1024` at exactly two callsites — line 79 (qsearch) and line 174 (alpha_beta). Per Pitfall 10. In include/search/options.hpp at the end of the EngineOptions struct (after `UseFortressEval`): add one line `int Threads = 1;  // Plan 04-01 D-04 — Lazy SMP worker count; valid range [1, 256]`. In src/uci_main.cpp at the option declaration block (alphabetical between `option name UseSingular` and `uciok`): add `std::cout << "option name Threads type spin default 1 min 1 max 256\n";`. In src/engine.cpp Engine::set_option dispatcher BEFORE the final "Unknown option" else branch: add the `else if (name_eq("Threads"))` case from RESEARCH Code Example §2 — parses int via `std::stoi`, catches and emits `info string Invalid value for Threads: <value>`, range-checks `[1, 256]` and emits `info string Threads out of range [1,256]: <value>`, then `options_.Threads = n; pool_.resize(n); return;`. CRITICAL: do NOT modify `src/python_bindings.cpp` — verify the `py::call_guard<py::gil_scoped_release>()` at lines 46-54 stays intact (FOUND-05 / Pitfall 11).

    (f) **CMakeLists update.** Add `src/thread_pool.cpp` to the V7_SOURCES list (lines 99-116) — additive only, no removals. Do NOT add `v7_eval_static` — that target is Plan 04-03's sole responsibility (CONTEXT D-07; PATTERNS Shared Pattern 7).
  </action>
  <verify>
    <automated>python3 -m uv run --group dev pytest tests/test_v7_lazy_smp.py tests/test_v7_uci_threads.py tests/test_v7_engine.py tests/test_v7_search.py tests/test_v7_history.py tests/test_v7_tt_lockless.py -x -q --tb=line 2>&amp;1 | tail -20 &amp;&amp; grep -c "% 1024" src/chess_engine/engine/v7/src/search.cpp &amp;&amp; grep -c "% 4096" src/chess_engine/engine/v7/src/search.cpp</automated>
  </verify>
  <acceptance_criteria>
    - All 7 new Task 1 tests turn GREEN (with benchmark-gated 2 skipped without RUN_BENCHMARKS=1).
    - All Phase 1-3 single-threaded V7 tests stay GREEN at default Threads=1 — zero regressions in test_v7_engine.py, test_v7_search.py, test_v7_history.py, test_v7_tt_lockless.py.
    - Exact-count grep: `grep -c "% 1024" src/chess_engine/engine/v7/src/search.cpp` returns >= 2 AND `grep -c "% 4096" src/chess_engine/engine/v7/src/search.cpp` returns 0 (no stale 4096 callsite remaining; Pitfall 10 enforcement).
    - `grep -c "py::call_guard&lt;py::gil_scoped_release&gt;" src/chess_engine/engine/v7/src/python_bindings.cpp` returns >= 1 (FOUND-05 not regressed).
    - `grep -c "int Threads" src/chess_engine/engine/v7/include/search/options.hpp` returns exactly 1.
    - `grep -c "option name Threads type spin default 1 min 1 max 256" src/chess_engine/engine/v7/src/uci_main.cpp` returns exactly 1.
    - `grep -c "name_eq(\"Threads\")" src/chess_engine/engine/v7/src/engine.cpp` returns exactly 1 (dispatcher case).
    - `grep -c "pool_\\.resize" src/chess_engine/engine/v7/src/engine.cpp` returns >= 1 (set_option case wires resize).
    - `grep -c "SkipDepths\\|SkipPhases" src/chess_engine/engine/v7/src/search.cpp` returns >= 2 (depth-stagger arrays present).
    - `grep -rn "history_\\[" src/chess_engine/engine/v7/src/engine.cpp src/chess_engine/engine/v7/include/engine.hpp | grep -v worker0_ | grep -v "^.*://" | grep -v "^#"` returns no matches OUTSIDE peek accessors and worker0_ references (lifted cleanly).
    - CMake build succeeds: `cmake --build build --target v7_engine` exits 0 (when build dir is present on the dev host) — if no build dir is available on the dev host, this falls to the build-host gate (see Task 3).
  </acceptance_criteria>
  <done>Worker + ThreadPool primitives exist; per-thread tables lifted; Berserk depth-stagger wired; Threads UCI option clamped + dispatched; cancellation poll tightened to 1024 nodes at both sites; GIL release verified intact; existing tests green; new tests green (benchmark gates respected).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: PAR-07 + PAR-08 build-host sentinel run (RUN_BENCHMARKS=1) + create Phase 4 .continue-here.md</name>
  <files>.planning/phases/04-lazy-smp-texel-tuning/.continue-here.md</files>
  <read_first>
    - .planning/phases/01-skeleton-smoke/.continue-here.md (full file — front-matter + per-gate pattern source)
    - .planning/phases/03-lockless-tt-search-refinements-endgame/.continue-here.md (full file — single-gate format the new file extends)
    - .planning/phases/04-lazy-smp-texel-tuning/04-PATTERNS.md (Shared Pattern 6 build-host deferred-gate .continue-here.md; assignment for .continue-here.md)
  </read_first>
  <what-built>
    Plan 04-01 Wave 0 + Task 2 are committed on the dev host. PAR-07 (NPS scaling ≥3.0×) and PAR-08 (cancellation <50ms with Threads=4) are pytest sentinels gated by `RUN_BENCHMARKS=1` — they cannot run on the Windows dev host (no MSVC-built v7_engine.pyd + no quiet measurement environment). Per CONTEXT D-16 + the Phase 1/3 `.continue-here.md` precedent, these gates land as plumbing in this plan and run on a build host downstream.

    The executor creates `.planning/phases/04-lazy-smp-texel-tuning/.continue-here.md` with two PAR-related gates (PAR-07 NPS scaling, PAR-08 cancellation) — plus a placeholder section for Plan 04-02's PAR-09 gauntlet (Plan 04-02 fills in the body). The file follows PATTERNS Shared Pattern 6 verbatim front-matter, per-gate body, pass-criterion block, fail-behavior block.
  </what-built>
  <how-to-verify>
    1. Verify `.planning/phases/04-lazy-smp-texel-tuning/.continue-here.md` exists with the front-matter block matching the Phase 3 template (created/owner-role/pattern-reference lines).
    2. Verify it contains two PAR-related gate sections: `## Plan 04-01 Task 3 — PAR-07 NPS scaling sentinel` and `## Plan 04-01 Task 3 — PAR-08 cancellation latency sentinel`. Each section has Status / Why deferred / Reproduction (bash block) / Pass criterion / Fail behavior subsections.
    3. PAR-07 reproduction block specifies: `RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest tests/test_v7_lazy_smp.py::test_nps_scales_3x_4t_vs_1t -q`. Pass criterion: exit 0, ratio printed >= 3.0.
    4. PAR-08 reproduction block specifies: `RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest tests/test_v7_lazy_smp.py::test_cancellation_under_4_threads_under_50ms -q`. Pass criterion: exit 0, elapsed_ms < 50.0 printed.
    5. After the developer runs both on the build host and they pass, the developer pastes the actual measured values (nps_1, nps_4, ratio; elapsed_ms) into the corresponding gate's "Result" subsection and marks each gate's Status: PASSED.
    6. Approve when both gates are PASSED on the build host AND the file contains the measured values.
  </how-to-verify>
  <resume-signal>Type "approved" with the measured nps_4/nps_1 ratio + cancellation elapsed_ms, OR describe failures (which test failed + the printed assertion message). On failure, executor diagnoses per RESEARCH Pitfall 1 (worker tables share via reference — most likely cause if 4t/1t < 1.5×) or Pitfall 3 (cancellation poll cadence — most likely if elapsed_ms > 50 in qsearch chain) and creates a fix task.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python → C++ (pybind11 binding) | `engine.set_option("Threads", value)` receives untrusted string from Python caller; must clamp + reject before spawning OS threads |
| C++ Engine ↔ N OS threads | Workers share TT memory (lockless XOR — Phase 3 mitigated) + `stop_flag_` atomic. Any other shared mutable state is a Pitfall 1 / Pitfall 12 violation |
| pybind11 GIL boundary | Search thread releases GIL (FOUND-05); helper std::threads never touch Python objects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | `Engine::set_option("Threads", ...)` (engine.cpp) | mitigate | Clamp to `[1, 256]` per D-04 + ASVS V5; reject non-integer with `info string`; refuses to spawn unbounded std::threads on hostile/typo input (Threads=999999 → emits `info string Threads out of range [1,256]` and leaves `options_.Threads` unchanged). Task 2 (e) implements; Task 1 test_threads_clamps_to_range verifies. |
| T-04-02 | Tampering | Lockless TT under SMP (Phase 3 03-05 tt_.{cpp,hpp}) — data race risk under multi-thread probe/store | mitigate | Hard precondition: PAR-03 TSan 16t×60s gate cleared on build host (Phase 3 .continue-here.md Gate 7). Lazy SMP on a non-TSan-verified TT = silent search-result corruption (Pitfall 36). Executor blocks Plan 04-01 merge until Gate 7 status is PASSED. |
| T-04-03 | Tampering | Per-thread heuristic tables (history / killers / counter_moves / continuation_history / capture_history) | mitigate | Worker struct stores fields as VALUE-TYPED COPIES, never references — Pitfall 1. Pattern verbatim from RESEARCH Code Example §1; PATTERNS Shared Pattern 3 calls out the failure mode literally. Test_worker_state_isolation verifies distinct memory addresses across workers. |
| T-04-04 | Information disclosure (latency) | Cancellation poll cadence under SMP | mitigate | Tighten from `% 4096` → `% 1024` at both poll sites per Pitfall 10. PAR-08 sentinel (50ms gate) is the empirical check on build host. Task 2 (e) edits the two callsites; Task 3 build-host gate confirms. |
| T-04-05 | Elevation of privilege (GIL bypass) | python_bindings.cpp `m.def("search", ..., py::call_guard<py::gil_scoped_release>())` | mitigate | DO NOT regress the existing FOUND-05 guard. Task 2 acceptance criteria includes `grep -c "py::call_guard<py::gil_scoped_release>"` >= 1. Stop() binding intentionally has no guard (Pitfall 11: single atomic write under GIL is fine). |
| T-04-06 | Repudiation | Berserk depth-stagger `[ASSUMED]` constants — silent drift if upstream retuned | accept | The exact numeric arrays are advisory per RESEARCH §Pattern 2 + Assumptions Log A4. PAR-09 (Plan 04-02) is the truth signal — if the arrays are wrong, 4t-vs-1t falls short of 40 Elo and the executor bisects per Pitfall 5 (revert to canonical published arrays + diagnose). Recorded in code comment in search.cpp. |

</threat_model>

<verification>
**Per-task commit (quick suite, no benchmark):**

```bash
python3 -m uv run --group dev pytest tests/test_v7_lazy_smp.py tests/test_v7_uci_threads.py -q -m "not benchmark and not gauntlet"
```

**Per-plan merge (full quick suite — Phase 1-3 regression remains green):**

```bash
python3 -m uv run --group dev pytest -q -m "not benchmark and not gauntlet"
```

**Build-host gates (Plan 04-01 only — Task 3 checkpoint):**

```bash
RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest tests/test_v7_lazy_smp.py::test_nps_scales_3x_4t_vs_1t -q
RUN_BENCHMARKS=1 python3 -m uv run --group dev pytest tests/test_v7_lazy_smp.py::test_cancellation_under_4_threads_under_50ms -q
```

**Phase-3 precondition (CHECK BEFORE STARTING ANY WORK):**

```bash
grep -E "Status:\s*(PASSED|GREEN)" .planning/phases/03-lockless-tt-search-refinements-endgame/.continue-here.md
```

If grep returns no match → BLOCK the plan; Phase 3 PAR-03 TSan gate is the hard precondition (CONTEXT precondition + Pitfall 36).
</verification>

<success_criteria>
Plan 04-01 is complete when:
- Hard precondition PAR-03 TSan gate marked PASSED on Phase 3 build host
- `tests/test_v7_lazy_smp.py` + `tests/test_v7_uci_threads.py` exist with all 7 enumerated test functions; pytest markers `benchmark` + `gauntlet` registered in pyproject.toml
- All quick-suite tests green at default Threads=1 (zero regression in Phase 1-3 V7 tests)
- PAR-04 / PAR-05 / PAR-06 unit tests pass (worker isolation + no-shared-state + depth-stagger)
- PAR-07 + PAR-08 sentinels marked PASSED in `.planning/phases/04-lazy-smp-texel-tuning/.continue-here.md` with measured values (4t/1t NPS ratio + cancellation elapsed_ms)
- Cancellation poll cadence is `% 1024` at both search.cpp sites; zero `% 4096` callsites remain
- GIL release guard at python_bindings.cpp m.def("search", ...) intact (grep verified)
- `Threads` UCI option declared in uci handler, clamped to [1, 256] in set_option dispatcher, wires `pool_.resize(n)`
- `.planning/phases/04-lazy-smp-texel-tuning/.continue-here.md` exists with PAR-07 + PAR-08 gates committed + placeholder section for Plan 04-02 PAR-09 gate
</success_criteria>

<output>
After completion, create `.planning/phases/04-lazy-smp-texel-tuning/04-01-SUMMARY.md` capturing:
- Final Worker struct field layout (so Plan 04-03's eval_quiet parity test can mirror it if needed)
- Berserk SkipDepths / SkipPhases arrays actually committed (record source: Berserk upstream SHA OR RESEARCH canonical published values)
- Measured PAR-07 NPS ratio (4t/1t) + PAR-08 cancellation elapsed_ms from Task 3 build-host run
- Verification that GIL release at python_bindings.cpp was NOT modified (FOUND-05 preserved)
- Any deviation from CONTEXT D-01..D-05 (expected: none)
- PAR-03 TSan gate confirmation timestamp from Phase 3 .continue-here.md
</output>
